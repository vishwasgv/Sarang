# Phase 67 — Five Signature Wins, Part 1 (23 verticals): Completion Report

### Maintained by Aszurex | Vishwas G V | 2026-08-19

**Status: 🟡 IN PROGRESS — 15 of 23 verticals meaningfully touched, Section 9.2's ranked item #1 fully CLOSED, GP Clinic's AND Physio Clinic's AND Diagnostic Lab's AND Vet Clinic's AND Specialist Clinic's AND Dental Clinic's AND Restaurant's AND Retail's AND Hardware's AND General's ENTIRE 5-item signature-win sets COMPLETE (first TEN of 23 verticals to reach this — all clinical verticals plus the first four Product & Retail verticals now fully closed), Electronics 2 of 5, ~47 of 115 items remaining** | Commits so far: `747ab74`, `1483864`, `601c706`, `b2cdcfd` (the last one covers everything through Hardware's full closure — GP's last 3 reports, all of Physio's work, all of Diagnostic Lab's work, all of Vet Clinic's work, all of Specialist Clinic's items 20.4/20.2/20.3/20.5, all of Dental Clinic's items 21.1/21.2/21.4/21.5, all of Restaurant's items 2/3/4/5, all of Retail's items 1-5, all of Hardware's items 1-5 — pushed to `origin/main`); General's items 1+2+3+4+5 (all of General, now fully closed) plus Electronics' items 1+2 built since, not yet committed

---

## Scope

115 total items — 23 verticals × 5 signature features/reports each:

- **14 Product & Retail verticals**: Restaurant, Retail, Hardware, Distributor, General, Pharmacy, Electronics, Clothing, Footwear, Manufacturing, Agri Inputs, Blood Bank, Rental, Jewellery.
- **3 Legacy Generic Service verticals**: Service, Consultant, Repair.
- **6 Clinical verticals**: Vet Clinic, GP Clinic, Specialist Clinic, Dental Clinic, Physio Clinic, Diagnostic Lab.

By a wide margin the largest phase in the roadmap so far. Unlike Phases 61-66, this phase is **not** being built in one continuous session — it is being worked one ranked item at a time, at the same full research→build→test→i18n→live-verify bar as every phase before it, explicitly because the founder asked for confirmation that nothing planned was silently rushed. This report reflects real, current progress — not a projection of the full phase.

## Grounding-check corrections made before any item was built

The same "verify, don't assume" discipline every phase in this arc has needed: the original audit's own component list was already several days stale in places by the time this phase's spec was written.

- Phase 64 had, in the meantime, shipped a generic kit mechanism that made Restaurant's combo pricing a *reuse* task instead of new invention.
- Phase 63 had shipped `PricingScheme`, making Distributor's scheme engine mostly-already-built.
- Several clinical-vertical and Pharmacy items turned out to already have real backend data, just never surfaced to the UI.

## Work completed so far, ranked by the phase's own day-to-day priority ordering

### 1. Clinical dashboard-spotlight fix (all 5 non-lab clinical verticals — Vet/Dental/Specialist/Physio/GP) — FULLY CLOSED

Closed the exact same "reuse-ready but wired wrong" bug class Phase 66's own closing audit already found once: all 5 non-lab clinical verticals were silently sharing the generic appointment-completion Dashboard card instead of their own real, already-built data. Re-grounding found Vet's vaccination-due tracker and Physio's pain/functional-score tracking were **both already fully built**, not greenfield as the original spec had tagged them — a real spec-correction, made directly in `PHASE_61_ROADMAP_MASTER_PROMPT.md` with a dated note rather than silently building around the error.

New `dashboard-spotlight.service.ts` `kind` branches: `vaccination` (Vet), `recall` (Dental), `referral` (Specialist), `outcomeProgress` (Physio), `chronicRecall` (GP). GP Clinic was initially deferred — its chronic-condition recall genuinely needed new schema, unlike the other four which were pure reuse — and was closed out in a dedicated follow-up pass (below).

- 6 new unit tests (Vet/Dental/Specialist/Physio) + 8 new unit tests (GP Clinic's `chronic-condition-record.service.ts`) + 3 new AI-intent tests.
- 11 + 1 new i18n keys × 13 languages, key-parity verified (3833/3833 final).
- Live-verified by switching through all 5 verticals via the real UI and confirming each dashboard shows correct real (honestly-zero, on a fresh dev DB) data.

**GP Clinic's chronic-condition schema (the piece that closes this item out):** genuinely new — no existing scaffold, unlike the other four. Two new models: `ChronicConditionRecord` (a patient can carry several independently-tracked conditions at once — deliberately NOT a reuse of Dental's `RecallRecord`, whose `patientId` is `@unique`) and `ChronicRecallComplianceLog` (an append-only history table; each upsert snapshots whether the *previous* recall period was met on time, before overwriting it with the new one — the only way to answer that question later, since the live record has no memory of its own past state). New `chronic-condition-record.service.ts` (upsert/list/dashboard-counts/a real trailing-12-month compliance report broken down per condition), new `chronic_recall` module + `ChronicRecallListScreen.tsx` (`/clinical/chronic-recalls`, deliberately English-only per this project's own vertical-screen `languageLock` convention), new `chronicRecall` dashboard-spotlight `kind`, new AI intent `gp.chronicRecallsDue`.

A real infra gotcha was hit and resolved without touching any existing table: `prisma migrate dev` refused to run — 7 migrations since Phase 61 had stale `_prisma_migrations` bookkeeping (schema/data confirmed genuinely live via direct `node:sqlite` queries first) plus a real schema-drift condition that made Prisma want to reset the whole shared dev database. Resolved by marking the stale migrations `--applied` (bookkeeping-only, no re-run) and hand-writing + directly applying just the 2 new `CREATE TABLE` statements, confirmed via a targeted before/after table-existence check.

Live-verified end to end against the real running app: tagged a real patient with an overdue chronic condition, confirmed the Dashboard's `overdueCount` correctly showed 1; logged a real *late* follow-up and confirmed the compliance log correctly recorded `onTime: false` and the compliance report correctly rolled it up to 0%; confirmed the new list screen and the new Dashboard card both render with zero error boundaries. Reverted to MANUFACTURING and independently re-confirmed zero leftover test data via direct DB query, not trusted from any script log line.

**GP Clinic's own item 19.2 (Recall Compliance report), closed as a natural follow-on:** the Dashboard/list-screen work above reused `generateChronicRecallComplianceReport()` internally, but the item itself calls for a real Reports-screen entry — built as `reportService.generateChronicRecallComplianceReport()` (a thin date-range adapter over the same underlying function, which gained an explicit `{dateFrom, dateTo}` mode alongside its existing trailing-months mode so both callers share one implementation), a new `chronicRecallCompliance` report registration (`chronic_recall`-gated), and a new `ChronicRecallComplianceView` — a small self-contained inline-SVG gauge (no shared gauge component existed anywhere in this codebase) plus a per-condition breakdown table. 9 new i18n keys × 13 languages (3843/3843 final), one new Manual paragraph × 13 languages, 4 new unit tests. A real, narrow TypeScript-narrowing gap was caught by the compiler itself and fixed as a genuine safety improvement, not just satisfied for the type-checker. Live-verified against the real running app: seeded a patient with one genuinely on-time and one genuinely late closed recall period (catching and fixing a real date-arithmetic direction bug in the verification script itself before trusting the first result), confirmed the report's own IPC channel returns `overallPercent: 50`, then drove it through the real Reports screen picker — found the tile, set the date range, generated, and confirmed the screen renders "50%" and the Diabetes row correctly with zero error boundary.

**GP Clinic's own item 19.3 (Walk-in vs. Appointment Ratio report), closed as a second natural follow-on:** genuinely new (unlike 19.1/19.2, no existing schema to reuse), but a grounding-check code read found the underlying data was already fully captured — `TokenQueue` (walk-ins) and `Appointment` (booked visits) — just never cross-tabulated. Confirmed via a direct read of `TokenQueueScreen.tsx`'s own "Add Walk-in" form that `TokenQueue.appointmentId` is never actually passed by the real UI, so every real TokenQueue row genuinely is a walk-in; counted `Appointment` independently by its own `scheduledDate` regardless, so the report can never double-count even if that ever changes. New `reportService.generateWalkInVsAppointmentRatioReport()` (day-bucketed, mirroring `generateOrderVolumeReport()`'s own established convention), new `walkInVsAppointmentRatio` report (`GP_CLINIC`-gated), new `WalkInVsAppointmentRatioView` (stacked daily bar + breakdown table), new AI intent `gp.walkInVsAppointmentRatio`. 7 new i18n keys × 13 languages (3850/3850 final), one new Manual paragraph × 13 languages, 4 new unit tests. Live-verified against the real running app: created 3 real walk-in tokens and 2 real appointments via the real IPC paths, confirmed the report's own IPC channel correctly returns `totalWalkIns: 3, totalAppointments: 2, walkInPercent: 60`, found the report tile in the real Reports picker, generated it, and confirmed zero error boundary. One verification-script assertion came back a false negative (checking for the literal string "Walk-in %" in the page text, which actually renders as "WALK-IN %" due to the `SummaryCards` component's own `uppercase` CSS class — Chromium's `innerText` respects CSS text-transform) — traced to the script, not the app, via code read, since the other 3 independent checks (IPC data, error boundary, un-transformed chart-legend text) all confirmed the report renders correctly.

**GP Clinic's own item 19.4 (Diagnosis-Category Trend report), closed as a third natural follow-on:** unlike items 19.1-19.3, this item's own spec tag was conditional ("if categorized — verify categorization exists"), and the grounding check found the condition was FALSE — `VisitNote.assessment` is unstructured free text with zero categorization anywhere in the schema, confirmed via a codebase-wide grep. Genuinely new capture needed first: added `VisitNote.diagnosisCategory`, a free-text tag field separate from `assessment`, additive on the shared `visit_notes` model (also used by Vet/Specialist/Physio) but surfaced only for GP_CLINIC via a new `diagnosis_categories` module gate — the same config-flag pattern (not a raw businessType check) already established for `chronic_recall`. New "Diagnosis Category" input added to the shared `VisitNoteScreen.tsx`, gated by the new module, with a suggested-categories datalist mirroring `ChronicConditionRecord.conditionName`'s own pattern — deliberately left out of the patient-facing Print Summary, since it's an internal analytics tag, not something a patient handout needs. New `reportService.generateDiagnosisCategoryTrendReport()` pivots visits into a month × category "wide" table using the existing `groupLabel()` helper, and the new `DiagnosisCategoryTrendView` is this codebase's first genuinely *dynamic*-category multi-line chart (the earlier Distributor dual-line chart has exactly 2 fixed series; here the series count is data-dependent, since categories are free text per install). New AI intent `gp.topDiagnosisCategory` sums each category across the period and surfaces the leader — a spoken-friendly answer, not a literal dump of the report's own pivoted shape. 9 new i18n keys × 13 languages (3859/3859 final), 2 new Manual insertions × 13 languages, 6 new unit tests. Full suite **2662/2662**. Live-verified against the real running app: created a real appointment + visit note tagged `diagnosisCategory: 'Infection'` via the real IPC path, confirmed the report's IPC channel correctly returns it, found the report tile in the real Reports picker, generated it, confirmed the "Infection" row renders with zero error boundary. Two more verification-script assertions came back false negatives from the exact same `uppercase`-CSS root cause found on item 19.3 (this time on `VisitNoteScreen.tsx`'s own `Section` title, confirming it as a real recurring pattern — now saved to memory as `feedback_uppercase_css_innertext_gotcha` so future verification scripts don't re-trip on it) — not real app bugs, confirmed via code read and the other passing checks.

**GP Clinic's own item 19.5 (Referral-Out Tracking with Outcome Follow-up), closing out GP Clinic's ENTIRE 5-item signature-win set:** a grounding-check code read found `specialist_referral` (Phase 46's real in-app "Refer to Another Provider" booking mechanism) was gated to SPECIALIST_CLINIC only — GP_CLINIC had no way to refer a patient out in-app at all, a genuine capability gap, not just a missing report. Added the module to GP_CLINIC's defaults (unlocking the already-built booking flow + referral letter printing, previously invisible), extended `listReferralsForVisitNote()` to surface `outcomeSummary` (the referred-to provider's own finalized assessment, gated on `isFinalized` so a draft note never counts as a real outcome) inline on the referring note, added a matching "Outcome" line to `VisitNoteScreen.tsx`'s Referrals list, and built `reportService.generateReferralOutcomeReport()` — a pure data-table report (no chart type named in this item's own spec, unlike 19.1-19.4). Deliberately gated by `specialist_referral` rather than `GP_CLINIC`-only, since SPECIALIST_CLINIC shares the exact same mechanism — not scope creep, just not artificially excluding a vertical the report already correctly applies to. New AI intent `gp.referralOutcomes`. 8 new i18n keys × 13 languages (3867/3867 final), 2 Manual paragraph edits × 13 languages, 8 new unit tests. Full suite **2669/2669**.

Live-verified end to end against the real running app, including a real environment gap caught and worked around rather than skipped: the dev DB had zero seeded `Employee` rows, so the verification script created a real test Employee via the real `hr.createEmployee` IPC path, then a real appointment, a real originating note, a real in-app referral, and a real second note on the referral appointment finalized with a genuine outcome — confirmed the outcome surfaces on the referring note, confirmed the report's IPC channel counts it, found the report tile in the real Reports picker, generated it, confirmed both patient name and outcome text render with zero error boundary. A real, correct distinction was independently confirmed: the referral appointment's own status stayed `SCHEDULED` throughout (never manually completed), yet `outcomeRecordedCount` still correctly showed 1 — proving `completedCount` (by status) and `outcomeRecordedCount` (by note finalization) are genuinely independent signals as designed, not conflated. One earlier attempt was killed mid-run by an external interruption before any test data existed (confirmed via direct DB query before retrying) — handled by re-running, not by weakening the check.

**GP_CLINIC is the first of Phase 67's 23 verticals to have its entire 5-item signature-win set genuinely complete, live-verified, and documented.**

### 2. Pharmacy's two reports (Doctor-wise prescription volume, Expiry-risk value)

Both turned out to be near-zero new capture work: the doctor-linked prescription data and the expiry/batch cost data had existed since Phase 2/58, just never aggregated or charted. Extended `generateBatchExpiryReport()` (added a `value` figure per bucket plus an `atRiskValue` summary) and `generatePrescriptionDrugSalesReport()` (added `byDoctor` aggregation), rather than building new report functions from scratch. New AI intent `pharmacy.prescriptionVolumeByDoctor`. Fixed 3 pre-existing hardcoded-English labels found incidentally while touching that component.

- 9 new unit tests.
- 9 new i18n keys × 13 languages.
- Live-verified through the real Reports screen.

### 3. Distributor's Scheme Cost vs. Incremental Volume report

Genuinely new — no existing scaffold. A grounding check first surfaced that this codebase has no counterfactual/baseline mechanism anywhere, so "incremental volume" cannot honestly be computed as a causal number. Shipped as an explicit **correlation** view instead — scheme cost plotted alongside covered-product sales volume, by week — with that limitation stated directly in the UI copy, not just a code comment, so the report doesn't overclaim what it shows.

Reused Phase 64's own `getProductCostsBatch()` cost-basis selector rather than inventing a new one. New `generateSchemeCostVsVolumeReport()`; first dual-line chart in the Reports screen (`recharts`' `LineChart` aliased as `RCLineChart` to avoid a naming collision with the already-imported `lucide-react` icon of the same name). New AI intent `distributor.schemeCostVsVolume` — closed a real, previously-undisclosed gap found while wiring it: Distributor had zero vertical AI templates at all before this.

- 8 new unit tests.
- 16 new i18n keys × 13 languages.
- Live-verified end to end.

### 4. Restaurant's Combo/Thali Auto-Pricing

Re-grounding before building found 2 of the item's 3 original scope assumptions were simply wrong: no module-gate concept exists to add, and the kit-configuration UI already existed and worked. The one real, previously-undisclosed bug: selling a combo already correctly deducted each dish's own top-level stock, but marking its kitchen ticket "Done" silently skipped ingredient-level deduction for every dish inside the combo, since a combo (a kit) has no recipe of its own — recipes are per-dish, and `deductIngredients()` never expanded a kit line into its components before its recipe lookup. Fixed by expanding kit lines into component dishes before the ingredient lookup runs.

- 3 new unit tests.
- A genuinely new Manual section (combos were never documented in the Restaurant chapter before) in all 13 languages.
- A full real end-to-end live verification: created real ingredients, dishes, recipes, and a combo, sold it through the real UI, advanced its kitchen ticket, and confirmed via direct database query that both ingredients from both different dishes inside the combo decreased by exactly the right amounts.
- A real E2E test-cleanup gotcha was found and fixed along the way: a verification script's own cleanup silently failed to delete test `Product` rows because two foreign-key-referencing tables (`InventoryMovement`, `LocationStock`) weren't covered by the project's existing cleanup pattern — the failure was swallowed inside a `try/catch{}`, so the script logged "cleanup done" while 8 leftover products remained, caught only by an independent follow-up query. Documented as a standing project memory so a future session doesn't re-pay the same discovery cost.

### 5. Physio Clinic's Pack Utilization report (item 22.4)

Tagged in the roadmap as a genuine REUSE item and a literal shared-component candidate with Gym/Studio — both verticals use the exact same `ClientSessionPack` model (Phase 27/41), so no new schema was needed. New `reportService.generatePackUtilizationReport()` reads packs by `purchaseDate` in range regardless of active/inactive status (a fully-used, deactivated pack is still real utilization history), computing per-pack remaining sessions and utilization %, plus an overall rollup. New `packUtilization` report gated by the shared `session_packs` module (not Physio-only) — matching the same non-scope-creep precedent as the Referral-Out Outcome report: GYM_STUDIO gets the exact same report mechanism for free, since it's genuinely the same feature. New `PackUtilizationView` (top-10 horizontal bar chart + full table). New shared AI intent `sessionPacks.utilization`, registered for both PHYSIO_CLINIC and GYM_STUDIO.

**Note on scope**: GYM_STUDIO is a Phase 68 vertical, not one of Phase 67's own 23 — its own signature-win items aren't formally specified yet, so this doesn't tick anything off for GYM_STUDIO's own checklist. What genuinely carries forward is that the report/AI-intent work is already done and reusable whenever Phase 68 reaches it.

- 12 new i18n keys × 13 languages (3879/3879 final), one new Manual paragraph × 13 languages, 6 new unit tests. Full suite **2676/2676**.
- Live-verified against the real running app: switched to PHYSIO_CLINIC, created a real customer and a real 10-session pack via the real IPC path, deducted 2 real sessions via the same `sessionPack:deduct` path a completed appointment would use, confirmed the report's IPC channel correctly returns `usedSessions: 2, remainingSessions: 8, utilizationPercent: 20`, found the report tile in the real Reports picker, generated it, confirmed both patient name and pack name render with zero error boundary — clean on the first attempt, no false negatives this time.

### 6. Physio Clinic's remaining two items (22.3 Session-pack alert, 22.5 Referring-doctor outcome feedback loop) — closing Physio Clinic's ENTIRE 5-item set

**Item 22.3, verified-not-rebuilt:** grounding check found `SessionPacksScreen.tsx` already ships a complete `Band` (`ACTIVE`/`LOW`/`EXPIRED`) classification via `getBand()`, filterable pill buttons with live per-band counts, and per-card color-coded alerting (danger-red expiry date when expired, warning-amber remaining-count when ≤2 sessions left). This is not a partial stub — it's a fully shipped in-app alert surface, tagged `REUSE` in the roadmap for exactly this reason. Zero new code; ticked off as verified, matching this phase's own precedent for Vet's vaccination reminder and Physio's own items 1-2.

**Item 22.5, genuinely new:** grounding check found `specialist_referral` (Phase 46's real in-app referral mechanism) had never been turned on for `PHYSIO_CLINIC` — the same class of gap item 19.5 closed for GP_CLINIC, not a new mechanism. Added the module flag, unlocking inbound `referredBy` capture and the already-built "Refer to Another Provider" UI. Then enriched (not replaced) `listReferralsForVisitNote()`: when a referred-to note is finalized and carries pain/functional scores (item 1's own already-existing data), the outcome shown to the referring doctor is now a quantified before/after computed across the *whole course of treatment* since the referral — e.g. `"Pain 7→3, Function 40→75 across 3 sessions"` — instead of just the single linked visit's free-text assessment. Non-physio referrals fall straight back to the existing plain-assessment behavior, unaffected, confirmed by a dedicated regression test. No new report and no new AI intent needed — this is an enrichment of an existing in-note referral list, not a new Reports-screen surface, so Section 1.2's "new report → new AI intent" rule doesn't apply.

- No new i18n keys — `VisitNoteScreen.tsx` confirmed English-only per the established vertical-screen language-lock convention; all label text reused verbatim.
- 2 new unit tests in `visit-note.service.test.ts`. Full suite **2678/2678** (one unrelated `backup.service.test.ts` timeout on the full run re-confirmed a pre-existing flake, not a regression, by re-running that file alone: 24/24 clean). Both tsconfigs clean.
- Live-verified against the real running app: created a real patient, a real originating note, a real in-app referral to a real provider Employee, 3 real linked visit notes across 3 dates with real pain/functional scores (7→5→3, 40→60→75), finalized the referral's own note, and confirmed `listReferrals` returns exactly `"Pain 7→3, Function 40→75 across 3 sessions"` — correct on the first attempt. Drove the originating note through the real `VisitNoteScreen` UI and confirmed the outcome text renders with zero error boundary. One assertion (exact-case "Refer to Another Provider" button text) came back a false negative — investigated via code read, confirmed as the same recurring `Section` title `uppercase`-CSS gotcha found twice earlier this session on this same file, not a real gap. Reverted to MANUFACTURING and independently re-confirmed zero leftover test data via direct DB query.

**PHYSIO_CLINIC is now the second of Phase 67's 23 verticals (after GP_CLINIC) to have its entire 5-item signature-win set genuinely complete, live-verified, and documented.**

### 7. Diagnostic Lab's entire 5-item set, closing Specialist Clinic's item 20.1 as a paired follow-on

**Item 23.3 (Critical-value auto-flagging), confirmed ALREADY BUILT:** `LabTestOrderItem.hasCriticalResult`/`criticalNotifiedAt`/`criticalNotifiedById`/`criticalNotifiedNotes` (Phase 58 §2) already fully implements this — `normal-range.service.ts`'s `evaluateAgainstNormalRange()` auto-detects values beyond a test's `criticalLow`/`criticalHigh`, and `LabOrdersScreen.tsx` already drives a real acknowledge-with-notes escalation workflow. One incidental fix: the renderer's own type annotation excluded `'CRITICAL'` from its literal union (functionally harmless, misleading for a future refactor) — corrected.

**Item 23.1 (Per-test TAT), genuinely new:** grounding found not just a missing target field but a missing per-item completion timestamp — `LabTestOrder.reportedAt` only existed at the whole-order level. New `ServiceCatalog.targetTATHours` (editable per lab test, gated by the `lab_orders` module) + `LabTestOrderItem.targetTATHours` (denormalized snapshot at order creation) + `LabTestOrderItem.resultReadyAt` (set exactly once — a later correction must never push it forward, or actual TAT would silently shrink). `LabOrdersScreen.tsx` shows a real-time On Time/Late badge with exact hours per item.

**Item 23.2 (TAT dashboard report):** new `reportService.generateLabTATReport()`, SLA color-flagged bar chart (green ≥50% on-time, red otherwise) plus a full table, `Reports → Per-Test TAT`, new AI intent `lab.tatCompliance`.

**Item 23.4 (Test volume by panel report):** new `reportService.generateTestVolumeByPanelReport()`, the same dynamic month × category multi-line-chart pattern item 19.4 established, `Reports → Test Volume by Panel`, new AI intent `lab.topPanel`.

**Item 23.5 (Referring-doctor test volume) + Specialist Clinic's item 20.1 (Referral-source leaderboard), closed together per this item's own "build once, apply to both" tag:** new `reportService.generateReferralLeaderboardReport({dateFrom, dateTo, businessType})` — one shared shape/UI/AI-intent, but two genuinely different queries (`LabTestOrder.referredByProviderId`, a real Employee FK, vs. `VisitNote.referredBy`, free text), since the two verticals' data isn't identically shaped. `ReportDef.requiredBusinessType` extended to accept `string | string[]`. New AI intents `lab.referralLeaderboard` and `specialist.referralLeaderboard`.

- 18 new i18n keys + 6 new `reports.defs.*` label/description keys × 13 languages (3903/3903 final). Manual: `business__diagnostic-lab.md` gained a "Turnaround time (TAT)" section + 3 new report paragraphs; `business__specialist-clinic.md` gained its first-ever "Reports" section — both × 13 languages.
- 19 new unit tests. Full suite **2699/2699**, both tsconfigs clean.
- **Live-verified against the real running app, including two real mistakes caught mid-verification rather than shipped**: (1) the first pass returned an empty TAT report despite real data — traced to the *verification script's own* `today` being UTC-derived while the app correctly brackets local calendar days, and the machine's local time (IST) was already past local midnight while UTC wasn't — fixed by computing `today` from local date parts, the same UTC-vs-local mistake class this project's reports were hardened against, this time caught in the test harness, not the app. (2) after that fix, none of the 3 new report tiles appeared in the Reports picker at all — traced to 6 missing `reports.defs.*.label`/`.description` i18n keys (the picker's own display text source) that had been skipped; added across all 13 languages, re-ran, all 3 tiles rendered correctly. End to end: real Service Catalog test with a 24h target, a real lab order referred by a real Employee, sample collected, result entered, confirmed `resultReadyAt` set and all 3 reports return correct data via IPC and render in the real picker with zero error boundaries, confirmed the On Time badge on the real `LabOrdersScreen`. Switched to SPECIALIST_CLINIC in the same session and confirmed the shared leaderboard correctly ranks a real free-text `referredBy` name. Reverted to MANUFACTURING; independently re-confirmed zero leftover test data across every entity type touched.

**DIAGNOSTIC_LAB is now the third of Phase 67's 23 verticals to have its entire 5-item signature-win set genuinely complete.** **Correction, checked against Section 9.1's own item list before writing this** (the same discipline that caught an earlier GYM_STUDIO overclaim): closing item 23.5 also closes Specialist Clinic's item 20.1, but Specialist Clinic's other 4 items (second-opinion conversion tracking, case-complexity mix, referral-loop auto-send, waitlist prioritization) remain fully untouched — **Specialist Clinic itself is NOT fully complete, only 1 of its own 5 items is.**

### 8. Vet Clinic's item 18.2 (Vaccination Compliance report) — its 2nd of 5 signature items

Deliberately a *different* question from the Dashboard's own live `vaccination` spotlight card (an inline "what's overdue right now" snapshot): this is a historical, date-ranged report over doses actually given in a picked period. A pet's first-ever dose of a vaccine has no prior due date to be judged "on time" against, so it's excluded from on-time/late counting entirely — the same "only count what's genuinely comparable" precedent GP's own chronic-recall compliance report established (item 19.2). No new schema: `VaccinationRecord` is already an append-only per-dose history, so a prior dose's own `nextDueDate` was already there to compare the next dose against.

New `reportService.generateVaccinationComplianceReport()`, `Reports → Vaccination Compliance`, reuses the `ComplianceGauge` component as-is (first reuse of it outside GP Clinic). New AI intent `vet.vaccinationCompliance`.

- 6 new i18n keys × 13 languages (3910/3910 final). Manual: `business__vet-clinic.md` gained its first-ever "Reports" section × 13 languages.
- 9 new unit tests (6 report classification cases + 3 AI-intent). Full suite **2708/2708**, both tsconfigs clean.
- **Live-verified against the real running app, clean on the first attempt**: created a real customer, a real pet, a first-ever Rabies dose 20 days ago with a next-due date 5 days ago, and a follow-up dose given today — confirmed the report correctly evaluates only the follow-up dose (the first dose has nothing prior to compare against) and correctly classifies it as late, found the report tile in the real Reports picker, generated it, confirmed the vaccine name and a 0% gauge render with zero error boundary. Reverted to MANUFACTURING; independently re-confirmed zero leftover test data.

**Vet Clinic now has 2 of its own 5 signature items done** (item 18.1, the vaccination-due reminder, was already confirmed built during the earlier cluster-level dashboard-spotlight fix). Items 3-5 (breed-specific health-alert flagging, case-type volume trend report, multi-pet household linkage) remain untouched.

### 9. Vet Clinic's item 18.3 (Breed-specific health-alert flagging at intake) — its 3rd of 5 signature items

Grounding found this needed genuinely new capture, not the zero-effort reuse its own `[REUSE]` tag implied — `Pet.species`/`.breed` are real existing fields, but no alert/flagging mechanism of any kind existed against them.

Built as a clinic-maintained reference list, deliberately **not pre-seeded with any clinical claims of our own** — this app has never authored veterinary medical content, the same caution behind every "convenience document, not a validated medical record" disclaimer already standard here. New `BreedHealthAlert` model, new `breed-health-alert.service.ts` (CRUD + case-insensitive substring-match lookup, since `Pet.breed` is free text), new `Breed Health Alerts` screen (`/vet/breed-alerts`) mirroring `NormalRangesScreen.tsx`'s own CRUD shape closely.

A live-matching alert banner shows in two places: `PetListScreen.tsx`'s Add Patient form (debounced species/breed lookup while typing — the item's own "at intake" framing) and, beyond the item's literal scope but clearly in its spirit, `PetProfileScreen.tsx`'s Overview tab every time a matching patient's profile is opened, not just once at registration.

- No new AI intent (an alert/flagging feature, not a report) and no new i18n keys (both screens are Vet Clinic service-vertical UI, English-only per the established language-lock convention).
- 11 new unit tests. Full suite **2719/2719**, both tsconfigs clean.
- **Live-verified against the real running app**: created a real breed alert via the real IPC path, confirmed both exact and partial (substring) breed matching work correctly, found the alert listed on the real management screen, opened the real Add Patient form and confirmed the matching alert banner renders live while typing the breed — zero error boundaries throughout. Reverted to MANUFACTURING; independently re-confirmed zero leftover test data.

**Vet Clinic now has 3 of its own 5 signature items done.** Items 4-5 (case-type volume trend report, multi-pet household linkage) remain untouched.

### 10. Vet Clinic's items 18.4 (Case-Type Volume Trend) AND 18.5 (Multi-pet household linkage) — closing Vet Clinic's ENTIRE 5-item set

**Item 18.4:** grounding found no dedicated case-type field or Surgery category exists anywhere in this codebase's own seed data — the item's own "surgeries/consults/vaccinations" list was aspirational, not literal. Built honestly: case types come from whatever categories a clinic's own Service Catalog actually has, same dynamic-category convention as items 19.4/23.4, so Surgery appears automatically once a clinic adds it. **Vaccinations** gets its own dedicated series sourced directly from `VaccinationRecord` rather than booked appointments — the authoritative source of "a vaccination actually happened." Only pet-linked, non-cancelled appointments count as a real case. New `Reports → Case-Type Volume Trend`, new AI intent `vet.caseTypeVolume`.

**Item 18.5:** its own `[VERIFY]` tag confirmed half-true — `Pet.customerId` already structurally supports several pets per owner (zero new schema needed), but nothing surfaced it anywhere. `getPet()` now also returns the owner's other active pets, reusing the exact same call `PetProfileScreen.tsx` already made on every load. New "Other Pets in This Household" card, one click to a sibling's own profile.

- No new i18n for either item (English-only Vet Clinic UI). Manual: new report paragraph + a new Overview-bullet sentence, both × 13 languages.
- 4 + 3 new unit tests (including a brand-new `pet.service.test.ts` — this service had no test coverage at all before this). Full suite **2729/2729**, both tsconfigs clean.
- **Live-verified against the real running app, both items clean except one known false negative**: item 18.4 — real Consultation appointment + real vaccination dose on the same day, confirmed the report correctly split them into their own series via IPC and the real picker. Item 18.5 — two real pets under one real owner, confirmed the sibling renders and clicking it navigates to *its own* profile with the original pet now showing as *its* sibling (full round-trip confirmed). One assertion (exact-case card title) came back a false negative — the same recurring `uppercase`-CSS gotcha hitting a third time this session, confirmed via code read, not a real gap. Reverted to MANUFACTURING both times; independently re-confirmed zero leftover test data.

**Vet Clinic is now the 4th of Phase 67's 23 verticals to have its entire 5-item signature-win set genuinely complete, live-verified, and documented.**

### 11. Specialist Clinic's item 20.4 (Referral-loop closure) — its 2nd of 5 signature items

The item's own `[BUILDS ON]` tag predicted reusing the existing WhatsApp/Email share infrastructure, and grounding confirmed that was exactly right: `ShareMenu` (`src/renderer/src/shared/ui/molecules/ShareMenu.tsx`) already handles the WhatsApp/Email-with-PDF pattern for bills/reports/quotations/credit-debit-notes/POs, so no new sharing mechanism was invented. What was actually missing was contact info to send *to* — `VisitNote.referredBy` was always just a free-text name with nowhere to route a reply. New `VisitNote.referredByPhone`/`referredByEmail` (both optional, non-breaking — a clinic with older notes that never captured these simply sees the share button disabled, not an error) plus two new fields, "Referring Doctor's Phone" and "Referring Doctor's Email", in the existing Referral Details section.

Once a note is **finalized** (a draft isn't a real outcome worth sending) **and** a referring doctor is on file, a `ShareMenu` (`variant="button"`) appears next to Print Summary. Its PDF export (`handleExportVisitSummaryPdf()`) had to be built as fully self-contained inline-styled HTML rather than reusing any of the screen's own Tailwind-based markup — `export.service.ts`'s `exportToPdf` renders the caller's HTML inside a sandboxed hidden `BrowserWindow` (`contextIsolation: true, sandbox: true, nodeIntegration: false`) with no access to the app's bundled CSS, confirmed by reading the service before writing against it. Free-text clinical fields are escaped before embedding into that HTML as a defensive measure (this codebase's other HTML generators don't escape, but there was no reason to skip it here).

- No new i18n for the screen itself (English-only Specialist Clinic UI, per this vertical's standing language lock). Manual: new bullet + a new paragraph describing the Share button, × 13 languages.
- 3 new unit tests (`createVisitNote` maps the two new fields into the Prisma `data` object; defaults to `null` when omitted; `updateVisitNote`'s `...rest` spread passes them through). Full suite **2732/2732**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 31, `tests/e2e/suites/31-specialistclinic-visitnotes.js`, extended rather than duplicated): filled the two new fields via real UI, confirmed persistence via API, confirmed the Share buttons are genuinely absent on a draft note even with a referring doctor and contact info already on file, finalized via the real Finalize button, then confirmed both WhatsApp and Email buttons render and the WhatsApp button is enabled. 20/20 checks passed (14 pre-existing + 6 new), zero leftover test data after cleanup.

Specialist Clinic's other 3 items (second-opinion conversion tracking, case-complexity mix report, waitlist prioritization by referral urgency) remain fully untouched.

### 12. Specialist Clinic's item 20.2 (Second-opinion conversion tracking) — its 3rd of 5 signature items

The item's own `[GREENFIELD]` tag was confirmed correct — no such tracking concept existed anywhere in the codebase before this. New `VisitNote.isSecondOpinion` boolean, surfaced as a "This is a second-opinion consultation" checkbox in the existing Referral Details section — deliberately distinct from the referral fields, since a second-opinion visit doesn't require anyone to have sent the patient (they may have walked in on their own after being diagnosed elsewhere), and a referred patient isn't necessarily seeking a second opinion. A checked note shows a **Second Opinion** badge next to the note title.

New `reportService.generateSecondOpinionConversionReport({dateFrom, dateTo})`: of the visits flagged as a second opinion in a date range, how many of those patients came back for a later completed appointment and became an ongoing patient — a total count, a converted count, a conversion rate, and a per-patient row with their visit date and (if they returned) their next visit date. Only patients linked to a real customer record can be tracked this way, since the report has to match a later appointment back to the same person; a walk-in with no customer record isn't counted either way — disclosed in both the Manual and the report's own empty-state text, not silently mis-scored. New AI intent `specialist.secondOpinionConversion`.

- No new i18n for the screen itself (English-only Specialist Clinic UI). Manual: a new sentence describing the checkbox + badge, and a new report paragraph, × 13 languages.
- 10 new unit tests: 3 in `visit-note.service.test.ts` (create maps `isSecondOpinion` into Prisma `data`, defaults to `false` when omitted, update's `...rest` spread passes it through), 4 in `report.service.test.ts` (converted case, not-converted case, walk-in-with-no-customerId excluded from tracking entirely, honest empty result), 3 in `ai-vertical-templates.service.test.ts` (intent registration, headline/details shape, empty-state headline). Full suite **2742/2742**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 31, extended again): checked the box via real UI, confirmed the flag and the badge both render, confirmed persistence via API — then, to prove the conversion logic genuinely works end-to-end rather than trusting the report's own math, booked a second real appointment for the same customer 5 days later, marked it COMPLETED via the real API, opened the Second-Opinion Conversion report tile through the real Reports screen, and confirmed via API that the patient's row now shows `converted: true` with the correct next-visit date. 28/28 checks passed (20 pre-existing + 8 new — 6 second-opinion checks plus 2 more that were needed after the report step's own navigation away from the visit-note screen broke a later step; fixed by navigating back before returning), zero leftover test data after cleanup.

Specialist Clinic's other 2 items (case-complexity mix report, waitlist prioritization by referral urgency) remain fully untouched.

### 13. Specialist Clinic's item 20.3 (Case-complexity mix report) — its 4th of 5 signature items

The item's own `[VERIFY]` tag ("needs a complexity-tagging field — not confirmed to exist") was confirmed correct by grounding — a repo-wide grep for "complexity" turned up nothing. New `VisitNote.caseComplexity` (`'ROUTINE' | 'COMPLEX'`, optional), surfaced via a "Case Complexity" dropdown right after the Assessment section. A real design decision was made before building: this field is gated on a **new, dedicated** `case_complexity` module flag rather than reusing `specialist_referral` — grounding found `specialist_referral` is shared with GP_CLINIC and PHYSIO_CLINIC (Phase 67 items 19.5/22.5 turned it on for them too), but this signature item is scoped to Specialist Clinic alone in Section 9.1's own list, so folding it into the shared flag would have leaked the feature into two verticals it wasn't meant for.

New `reportService.generateCaseComplexityMixReport({dateFrom, dateTo})`: a month-by-month stacked bar chart of Routine vs. Complex cases (reusing the same `groupLabel()` month-pivot helper as items 18.4/19.4/23.4), plus summary cards for total tagged, Routine count, Complex count, and Complex percentage. Untagged notes are excluded from the mix entirely rather than assumed Routine — the same "only count what's genuinely comparable" precedent this phase's chronic-recall and vaccination-compliance reports already established. New AI intent `specialist.caseComplexityMix`.

- No new i18n for the screen itself (English-only Specialist Clinic UI). Manual: a new paragraph describing the dropdown, and a new report paragraph, × 13 languages.
- 10 new unit tests: 3 in `visit-note.service.test.ts` (create maps `caseComplexity` into Prisma `data`, defaults to `null` when omitted, update passes it through), 2 in `report.service.test.ts` (month-pivot split, honest empty result), 3 in `ai-vertical-templates.service.test.ts` (intent registration, headline/details shape, empty-state headline), plus 2 more from extending the `TemplateModule` union type in both the main-process (`industry-template.service.ts`) and renderer (`industry.store.ts`) copies — a real cross-cutting spot this codebase requires touching whenever a new module flag is introduced, confirmed by grepping for the existing `diagnosis_categories` flag's own footprint before adding this one. Full suite **2750/2750**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 31, extended again): selected "Complex" via the real dropdown, confirmed persistence via API, opened the Case-Complexity Mix report tile through the real Reports screen, and confirmed via API that the tagged case actually appears in the report's Complex count (not just that the screen renders without crashing). 32/32 checks passed (28 pre-existing + 4 new), zero leftover test data after cleanup.

**Specialist Clinic is now 4 of its own 5 signature items complete — only item 20.5 (waitlist prioritization by referral urgency) remains.**

### 14. Specialist Clinic's item 20.5 (Waitlist prioritization by referral urgency) — its 5th and FINAL signature item, closing Specialist Clinic's entire set

The item's own `[GREENFIELD]` tag was confirmed correct — grounding found `TokenQueue` had no urgency/priority concept at all (`status` only tracks WAITING/CALLED/SEEN/SKIPPED). New `TokenQueue.isUrgent` boolean (default false), set via a "Mark as urgent (referring doctor flagged this as urgent)" checkbox added to the existing **Add Walk-in** form, gated on a **new, dedicated** `referral_urgency` module flag — same design reasoning as item 20.3's `case_complexity`: this is a Specialist-Clinic-only signature item, not something that should silently start showing up for GP/Vet/Dental/Physio's own shared Token Queue screen.

The actual prioritization is a one-line change with an outsized effect: `getTodayQueue`'s `orderBy` moved from plain `{ tokenNumber: 'asc' }` to the compound `[{ isUrgent: 'desc' }, { tokenNumber: 'asc' }]`. Because the existing **Call Next** button already just calls `tokens.find(t => t.status === 'WAITING')` — the first waiting token in whatever order the API returned — it inherits the new priority automatically with zero UI logic changes. A `red` **Urgent** badge on the token card and a `border-danger/40` card outline make the reprioritization visible to front-desk staff, not just an invisible backend reorder. The design is provably harmless to every other clinic vertical using the same shared `TokenQueueScreen.tsx`: `isUrgent` is always `false` there (the checkbox that sets it is gated off), so the compound sort collapses back to exactly the old plain token-number order — confirmed live, not just asserted, in the verification below.

- No new i18n for the screen itself (English-only Specialist Clinic UI). Manual: a new paragraph in the Token Queue section describing the checkbox/badge/priority behavior, × 13 languages.
- 3 new unit tests in `token-queue.service.test.ts`: `createToken` stores `isUrgent` when true, defaults to `false` when omitted, and `getTodayQueue`'s compound `orderBy` is asserted directly against the mocked Prisma call (not just inferred from behavior). Full suite **2753/2753**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 31, extended a final time) — the most convincing check this item got wasn't "does the flag persist," it was proving the actual reordering: issued a normal walk-in (token #1), then a SECOND walk-in explicitly marked urgent (token #2, later check-in, higher token number), confirmed the red Urgent badge renders, then fetched the live queue via API and confirmed token #2 (urgent) is returned at array index 0 while token #1 (normal) sits at index 1 — a later-arriving patient genuinely called ahead of an earlier one, not merely a stored boolean. 37/37 checks passed (32 pre-existing + 5 new), zero leftover test data after cleanup (2 tokens cleaned this time, up from 1).

**Specialist Clinic is now the 5th of Phase 67's 23 verticals to have its entire 5-item signature-win set genuinely complete, live-verified, and documented — joining GP Clinic, Physio Clinic, Diagnostic Lab, and Vet Clinic.**

### 15. Dental Clinic's items 21.1 (Treatment-plan conversion tracking) and 21.2 (Treatment acceptance rate report) — its 2nd and 3rd of 5 signature items, closed together

Grounding found `TreatmentPlan` already had a real status flow (PROPOSED/ACCEPTED/IN_PROGRESS/COMPLETED/DECLINED, built back in Phase 25) — so item 21.1's own `[REUSE]` tag ("mirrors Quotation's status-flow precedent") wasn't literally what was missing, since an equivalent flow already existed independently, not borrowed from Quotation. The real gap was the "billed" stage the item's own name calls out: nothing recorded whether an accepted plan ever actually turned into real revenue, so a plan could sit at ACCEPTED indefinitely with zero connection to billing. New `TreatmentPlan.invoiceId` (unenforced FK, same convention as `Appointment.invoiceId`/`TimeEntry.invoiceId` elsewhere in this schema) plus a **Generate Invoice** action on the Treatment Plans tab — mirrors `generateInvoiceForServiceProject`'s own already-proven pattern (`time-entry.service.ts`: one shared "service" product, one invoice line per real chargeable item) rather than inventing a new invoice-generation shape. Each procedure bills as its own line, tooth-tagged where a tooth number was set (e.g. "Tooth #14 — Root Canal"). Guardrails: only a plan that's moved past PROPOSED (and isn't DECLINED) can be billed — billing an unaccepted plan would silently treat a quote as if the patient had already agreed to it — and a plan can only be billed once, enforced by checking `invoiceId` is still null.

Item 21.2 depends directly on 21.1's new data: `reportService.generateTreatmentAcceptanceRateReport({dateFrom, dateTo})` computes a genuine 3-stage funnel over the SAME cohort of plans created in a date range — Proposed (all of them), Accepted (status moved past PROPOSED/DECLINED), Billed (`invoiceId` set) — so `billedCount <= acceptedCount <= proposedCount` always holds, plus acceptance-rate and billed-rate percentages. This codebase has no dedicated funnel-chart component, so the view reuses `ReferralLeaderboardView`'s own horizontal `layout="vertical"` bar-chart pattern rather than introducing a new recharts subcomponent — three ordered, narrowing stages read just as clearly as a ranked bar list. New AI intent `dental.treatmentAcceptanceRate`.

- No new i18n for the screen itself (English-only Dental Clinic UI). Manual: a new paragraph in the existing Treatment Plans section describing Generate Invoice/Billed, plus a brand-new "Reports" section (this chapter didn't have one before), × 13 languages.
- 18 new unit tests: 5 in `treatment-plan.service.test.ts` (bills an accepted plan with one invoice line per priced item; rejects PROPOSED; rejects DECLINED; rejects re-billing an already-billed plan; rejects a plan with no positively-priced items), 2 in `report.service.test.ts` (the funnel narrows correctly across a mixed batch of statuses; an honest empty result), 3 in `ai-vertical-templates.service.test.ts` (intent registration, headline/details shape, empty-state headline), plus regression coverage confirming the pre-existing `createTreatmentPlan`/`updateTreatmentPlan` tests still pass unchanged. Full suite **2763/2763**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 29, extended — not duplicated): created a real treatment plan via the actual New Plan modal, set it to Accepted with one priced, tooth-tagged procedure, confirmed via API it's accepted-and-unbilled, clicked the real **Generate Invoice** button, then confirmed via API that (a) the plan's `invoiceId` is now set, (b) a real `Invoice` row exists with the correct `customerId` and a tax-inclusive total (₹5,000 procedure → ₹5,900 invoice, an 18% tax rate applied correctly), (c) the **Billed** badge renders, and (d) the Treatment Acceptance Rate report's own live API response counts this exact plan in its Billed stage — not just that each piece works in isolation, but that the whole conversion pipeline (UI → billing service → report) is connected end to end. 21/21 checks passed (8 pre-existing + 13 new), cleanup removed the real invoice created (`invoicesRemoved: 1`), zero leftover test data.

Dental Clinic's other 2 items (recall compliance report, tooth-chart-linked treatment timeline) remain fully untouched.

### 16. Dental Clinic's item 21.4 (Recall compliance report) — its 4th of 5 signature items

Grounding confirmed a real structural gap the item's own `[NEW REPORT]` tag undersold: `RecallRecord.patientId` is `@unique` — one row per patient, overwritten in place on every upsert — so a genuine on-time/late gauge report needs history the live table itself cannot provide, since the PRIOR recall period's own due date is destroyed the instant the next upsert overwrites it. Rather than design this from scratch, a repo-wide check for how this exact problem had already been solved turned up `ChronicRecallComplianceLog` (GP Clinic, Section 9.1 item 19) — and its own schema comment, written in an earlier session, explicitly named `RecallRecord`'s upsert-overwrite design as the reason that log had to be its own model rather than reusing `RecallRecord` directly. That comment effectively pre-designed this item's solution before it was ever picked up.

New `RecallComplianceLog` (identical shape to `ChronicRecallComplianceLog`, tied to `RecallRecord` instead of `ChronicConditionRecord`): `id`, `recordId`, `scheduledDate` (the due date being closed), `actualDate` (the new `lastVisitDate`), `onTime`, `createdAt`. `upsertRecall` now wraps its read-then-write in a `db.$transaction`: if a record already exists for the patient, it snapshots `{scheduledDate: existing.nextRecallDate, actualDate: newLastVisitDate, onTime: actualDate <= scheduledDate}` into the log BEFORE the upsert overwrites the record — mirroring `upsertChronicCondition`'s own proven capture pattern exactly. A patient's first-ever recall has no prior due date to compare against, so no log is written on the create path — same "only count what's genuinely comparable" precedent as every other compliance report this phase.

`reportService.generateDentalRecallComplianceReport({dateFrom, dateTo})` (implemented in `recall-record.service.ts`, thin-adapted into `report.service.ts` exactly like `generateChronicRecallComplianceReport`'s own wrapper) reads `RecallComplianceLog`, not the live `RecallRecord` table. The view reuses `ComplianceGauge` for the **third** time this phase (after GP's chronic recall and Vet's vaccination compliance) — proof the component was worth building generically — with a per-`recallType` breakdown table (6-Month Hygiene, 12-Month Hygiene, Crown Review, Custom) instead of per-condition or per-vaccine. New AI intent `dental.recallCompliance`.

- No new i18n for the screen itself (English-only Dental Clinic UI). Manual: a new paragraph in Recall Schedule describing the quiet compliance capture, plus a new Reports paragraph, × 13 languages.
- 14 new unit tests: 6 in a **brand-new** `recall-record.service.test.ts` (this service had zero prior test coverage before this item — create logs nothing; update logs onTime=true when returned early; update logs onTime=false when returned late; the compliance report breaks down per recall type; an honest empty result; respects an explicit date range), 3 in `ai-vertical-templates.service.test.ts` (intent registration, headline/details shape, empty-state headline), plus regression coverage. A real test-authoring mistake was caught and fixed before the suite passed: the mock database initially only stubbed `notificationQueue.deleteMany`, but the real `upsertRecall` also calls `notificationQueue.create` for the 30-day/7-day reminder rows — the first test run failed with `res.success: false` until that was added. Full suite **2772/2772**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 29, extended a third time): updated the SAME patient's recall via the real UI **twice** — the second update genuinely closes out the period the first one opened. Confirmed via the report's own live API response (scoped tightly to the actual due-date window, not "this month") that the closure was captured with `onTime: true` for the `HYGIENE_6M` type, matching the real dates used — proof the whole snapshot-then-report pipeline works end to end, not just that each piece compiles. 24/24 checks passed (21 pre-existing — including this session's own earlier item 21.1/21.2 checks in the same suite — + 3 new), zero leftover test data after cleanup.

Dental Clinic's last remaining item (tooth-chart-linked treatment timeline) is now the only one untouched.

### 17. Dental Clinic's item 21.5 (Tooth-chart-linked treatment timeline) — its 5th and final signature item

Grounding confirmed both halves of this item already existed independently but had never been merged: `ToothRecordHistory` (Phase 58) already gave each tooth a chronological condition-change log, and `TreatmentPlanItem.toothNumber` (Phase 25) already tied individual procedures to a specific tooth — but the tooth **History** panel only ever showed the condition side, so a clinician reviewing one tooth's story couldn't see the procedures that tooth had been through. No new schema was needed. New `getToothTimeline(patientId, toothNumber)`: fetches the tooth's `ToothRecordHistory` rows (`type: 'CONDITION'`), separately fetches every `TreatmentPlan` for the patient and scans each one's `planItems` JSON for entries matching the requested tooth number (`type: 'TREATMENT'`), merges both arrays, sorts newest-first. Treatment-entry dates are deliberately the owning plan's own `createdAt` rather than a fabricated per-procedure completion timestamp, since `TreatmentPlanItem` carries no such field — an honest-over-precise choice consistent with this project's standing convention of never inventing data the schema doesn't actually have.

The tooth History panel (`DentalPatientScreen.tsx`) now renders a genuinely merged timeline: condition entries exactly as before, plus new treatment-entry cards tagged **Treatment Planned** or **Treatment Done** (read off the plan item's own `itemStatus`) showing the procedure name and the originating plan's title — so re-saving a tooth's condition after treatment (say, Caries → Filled) never erases the earlier finding, and the whole "what was found → what was proposed → what was actually done" story for that one tooth is visible in one place for the first time.

- No new i18n for the screen itself (English-only Dental Clinic UI). Manual: the existing Tooth Chart "Click History" paragraph rewritten to describe the merged timeline, × 13 languages.
- 3 new unit tests in `tooth-record.service.test.ts`: merges condition history and matching treatment-plan procedures sorted newest-first; excludes treatment-plan items tied to a different tooth number; includes a treatment-only entry (no condition ever recorded for that tooth). Full suite **2775/2775**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 29, extended a fourth time): tooth #14 already carried a real, billed Root Canal procedure from item 21.1's earlier treatment plan in this same suite run — gave that same tooth a real condition entry via API (`ROOT_CANAL`, with a distinguishing note), then clicked the real tooth-chart button and the real **History** toggle in the UI, confirming both the "Treatment Planned" badge and the condition note's own text render on screen — not just that the panel doesn't crash — and separately confirmed via `window.api.toothRecord.getTimeline` that the raw API response itself contains both a `CONDITION` entry (`condition: 'ROOT_CANAL'`) and a `TREATMENT` entry (`procedure: 'Root Canal'`) for that one tooth. 29/29 checks passed (24 pre-existing + 5 new), zero leftover test data after cleanup.

**Dental Clinic is now the 6th of Phase 67's 23 verticals to have its entire 5-item signature-win set genuinely complete, live-verified, and documented — joining GP Clinic, Physio Clinic, Diagnostic Lab, Vet Clinic, and Specialist Clinic. All 6 clinical verticals in Phase 67's scope are now fully closed.**

### 18. Restaurant's item 2 (Happy-hour pricing windows) — its 2nd of 5 signature items

Grounding confirmed the roadmap's own `[LIKELY GREENFIELD]` prediction: `PricingScheme` only had date-level (`startDate`/`endDate`) windows — no time-of-day concept anywhere. New `startTimeMinutes`/`endTimeMinutes` (minutes since midnight, `0`-`1439`) gate ANY rule type the exact same way `startDate`/`endDate` already gate any rule type generically, rather than being special-cased to one offer type. Neither existing rule type (`BUY_X_GET_Y_FREE`, `SLAB_DISCOUNT`) honestly fits a flat "20% off during these hours" offer — `SLAB_DISCOUNT` with a single breakpoint could technically be repurposed, but its own UI copy talks about quantity thresholds, which would misrepresent what the scheme actually does — so a genuinely new `FLAT_PERCENT_OFF` rule type was added instead. Windows are non-wrapping by design (`endTimeMinutes` must be after `startTimeMinutes`) — an honest, explicit scope limit (a happy hour crossing midnight isn't supported) rather than a silently mishandled edge case, the same "don't overclaim" discipline this phase held for Distributor's incremental-volume report.

`evaluateCart` now takes an injectable `now: Date = new Date()` parameter — defaults to the real clock for production, but lets the new time-window tests assert deterministic behavior without mocking the global `Date`. The per-scheme loop gained a half-open `[startTimeMinutes, endTimeMinutes)` check alongside the pre-existing date-range check. UI (`PricingSchemesScreen.tsx`): a new "Happy-Hour / Flat % Off" offer type in the dropdown, a matching discount-percent input, and a new "Happy-Hour Window (optional)" time-picker pair available on the create form for **every** rule type, not just the new one — a lunch-only combo discount is just as real a use case as a drinks happy hour.

- New i18n: 7 new keys (`ruleType.FLAT_PERCENT_OFF`, `flatPercentSummary`, `timeWindowSuffix`, `happyHourWindow`, `startTimeOptional`, `endTimeOptional`, `happyHourWindowHint`) plus the existing `subtitle` key's value updated to mention the third offer type — all in 13 languages, key parity re-verified (3957/3957 leaf keys). Manual: the shared Pricing Schemes paragraph (`sales-orders-pricing.md`, used across all business types) rewritten to describe the third offer type and the time-window option, plus a new "Happy-Hour Pricing" cross-reference section added to Restaurant's own chapter, both in all 13 languages.
- 6 new unit tests in `pricing-scheme.service.test.ts`: `createPricingScheme` stores `flatDiscountPercent`+the time window; `evaluateCart` applies the flat percent regardless of quantity; applies inside the window (17:00, window 16:00-18:00); skips before the window (14:00); skips exactly at the window's end minute (the half-open boundary, 18:00 sharp); a scheme with no window set applies at any hour. Full suite **2781/2781**, both tsconfigs clean.
- **Live-verified against the real running app, against the genuine system clock — not a mocked one** (suite 64, extended): created two real `FLAT_PERCENT_OFF` schemes via the actual New Scheme modal — one with a window computed to genuinely span the real current wall-clock time (±30 minutes), one with a window fixed at 00:01–00:02 (virtually never "now" whenever this suite actually runs) — then called the real `evaluateCart` API (no `now` override possible over IPC by design, so this is the true production code path) and confirmed the first scheme's 20% discount is suggested right now while the second's 15% discount genuinely is not. 47/47 checks passed (41 pre-existing + 6 new). A real test-authoring bug was caught mid-run: the new cleanup-block variables (`happyHourSchemeName` etc.) were first declared inside the `try` block, invisible to the separate `finally` block's cleanup scope — this threw a `ReferenceError` inside cleanup itself on the first run, silently skipping the price-list/product/customer rows further down the cleanup function (everything before the crash point, including the schemes and sales orders, had already been deleted). Fixed by hoisting the declarations to function scope alongside the suite's other shared IDs; the one leaked run's orphaned rows were swept up manually (verified zero remaining via direct SQL) before re-running clean.

Restaurant's other 3 items (dish-wise contribution margin report, table turnover by hour heatmap, recipe-vs-actual waste variance) remain fully untouched.

### 19. Restaurant's item 3 (Dish-wise contribution margin report) — its 3rd of 5 signature items

Grounding found a real, pre-existing `generateFoodCostReport` (Phase 64) — but it answers a genuinely different question: it totals ACTUAL ingredient consumption from `InventoryMovement` rows tagged by `deductIngredients()`'s own remarks prefix, aggregated per ingredient, not per dish. This item's own name ("dish-wise") needs the opposite direction: revenue minus THEORETICAL recipe cost, per dish, the standard "menu engineering" margin question — which the movement log genuinely can't answer, since a movement's remarks carry only a recipe name string, not which specific sale caused it.

New `getDishIngredientCostsBatch(productIds)` (restaurant.service.ts, alongside `deductIngredients()`): for each requested product, checks `KitComponent` first — if it's a combo/thali, expands into its real component dishes (mirroring `deductIngredients()`'s own kit-expansion exactly, not a parallel reimplementation) and sums each component's own recipe cost, weighted by the kit's component quantity; otherwise looks up the product's own `Recipe` directly. Ingredient costs are resolved via `getProductCostsBatch` (Phase 64's valuation-method-aware selector), batched across every ingredient touched in one call — not a per-product N+1. A dish with no recipe configured returns 0, not a guess, matching this project's own "recipes are entirely optional" precedent.

New `reportService.generateDishContributionMarginReport({dateFrom, dateTo})`: aggregates `InvoiceItem` by product (same RETURN-sign correction as `analytics.service.ts`'s `getTopProducts` — a returned dish must reduce both revenue and quantity, not inflate them), looks up each product's ingredient cost via the batch above, and returns `revenue`, `ingredientCost`, `contributionMargin`, and `marginPercent` per dish, sorted margin-descending. UI reuses the existing bar-chart + `DataTable` pattern already established for `FoodCostReportView`/`ReferralLeaderboardView`. New AI intent `restaurant.dishContributionMargin`.

- New i18n: 12 new keys across `defs`, `col`, `summary`, and `empty` sections, all in 13 languages, key parity re-verified (3969/3969 leaf keys). Manual: a new "Reports" section added to Restaurant's own chapter, explicitly distinguishing this report from Food Cost Report (not previously documented in this chapter either), all in 13 languages.
- 14 new unit tests: 6 in `report.service.test.ts` (revenue/cost/margin computed correctly for a recipe'd dish; a dish with no recipe shows 0 cost/100% margin; a combo correctly expands into its component dishes' recipes; RETURN sign correction; sort order; honest empty result), 5 in `restaurant.service.test.ts` (no-recipe returns 0; non-kit cost computation; kit-expansion mirrors `deductIngredients()`'s own logic; multi-product batching doesn't cross-contaminate), 3 in `ai-vertical-templates.service.test.ts` (intent registration, headline/details shape, empty-state headline). Full suite **2794/2794**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 40, extended): the suite's own "E2E Rest Butter Naan" dish — already sold (2 units) and its KOT already marked Done earlier in the same suite run — was given a real recipe (a known-cost ingredient) AFTER the sale, proving the report is a genuine live aggregation evaluated at report time, not a snapshot frozen at sale time. Confirmed via the real Reports screen (tile click, date range, Generate Report, no crash, the dish's name visible on screen) AND the live API that revenue, ingredient cost (2 units × 2 recipe qty × ₹8 ingredient cost = ₹32), and contribution margin (`revenue - ingredientCost`, exactly) all compute correctly for that exact dish. 22/22 checks passed (12 pre-existing + 10 new), 1 recipe + 2 products swept up in cleanup — confirmed zero leftover test data.

Restaurant now has 3 of its own 5 signature items complete — table turnover by hour heatmap and recipe-vs-actual waste variance remain.

### 20. Restaurant's item 4 (Table turnover by hour heatmap) — its 4th of 5 signature items

Grounding confirmed `KOT.createdAt` gives real hour-level granularity (the roadmap's own open question) — but also surfaced a real structural detail the item's `[VERIFY]` tag hadn't named: `KOT.tableId` is **optional**. `restaurant.service.ts`'s `createKOT` only receives a `tableId` when the invoice itself was opened from a table (`InvoiceDetailScreen.tsx`'s "Send to Kitchen" passes `invoice.tableId ?? undefined`); a counter or takeaway sale produces a KOT with `tableId: null`. A "table turnover" report can only honestly speak to actual dine-in seatings, so this is filtered at the query level (`tableId: { not: null }`), not silently miscounted.

New `reportService.generateTableTurnoverByHourReport({dateFrom, dateTo})`: fetches every table-bound KOT in range, buckets each by its real LOCAL day-of-week and hour-of-day (`Date.getDay()`/`getHours()` — always local-time accessors in Node, no UTC string-parsing risk since this reads a genuine `Date` object rather than a `"YYYY-MM-DD"` string). Returns the complete 168-cell (7×24) grid, zero-filled, rather than only non-zero cells — a heatmap needs every cell colored, including "no activity," not gaps. Summary includes total turns and the single peak day/hour/count.

This codebase has no charting-library heatmap primitive (recharts has no such component), so the UI (`TableTurnoverHeatmapView`) is a hand-built CSS grid — 7 rows × 24 columns, cell background opacity scaled to `count / maxCount`. Rather than hand-translating 7 day names into all 13 locale files, the day-of-week axis is resolved via `Intl`/`toLocaleDateString(i18n.language, {weekday: 'short'})` — a genuinely correct, zero-extra-i18n-content approach that renders in whatever language the user has the app set to, automatically.

- New i18n: 10 new keys across `defs`, `col`, `summary`, `section`, and `empty`, all in 13 languages, key parity re-verified (3978/3978 leaf keys). Manual: a new paragraph in Restaurant's existing Reports section, all in 13 languages.
- 7 new unit tests: 4 in `report.service.test.ts` (buckets a KOT into its correct real local day/hour; sums multiple KOTs landing in the same bucket; the query itself filters `tableId: { not: null }` — a real assertion on the WHERE clause, not just the output; returns the full zero-filled 168-cell grid on an honest empty result), 3 in `ai-vertical-templates.service.test.ts` (intent registration, headline/details shape with the peak day/hour named, empty-state headline). Full suite **2801/2801**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 40, extended a third time): the suite's own KOT (created earlier in the same run via the real "Send to Kitchen" button, but with no table attached since its invoice wasn't opened from one) was bound to its real table via a direct DB update — seeding the precondition, not the feature under test, since the table-binding UI flow itself is already covered by suite 48. Confirmed via the real Reports screen (tile click, date range, Generate Report, no crash, summary cards visible) AND the live API that the report returns a genuine 168-cell grid and that the KOT's own real `createdAt` timestamp — read back independently, not assumed — lands in the exact matching day-of-week/hour cell with the correct count. 29/29 checks passed (22 pre-existing + 7 new), zero leftover test data after cleanup.

Restaurant's last remaining item (recipe-vs-actual waste variance) is now the only one untouched.

### 21. Restaurant's item 5 (Recipe-vs-actual waste variance) — its 5th and final signature item

Grounding confirmed both halves of this comparison already existed as real data, just never paired: recipe-IMPLIED ingredient usage (derivable from dishes sold × their recipe formulas, kit-expansion-aware — the exact same traversal item 3's `getDishIngredientCostsBatch` already established) and ACTUAL ingredient drawdown (the same `InventoryMovement` rows `generateFoodCostReport` already reads, tagged with `INGREDIENT_DEDUCTION_REMARKS_PREFIX`). New `getRecipeImpliedIngredientUsageBatch(dishSales)` (restaurant.service.ts) is a direct sibling to `getDishIngredientCostsBatch` — same `KitComponent`/`Recipe` lookups, same kit-expansion for combos — but aggregates ingredient QUANTITY across every dish sold into one `Map<ingredientId, quantity>`, rather than a cost figure per dish.

New `reportService.generateRecipeWasteVarianceReport({dateFrom, dateTo})` fetches both sides in parallel (`invoiceItem`/`inventoryMovement`, same RETURN-sign correction and remarks-prefix filter as their respective established precedents), merges them by ingredient, and computes `varianceQuantity = actual - implied` plus a percent (left `null`, not a divide-by-zero artifact, when implied is 0). Rows sort by absolute variance descending — the biggest real discrepancies surface first, in EITHER direction: a positive variance (actual exceeds recipe) hints at over-portioning, spillage, or theft; a negative variance (recipe implies more than was drawn) hints at a stale or wrong recipe. Neither Food Cost Report nor Dish Contribution Margin can show this — each only has one side of the same ingredient's story. New AI intent `restaurant.recipeWasteVariance`.

- New i18n: 10 new keys across `defs`, `col`, `summary`, and `empty`, all in 13 languages, key parity re-verified (3988/3988 leaf keys). Manual: a new paragraph in Restaurant's existing Reports section — explicitly distinguishing this report from both reports above it — all in 13 languages.
- 8 new unit tests: 4 in `restaurant.service.test.ts` (empty map for a no-recipe dish; recipe-quantity × units-sold for a non-kit dish; kit/combo expansion weighted by both kit quantity and units sold; the SAME ingredient correctly aggregated across multiple different dishes sold), 4 in `report.service.test.ts` (positive variance/overage computed correctly; an ingredient with real movements but no recipe gets `variancePercent: null`, not a division artifact; sort order by absolute variance; honest empty result). Full suite **2812/2812** (2811 + 1 pre-existing flaky `backup.service.test.ts` timeout re-confirmed clean standalone, per this project's established re-run protocol), both tsconfigs clean.
- **Live-verified against the real running app** (suite 40, extended a fourth and final time): seeded a real `InventoryMovement` row matching the EXACT shape and remarks tag `deductIngredients()` itself creates (6 units actually drawn down for the ghee ingredient), against the recipe+sale already established earlier in the same suite run (implying 4 units should have been used — 2 recipe qty × 2 units of Butter Naan sold). Confirmed via the real Reports screen (tile click, date range, Generate Report, no crash, the ingredient's name visible on screen) AND the live API that `impliedQuantity: 4`, `actualQuantity: 6`, and `varianceQuantity: 2` all compute correctly — a genuine, non-fabricated overage. **A real test-authoring bug was caught and fixed mid-build**: this suite's own earlier seed steps (Product/Inventory/Recipe/RecipeItem, introduced for items 3-4) had all been using SQLite's own `CURRENT_TIMESTAMP` function for `createdAt`/`updatedAt` columns — which produces a TEXT string ("YYYY-MM-DD HH:MM:SS"), not the INTEGER (epoch ms) format every Prisma-native row in this app actually uses, confirmed via a direct query against real rows. A `gte`/`lte` date-range filter silently excludes TEXT-stored rows instead of erroring, so this had gone completely unnoticed through items 3 and 4 (neither of their queries date-filtered the affected tables) until THIS item's own date-filtered `InventoryMovement` query exposed it as a genuine `actualQuantity: 0` failure. Fixed by inserting a real `Date.now()` integer everywhere in the suite instead of `CURRENT_TIMESTAMP`. 37/37 checks passed (29 pre-existing + 8 new), zero leftover test data after cleanup — and as a side effect of finally running with correct cleanup semantics, this run also swept up a substantial backlog of accumulated stale test products from earlier in this session (`productsHandled` dropped from 80 to 8 between runs).

**Restaurant is now the 7th of Phase 67's 23 verticals to have its entire 5-item signature-win set genuinely complete, live-verified, and documented — joining GP Clinic, Physio Clinic, Diagnostic Lab, Vet Clinic, Specialist Clinic, and Dental Clinic.**

### 22. Retail's item 1 (Dead-stock clearance list) — its 1st of 5 signature items, the first item of the first genuinely fresh non-clinical vertical this phase

Grounding found a real, working precedent: `getDeadStock()` (`ai-aggregations.service.ts`, Phase 57) already computes "still in stock, hasn't sold in N days" for the AI Assistant — but its own header comment explains it was deliberately kept OUT of `report.service.ts`, since at the time it had no Reports-screen entry of its own. That reasoning no longer applies now that this item builds exactly that entry — but rather than refactor a working, tested, already-shipped AI-facing function to serve a UI need, this item mirrors its query shape in a new, separate function. The two now answer genuinely different questions anyway: `getDeadStock()` sorts by staleness for a spoken/chat answer, while this report needs actual COST data (which `getDeadStock()` never fetched) to sort by **capital locked** (stock × cost) — the real money sitting idle, the item's own explicit framing.

New `reportService.generateDeadStockClearanceReport({days = 90})`: fetches active products with their current stock and most recent ACTIVE-invoice sale date, filters to those with stock but no sale inside the lookback window, resolves cost via `getProductCostsBatch` (Phase 64's valuation-method-aware selector, the same one every cost-reading report in this phase already uses), and sorts by capital locked descending. A product that has genuinely never sold shows `lastSoldDate: null` rather than an arbitrary date — the UI renders this as "Never Sold," an honest distinction from a product that simply hasn't sold *recently*. New AI intent `retail.deadStockClearance` — deliberately pattern-scoped narrowly (`capital\s+locked`, `clearance\s+list`, not a bare "dead stock" phrase) so it can't shadow the pre-existing universal `inventory.deadStock` fast-path intent, which is checked earlier in an order-sensitive, first-match-wins array; a genuine collision risk caught and avoided during grounding rather than discovered as a bug later.

- New i18n: 10 new keys across `defs`, `col`, `summary`, and `empty` (2 of the needed `col` keys — `product`, `stock` — already existed and were reused rather than duplicated), all in 13 languages, key parity re-verified (3998/3998 leaf keys). Manual: a new "Reports" section added to Retail's chapter — this vertical's manual chapter had none before — in all 13 languages.
- 5 new unit tests in `report.service.test.ts`: a product with stock and no sale in-window is included with correct capital-locked math; a product that sold recently is excluded; a product with 0 stock left is excluded regardless of sale history; sort order by capital locked descending; an honest empty result. A real mocking subtlety handled correctly from the start (not a bug, but worth noting): `db.product.findMany` is called TWICE per report run — once for this report's own query, once internally by `getProductCostsBatch`'s own cost lookup — so tests chain `mockResolvedValueOnce` in that exact call order rather than a single `mockResolvedValue`, which would have answered both calls identically and silently broken one of the two shapes. Full suite **2820/2820**, both tsconfigs clean.
- **Live-verified against the real running app** (new suite 67, `67-retail-dead-stock.js`): created two real products via the actual product-creation API — one left untouched (genuinely never sold), one sold via a real invoice created TODAY (no backdating needed — a sale happening right now is the cleanest possible proof the report correctly excludes recently-active stock). Confirmed via the real Reports screen (tile click, Generate Report — this report has no date range, same category as Inventory Report/Batch Expiry — no crash, the dead product's name visible on screen, the recently-sold product's name genuinely absent) AND the live API that `capitalLocked` (20 units × ₹200 cost = ₹4,000), `currentStock`, and `lastSoldDate: null` all compute correctly for the dead product, while the recently-sold product is correctly absent from the results entirely. 14/14 checks passed, zero leftover test data after cleanup.

Retail's other 4 items (time-boxed markdown workflow, category sell-through rate report, simple loyalty punch-card, basket composition report) remain fully untouched.

### 23. Retail's item 2 (Time-boxed markdown workflow) — its 2nd of 5 signature items

Grounding confirmed this item was genuinely greenfield — no scheduled-price-revert mechanism existed anywhere in the codebase. Rather than invent new scheduling infrastructure, this mirrors `RecurringProfile`'s own already-proven pattern (Phase 63): no real background job scheduler exists in this app at all, just `revertDuePriceMarkdowns()` called on app startup and again every hour via the existing `setInterval` in `src/main/index.ts`, the same mechanism every other scheduled evaluator here already uses.

New `PriceMarkdown` model: `productId`, `originalPrice` (captured automatically from the product's price at creation time), `markdownPrice`, `startDate`/`endDate`, `status` (ACTIVE/REVERTED/CANCELLED/SKIPPED_MANUAL_OVERRIDE), `revertedAt`. Creating a markdown applies the price cut to `Product.sellingPrice` immediately (in the same transaction as the record's creation) — only one ACTIVE markdown is allowed per product at a time (`MKD-002` if violated), forcing a deliberate cancel-then-recreate rather than silently letting two markdowns stack.

The one real design decision this item needed: **what happens if the owner manually changes the product's price while a markdown is still running?** The evaluator checks `product.sellingPrice === markdown.markdownPrice` before reverting — if true (price genuinely untouched since the markdown started), it reverts cleanly to `originalPrice` and marks the record REVERTED. If false (the owner made a manual pricing decision in the meantime), the automatic revert is SKIPPED rather than silently overwriting that decision — the record still closes out, marked SKIPPED_MANUAL_OVERRIDE ("Manually Changed" in the UI) rather than REVERTED, so this is visible in the markdown's own history rather than indistinguishable from a normal close. Cancel applies the identical check.

- New i18n: full `priceMarkdowns.*` namespace (~24 keys: title/subtitle, table columns, status labels, modal fields, toasts, empty state) plus `nav.priceMarkdowns` and a new flagship Tutorial entry (`tour.items.priceMarkdowns`), all in 13 languages, key parity re-verified (4029/4029 leaf keys). Manual: a new "Price Markdowns" section added to Retail's chapter (between the existing Reports section and the shared "What's shared with every business" section), all 13 languages, heading-count parity re-verified (4→5 headings per file across all 13).
- 11 new unit tests in a new `price-markdown.service.test.ts` file: create validation (product not found, end date not in future, duplicate active markdown rejected), price capture and immediate application on create, cancel's two branches (price still at markdown reverts; price already changed leaves it alone), the evaluator's two branches (clean revert; skip-on-manual-override) plus its due-query shape and an honest empty-result case. Two real test-authoring bugs were found and fixed along the way, not shipped: the default mock `product` object was initially missing an `update` spy (`TypeError: undefined is not a spy` in 5 tests), and several individual test overrides of the nested `priceMarkdown` mock object omitted `update` — this test helper's `{...overrides}` spread is a shallow merge, so overriding one nested key silently drops any other sub-mock (like `update`) not re-specified in that same override. Full suite **2831/2831**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 67 extended a second time, 20 new checks, 34/34 total for the file): created a markdown for a real product via the actual `PriceMarkdownsScreen` UI (product select, markdown price, end date) and confirmed via the live API that the product's `sellingPrice` changed immediately, matching the new markdown row's own fields exactly. Confirmed a second markdown attempt on the same still-active product is correctly rejected (`MKD-002`). Seeded two more markdowns directly via raw SQL with a genuinely **past** `endDate` — using a literal `Date.now()` epoch-ms integer, never SQLite's `CURRENT_TIMESTAMP`, the exact lesson learned the hard way while building Restaurant's item 5 earlier this phase (`CURRENT_TIMESTAMP` produces a TEXT string; every Prisma-native `DateTime` column in this app is actually stored as an INTEGER, so a `lte` filter silently excludes TEXT-seeded rows) — then called the real `evaluateNow` IPC handler and confirmed both evaluator branches live: the clean case reverted the product's price back to original and marked the record REVERTED; the manually-overridden case (product's price set away from the markdown price before evaluation) left the price genuinely untouched and marked the record SKIPPED_MANUAL_OVERRIDE. Confirmed Cancel's own two branches the same way against two more real markdowns: price-still-at-markdown reverts to original immediately on cancel; price-already-changed is left alone by cancel. One test-authoring mistake was caught and fixed mid-build: `products.update` requires the full product payload (`UpdateProductSchema` has no partial-patch mode) and rejects an explicit `null` on several `.optional()`-but-not-`.nullable()` string fields — fixed by fetching the current product row and stripping `null`-valued keys before resubmitting with the one changed field. Also fixed a related, more general E2E-harness gap while here: `cleanupByNamePrefix()` deleted `Inventory` rows for a test product before deleting the product itself, but never deleted its `PriceMarkdown` rows — a lingering FK reference that would have silently forced the harness's soft-delete fallback (leaking rows) for any future test creating a markdown on a prefix-matched product; added the missing `DELETE FROM PriceMarkdown WHERE productId = ?` step.

Retail's remaining 3 items (category sell-through rate report, simple loyalty punch-card, basket composition report) remain fully untouched.

### 24. Retail's item 3 (Category sell-through rate report) — its 3rd of 5 signature items

Grounding confirmed `ProductCategory` already exists as the original tag predicted (pure aggregation) — but also found this app has **no historical opening-stock snapshot per month anywhere**. Only two data sources exist: `InventoryMovement`'s append-only ledger (every stock change, no running-balance column) and `Inventory.quantity`'s current total. A genuinely correct per-month opening balance would require walking that ledger backward from today for every product in every category across the requested range — a much heavier, more fragile computation for a marginal precision gain, and a real risk surface (products created/deleted mid-range, location dimensions, edge cases in ledger completeness).

Deliberate, disclosed simplification shipped instead — the same honesty standard as Distributor's own Scheme Cost report shipping as a correlation view rather than fabricating a causal one: every month in the requested range is compared against the **same current stock-on-hand per category**, not that month's own historical level. This is a trend-over-current-inventory view, not a true point-in-time historical one, and both the UI (a caption directly under the chart heading) and the Manual chapter say so explicitly, in those words.

New `reportService.generateCategorySellThroughReport({dateFrom, dateTo})`: fetches every `ProductCategory`, sums `Inventory.quantity` per category from its active, categorized products (the current-stock side of the ratio), then buckets `InvoiceItem` rows by real local month and category (same `groupLabel(date, 'month')` helper every other monthly report in this file already uses) to compute `unitsSold`. Every month × category combination is zero-filled across the full requested range — a category with zero sales in a given month still renders as a real 0% bar, not a gap the chart would silently skip. Same RETURN-sign correction as every other report in this file (`dishContributionMargin`, `tableTurnoverByHour`, etc.) — a returned sale reduces `unitsSold`, never inflates it. A month where returns genuinely exceed sales clamps its **rate** to 0% (never negative), while the raw signed `unitsSold` is still shown honestly in the table rather than hidden — the same "show the real number, don't paper over an edge case" convention as `variancePercent: null` in the waste-variance report and `capitalLocked` in dead-stock.

The UI is this codebase's first genuinely new **grouped bar chart** — multiple `<Bar>` series (one per category) sharing a single month-keyed x-axis, using an 8-color cycling palette local to the new view component (no existing shared multi-series palette to reuse). New AI intent `retail.categorySellThrough`, added alongside `retail.deadStockClearance` in `RETAIL`'s vertical-template array; its fast-path pattern (`/sell[\s-]?through/i`, a category-framed variant) was checked against the existing pattern array before being added — no collision found with the pre-existing revenue-framed `topCategories` intent, a genuinely different question.

- New i18n: `defs.categorySellThrough` (label/description), 3 new `summary.*` keys (chart heading, the honest current-stock-comparison caption, the export-summary chart title), 2 new `col.*` keys (`unitsSold`, `sellThroughRate` — `month`/`category`/`stock` all reused from existing keys, not duplicated), 1 new `empty.*` key — 8 new keys total, all 13 languages, key parity re-verified (4037/4037 leaf keys). Manual: a new paragraph appended to the end of Retail's existing "## Reports" section (no new heading needed — this is a second report within the same section, matching how Restaurant's dish-margin/table-turnover/waste-variance items each added a paragraph rather than a heading), all 13 languages.
- 7 new unit tests in `report.service.test.ts`: the core rate formula (`unitsSold ÷ (unitsSold + currentStock)`), zero-filling a quiet single month, zero-filling every month across a 3-month range, the RETURN sign correction reducing `unitsSold`, the returns-exceed-sales clamp-to-0%-not-negative case (with the raw signed figure still asserted honestly), and an empty-result case when no categories exist. Plus 2 new AI-intent tests in `ai-vertical-templates.service.test.ts` (registration alongside `retail.deadStockClearance`; leading-category-this-month headline with details sorted by rate descending; the empty-this-month case). Full suite **2840/2840** (2839 + 1 pre-existing flaky `backup.service.test.ts` timeout re-confirmed clean standalone, per this project's established re-run protocol), both tsconfigs clean.
- **Live-verified against the real running app** (suite 67 extended a third time, 12 new checks, 45/45 total for the file): created a real product category via the actual category-creation API, a real product inside it with a genuine 30-unit opening stock, and a real 10-unit sale via the actual billing/invoice API. Confirmed via the real Reports screen (tile click, fill the month's date range, Generate Report — no crash, the category name visible on screen, the grouped bar chart rendering) AND the live API that `unitsSold` (10), `currentStock` (20 — read from the product's real post-sale inventory via a live API call, never hardcoded), and `sellThroughRate` (33.3%, computed independently in the test as `10÷(10+20)×100` and matched exactly) all compute correctly against genuinely current, non-fabricated data. Also fixed a related, more general E2E-harness gap discovered while building this: `cleanupByNamePrefix()` (`tests/e2e/harness.js`) had no handling for `ProductCategory` rows at all — this is the first-ever E2E test in this codebase to create one, and without the fix it would have leaked forever (no other suite has ever needed to clean one up). Fixed by adding a category-deletion pass, deliberately ordered LAST in the function (after every referencing product is already deleted above it) so the foreign key never blocks it, wrapped in try/catch so an unexpected still-referenced category doesn't fail the rest of cleanup.

Retail's remaining 2 items (simple loyalty punch-card, basket composition report) remain fully untouched.

### 25. Retail's item 4 (Simple loyalty punch-card) — its 4th of 5 signature items

Grounding confirmed this item was genuinely greenfield — grepping the entire schema for any loyalty/punch-card/rewards concept returned zero hits. The roadmap's own grounding note explicitly flagged a real scoping decision here rather than a default: build the mechanism Retail-specific (matching this item's own actual audit placement) or generic enough for any product vertical to opt into later. Resolved toward Retail-specific — no other item across Phases 67, 68, or 69 needs a loyalty concept by name, so generalizing now would be premature abstraction rather than a real, evidenced requirement, consistent with this project's standing "don't design for hypothetical future requirements" discipline.

New singleton `LoyaltyProgram` config row — one program per install, mirroring `BusinessProfile`'s own already-established `findFirst()`-based singleton pattern rather than inventing a new one. `LoyaltyCard` holds one row per customer (`currentPunches`, `totalPunchesEarned`, `totalRewardsRedeemed`), with append-only `LoyaltyPunchEvent` and `LoyaltyRedemption` history tables underneath it for a genuine audit trail, not just a running counter. A punch is earned **fully automatically** — `loyaltyProgramService.recordPunchTx()` is called from inside `billing.service.ts`'s own `createInvoice` transaction whenever a sale's `totalAmount` meets the configured `minPurchaseAmount`, immediately after GL posting, mirroring exactly where the credit-limit and GL-posting hooks already sit in that same transaction. `recordPunchTx` never throws — any failure inside it is caught internally — so a loyalty-tracking problem can never block a real sale from completing, the same resilience principle this codebase already applies to non-critical side effects elsewhere.

The one genuine design decision this item needed: **what happens to punches beyond the reward threshold?** Redeeming subtracts exactly `punchesRequired` from `currentPunches` rather than resetting the counter to 0 — a customer who accumulated 7 punches against a 5-punch threshold keeps 2 toward their next reward after redeeming, rather than losing them. `LoyaltyRedemption` snapshots the reward text at redemption time, so editing the program's `rewardDescription` later never silently rewrites what past redemptions say they gave out.

New AI intent `retail.loyaltyProgress` (via a new `loyaltyProgramService.getSummary()` aggregate: total cards, how many are currently ready to redeem, rewards redeemed this month) — checked against the fast-path pattern array for collisions before writing (`loyalty`, `punch[\s-]?card`, `ready.*(redeem|reward)`), none found.

- New i18n: `nav.loyaltyProgram` plus the full `loyaltyProgram.*` namespace (~26 keys: title/subtitle, program-settings fields, table columns, redeem flow, empty states, toasts) and a new flagship Tutorial entry — 29 new keys total, all 13 languages, key parity re-verified (4066/4066 leaf keys). Manual: a new "Loyalty Program" section added to Retail's chapter (after Price Markdowns, before the shared closing section), all 13 languages, heading-count parity re-verified (5→6 headings per file across all 13).
- 16 new unit tests in a new `loyalty-program.service.test.ts` file: program create-vs-update branching, card `readyForReward` flagging at and below the threshold, the `readyForRewardOnly` filter, redemption's three rejection paths (no program / inactive program / insufficient punches) plus the surplus-preserving subtract-exactly-`punchesRequired` success path, `recordPunchTx`'s three no-op guards (no program / inactive / below minimum) plus its qualifying-sale success path and its never-throws guarantee under a simulated DB failure, and `getSummary`'s not-configured and fully-populated cases. Plus 2 new AI-intent tests. One real test-mock bug was caught and fixed along the way — the exact shallow-merge class this session already hit twice before with `PriceMarkdown`'s own test file: overriding the `loyaltyCard` mock's `findUnique` in one test silently dropped the default `update` sub-mock, since this test helper's `{...overrides}` spread replaces the whole nested object rather than merging into it. Full suite **2859/2859**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 67 extended a fourth time, 20 new checks, 58/58 total for the file): configured the program via the real Settings modal on the actual `LoyaltyProgramScreen` UI (3 punches required, a named reward, a ₹100 minimum purchase) and confirmed the saved values round-tripped correctly through the live API. Created a real customer and made 3 real qualifying sales via the actual billing API — confirmed via the live API that the customer's card genuinely reached exactly 3/3 punches (`readyForReward: true`) with zero manual staff action anywhere in the flow. Made a fourth sale below the configured minimum and confirmed the punch count stayed at 3, not 4 — proving the `minPurchaseAmount` gate is real, not decorative. Redeemed the reward via the real UI (customer row → Redeem → confirm) and confirmed via the live API that `currentPunches` dropped to exactly 0 (the surplus-preserving subtraction proven with a value that has no surplus to hide a reset-to-0 bug behind) and `totalRewardsRedeemed` incremented to 1. Three real test-authoring bugs were caught and fixed mid-build: two Playwright strict-mode violations (two "Program Settings" buttons rendering simultaneously on the program's empty state — the header's own plus the empty-state CTA's identical one — fixed with `.first()`; and `getByLabel('Reward')` substring-matching the unrelated "Punches Needed for a **Reward**" field too, fixed with `{exact: true}`) and a stale-data gotcha where re-issuing the same hash-route navigation is a same-route no-op that never re-triggers the screen's own data fetch — fixed by navigating away and back, exactly what a real user clicking elsewhere and returning would do. Also fixed a related, more general E2E-harness gap while here: `cleanupByNamePrefix()` had no `LoyaltyCard`/`LoyaltyPunchEvent`/`LoyaltyRedemption` handling at all — since `LoyaltyCard.customerId` is `ON DELETE RESTRICT`, a leftover card on any future test customer would have silently forced the soft-delete fallback (leaking rows) instead of a real cleanup; added deletion in FK-dependency order, before the customer-deletion loop.

**A separate, more consequential gap was found and fixed while verifying migration state for this item, unrelated to the loyalty feature itself but discovered in the course of building it**: running `prisma migrate status` (part of this item's own migration-application check) revealed that 12 of this phase's earlier migrations — spanning dental, specialist, vet, restaurant, and retail price-markdown work from earlier in this same session — had genuinely live schema changes (their tables demonstrably existed and had already been live-tested end-to-end) but were **missing their `_prisma_migrations` bookkeeping rows entirely**. This is the exact failure mode this project's own memory already documents for Prisma migration commands run in the wrong shell against a Windows sqlite path — the earlier `prisma migrate resolve --applied` calls this session had silently not persisted. All 12 were re-resolved via PowerShell with an explicit absolute `DATABASE_URL`, confirmed clean via `prisma migrate status` reporting "Database schema is up to date!" This was a real, consequential fix rather than pure bookkeeping hygiene: an eventual `prisma migrate deploy` (e.g. while building the next installer) would have attempted to `CREATE TABLE` for 12 already-existing tables and failed loudly, a genuinely bad time to discover a session-old gap.

Retail's last remaining item (basket composition report) is now the only one untouched.

### 26. Retail's item 5 (Basket composition report) — its 5th and final signature item, closing the vertical

Built as predicted: a pairwise co-occurrence aggregation over `InvoiceItem` rows grouped by invoice — "which products get bought together in the same basket." `reportService.generateBasketCompositionReport({dateFrom, dateTo})` deduplicates products per basket before pairing (a repeat-quantity line of the same product is never counted as pairing with itself), and keeps each pair key order-independent via `[aId, bId].sort()` — the same pair of products seen across two different baskets always collapses into a single row with an incrementing `basketCount`, rather than splitting into two rows depending on which product happened to appear first on each invoice. RETURN invoices are excluded from the query entirely rather than sign-corrected, a deliberate choice distinct from how every quantity/revenue-style report in this file handles returns: a returned basket's item PAIRING was never a genuine co-purchase decision the buyer actually made at checkout, so there is no honest "negative pairing" to compute the way a negative quantity or revenue figure can be. Single-item baskets are still counted toward the summary's `totalBaskets`/`avgItemsPerBasket`/`avgBasketValue` — they're real baskets — but correctly produce zero pairing rows, since a lone item has nothing to pair with; this required accumulating those summary totals for every basket before the `< 2 products` pairing-skip, not after, a genuine off-by-scope bug caught and fixed during the function's own first draft (an initial version that accumulated the totals only for pairing-eligible baskets would have silently understated both averages).

New AI intent `retail.basketComposition` — checked against the fast-path pattern array for collisions before writing (`basket\s+(composition|analysis)`, `bought\s+together`, `(products?|items?).*(together|pair)`), none found.

- New i18n: `defs.basketComposition` (label/description), 4 new `summary.*` keys (total baskets, avg items per basket, avg basket value, the top-pairs chart title), 3 new `col.*` keys (`productA`, `productB`, `basketCount`), 1 new `empty.*` key — 10 new keys total, all 13 languages, key parity re-verified (4076/4076 leaf keys). Manual: a new paragraph appended to the end of Retail's existing "## Reports" section (its third report paragraph now, alongside Dead-Stock Clearance and Category Sell-Through — no new heading needed), all 13 languages.
- 8 new unit tests in `report.service.test.ts`: a single 2-item basket producing exactly one pair, the same pair seen with reversed item order across two baskets collapsing into one row with `basketCount: 2` (not two separate rows), a 3-item basket producing exactly 3 pairs (`C(3,2)`, verifying real combinatorial correctness rather than a hardcoded pair count), a single-item basket counting toward the summary averages while producing zero pairing rows, descending sort order, an honest empty result, and a direct assertion that the underlying query excludes RETURN invoices via `invoiceType: { not: 'RETURN' }` rather than fetching and sign-correcting them. Plus 2 new AI-intent tests. Full suite **2869/2869**, both tsconfigs clean.
- **Live-verified against the real running app** (suite 67 extended a fifth time, 9 new checks, 67/67 total for the file): created two real, separate invoices via the actual billing API, each genuinely pairing the same two real products — confirmed via the live API that the pair's `basketCount` aggregates to exactly 2 (a real cross-transaction aggregation, not a per-invoice flag or a hardcoded value), and via the real Reports UI that both product names render on screen. Created a third real single-item invoice for an unrelated product and confirmed via the live API that it produces zero pairing rows for either of the two paired products, while the summary's `totalBaskets` genuinely reflects all of this month's real invoices in the dev DB (not a scoped-down count limited to this test's own fixtures) — a live aggregation over real, pre-existing accumulated data, the strongest possible proof this isn't reading from a mock.

**Retail is now the 8th of Phase 67's 23 verticals with its entire 5-item signature-win set genuinely complete, live-verified, and documented** — joining GP Clinic, Physio Clinic, Diagnostic Lab, Vet Clinic, Specialist Clinic, Dental Clinic, and Restaurant. Retail is the second of the 17 Product & Retail / Legacy Generic Service verticals to fully close, after Restaurant.

### 27. Hardware's items 1+2 (Contractor running account + Contractor monthly statement) — its 1st and 2nd of 5 signature items, closed together

Dedicated grounding research before writing any code — not assumed from the roadmap's own item tags — found both items were already fully solved by infrastructure this project shipped back in Phase 61, a genuinely different outcome than either item's own predicted tag (`[VERIFY]` and `[NEW REPORT]`).

**Item 1's "running account" is already exactly how CREDIT sales work today, for any customer, not something Hardware-specific needed building.** Reading `billing.service.ts`'s `createInvoice` confirmed a CREDIT sale never creates a `Payment` row at sale time — instead it posts a `CustomerLedger` debit entry, and `Customer.outstandingBalance` stays denormalized-in-sync via `customer-ledger.service.ts`'s own `addEntry`. Nothing about this is scoped to one invoice at a time; a contractor's balance simply accumulates across every CREDIT sale they make until they settle it. Zero new schema, zero new service code.

**Item 2's "monthly statement" already exists too, under a different name.** `generateCustomerLedgerReport()` (Phase 61) already produces exactly a customer statement — search/select the customer, an optional date range, opening balance, every transaction in date order, closing balance — and its screen already has PDF export for free via the `ShareMenu` component every single report in this file already gets, confirmed by reading `ReportsScreen.tsx`'s own `handleExportPdfForShare`. The one genuinely missing piece across both items, after this grounding: **no chart existed on this report at all.**

Added a running-balance trend line to `LedgerReportView` — the one shared component behind both the Customer Ledger AND Supplier Statement reports, so this benefits both sides of the same mechanism, not just Hardware's own customer-facing half.

**A real crash was found via live stress-test verification, not a unit test** — the same "mocked Prisma can't reproduce real-scale behavior" lesson this project has learned the hard way in prior phases. Plotting one point per ledger row worked fine on small fixtures but crashed the Electron renderer entirely against `tests/e2e/suites/09-stress.js`'s existing 5,000-row heavy-ledger customer — thousands of unique XAxis category tick labels is a documented Recharts performance cliff, not a data-volume issue with the line itself. Fixed by evenly sampling down to at most `LEDGER_CHART_MAX_POINTS` (150) plotted points rather than every row, while always keeping the genuine opening-balance point and the true final row's point so the trend's start and end are never distorted by sampling — only its interior resolution is. A disclosed "showing a representative sample, not every entry" caption appears whenever sampling actually kicks in; the full table below the chart is never sampled, every row still lists there untouched.

**A second, entirely unrelated pre-existing bug was uncovered while stress-testing this fix, not introduced by it**: suite 09's own 5,000-row bulk ledger seed had never set `createdAt` on its raw `INSERT` statement, silently falling back to SQLite's own `CURRENT_TIMESTAMP` function — which produces a TEXT string, not the epoch-ms INTEGER every Prisma-native `DateTime` column in this app actually stores (the exact lesson this project already learned once this phase, in Restaurant's own item 5). `generateCustomerLedgerReport`'s date-range filter silently excluded every one of those 5,000 TEXT-stored rows — meaning this "5,000-row stress test" had been quietly exercising a completely EMPTY report the entire time its "no crash" and "renders within 8s" checks were passing, undetected until this exact live-verification pass finally needed the real data to actually be there. Fixed with a literal epoch-ms integer per row, spread across the last 30 days so the report's own date range genuinely includes them; the E2E test itself was also hardened to explicitly set a wide date range rather than relying on whatever the screen's own default happened to be, and to wait for the chart's own heading element rather than a blind timeout that a 5,000-row DataTable render could legitimately outlast.

- i18n: no new namespace — one new `reports.summary.balanceTrend`/`balanceTrendSampledNote` key pair, reusing the already-existing `reports.col.openingBalance` and `common.balance` labels rather than duplicating them. All 13 languages, key parity re-verified (4078/4078 leaf keys). Manual: the existing universal `reports.md` chapter's Customer Ledger paragraph was extended in place (this is genuinely universal, not Hardware-specific — every business type with customers/suppliers benefits, not just Hardware's contractors), all 13 languages.
- No new unit tests: this is a pure UI-layer reuse of an already-tested, already-shipped report function (`generateCustomerLedgerReport`, covered by its own Phase 61 test suite) with zero new backend logic — nothing new to unit-test at the service layer.
- **Live-verified against the real running app** — the strongest available proof, a genuine 5,000-row account rather than a toy fixture (`tests/e2e/suites/09-stress.js` extended, 22/22 total for the file after both fixes): confirmed the balance-trend chart heading renders without crashing at real scale, the sampled-note caption correctly appears (proving the sampling code path is genuinely exercised, not just the small-account path), and — via the live API, independent of the UI — that the report now genuinely returns close to all 5,000 rows (`rows=5000` observed), proving the `CURRENT_TIMESTAMP` fix actually surfaced the real data rather than just suppressing the crash symptom.

Hardware's other 3 items (smart carton-break reorder trigger, fast-mover vs. slow-mover matrix, live area-pricing margin preview) remain fully untouched.

### 28. Hardware's item 3 (Smart carton-break reorder trigger) — its 3rd of 5 signature items

Grounding research confirmed `sellByPack`/`unitsPerPack` (Phase 58) was already a fixed-ratio pack-to-piece conversion, but reading the codebase found it was consumed in exactly one place: `StockAdjustmentModal.tsx`'s "packs received" stock-in entry mode. Neither of the two places this item's own name actually points at — the reorder-PO generator, and the Inventory Report's own stock display — read it at all, both ordering and displaying purely in raw pieces even for a pack-billed product.

**A deliberate scope-narrowing decision was made before writing any code.** The research also surfaced a much larger possible feature: true per-carton "opened vs. sealed" state tracking, where breaking a carton open is itself a trackable inventory event with its own timestamp and remaining-loose-piece state. That is a materially bigger, different feature — a real inventory-state model, not a display/rounding change — and this item's own name ("reorder trigger") doesn't ask for it. Built the narrower, honestly-scoped interpretation instead: reframe the *existing* reorder trigger and stock display in carton terms, nothing more.

New `roundUpToCartonMultiple(quantity, sellByPack, unitsPerPack)` in `purchase-order.service.ts` — a pure function, `Math.ceil(quantity / unitsPerPack) * unitsPerPack` when the product is pack-billed, an identity pass-through otherwise. Wired into `generateReorderDraftPOs()`'s per-supplier item-building loop, applied to `inv.reorderQuantity` before it's pushed onto the draft PO. A supplier sells whole cartons, not a fractional piece count — a draft asking for "40 pieces" was never actually orderable as written; for a 24-per-carton product it now correctly drafts 48 (2 whole cartons), not 40.

`generateInventoryReport()`'s `InventoryReportRow` gained a `cartonBreakdown: {unitsPerPack, fullCartons, loosePieces} | null` field, computed via floor division (`Math.floor(stock / unitsPerPack)` cartons, `stock % unitsPerPack` loose pieces), `null` when the product isn't pack-billed or has no `unitsPerPack` set. `InventoryReportView`'s stock cell now shows the flat count alongside this breakdown when present — e.g. "30 (1 cartons + 6 pcs)" — never replacing the flat number, since the flat piece count is still what every other part of the app (billing, valuation) actually operates on.

- Unit tests: 6 new (`purchase-order.service.test.ts` — rounds up to the next carton multiple, leaves a non-pack-billed product's quantity untouched, a quantity that's already an exact carton multiple is a no-op; `report.service.test.ts` — full-carton-plus-loose breakdown, exact-multiple breakdown with zero loose pieces, `null` breakdown for a non-pack-billed product). Full suite **2875/2875**. Both tsconfigs clean.
- i18n: no new namespace needed — reused the existing `reports.val.cartonsAndPieces` key (already added earlier this phase for a different Hardware-adjacent display). All 13 languages, key parity unchanged.
- Manual: two new paragraphs added to Hardware's existing chapter — one appended to the already-existing "Carton/box unit conversion" section (describing both the Inventory Report display and the reorder-PO rounding), one appended to the already-existing "Credit limit enforcement" section (a small unrelated addition carried over from item 1+2's balance-trend-chart work that hadn't yet been documented in this vertical's own chapter). No new headings — heading count stayed at 6 per language file. All 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/17-hardware-area-pricing.js` extended a third time, 23/23 total for the file): seeded a real Supplier and a real 24-units-per-carton product with a genuine 30-piece opening stock (1 full carton + 6 loose) and a 50-piece reorder level — a real due-for-reorder product, not a contrived edge case — with `reorderQuantity: 40`. Called the real `generateReorderDraftPOs()` IPC handler and confirmed via the real drafted `PurchaseOrderItem` row that its quantity is 48, not the raw 40. Separately navigated to the real Reports screen, generated a real Inventory Report, and confirmed the product's row genuinely renders "1 cartons + 6 pcs" next to its flat 30.
- **Two real, unrelated bugs were found and fixed via this same live-verification pass**: the new E2E step itself had called a nonexistent `window.api.purchaseOrders.getPO` (the real preload-exposed method is `.get`) — caught immediately as a thrown error, not a silent false-pass. And a pre-existing area-calculator test in the same suite intermittently hit a genuine Playwright strict-mode double-match on the "L" input during a fast page transition (confirmed via a one-off debug dump of the matched elements' `outerHTML`, then confirmed the duplicate was a transient render, not a real second calculator instance) — hardened with `.first()`.
- Also generalized `tests/e2e/harness.js`'s `cleanupByNamePrefix()` a fourth time this phase, adding `Supplier`/`PurchaseOrder`/`PurchaseOrderItem` cleanup (both RESTRICT FKs, ordered dependents-first) — this is the first-ever E2E suite in the codebase to create a real Supplier + a real drafted PurchaseOrder, so without this addition the fallback soft-delete path would have silently leaked rows on every future run.

Hardware's remaining 2 items (fast-mover vs. slow-mover matrix, live area-pricing margin preview) remain fully untouched.

### 29. Hardware's item 4 (Fast-mover vs. slow-mover matrix) — its 4th of 5 signature items

Grounding research (a codebase-wide grep for "velocity") found no sales-velocity concept existed anywhere in this codebase before this item — a genuinely new report, not the reuse the item's own spec tag ("aggregation over existing sales-velocity data") speculated might be possible.

New `reportService.generateFastSlowMoverMatrixReport({dateFrom, dateTo})`: aggregates `InvoiceItem` rows per product over the requested range (RETURN-sign-corrected the same way every other report in this file already is), computes velocity as net units sold ÷ days in range, and margin % from the same `getProductCostsBatch()` cost basis the Dead-Stock and GSTR reports already use. **Quadrant split by the MEDIAN of each axis, not a fixed absolute threshold** — computed only over products that actually sold in the period, since a flat "$X margin, Y units/day" cutoff would be meaningless comparing a single small hardware shop against a much larger one. This directly matches the item's own "matrix" framing (a real 2×2 grouping, not just an unstructured scatter): fast+high-margin, fast+low-margin, slow+high-margin (worth keeping despite low velocity), slow+low-margin (the clearest candidates for discontinuation).

First genuinely new scatter-plot chart in this codebase's Reports module — `recharts`'s `ScatterChart`/`Scatter`, one series per quadrant (for legend + color), dashed `ReferenceLine`s marking both medians so the four quadrants are visually obvious, not just implied by dot color. New AI intent `hardware.fastSlowMoverMatrix` — the first-ever Hardware vertical AI intent this codebase has had, which required adding a brand-new `HARDWARE` case to `ai-vertical-templates.service.ts`'s dispatcher switch (previously absent entirely) and a `hardware: 'vertical'` category mapping to `ai-llama-provider.ts`'s `CATEGORY_BY_PREFIX`.

- Unit tests: 8 new (`report.service.test.ts` — 5 tests: velocity-from-days-in-range computation, RETURN sign-correction, exclusion of a net-zero-or-negative product, a 4-product median-split quadrant-classification test proving all four quadrants assign correctly, and an honest-empty-result test; `ai-vertical-templates.service.test.ts` — 3 tests: registration, headline/laggard-count reporting, empty-state). Full suite **2883/2883** (the one failure surfaced on a full-suite run is the pre-existing, previously-documented `backup.service.test.ts` timeout flake — confirmed clean on an immediate standalone re-run, not a regression). Both tsconfigs clean.
- i18n: 13 new keys (report label/description, 3 new column headers — velocity/marginPercent/quadrant, 4 new summary labels, 4 quadrant-value labels, 1 empty-state message), all 13 languages, key parity re-verified (**4092/4092 leaf keys**, up from 4079).
- Manual: new "Fast-mover vs. slow-mover matrix" section added to Hardware's chapter (7 headings now, up from 6), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/17-hardware-area-pricing.js` extended a fourth time, 31/31 total for the file): created a real 80%-margin product that sold 20 units and a real 10%-margin product that sold only 2 units via the real billing API, then confirmed via the live `reports.fastSlowMoverMatrix` IPC channel that both appear in the report with the correct relative velocity and margin ordering (asserted independently of exact quadrant labels, which legitimately depend on whatever else genuinely sold that day in the shared dev DB — the per-product figures themselves are what's asserted as stable). Separately confirmed the real Reports UI tile renders both products by name with zero crash.

**Two real, unrelated bugs were found and fixed via this item's own live-verification pass — neither caught by either typecheck or the unit suite:**

1. **A fatal renderer-crashing bug, the most serious found this entire phase.** A new `QUADRANT_COLORS` module-level constant referenced `STATUS_COLORS.success`/`.brand`/`.warning`/`.danger`, but was declared far earlier in `ReportsScreen.tsx` than `STATUS_COLORS` itself — a genuine JavaScript temporal-dead-zone `ReferenceError` at module load time. This crashed the ENTIRE renderer before React ever called `.render()` — not just this one report, every screen in the app, since `main.tsx`'s own top-level import chain never completed. The symptom was a completely blank white window with no login form, no error boundary, and no console/pageerror output visible through the normal E2E harness's own `pageerror` listener (which attaches only after the window is already considered "loaded," by which point the crash had already silently halted execution). `tsc --noEmit` on both configs reported zero errors throughout — TypeScript's default checks do not include out-of-order module-level `const` reference analysis, and the unit suite never loads the real renderer bundle at all, so neither of this project's two automated safety nets could have caught this class of bug. It was found only by writing a dedicated one-off diagnostic script that attached a `pageerror` listener to the Electron app's very first `window` event (before any navigation or timeout), which surfaced the exact `ReferenceError` and file/line. Fixed by hardcoding the four quadrant colors' literal hex values instead of referencing `STATUS_COLORS`, with a comment explaining why (to prevent a future contributor from "cleaning this up" back into the same bug).
2. **A UTC-vs-local-date mismatch in the new E2E test itself**, not the app: the test's "today" date was computed via `new Date().toISOString().slice(0,10)`, which returns the UTC calendar date — this project has hit and documented this exact anti-pattern before (`ai-aggregations.service.ts`'s own dated bug-fix comment). Depending on the local machine's timezone offset and time of day, this can silently resolve to the *previous* calendar day relative to what the app itself considers "today" (which uses local-timezone date arithmetic throughout). This caused the test's own live-API date-range query to miss the just-created test invoices entirely (`totalRows: 0`) even though the real UI, driven by the app's own correct local-date default, rendered both products fine — a genuine test-authoring bug, not an app bug, caught by the two checks disagreeing with each other rather than either silently passing. Fixed by computing the local calendar date via `getFullYear()`/`getMonth()`/`getDate()` instead.

Hardware's remaining item (live area-pricing margin preview) remains fully untouched.

### 30. Hardware's item 5 (Live area-pricing margin preview) — its 5th and final signature item, closing the vertical

Grounding research (a codebase-wide grep for "margin"/"profit" inside `BillingScreen.tsx`) confirmed no margin or profit figure existed anywhere on the Billing screen — genuinely new, not something to verify as already partially built despite the item's own `[VERIFY]` tag (which had speculated the existing area-pricing mechanism might already have a margin hook).

Added a live margin preview directly inside the existing L×W area-calculator popover (Hardware's own pre-existing area-pricing mechanism, which pre-dates this roadmap entirely). As soon as both dimensions are filled in, the popover now shows the exact margin percentage this cart line will earn at the computed area and the line's current price, colour-coded green (≥20%), amber (0–20%), or red (negative) so a cashier or owner can spot a thin or negative margin before committing to an unusually-shaped sale. Cost basis is fetched once per popover-open via `products.get()` — an IPC call that has always returned `costPrice`/`inventory.averageCost` unrestricted, just never consumed by this screen before — and cached client-side by product ID, so retyping the L/W values recomputes the margin instantly with zero additional round-trips.

**A deliberate permission-gating decision, made explicitly rather than defaulted**: the margin preview only renders for a user with the `analytics.viewProfit` permission, matching the exact pattern the Dashboard screen already established (`canViewProfit`) for every other profit-facing UI element in this app. A cashier role without that permission never sees the preview at all — closing a real display-layer gap (margin was never shown anywhere on Billing) without opening a new data-exposure one (the cost data itself was already technically reachable via the same unrestricted IPC call, this item just makes it visible, and only to the right people).

- Unit tests: none needed — pure UI logic with no new service function, matching the precedent already established by Hardware's own items 1+2 (a pure UI-layer feature needs no new backend test coverage). Full suite **2883/2883** unchanged, both tsconfigs clean.
- i18n: 1 new key (`billing.areaMarginPreview`), all 13 languages, key parity re-verified (**4093/4093 leaf keys**, up from 4092).
- Manual: the existing "Area pricing (L × W calculator)" paragraph in Hardware's chapter extended in place with a second paragraph describing the margin preview and its permission gate — no new heading needed (still 7 headings), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/17-hardware-area-pricing.js` extended a fifth time, 32/32 total for the file, stable across 2 consecutive runs): using the suite's own pre-existing 60-cost/100-price test product, filled L=10 × W=5 in the real area calculator and confirmed the popover genuinely renders the literal text "Margin: 40%" — the correct value for a ₹40 margin on a ₹100 selling price.

**Hardware is now the 9th of Phase 67's 23 verticals with its entire 5-item signature-win set genuinely complete, live-verified, and documented** — joining GP Clinic, Physio Clinic, Diagnostic Lab, Vet Clinic, Specialist Clinic, Dental Clinic, Restaurant, and Retail, and becoming the 3rd of the 17 Product & Retail / Legacy Generic Service verticals to close (after Restaurant and Retail).

### 31. General's item 1 ("Which template fits you?" wizard) — its 1st of 5 signature items, the first-ever work on this vertical

Grounding research (a dedicated pass checking for `suggestedBusinessType`/`businessTypeConfidence`/`templateScore`/`inferBusinessType`/`classifyBusiness` and similar across the whole codebase) confirmed this was genuinely greenfield — no confidence or similarity concept between business types existed anywhere. Equally, no "first N days of usage" onboarding-window pattern existed to reuse either — every existing report or AI aggregation in this codebase just filters `createdAt` against an explicit caller-supplied date range, never against its own install age.

**A deliberate scope-narrowing decision, made explicitly before writing any code**: rather than build a full classifier scoring a `GENERAL` business against all 41 `BusinessType` values, the feature is scoped to 7 genuinely unambiguous signals, each pointing at exactly one clearly-distinct template:

| Signal | Model / field | Suggests |
|---|---|---|
| Carton-pack products | `Product.sellByPack` | HARDWARE |
| Metal-weight-priced products | `Product.metalType` | JEWELLERY |
| Rentable products | `Product.isRentable` | RENTAL |
| Kitchen order tickets | `KOT` | RESTAURANT |
| Repair/job tickets | `RepairTicket` + `JobCard` | REPAIR |
| Appointments | `Appointment` | SERVICE |
| Size/colour variants or loose-weight products | `ProductVariant` + `Product.sellByWeight` | RETAIL |

Each signal requires a genuine minimum row count (3-5, not a single stray test product) before it counts as a real usage pattern rather than incidental or test data. If more than one signal clears its threshold, the strongest (highest count) wins — the feature only ever surfaces one suggestion at a time, never a ranked list, matching the item's own "wizard" framing as a single clear nudge, not an ambiguous menu.

New `template-suggestion.service.ts`'s `getTemplateSuggestion()`, gated on two conditions: the business's current type must be `GENERAL` (every other business type either already picked, or already dismissed, a template), and `BusinessProfile.createdAt` must be at least 7 days in the past — a brand-new install with a handful of setup-time test rows should never be nudged before it's had a real week of actual trading.

**Persistence and presentation deliberately reused existing conventions rather than inventing new ones.** The dismissal mechanism is the exact same `Setting.settingKey` pattern already established by the pre-existing `backup_prompt_dismissed`/`tutorial_prompt_dismissed` prompts (found via research, not re-derived) — a new `template_suggestion_dismissed` key, a global one-time flag that never re-shows once dismissed. Unlike those two prompts, though, this is presented as a dismissible **Dashboard banner**, not a forced full-screen prompt gating the whole app — a deliberate distinction, since this observation can become true weeks into genuine use, not only at first launch, and shouldn't block a returning user's whole session the way a mandatory first-run setup step legitimately can. The banner's "Explore business templates" action navigates to the already-built `/settings/industry` screen rather than duplicating its confirm/apply switching logic — one single source of truth for actually changing a business's template.

New AI intent `general.templateSuggestion` — General's first-ever vertical AI intent. A dedicated research pass confirmed General previously had zero vertical-specific AI intents by design (an earlier phase's own comment states there was "no vertical-specific question to ask that the 18 universal templates don't already answer" for General) — this item genuinely changes that, since the template-suggestion signal computation is a real General-specific question with a real General-specific answer.

- Unit tests: 14 new (`template-suggestion.service.test.ts` — 10 tests: not-GENERAL returns null, too-new-business returns null even with strong signals, below-threshold returns null, per-signal suggestion tests for HARDWARE/JEWELLERY/SERVICE, a multi-signal "strongest wins" test, combined-signal tests for REPAIR (RepairTicket + JobCard) and RETAIL (ProductVariant + sellByWeight products), and an honest-error-on-query-failure test; `ai-vertical-templates.service.test.ts` — 4 tests: registration, headline reporting, empty-state, error-state). Full suite **2897/2897**, both tsconfigs clean.
- i18n: 18 new keys (banner title/explore/dismiss labels, 7 template-name labels, 7 per-signal reason strings with `{{count}}`/`{{template}}` interpolation), all 13 languages, key parity re-verified (**4110/4110 leaf keys**, up from 4093).
- Manual: General's existing "Growing into a specialty" section extended with a new paragraph describing the automatic suggestion (no new heading — still 2 headings per language file), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/68-general-template-suggestion.js`, a brand-new suite — the first-ever E2E test for the General vertical, previously fully untouched — 15/15, stable across 3 runs on a fresh dev server): backdated a real `BusinessProfile.createdAt` row to 8 days ago via a direct DB write (restored in the suite's own cleanup), seeded 3 real carton-pack products via the real product-creation API, confirmed via the live `templateSuggestion.get()` API and the real Dashboard UI that a suggestion genuinely renders, confirmed the "Explore business templates" button genuinely navigates to the real `/settings/industry` screen, and confirmed dismissing the banner both hides it immediately and persists through a real route remount (not just in-memory component state) via the live `isDismissed()` API. **The suite's own assertions were deliberately written to be honest about reading real, shared, cross-suite data rather than a clean fixture**: the first run returned `RESTAURANT` (53 matched KOT rows) instead of the seeded `HARDWARE` signal (3 rows), because 53 genuine KOT rows already existed in the shared dev DB from earlier restaurant-suite runs this session — this is the scoring working *correctly* (the strongest real signal legitimately wins), not a bug, so the suite's assertions were written to accept whichever valid suggestion the live API actually returns and verify the UI matches it, rather than hardcoding an expected winner that depends on incidental DB state.
- **A real test-authoring bug was found and fixed via this same live-verification pass**: the suite's first draft selected the GENERAL tile in the Industry Settings screen by matching the substring `"General"` — which also matches RETAIL's own tile label, `"Retail / General Store"`. Playwright's `.first()` silently picked whichever tile came first in DOM order (RETAIL), so the entire suite ran against the wrong business type until this was caught (the API correctly returned `null` for a non-GENERAL business, which is what surfaced the mismatch). Fixed by matching the exact, unique label `"General Business"` instead.

General's remaining 4 items (custom document/field builder, user-defined category mix report, combined cash position trend, universal quote→order→invoice pipeline) remain fully untouched.

### 32. General's item 2 (Custom document/field builder) — its 2nd of 5 signature items

Grounding confirmed the overlap exactly as the item's own `[PARTIAL OVERLAP]` tag speculated: Phase 66's `CustomFieldDefinition`/`customFields` mechanism (text/number/date/select fields addable to Invoice/Customer/Supplier/Product/Expense) already fully covers the "field builder" half — reused as-is, not rebuilt. The genuinely greenfield half is "custom document" itself: a brand-new document TYPE a business defines for itself (e.g. "Visitor Register", "Complaint Log"), not just extra fields bolted onto an entity that already exists.

**The design deliberately reuses Phase 66's mechanism rather than building a parallel one.** A new `CustomDocumentType` row is the document type itself; its field schema is defined via the SAME `CustomFieldDefinition` table every other entity already uses, keyed by a namespaced `entityType` value — `CUSTOM_DOCUMENT:<the document type's own id>` — instead of one of the 5 fixed built-in literals. This required widening `custom-field.validation.ts`'s `entityType` schema from a strict `z.enum(...)` to a union that still validates the 5 original literals exactly as before, plus a new pattern matched by a real-cuid regex (not loosened to an arbitrary string, which would have weakened validation for the 5 existing entity forms too). `CustomFieldsEditor.tsx`'s prop type was similarly widened (a TypeScript template-literal type addition, `` `CUSTOM_DOCUMENT:${string}` ``) so the existing shared field-value editor component — already used by 5 entity forms across the app — works completely unmodified for a document type that couldn't possibly have been known at compile time. Each logged `CustomDocumentEntry` stores its field values in a `customFields` column using the exact same JSON-string convention every other entity's own column already uses, reusing `serializeCustomFieldValues()`/`parseCustomFieldValues()` unchanged.

New `custom-document.service.ts` (`listTypes`/`createType`/`updateType`/`listEntries`/`createEntry`/`updateEntry`/`deleteEntry`) + `custom-document.handler.ts`. New `CustomDocumentsScreen.tsx` — a list-detail layout: document types in a left sidebar, and for the selected type, a collapsible "Manage Fields" panel (reusing the same field-CRUD interaction pattern `SettingsScreen.tsx`'s own Custom Fields section already established) plus a dated-entries table with create/edit/delete. New `customDocuments.view`/`.manage` permissions, Manager-tier (day-to-day operational actions, not Admin-only settings). New `custom_documents` `TemplateModule` flag, added to `GENERAL`'s own `TEMPLATE_DEFAULTS` — the one deliberate exception to GENERAL's previously-accurate "zero vertical-specific modules" identity (confirmed via research before this item), since this genuinely is GENERAL's own signature feature, not incidental scope creep. New AI intent `general.customDocumentSummary` — General's second vertical AI intent, summing entries across every active document type.

- Unit tests: 16 new (`custom-document.service.test.ts` — 10 tests covering create/update/list/delete across both types and entries, including two "returns a real error rather than a silent no-op" tests for missing rows; `custom-field.validation.test.ts` — a new dedicated file, 6 tests locking in that the widened `entityType` schema still accepts all 5 original built-ins, accepts a genuine namespaced pattern, and rejects both garbage values and malformed namespaced IDs) + 3 more AI-intent tests (`ai-vertical-templates.service.test.ts`, extending the existing `general.templateSuggestion` describe block's own registration test to account for the second intent, plus 3 new tests for `general.customDocumentSummary`). Full suite **2916/2916**, both tsconfigs clean.
- i18n: 47 new keys (nav label, 22-key `customDocuments` namespace, 2-key tour entry — reused `customFields.type.*`/`.title`/`.newField`/etc. rather than duplicating them), all 13 languages, key parity re-verified (**4135/4135 leaf keys**, up from 4110).
- Manual: General's chapter gained a new "Custom Documents" section (heading count 2→3), and its own opening paragraph was updated to reflect Custom Documents as a genuine default module (previously accurately said "no specialty feature module" — no longer fully accurate once this item shipped), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/68-general-template-suggestion.js` extended a second time, 24/24 total for the file, stable across 3 consecutive fresh-dev-server runs): created a real document type via the real UI, defined a real custom field on it via the reused field-builder UI, logged a real entry with that field filled in and notes, confirmed via the real UI that the entry lists with its field value, cross-checked via the live API that the entry's `customFields` JSON blob genuinely contains the typed value (not just rendered client-side), and confirmed deleting the entry via the real UI actually removes it (re-checked via the live API, not just the UI disappearing).
- **A real, genuinely leaking bug was caught and fixed via this exact live-verification pass**: `cleanupByNamePrefix()` had no handling at all for `CustomDocumentType`/`CustomDocumentEntry` — the first suite run passed cleanly, but a second consecutive run found the exact same document-type ID still present in the database, proving test data was silently accumulating with every single run rather than being cleaned up. Fixed by extending `cleanupByNamePrefix()` a fifth time this phase: delete `CustomDocumentEntry` rows first (the RESTRICT-FK child), then their namespaced `CustomFieldDefinition` rows (which have no `name` of their own to prefix-match against, only reachable via the parent type's id), then the `CustomDocumentType` row itself. Confirmed genuinely fixed, not just newly masked, via 2 further consecutive clean runs each reporting a fresh document-type id and `customDocumentTypesHandled: 1`.

General's remaining 3 items (user-defined category mix report, combined cash position trend, universal quote→order→invoice pipeline) remain fully untouched.

### 33. General's item 3 (User-defined category mix report) — its 3rd of 5 signature items

Grounding confirmed this was genuine `[REUSE]`, not just a hopeful tag: `ProductCategory` already existed (used by Retail's own Category Sell-Through report, item 3 of Retail's own 5), so no new schema or capture was needed — purely a new aggregation query plus a new report screen. A dedicated check ruled out a near-duplicate: the pre-existing `sales.byCategory` AI intent already answers "top categories by revenue," but only all-time and without a percent-of-total breakdown; no report anywhere in the codebase grouped REVENUE by category over an arbitrary date range with a share-of-total figure attached — that gap is what this item fills. It is also deliberately distinct from Category Sell-Through itself: Sell-Through is a month-by-month rate-vs-current-stock trend view, while Category Mix is a single-period "what share of my business comes from each category" snapshot — a genuinely different question the codebase didn't yet answer, not a redundant second report of the same thing.

New `generateCategoryMixReport()` in `report.service.ts`: groups `InvoiceItem` rows (ACTIVE invoices only, product must have a `categoryId`) by category, summing `quantitySold` (sign-corrected for RETURN, the same `sign = invoiceType === 'RETURN' ? -1 : 1` convention every other report in this file already uses) and `revenue` (summing `lineTotal` directly, unsigned — `lineTotal` is already correctly negative for a RETURN at the DB level, confirmed by re-checking `getBottomRevenueProducts()`'s own precedent in `ai-aggregations.service.ts` rather than re-deriving it fresh). `revenuePercent` is each category's share of the report's own total revenue, rounded to 1 decimal, explicitly guarded against division by zero when total revenue nets to 0. New IPC handler `reports:categoryMix`, gated on the existing `reports.sales` permission (no new permission needed — this is a read-only report, same tier as every other Reports-screen entry).

On the renderer side, this is this file's **first live Pie chart** — every other Phase 67 report so far used a bar, scatter, treemap, or stacked-bar shape; Category Mix's "share of revenue" framing is a genuinely better fit for a pie than a forced bar re-use, and the codebase's own `ReportChart` type already had an unused `type: 'pie'` variant defined (confirmed via a repo-wide grep — zero prior usages) sitting ready for exactly this. New `CategoryMixView` component renders both a real on-screen recharts `Pie` (with a small local `CATEGORY_MIX_COLORS` cycling palette, since category count is user-defined/unbounded — unlike e.g. `QUADRANT_COLORS`'s fixed 4-key map elsewhere in this file) and a `DataTable`; a matching `getReportCharts()` case (using the previously-unused `'pie'` type) covers the PDF-export path, following the established precedent that every Phase 67 report gets both a dedicated on-screen view and its own PDF-chart case, not just one or the other.

New AI intent `general.categoryMix` — General's third vertical AI intent, reporting the top revenue category and its percentage share for the current month. Its fast-path regex (`/category\s+mix/i`, `/revenue\s+by\s+category/i`, `/(category|categories).*(mix|share|breakdown)/i`) was deliberately scoped narrower than a bare "category" + "revenue" match specifically so it doesn't steal traffic from the pre-existing `sales.byCategory` intent (which has no fast-path pattern of its own, LLM-routed only) — confirmed via a dedicated fast-path routing test that "category mix" resolves to the new intent, not the old one.

- Unit tests: 12 new (`report.service.test.ts` — 9 tests: category+revenue grouping, RETURN sign-correction on quantity vs. unsigned `lineTotal`, revenuePercent computation and its divide-by-zero guard, sort-by-revenue-descending, empty-category-list edge case, no-category-assigned exclusion, ACTIVE-invoice-only filtering, currency rounding; `ai-vertical-templates.service.test.ts` — 2 tests: headline/details reporting plus the registration-list update to include the third intent; `ai-query.service.test.ts` — 1 test: the fast-path routing test above). Full suite verified clean (see Cumulative testing state below).
- i18n: 6 new keys (`defs.categoryMix.{label,description}`, `summary.{categoryCount,revenueByCategory}`, `col.revenuePercent`, `empty.categoryMix` — `col.category`/`col.revenue`/`col.unitsSold`/`summary.totalRevenue` were all already-existing keys, reused as-is), all 13 languages, key parity re-verified (**4141/4141 leaf keys**, up from 4135).
- Manual: General's chapter gained a new "Category Mix Report" section (heading count 3→4), explicitly cross-referencing and distinguishing it from Category Sell-Through so a reader doesn't confuse the two, all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/68-general-template-suggestion.js` extended a third time, 34/34 total for the file, confirmed clean across 2 consecutive fresh-dev-server runs): created a real `ProductCategory` and a real product assigned to it via the real product-creation API, sold 4 units via a real `billing.createInvoice` call, confirmed via the live `reports.categoryMix` API that the row's `unitsSold`/`revenue`/`revenuePercent` all compute correctly, then confirmed via the real Reports UI that the tile is present, the report renders without an error boundary, and the seeded category name genuinely appears on screen.
- **A real test-authoring bug was found and fixed via this same live-verification pass** (not a product bug): the suite's first draft asserted `revenue === 400` for a 4-unit sale at ₹100/unit, not accounting for the 5% tax rate the invoice item was created with — `lineTotal` (and so this report's revenue) is tax-inclusive, same as every other revenue-bearing report in this codebase, so the real correct value was ₹420. Fixed the assertion, not the report; re-run confirmed 34/34 clean.

General's remaining 2 items (combined cash position trend, universal quote→order→invoice pipeline) remain fully untouched.

### 34. General's item 4 (Combined cash position trend) — its 4th of 5 signature items

Grounding surfaced two existing cash-adjacent reports worth distinguishing against rather than duplicating: `generateCashFlowProjection` (Phase 65) shows daily NET movement — money in minus out, split into an actual (past) and projected (future) half — never a cumulative running position; `generateCashBookReport` shows a running balance, but synthesizes it from three transactional tables directly (`Payment`/`Expense`/`SupplierLedger`), the same "synthesize from source tables" approach `generateTrialBalanceReport`'s own Phase 62 rewrite explicitly moved away from once a real double-entry GL existed to read from instead. Neither report reads the actual posted ledger for a genuine day-by-day cumulative balance — that gap is what this item fills, using the same GL-based approach the Trial Balance rewrite established as this codebase's preferred pattern.

New `generateCashPositionTrendReport()` reads real `JournalEntryLine` rows against the single `ChartOfAccounts` row every cash-touching transaction posts to (`accountCode: '1000'`, "Cash & Bank" — confirmed via the `expense.service.ts`/`payment.service.ts`/`supplier-payment.service.ts` comments that already document this convention). "Combined" because every distinct transaction type (payments received, expenses, supplier payments, bills, POS sales, and more) posts to this one shared GL bucket by construction, so the resulting trend is inherently the consolidated position across every instrument, not a per-account or per-payment-method view. Opening balance is the net debit-credit of every line strictly before `dateFrom`; each day in the range then accumulates that day's own net movement into a running balance — same debit-normal-asset sign convention (`net = debit − credit`) Trial Balance already established. New `reports:cashPositionTrend` IPC handler, gated on `reports.financial` (the same tier as the adjacent `reports:cashBook`).

On the renderer side, a new `CashPositionTrendView` renders a single-series `AreaChart` (reusing `CashFlowProjectionView`'s own visual style one section above it, but a single cumulative line instead of a dual actual/projected split) plus a matching `getReportCharts()` PDF-export case using the already-established `'line'` `ReportChart` type (first used by the shipment-trend and groupBy-summary charts elsewhere in this file).

New AI intent `general.cashPositionTrend` — General's fourth vertical AI intent, reporting whether the cash position grew or fell this month and by how much. Its fast-path regex (`/cash\s+position/i`, `/combined\s+cash/i`, `/cash.*(trend|running\s+balance)/i`) was deliberately scoped to "position"/"trend" phrasing so it doesn't collide with the pre-existing `cashFlow.projectionNextMonth` intent's own "cash flow"/"how does my cash look" patterns.

- Unit tests: 12 new (`report.service.test.ts` — 8 tests: opening-balance computation from prior lines only, running daily balance across a range, a flat line when nothing moved, exactly one point per day inclusive of both ends, same-day multi-line summation, negative/overdrawn balances allowed without flooring, an honest empty result when no "Cash & Bank" account exists at all, currency rounding; `ai-vertical-templates.service.test.ts` — 3 tests: grew/fell headline framing plus the registration-list update to include the fourth intent; `ai-query.service.test.ts` — 1 test: the fast-path routing test above). Full suite **2940/2940**, both tsconfigs clean.
- i18n: 6 new keys (`defs.cashPositionTrend.{label,description}`, `summary.cashPositionTrend`, `col.balance`, `col.netChange`, `empty.cashPositionTrend` — `col.openingBalance`/`col.closingBalance` were already-existing keys, reused as-is), all 13 languages, key parity re-verified (**4147/4147 leaf keys**, up from 4141).
- Manual: General's chapter gained a new "Combined Cash Position Trend Report" section (heading count 4→5), explicitly cross-referencing and distinguishing it from Cash-Flow Projection, all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/68-general-template-suggestion.js` extended a fourth time, 41/41 total for the file, confirmed clean across 2 consecutive fresh-dev-server runs): rather than asserting an absolute value (this reads the real shared dev DB's GL, which already carries substantial activity from every earlier suite this session), the suite snapshots today's closing balance, seeds one real CASH sale via `billing.createInvoice` for a known tax-free amount, then confirms the closing balance moved by exactly that amount — confirmed correct on both runs (300 exactly, against two different pre-existing baseline totals). Also confirmed via the real Reports UI that the tile is present, the report renders without an error boundary, and the Closing Balance summary card genuinely appears.
- **A real, previously-unnoticed test-data leak was found and fixed proactively before it could bite**: `cleanupByNamePrefix()` has no concept of `JournalEntry` at all (no name field to prefix-match against) — every invoice-creating E2E suite across this whole codebase already leaves its own GL postings behind as permanent orphans once the invoice itself is hard-deleted (a pre-existing, session-wide gap, not unique to this item — `66-cost-centres-budgets-payroll.js` had already worked around the identical gap locally for its own payroll postings). Since this item's own test assertion reads the real GL total, a growing pile of orphaned postings would have silently inflated the shared dev DB's Cash & Bank balance a little more on every future run. Fixed with the same targeted, suite-local pattern `66-cost-centres-budgets-payroll.js` already established: look up the `JournalEntry` by `sourceType`/`sourceId` before the suite's own cleanup deletes the source invoice, and delete its lines and itself first. Confirmed genuinely working via 2 consecutive runs, both producing the exact expected 300 delta despite different absolute baselines.
- **A real test-authoring bug was found and fixed via this same live-verification pass** (not a product bug): the on-screen-rendering assertion checked `bodyText.includes('Closing Balance')` with exact casing, which spuriously failed — `SummaryCards` labels are CSS-uppercased, and Chromium's own `innerText()` honors that transform (the same "Uppercase CSS innerText Gotcha" this project has hit before). Fixed with a case-insensitive check; re-run confirmed 41/41 clean.

General's remaining 1 item (universal quote→order→invoice pipeline) remains fully untouched.

### 35. General's item 5 (Universal quote→order→invoice pipeline) — its 5th and final signature item, closing out General

Grounding found `Quotation` already converts directly to `Invoice` (`convertToInvoice`), and `SalesOrder` already converts to `Invoice` (`salesOrderService.createInvoiceFromSalesOrder`, partial-invoicing aware via `SalesOrderItem.invoicedQty`) — but nothing ever connected `Quotation` to `SalesOrder` itself, so a business could never chain all three. That missing middle link is what this item builds.

New `SalesOrder.quotationId` (1:1, `@unique`, same shape as the pre-existing `Invoice.quotationId`) plus the reverse `Quotation.salesOrder` relation. New `quotationService.convertToSalesOrder()` mirrors `convertToInvoice()`'s own product-resolution logic (existing product / find-by-name / Misc fallback) rather than reusing `salesOrderService.createSalesOrder()` — that function expects a fresh caller-supplied payload, not an existing quotation's already-priced items, and this needs the same transaction to also flip the Quotation's own status, matching `convertToInvoice`'s exact shape. `generateSONumber` was exported from `sales-order.service.ts` for this reuse, the same cross-service pattern `billing.service.ts` already established by exporting `generateInvoiceNumber`.

**A genuine schema-shape mismatch surfaced during grounding, not before**: `SalesOrderItem` has no discount column at all (unlike `QuotationItem`/`InvoiceItem`). Rather than silently dropping a quotation's line discounts, or adding a new column purely for this one conversion path, each discount is folded into a reduced effective unit price — the Sales Order's own total still exactly reproduces what the customer agreed to on the quotation, it simply reflects the final negotiated per-unit price with no separate discount line to show.

**A real, previously-dead-code bug was found and fixed along the way, unrelated to this item but caught only because this item required re-reading the exact same function**: `quotationService.list()` (which backs the Quotations screen's own list view) never included the `invoice` relation in its query at all, even though `QuotationsScreen.tsx`'s own row rendering branches on `q.invoice` to decide whether to show a link to the converted invoice or the "Convert to Invoice" button. Since `q.invoice` was always `undefined` on that screen, an already-converted quotation kept showing "Convert to Invoice" indefinitely, until the backend rejected the inevitable repeat attempt with `QT-002` — a real, silent UI-state bug, not a hypothetical one. Fixed by including both `invoice` and the new `salesOrder` relation in the same query.

On the renderer side, `QuotationsScreen.tsx` gained a second conversion button, **Convert to Sales Order**, shown alongside the existing **Convert to Invoice** for any accepted, non-retainer quotation (retainer-flagged quotations are deliberately excluded — a recurring billing engagement doesn't fit the "discrete order to fulfil" shape a Sales Order implies). Once converted, the row shows a link to the real Sales Order instead of either conversion button, mirroring the existing converted-to-invoice link pattern exactly. The Sales Order's own `notes` field is set to reference the source quotation number, which the pre-existing `SalesOrderDetailScreen.tsx` already renders — no new UI surface needed there for basic traceability.

New AI intent `general.quotePipelineSummary` — General's fifth and final vertical AI intent for this phase, reporting how many quotations this month went direct-to-invoice vs. via a Sales Order vs. are still pending, read-only like every other intent in this file (never triggers a conversion itself, matches the established "point at the screen, don't act on it" convention).

- Schema: new migration `20260820110000_phase67_general_quote_order_invoice_pipeline` (SQLite table-rebuild, since SQLite requires it for adding a column with a real FK constraint). **A genuine Prisma path-resolution gotcha was hit and worked around**: `prisma migrate deploy`/`db push` resolve a relative `DATABASE_URL` against `prisma/schema.prisma`'s own directory, landing on `prisma/.dev-data/sarang.db` — a stale, unused copy — while the real running app resolves the same relative path against its own process cwd, landing on the repo-root `.dev-data/sarang.db`. The migration was applied directly via raw SQL execution against the correct file instead, then verified via `PRAGMA foreign_key_check`/`integrity_check` (both clean) and a live column check. This is a different flavor of the same family of gotcha as [[feedback_gitbash_mangles_prisma_paths]] — worth checking for on any future schema change in this project, in either shell.
- Unit tests: 13 new (`quotation.service.test.ts` — 9 tests: SalesOrder creation linked via quotationId, Quotation status flip in the same transaction, discount-folding into effective unit price, rejecting an already-invoiced or already-converted quotation, rejecting a walk-in with no customer, rejecting an archived customer, honest not-found, Misc-product fallback; `ai-vertical-templates.service.test.ts` — 3 tests: funnel breakdown, empty-state, singular/plural phrasing; `ai-query.service.test.ts` — 1 test: fast-path routing). Full suite **2953/2953**, both tsconfigs clean.
- i18n: 4 new keys (`quotations.convertToSalesOrder`, `.convertToSalesOrderHint`, `.convertedToSalesOrder`, `.failedConvertSalesOrder`), all 13 languages, key parity re-verified (**4151/4151 leaf keys**, up from 4147).
- Manual: General's chapter gained a new "Quote → Order → Invoice Pipeline" section (heading count 5→6), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/68-general-template-suggestion.js` extended a fifth time, 52/52 total for the file, first attempt clean): created a real customer, product, and quotation (with a real 10% line discount) via the live API, converted it to a Sales Order, confirmed via the live API that the total exactly reproduces the quotation's own agreed total (₹318.60, discount-folded and taxed correctly) and that the Sales Order is genuinely linked back via `quotationId`, confirmed a second conversion attempt is correctly rejected with `QT-008`, then confirmed via the real Quotations UI that the row now shows a link to the real Sales Order (not the conversion buttons), and that navigating to the Sales Order's own detail screen shows the "Converted from quotation..." note. This is also the first-ever E2E test in this codebase to touch `Quotation` or `SalesOrder` at all — `cleanupByNamePrefix()` was extended with a new block for both (SalesOrder deleted first, since its `customerId` is a plain RESTRICT foreign key, same ordering reasoning as the pre-existing Supplier/PurchaseOrder block; Quotation deleted after, its own items cascading automatically in both cases) — confirmed working via the run's own cleanup counts (`salesOrdersHandled: 1, quotationsHandled: 1`).

**General is now fully complete — all 5 of its own signature items done.** This is the 10th of Phase 67's 23 verticals to reach that: all 6 clinical verticals, plus the first four of the 17 Product & Retail / Legacy Generic Service verticals (Restaurant, Retail, Hardware, then General).

### 36. Electronics' item 1 (RMA SLA tracker) — its 1st of 5 signature items, the first-ever work on this vertical this phase

Grounding confirmed Electronics already had a real, working RMA workflow (`RepairTicket`, Phase 58 §2: claim → vendor RMA → replacement linkage, with `sentToVendorDate`/`vendorResponseDate` already tracked) — the audit's own 7.0/10 rating and "Serial/IMEI/warranty tracking plus a real RMA workflow" verdict for this vertical reflected that. What was genuinely missing, matching the audit's own named gap, was the SLA half: no due-date concept existed at all, so a unit sitting with a vendor for months had no mechanism to surface that fact.

New `RepairTicket.vendorSlaDueDate` (nullable `DateTime`, plain column — no FK, so a simple `ALTER TABLE ADD COLUMN` sufficed, unlike the prior item's FK-carrying table rebuild), set automatically and only once, the moment a ticket first transitions to `SENT_TO_VENDOR` (`sentToVendorDate + 30 days` — the exact "30 days" framing the audit's own field note used, via a new `VENDOR_SLA_DAYS` constant). `toRecord()` now also returns two computed fields mirroring the existing `turnaroundDays` pattern: `daysWithVendor` (freezes once `vendorResponseDate` is set, rather than continuing to count against today) and `isOverdue` (true only while the ticket is genuinely still `SENT_TO_VENDOR`/`AWAITING_PARTS` — a unit that came back late is a fact for a future aging report to show, not an ongoing alert).

Escalation surfaces in three places, matching the audit's own "escalation" framing rather than a passive field nobody checks: (1) a per-row **Overdue** badge with live days-with-vendor count directly on the Repair Tickets screen (English-only, per this vertical's own established screen-language-lock convention); (2) a running overdue count in that screen's own header; (3) a new `RMA_OVERDUE` Dashboard alert, added to `getDashboardAlerts()` following the exact same "computed live, never a stored status" convention the pre-existing `RENTAL_OVERDUE` alert already established, with a one-click link straight to the Repair Tickets screen. New AI intent `electronics.rmaOverdueSummary` — Electronics' second-ever vertical AI intent (after the pre-existing `electronics.serialWarranty`), listing the top overdue units by how long they've been out.

- Schema: new migration `20260820120000_phase67_electronics_rma_sla_tracker`, applied directly via raw SQL against the real dev DB (same Prisma relative-path-resolution workaround the prior item's own migration required), verified via `PRAGMA integrity_check`.
- Unit tests: 16 new (`repair-ticket.service.test.ts` — 6 tests: 30-day due-date computation, no re-stamping on a later transition, `isOverdue`/`daysWithVendor` correctness including the never-sent and returned-late edge cases, `daysWithVendor` freezing once the vendor responds; `analytics.service.test.ts` — 4 tests: warning/danger severity thresholds, no-alert-when-clean, the exact live-computed query shape; `ai-vertical-templates.service.test.ts` — 5 tests: registration alongside the pre-existing intent, headline/details, singular phrasing, empty state, query shape; `ai-query.service.test.ts` — 1 test: fast-path routing). Full suite **2969/2969**, both tsconfigs clean.
- i18n: 1 new key (`dashboard.reviewRepairTickets`, reusing each locale's own existing translation of `reviewRentals` — a generic "Review" action word), all 13 languages, key parity re-verified (**4152/4152 leaf keys**, up from 4151). The Repair Tickets screen's own new Overdue badge and header count are deliberately hardcoded English, matching this vertical-specific screen's own pre-existing English-only convention (see [[feedback_vertical_screens_language_lock]]).
- Manual: Electronics' existing "Repair / RMA tickets" section extended with a new paragraph (no new heading), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/15-electronics-serials.js` — the pre-existing Electronics suite, extended for the first time since its original creation — 33/33, first-ever E2E coverage of `RepairTicket` in this codebase): created a real repair ticket for a genuinely sold serial, moved it to Sent to Vendor via the live API, confirmed the due date is set to exactly 30 days out and the ticket correctly reports not-yet-overdue; backdated the ticket 45 days via a direct DB write to simulate elapsed time (same technique suite 68 already established), then confirmed via the live API, the real Repair Tickets UI (badge + header count), and the real Dashboard alert API that the overdue state is now correctly surfaced everywhere.
- **Two real bugs were found and fixed via this exact live-verification pass, neither hypothetical**: (1) the backdating write initially used `.toISOString()` — a genuine, confirmed-live bug in the test's own setup, not the product: this SQLite DB stores every `DateTime` column as epoch-ms `INTEGER` (confirmed via a direct `typeof()` query against `Invoice`/`BusinessProfile`), not text. Writing an ISO string left single-row reads working (Prisma's client-side deserialization tolerated it) while silently breaking every SQL-level `WHERE`-clause comparison — which is exactly the mechanism both the Dashboard alert's count query and the per-row Overdue badge's underlying data both depend on, so the bug was invisible in the "is the flag true" check but visible in "does the alert actually fire." Fixed by writing raw epoch-ms integers instead, matching suite 68's own established convention. (2) The on-screen Overdue-badge assertion used exact-case `bodyText.includes('Overdue')`, which spuriously failed — `Badge` labels are CSS-uppercased and Chromium's own `innerText()` honors that transform, the same [[feedback_uppercase_css_innertext_gotcha]] class of false positive already hit multiple times this phase. Fixed with a case-insensitive check.
- **First-ever `RepairTicket` E2E test also meant a first-ever cleanup gap, found and fixed proactively**: `RepairTicket.serialId` has no `onDelete` clause (defaults to RESTRICT), so this suite's own pre-existing `ProductSerial` cleanup would have silently started leaking both the serial and its ticket on every future run once a repair ticket existed against it. Fixed by deleting `RepairTicket` rows (matched by `serialId`/`replacementSerialId`) before the `ProductSerial` delete, in FK-dependency order — confirmed via the run's own cleanup counts (`repairTickets 1`).

Electronics' remaining 4 items (RMA aging report, vendor warranty-claim recovery ledger, repair turnaround by technician, serial-number service lookup) remain fully untouched. Grounding along the way found `getSerialServiceHistory()` already exists and partially covers item 5 (repair history for a serial) but not the "purchase" half the audit's own item description calls for — worth checking that function first rather than starting item 5 from a blank page.

### 37. Electronics' item 2 (RMA aging report) — its 2nd of 5 signature items

Grounding confirmed this genuinely builds on item 1 rather than duplicating it: item 1's `RMA_OVERDUE` Dashboard alert and `electronics.rmaOverdueSummary` AI intent are alert-style — they surface only the count and top few units already past the 30-day SLA. This item is the full analytical breakdown the audit itself tags separately as a **Report** (vs. item 1's own **Feature** tag): every currently-open unit (whether overdue or not), ranked by days with vendor, as a proper Reports-screen entry with its own chart — the artifact's own "(bar: days with vendor)" framing.

New `generateRmaAgingReport()` in `report.service.ts`, deliberately reusing the exact same `daysWithVendor`/`isOverdue` definitions `repair-ticket.service.ts`'s own `toRecord()` already established (same `VENDOR_SLA_DAYS = 30` rule) rather than re-deriving a second, driftable copy. New `reports:rmaAging` IPC handler on `reports.inventory` (same tier as the adjacent Serial & Warranty Report). On the renderer side, the report is gated via `requiredModule: 'repair_rma'` — matching the Repair Tickets sidebar entry's own module gate — rather than `requiredBusinessType: 'ELECTRONICS'`, consistent with how `serialWarranty` itself is module-gated, not vertical-gated. New `RmaAgingView` renders a horizontal bar chart (top 10 by days with vendor, overdue ones colored distinctly via `STATUS_COLORS.dangerDeep`, matching the existing `WARRANTY_BUCKET_COLOR` convention) plus a full `DataTable`.

**Deliberately did NOT add a second, near-duplicate AI intent for this item.** Section 1.2's own rule is satisfied by item 1's already-built `electronics.rmaOverdueSummary` — a second intent asking essentially the same underlying question ("what's aging/overdue with vendors") at a slightly different granularity would be genuine redundancy, not a new capability; the report itself is the item's real, artifact-tagged deliverable.

- Unit tests: 6 new (`report.service.test.ts` — daysWithVendor computation, overdue flagging, descending sort, query-shape lock-in for the two open statuses, null-vendor handling, honest empty result). Full suite **2975/2975**, both tsconfigs clean.
- i18n: 8 new keys (`defs.rmaAging.{label,description}`, `summary.{totalOpen,daysWithVendorByUnit}`, `col.{claimNumber,daysWithVendor}`, `val.{overdue,onTrack}` — `col.supplier`/`empty.serials`-adjacent `empty.rmaAging` also added), all 13 languages, key parity re-verified (**4160/4160 leaf keys**, up from 4152).
- Manual: Electronics' existing "Repair / RMA tickets" section extended with a second new paragraph (still no new heading), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/15-electronics-serials.js` extended a second time, 41/41, second consecutive clean run): confirmed via the live API that the report includes the real backdated ticket with the correct `daysWithVendor`/`isOverdue`/summary counts, then confirmed via the real Reports UI that the tile is present, the report renders without an error boundary, and the real claim number appears on screen.
- **A real, generalizable E2E test-authoring bug was found and fixed via this exact live-verification pass — not a product bug, but one worth flagging broadly**: the tile-click locator `page.locator('button, div', { hasText: '<report name>' })` matches BOTH the actual clickable `<button>` tile AND its own parent category-group `<div>` (whose full text content, being the concatenation of every nested tile's label, also contains the target string) — `.first()` picks the non-clickable parent div, since it precedes its own child button in DOM order, so the click silently no-ops and whatever report was already active (Sales Report, by default) stays active. This was caught only because this item added a genuine on-screen content assertion (`bodyText.includes('RMA-')`) where prior reports' own tile-click steps only checked "no crash" — a check so weak it would trivially pass even if the click never worked at all. Fixed both this item's own new locator and the adjacent pre-existing Serial & Warranty Report one in the same suite file (confirmed it had the identical latent bug) to `button`-only. **This same `'button, div'` pattern is used for report-tile clicks in several other E2E suites across this codebase and was not audited beyond this one file** — worth a dedicated sweep at some point, flagged in memory rather than fixed everywhere right now (out of scope for this specific item).

Electronics' remaining 3 items (vendor warranty-claim recovery ledger, repair turnaround by technician, serial-number service lookup) remain fully untouched.

### 38. Electronics' item 3 (Vendor warranty-claim recovery ledger) — its 3rd of 5 signature items

Grounding weighed reusing the pre-existing `DebitNote` model (already relabeled "Vendor Credit" in the UI) against extending `RepairTicket` directly. `DebitNote` is a flat, single-document claim against a Supplier with no payment-tracking/ledger concept, and this item's actual granularity is one claim per repair ticket with a running-total "recovery" concept — closer to `Invoice.paidAmount`'s own convention than to a new document type. Extending `RepairTicket` directly was the simpler, better-fitting choice: three new nullable/defaulted columns (`vendorClaimAmount`, `vendorRecoveredAmount` defaulting to 0, `vendorClaimClosedAt`) rather than inventing a child ledger-entries table.

New `recordVendorClaim`/`recordVendorRecovery`/`writeOffVendorClaim` in `repair-ticket.service.ts`: claim amount must be non-negative, recovery amount must be positive and requires a claim to already exist, and a recovery that brings the recovered total to or past the claimed amount auto-closes the claim (stamping `vendorClaimClosedAt`) the same moment it happens — no separate "close" step needed for the common full-recovery case. `writeOffVendorClaim` handles the uncommon case (vendor rejects the claim outright) by closing it without changing the recovered amount, and is rejected outright on an already-closed claim. `toRecord()` now also returns a computed `vendorClaimOutstanding` (claimed minus recovered) alongside the raw fields, mirroring `daysWithVendor`/`isOverdue`'s own precedent from item 1. New `generateVendorRecoveryLedgerReport()` in `report.service.ts` lists every ticket that has ever had a claim raised against it, sorted open-before-closed then by outstanding amount descending, with a summary of total claimed/recovered/outstanding and open/closed counts.

On the renderer side, `RepairTicketsScreen.tsx`'s detail modal gained a "Vendor Recovery" section (English-only, per this screen's own established language-lock convention): a claim-amount input + Record Claim button when no claim exists yet; once a claim exists, three summary tiles (Claimed/Recovered/Outstanding) plus either a "Closed" badge or a recovery-amount input with Record Recovery/Write Off buttons. The Reports-screen entry follows item 2's `RmaAgingView` template exactly: SummaryCards, a horizontal bar chart of outstanding amount for the top 10 open claims (colored via `STATUS_COLORS.dangerDeep`), and a full DataTable of every claim.

New AI intent `electronics.vendorRecovery` — unlike item 2, this one **does** get its own intent rather than reusing an existing one, since it answers a genuinely different question (money owed to the business) from item 1/2's own (time spent with a vendor). Registered alongside the two pre-existing Electronics intents, with its own fast-path query-routing patterns (`vendor.*recovery|claim`, `warranty claim`) deliberately scoped away from the bare `overdue` wording item 1's own intent already owns.

- Schema: new migration `20260820130000_phase67_electronics_vendor_recovery_ledger` (three plain `ALTER TABLE ADD COLUMN`s, no FK), applied via the same direct-raw-SQL-against-the-real-dev-DB workaround every migration this phase has required, verified via `PRAGMA integrity_check`.
- Unit tests: 21 new (`report.service.test.ts` — 7: outstanding-amount computation, `isClosed`/`closedAt` correctness, open-before-closed-then-outstanding-descending sort, query-shape lock-in on `vendorClaimAmount: { not: null }`, null-vendor handling, summary totals, honest empty result; `repair-ticket.service.test.ts` — 10: negative-claim rejection, not-found, claim stamped; zero/negative-recovery rejection, recovery-without-a-claim rejection, partial-recovery accumulation without closing, full-recovery auto-close; write-off-without-a-claim rejection, write-off-on-an-already-closed-claim rejection, write-off stamps `vendorClaimClosedAt`; `ai-vertical-templates.service.test.ts` — 3: headline/details with total outstanding and top claims, plural phrasing, empty state — plus the pre-existing registration test updated to expect all three Electronics intents; `ai-query.service.test.ts` — 1: fast-path routing). Full suite **2996/2996**, both tsconfigs clean.
- i18n: 9 new keys (`defs.vendorRecoveryLedger.{label,description}`, `summary.{totalOutstanding,outstandingByClaim}`, `col.{claimedAmount,recoveredAmount}`, `val.{open,closed}`, `empty.vendorRecoveryLedger`), all 13 languages, key parity re-verified (**4169/4169 leaf keys**, up from 4160).
- Manual: Electronics' existing "Repair / RMA tickets" section extended with a third new paragraph (still no new heading, consistent with items 1 and 2's own pattern), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/15-electronics-serials.js` extended a third time, 58/58, two consecutive clean runs): recorded a real ₹5,000 claim against the live backdated ticket, confirmed `vendorClaimOutstanding` computed correctly before any recovery; recorded a real partial ₹2,000 recovery via the live API, confirmed the outstanding amount recomputed to ₹3,000 and the claim correctly did NOT auto-close; confirmed the real Vendor Recovery Ledger report API includes the claim with the correct outstanding amount, then confirmed via the real Reports UI that the tile is present, the report renders without an error boundary, and the real claim number appears on screen; wrote off the remaining outstanding amount via the live API, confirmed the claim closed and the recovered amount stayed unchanged; confirmed a second write-off attempt on the now-closed claim is correctly rejected with `RPR-022`.
- No new bugs surfaced during this item's own live-verification pass — the pattern established across items 1 and 2 (direct-DB-write date backdating, `button`-only tile locators, case-sensitive content assertions) was applied correctly from the start this time.

Electronics' remaining 2 items (repair turnaround by technician, serial-number service lookup) remain fully untouched. Grounding from item 1 already found `getSerialServiceHistory()` partially covers item 5 (repair history for a serial) but not the "purchase history" half the audit's own item description calls for — worth checking that function first rather than starting item 5 from a blank page.

### 39. Electronics' item 4 (Repair turnaround by technician) — its 4th of 5 signature items

Grounding confirmed `RepairTicket` had no technician concept at all before this item — only `vendorId` (the external vendor a unit is sent OUT to for warranty repair). The artifact's own item text is a service-quality **Report**, tagged with a bar chart, so this item needed a real "who did the work" field before any report could exist. Considered `CarJobCard`'s own precedent (`technicianIds: String @default("[]")`, a JSON array of Employee IDs for multi-technician vehicle jobs) and deliberately did NOT follow it — a phone/device repair is one unit worked on by one person, not a multi-person job, so a single nullable `technicianId` FK to `Employee` is the better-fitting, simpler shape.

New `RepairTicket.technicianId` (nullable, `onDelete: SetNull`, own index), settable at intake via `createRepairTicket`'s payload or reassignable any time afterward via `updateRepairTicketStatus`'s same-status no-op-transition path (the exact mechanism already established for re-saving `vendorRmaNumber`). New `generateRepairTurnaroundByTechnicianReport()` in `report.service.ts`: only tickets with BOTH a technician assigned AND a real `deliveredDate` count (a still-open ticket has no finished turnaround yet; an unassigned ticket can't be attributed to anyone), grouped by technician into avg/min/max turnaround days, sorted fastest-average-first — deliberately reuses the exact `receivedDate`→`deliveredDate` span `repair-ticket.service.ts`'s own `turnaroundDays()` already computes per-ticket, not a second definition of the same number.

On the renderer side, `RepairTicketsScreen.tsx` gained a Technician select in the create-ticket form plus a standalone "Technician" reassignment control in the detail modal (English-only, per this screen's own language-lock convention), both fed from `window.api.hr.listEmployees({ isActive: true })`. The Reports-screen entry follows items 2/3's own established template (`RmaAgingView`/`VendorRecoveryLedgerView`): SummaryCards, a horizontal bar chart of average turnaround per technician, and a full DataTable.

New AI intent `electronics.repairTurnaround` — its own genuinely new question (how fast the shop's own staff complete work), distinct from item 1/2's SLA/aging questions (time with an external vendor) and item 3's vendor-recovery question (money owed), so it gets a fourth, real intent rather than reusing any of the first three.

- Schema: new migration `20260820140000_phase67_electronics_repair_turnaround_by_technician` (one plain `ALTER TABLE ADD COLUMN` + its index, no FK constraint at the SQLite level since the relation is enforced at the Prisma layer only), applied via the same direct-raw-SQL-against-the-real-dev-DB workaround every migration this phase has required, verified via `PRAGMA integrity_check` and `PRAGMA foreign_key_check`.
- Unit tests: 13 new (`report.service.test.ts` — 6: avg/min/max computation, per-technician grouping, fastest-first sort, query-shape lock-in on `technicianId: { not: null }, deliveredDate: { not: null }`, overall-average computation across all technicians, honest empty result; `repair-ticket.service.test.ts` — 3: technicianId passed through at creation, reassignment via a same-status no-op transition, existing technicianId preserved when not provided on an update; `ai-vertical-templates.service.test.ts` — 3: headline/details with overall average and fastest technician, singular phrasing for exactly one technician, empty state — plus the pre-existing registration test updated to expect all four Electronics intents; `ai-query.service.test.ts` — 1: fast-path routing). Full suite **3009/3009**, both tsconfigs clean.
- i18n: 11 new keys (`defs.repairTurnaroundByTechnician.{label,description}`, `summary.{technicianCount,totalTicketsCompleted,avgTurnaroundByTechnician}`, `col.{technician,ticketCount,avgTurnaroundDays,minTurnaroundDays,maxTurnaroundDays}`, `empty.repairTurnaroundByTechnician`), all 13 languages, key parity re-verified (**4180/4180 leaf keys**, up from 4169).
- Manual: Electronics' existing "Repair / RMA tickets" section extended with a fourth new paragraph (still no new heading, consistent with items 1–3's own pattern), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/15-electronics-serials.js` extended a fourth time, 70/70, two consecutive clean runs — the first-ever E2E-created `Employee` in this codebase): created a real technician via the live API, backdated the ticket's `receivedDate` 7 days via direct DB write (same epoch-ms-integer convention every backdating write in this suite already established), moved the ticket through `REPAIRED` (assigning the technician in the same call) then `RETURNED_TO_CUSTOMER` via the live API, confirmed the resulting `turnaroundDays` and technician both came back correctly from a real read; confirmed the real report API includes the technician with the correct ticket count and average; confirmed via the real Reports UI that the tile is present, the report renders without an error boundary, and the real technician's name appears on screen.
- **A genuine, if minor, new-model E2E cleanup gap was found and fixed proactively, before it could leak**: this is the first E2E-created `Employee` row in the codebase, and no suite had ever needed to clean one up before. `RepairTicket.technicianId` is `onDelete: SetNull` (not `RESTRICT`, unlike `serialId`), so deleting the `Employee` first — rather than the `RepairTicket`-then-`ProductSerial` dependency order item 1 established — is actually safe either way; added a targeted `DELETE FROM Employee WHERE fullName LIKE 'E2E Elec Tech%'` to this suite's own `finally` block regardless, confirmed via the run's own cleanup log (`technicians 1`).
- **One unrelated pre-existing dev-DB pollution issue was found and cleaned up along the way, not a regression from this item**: a full-suite run surfaced a failing `ai-readonly-db.test.ts` assertion — a leftover `Notification` row (`id: 'ai-readonly-test-row'`) from an earlier session's test run had never been cleaned up, which meant the test's own "reject a genuine write" assertion had been passing for the wrong reason (a UNIQUE-constraint violation on the stale row's `id`, not the read-only-connection rejection the test actually exists to verify). Deleted the stale row directly from the dev DB; re-ran the test standalone to confirm the read-only enforcement genuinely throws for the right reason once the row is gone, then re-ran the full suite clean.

Electronics' remaining 1 item (serial-number service lookup) remains fully untouched.

### 40. Electronics' item 5 (Serial-number service lookup) — its 5th and FINAL signature item, closing Electronics' entire 5-item set

Grounding confirmed `getSerialServiceHistory()` already existed and covered the repair-history half (an already-resolved `serialId` in, ticket list out), exactly as flagged after item 1 — but nothing in the codebase resolved a raw scanned/typed serial number or IMEI to a record, and nothing surfaced the "purchase" half at all. The pre-existing **IMEI Lookup** box on `SerialTrackingScreen.tsx` came closest but was narrower than the item calls for on every axis: IMEI-only (not serial number), gated behind `imei_tracking` specifically, and showed only current status — no purchase price, no customer, no repair history. The audit's own wording — "scan/search a serial to see full purchase-plus-repair history instantly" — needed all three closed at once.

Considered what "purchase" means here and deliberately read it as **what the customer paid this shop**, not this shop's own upstream supplier cost — the real question a service-desk person asks a walk-in with a broken device and no paperwork is "did you actually buy this here, when, do you still have warranty," not an internal procurement figure. New `lookupSerialService()` in `repair-ticket.service.ts` resolves a single free-text term against `serialNumber` OR `imeiNumber` OR `imei2Number` in one query, then (if the unit was ever sold) fetches its linked `Invoice` and the matching `InvoiceItem` line by `productId` for the unit price, alongside the full repair ticket history `getSerialServiceHistory()` already knew how to build — reusing that same query shape rather than a third copy of it.

Left the existing IMEI Lookup box completely untouched (still useful for a fast IMEI-only status check) and added a new, separate **Service Lookup** box directly below it on `SerialTrackingScreen.tsx`, gated on `repair_rma` (matching the Repair Tickets sidebar entry's own module, not `imei_tracking` — a jewellery shop with serial tracking but no RMA workflow has no use for repair history). Unlike `RepairTicketsScreen.tsx`'s own deliberate English-only convention, `SerialTrackingScreen.tsx` is a shared, i18n'd screen used by every serial-tracking vertical, so every new string here got real translations across all 13 languages, not hardcoded English.

New AI intent `electronics.serialServiceLookup` — its own genuinely new *shape* of intent, not just a new topic: unlike items 1-4's aggregate/summary questions, this one needs a specific serial or IMEI value pulled out of the question text. Reused the existing generic `extractSearchTerm`/`params.searchTerm` mechanism `ai-query.service.ts` already established for customer/invoice lookups (a quoted string, then a code-like alphanumeric token, then a bare name) rather than inventing a second extraction path — the same mechanism already correctly picks a serial number or IMEI out of a sentence since both are code-like tokens.

- No schema migration needed — every field this item surfaces (`invoiceId`, `soldDate`, `purchaseDate`, `unitCost` on `ProductSerial`; `invoiceNumber`/`invoiceDate`/`customer`/`items` on `Invoice`) already existed from earlier phases.
- Unit tests: 11 new (`repair-ticket.service.test.ts` — 6: empty-search-term rejection, not-found for no match, single-query OR-search across serial/IMEI/IMEI2 (query-shape lock-in), purchase info correctly assembled from the linked invoice, `purchase: null` when never sold, full repair ticket history included; `ai-vertical-templates.service.test.ts` — 4: headline/details with ticket count and purchase line, "never sold" phrasing, `isEmpty` true with no search term extracted, `isEmpty` true when the term matches no device — plus the pre-existing registration test updated to expect all five Electronics intents; `ai-query.service.test.ts` — 1: fast-path routing with real searchTerm extraction, locking in that a scanned/typed serial number ends up as the literal argument passed to `lookupSerialService`). Full suite **3020/3020**, both tsconfigs clean.
- i18n: 7 new keys (`inventory.{serviceLookup,serviceLookupPlaceholder,serviceLookupNotFound,purchaseHistory,neverSold,repairHistory,noRepairHistory}`), all 13 languages, key parity re-verified (**4187/4187 leaf keys**, up from 4180).
- Manual: Electronics' existing "IMEI tracking" section (not "Repair / RMA tickets" — this feature lives on the Serial Tracking screen, not the Repair Tickets screen) extended with a new paragraph describing the Service Lookup box, all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/15-electronics-serials.js` extended a fifth time, 82/82, two consecutive clean runs): looked up the suite's own real sold serial by its serial number via the live API, confirmed the correct product, the correct purchase info (real invoice number/date/customer/₹25,000 unit price from the earlier live billing step), and the ticket history correctly includes the suite's own repair ticket; looked up the SAME unit by its IMEI instead and confirmed it resolves to the identical serial record; confirmed a bogus search term is correctly rejected with `RPR-025`; confirmed via the real Serial Tracking UI that the Service Lookup box is present, accepts the serial number, renders without an error boundary, and shows both the real product name and the real repair claim number on screen.
- No new bugs surfaced during this item's own live-verification pass.

**Electronics' entire 5-item signature-win set is now complete** — the 11th of Phase 67's 23 verticals to reach that, and the fifth of the 17 Product & Retail / Legacy Generic Service verticals (after Restaurant, Retail, Hardware, General).

### 41. Clothing's item 1 (Season/collection sell-through report) — its 1st of 5 signature items, the first-ever work on this vertical this phase

Grounding confirmed the artifact's own 5.0/10 rating for Clothing ("Variant tracking + returns, with a dedicated variant-stock report — reasonable but thin") and its named gap: no season/collection grouping exists anywhere in the schema, and no report answers "which collection is actually selling." Found the exact right precedent to reuse rather than re-derive: Retail's own `generateCategorySellThroughReport()` (Phase 67 earlier this same session) already established the sell-through formula (`unitsSold / (unitsSold + currentStock)`, month-by-month, zero-filled, compared against current stock as a disclosed trend simplification rather than true point-in-time history) — this item is byte-for-byte the same shape, grouped by a new `Product.season` field instead of `ProductCategory`.

New `Product.season` (nullable free-text, not a controlled enum — a shop's own collection naming varies too much for a fixed list), surfaced in `ProductFormModal.tsx` right next to the existing `gender` field (same `variantTrackingEnabled` gate, same apparel-only relevance). New `generateSeasonSellThroughReport()` reuses `generateCategorySellThroughReport()`'s exact algorithm and RETURN-sign-correction convention. New Reports-screen entry gated on `requiredBusinessType: ['CLOTHING', 'FOOTWEAR']` (the audit's own field note flags Footwear as sharing Clothing's module set byte-for-byte) — its on-screen view is this file's first genuine line+bar combo chart (`ComposedChart`, matching the artifact's own explicit "(line + bar combo)" chart-form note): a `Bar` per season/collection across months, overlaid with a `Line` showing the overall average sell-through trend.

New AI intent `clothing.seasonSellThrough` registered for both CLOTHING and FOOTWEAR alongside the pre-existing `retail.variantStock` intent, mirroring `retail.categorySellThrough`'s own intent almost exactly (same "leading category/season this month" framing) — a real Report with a real question, so it earns its own intent per Section 1.2's rule, same reasoning as every other new report this phase.

- Schema: new migration `20260820150000_phase67_clothing_season_field` (one plain `ALTER TABLE ADD COLUMN`, no FK), applied via the same direct-raw-SQL-against-the-real-dev-DB workaround every migration this phase has required, verified via `PRAGMA integrity_check`.
- Unit tests: 9 new (`report.service.test.ts` — 5: sell-through-rate computation, multi-month zero-fill, RETURN sign correction, negative-rate clamping, honest empty result when no product has a season — all mirroring `generateCategorySellThroughReport`'s own existing test coverage 1:1; `ai-vertical-templates.service.test.ts` — 3: registration for both CLOTHING and FOOTWEAR, leading-season headline sorted highest-first, empty-state phrasing; `ai-query.service.test.ts` — 1: fast-path routing). Full suite **3029/3029**, both tsconfigs clean.
- i18n: 7 new keys (`defs.seasonSellThrough.{label,description}`, `col.season`, `summary.{sellThroughBySeasonMonth,avgSellThroughBySeason,overallAverage}`, `empty.seasonSellThrough`), all 13 languages, key parity re-verified (**4194/4194 leaf keys**, up from 4187).
- Manual: Clothing's existing "Variant tracking (size & colour)" section's Gender paragraph extended to also describe the new Season/Collection field; a brand-new `## Season/Collection Sell-Through Report` heading added before the pre-existing Returns section — Clothing's first-ever new Manual heading this phase — all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/16-clothing-variants.js` — the pre-existing Clothing suite, extended for the first time since its original creation, 36/36, two consecutive clean runs): tagged the suite's own real product with a season via the live API, made a fresh, deliberately-unreturned sale of 2 units so the report had a genuine non-zero number to verify independent of the suite's own pre-existing return-tested sale, confirmed the report API returns the exact right `unitsSold`/`currentStock`/`sellThroughRate` for the current month, then confirmed via the real Reports UI that the tile is present, the report renders without an error boundary, and the real season name appears on screen.
- **A real, generalizable E2E test-authoring bug — already found and documented in Electronics' own suite this phase (see [[feedback_button_div_locator_gotcha]]) — was found to also be present here and fixed proactively, not just in the new step**: this suite's own pre-existing `'button, div'` combined locator for the Variant Stock Report tile had the identical latent bug (the non-clickable parent category-group div matches first). Fixed to `button`-only, confirmed still passing.

Clothing's remaining 4 items (size-curve reorder suggestion, size × style heatmap report, size/color exchange workflow, margin by brand/vendor report) remain fully untouched.

### 42. Clothing's item 2 (Size-curve reorder suggestion) — its 2nd of 5 signature items

Grounding surfaced a genuine architectural boundary before any code was written: `PurchaseOrderItem` has no `variantId` field at all — the entire PO/GRN pipeline only ever orders at the parent-`Product` level. Actually rebuilding variant-level purchasing (new schema, new GRN receiving logic, new PO line-item UI) would be a far larger change than a single signature item calls for, and isn't what the artifact's own wording — "auto-weights the reorder ratio toward the sizes that actually sell" — asks for either. Scoped this correctly as a read-only **suggestion/breakdown** the owner consults before placing the real order manually, not a new ordering mechanism.

New `getSizeCurveReorderSuggestion()` in `variant.service.ts`: given a product and a total quantity to reorder (defaulting to that product's own configured `Inventory.reorderQuantity` if not supplied), looks at real sales per variant over a 90-day lookback (the same recency window Dead Stock Clearance already established), then weights the quantity proportionally to each variant's own share of recent sales — using largest-remainder rounding so the suggested quantities always sum exactly to the requested total, never drifting a unit short or over. A product with zero sales signal for every variant falls back to a neutral even split rather than suggesting zero across the board, which would be actively unhelpful for a brand-new line. Same RETURN sign-correction convention as every other report/aggregation in this codebase, clamped at zero per variant.

Surfaced as a new collapsible **Suggested Reorder Split** section in `VariantManagementModal.tsx` (the existing "Manage Variants" screen) — enter a quantity (or leave blank to use the product's own reorder setting) and see a per-variant table of recent sales and suggested quantity. This screen is hardcoded English throughout (same as `ProductFormModal.tsx`), so no i18n keys needed for the new UI text, consistent with the rest of the screen.

New AI intent `clothing.sizeCurveReorderSuggestion` — unlike item 1's own aggregate sell-through question, this one genuinely needs a specific product named in the question. Reuses the exact product-name-or-SKU lookup shape `ai-query.service.ts`'s own pre-existing `inventory.productCostBasis` intent already established, rather than inventing a new resolution mechanism.

- Schema: none needed — every field this item touches already existed.
- Unit tests: 13 new (`variant.service.test.ts` — 8: no-variants rejection, no-quantity-and-no-configured-reorderQuantity rejection, falls back to the product's own reorderQuantity, proportional weighting by recent sales, largest-remainder rounding sums exactly to the requested total, even-split fallback when no variant has any sales signal, RETURN sign correction clamped at zero, query-shape lock-in on the 90-day/ACTIVE-invoice window; `ai-vertical-templates.service.test.ts` — 4: reports the suggested split for a named product, `isEmpty` true with no search term, `isEmpty` true when no product matches, `isEmpty` true when the matched product has no configured reorder quantity; `ai-query.service.test.ts` — 1: fast-path routing). Full suite **3042/3042**, both tsconfigs clean.
- i18n: none needed — `VariantManagementModal.tsx` is a hardcoded-English screen, same convention as `RepairTicketsScreen.tsx` and `ProductFormModal.tsx`.
- Manual: Clothing's existing "Variant tracking (size & colour)" section extended with a new paragraph describing the Suggested Reorder Split feature, all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/16-clothing-variants.js` extended a second time, 44/44, two consecutive clean runs): reused the suite's own real product and its existing sales state (M/Blue at net-zero recent sales from an earlier return, L/Red with 2 fresh unreturned sales from item 1's own step) as a clean, deterministic case — requested a 30-unit reorder split via the live API and confirmed the entire quantity weighted onto L/Red (the only variant with any recent sales signal) while M/Blue correctly got zero, then confirmed via the real Manage Variants UI that the section toggles open, computes on click, renders without an error boundary, and shows the real suggested quantity on screen.
- No new bugs surfaced during this item's own live-verification pass.

Clothing's remaining 3 items (size × style heatmap report, size/color exchange workflow, margin by brand/vendor report) remain fully untouched.

### 43. Clothing's item 3 (Size × style heatmap report) — its 3rd of 5 signature items

Grounding confirmed the artifact's own framing needs no new capture at all: `Product` already IS the "style" (its `productName`), and `ProductVariant.size` already IS the size — this is a live grouping over existing `InvoiceItem` rows, same no-new-capture-needed precedent as General's own Category Mix report earlier this phase. The one real design decision was HOW to resolve size per sale: `InvoiceItem.variantInfo` is a free-text sale-time snapshot (`"M / Blue"`), ambiguous to re-parse (colour-only vs. size-only vs. both) — resolved via a real `ProductVariant` join by `variantId` instead, the actual source of truth.

New `generateSizeStyleHeatmapReport()` aggregates units sold per (style, size) cell within a date range, with the same RETURN sign correction every other report in this file already uses, clamped at zero per cell. Capped to the top 15 styles by net units sold so the grid stays legible on a large catalog — the same table-scaling instinct this file's other bounded reports already follow. New `compareSizes()` helper sorts sizes using a real clothing-size-aware order (XS through 3XL) rather than plain alphabetical, which would otherwise put L before M before S — a genuinely wrong reading order for a size chart.

Found a real, directly reusable precedent for the on-screen rendering: Restaurant's own Table Turnover by Hour report (Phase 67 earlier this session) is already a hand-built CSS-grid heatmap — this codebase has no charting-library heatmap primitive, and a small grid doesn't need one. `SizeStyleHeatmapView` follows that exact same structural pattern (colour-intensity cells, a `title` tooltip per cell, graceful empty state), just with a variable-width style×size grid instead of a fixed 7×24 day/hour one. Same "no PDF chart representation" decision as Table Turnover too — a heatmap grid isn't expressible in any of this file's existing PDF chart types (bar/line/pie/scatter), so `getReportCharts()` returns `[]` and the PDF export relies on the already-complete cell table instead.

New AI intent `clothing.sizeStyleHeatmap` — an aggregate whole-catalog question (unlike item 2's own per-product one), reporting this month's single top-moving style/size combination.

- Schema: none needed — every field this item touches already existed.
- Unit tests: 11 new (`report.service.test.ts` — 8: groups by style+size via a real ProductVariant join, confirms it resolves size via the join rather than the free-text `variantInfo` snapshot, RETURN sign correction clamped at zero, caps the grid to the top 15 styles by net units sold, computes the summary (total + single top cell), sorts sizes in clothing-size-aware order rather than alphabetical, honest empty result, query-shape lock-in on the ACTIVE-invoice/variantId-not-null filter; `ai-vertical-templates.service.test.ts` — 2: reports the top-moving combination this month, `isEmpty` true with no cells; `ai-query.service.test.ts` — 1: fast-path routing). Full suite **3053/3053**, both tsconfigs clean.
- i18n: 8 new keys (`defs.sizeStyleHeatmap.{label,description}`, `col.style`, `summary.{totalUnitsSold,topCombination,topCombinationUnits,sizeStyleHeatmap}`, `empty.sizeStyleHeatmap`), all 13 languages, key parity re-verified (**4202/4202 leaf keys**, up from 4194).
- Manual: a brand-new `## Size × Style Heatmap Report` heading added to Clothing's chapter, right after the Season/Collection section — Clothing's second new Manual heading this phase, all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/16-clothing-variants.js` extended a third time, 51/51, two consecutive clean runs): confirmed via the live API that this month's heatmap correctly shows the suite's own M/Blue at a real zero cell (1 sale, 1 return — a genuine net-zero row, not a missing one) alongside L/Red's real 2 unreturned units, and that both sizes appear in the response's own `sizes` list in the right order; confirmed via the real Reports UI that the tile is present, the grid renders without an error boundary, and the real style name appears on screen.
- No new bugs surfaced during this item's own live-verification pass.

Clothing's remaining 2 items (size/color exchange workflow, margin by brand/vendor report) remain fully untouched.

### 44. Clothing's item 4 (size/color exchange workflow) — its 4th of 5 signature items

Grounding surfaced a real architectural landmine before any code was written: this codebase's revenue/tax/analytics reports (`report.service.ts`, `analytics.service.ts`, `ai-aggregations.service.ts` — 15+ call sites) all derive an item's sign purely from its OWN invoice's `invoiceType` (`'RETURN'` = −1, everything else = +1), applied uniformly across every line on that invoice. A single combined "EXCHANGE" document carrying both a surrendered-item line (negative) and an acquired-item line (positive) would silently corrupt every one of those reports — the returned leg would get counted as positive revenue right alongside the new sale. Deliberately rejected that design in favor of orchestrating two ALREADY-correct, separately-proven primitives instead: a real `invoiceType='RETURN'` invoice for the surrendered item (by calling `createReturn()` wholesale, not reimplementing it) and a real `invoiceType='RETAIL'` sale for the replacement (via `billingService.createInvoice()`, the same cross-service pattern ~15 other services in this codebase already use), linked by a new `Invoice.exchangeReturnId` self-relation column mirroring `originalInvoiceId`'s/`splitFromInvoiceId`'s own lineage precedent. Zero report-layer changes needed anywhere — both documents are ordinary, already-correctly-handled invoice types.

New `exchange.service.ts`'s `createExchange()`: a courtesy pre-check on the replacement variant's stock BEFORE the return leg runs (so the common "we don't have that size" case never leaves a customer with a completed return and no replacement in hand — the real, race-safe guard is still `decrementVariantStockTx`'s own check inside the sale leg), then the return leg, then the sale leg priced at the replacement's own current price and tax rate (not the old item's), then the link. If the sale leg fails after the return already committed, the error message says so explicitly rather than reading as a bare, misleading failure. Extended `returns.service.ts`'s own already-returned-quantity guard (`getReturnedAwayQuantities`, extracted as a shared helper) to also scan `EXCHANGE`-linked `RETURN` rows — a `RETURN`/`RETAIL` pair from a prior exchange now correctly blocks a second return OR a second exchange from double-dipping on the same original line, closing what would otherwise have been a real gap between the two workflows.

Renderer: `ReturnScreen.tsx` (shared/i18n'd, app-wide, not Clothing-specific — the feature naturally scopes itself to variant-tracked lines only) gained a per-item **Exchange** button next to the existing return-quantity stepper, opening an inline panel to pick a replacement size/colour from live stock, a quantity, a settlement payment method, and a reason; on success it shows the exact net amount due or refundable (or "no balance due" for an exact-price exchange) rather than leaving the cashier to work it out. New AI intent `clothing.exchangeSummary` — deliberately "exchange"-scoped so it doesn't collide with the pre-existing `sales.returnsAndRefunds` intent's own bare "return" wording, and answers a genuinely different question: `sales.returnsAndRefunds` already counts an exchange's own RETURN leg bundled in with every plain refund return, unable to tell them apart, while this counts only the unambiguous exchange-linked `RETAIL` invoices specifically.

- Schema: new nullable `Invoice.exchangeReturnId` self-relation column + index (`ADD COLUMN`, no table rebuild).
- Unit tests: 10 new (`exchange.service.test.ts` — 6: full exchange with correct netAmountDue sign in both directions, rejects same-variant "exchange," rejects out-of-stock replacement before the return leg runs, propagates the return leg's own guard failure without touching the sale leg, explicit dual-failure messaging when the sale leg fails after the return committed; `ai-vertical-templates.service.test.ts` — 3: registration, count/value computation scoped to `exchangeReturnId IS NOT NULL` only, singular-vs-plural phrasing; `ai-query.service.test.ts` — 1: fast-path routing). Full suite **3063/3063**, both tsconfigs clean.
- **A real bug in this item's own new code, caught by its own unit tests before it ever reached live verification**: `createExchange()` initially read `returnRes.data.returnInvoiceNumber` off `createReturn()`'s result — `createReturn()` actually returns the field as plain `invoiceNumber`. The very first assertion of the very first test (checking the exchange's own returned `returnInvoiceNumber`) failed immediately, caught and fixed before any live-app testing began — exactly the kind of interface-mismatch bug unit tests exist to catch cheaply.
- i18n: 20 new keys (`returns.exchange.*` — action/title/newVariant/selectVariant/outOfStock/quantity/paymentMethod/reason/reasonPlaceholder/reasonRequired/variantRequired/noVariants/submit/cancel/success/successMsg/amountDue/amountRefund/amountEven/error), all 13 languages, key parity re-verified (**4222/4222 leaf keys**, up from 4202) — `ReturnScreen.tsx` is a shared, app-wide screen (unlike Clothing's own English-only vertical screens), so this got full translation, not hardcoded English.
- Manual: extended Clothing's existing `## Returns` section with a new paragraph (no new heading), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/16-clothing-variants.js` extended a fourth time, 66/66, two consecutive clean runs): via the real API, a fresh sale exchanged for a different variant restored the old variant's stock, decremented the new variant's stock by exactly the right amount, computed `netAmountDue === 0` for two same-priced variants, and produced a real linked `RETAIL` invoice whose `exchangeReturnId` was verified via a direct DB read (not just trusting the API response) to point at the real `RETURN` invoice created alongside it; a second exchange attempt against the same already-exchanged line was correctly rejected with `RET-007`, confirming the shared guard extension actually works end-to-end, not just in a unit-test mock. Separately, via the real Returns screen UI end-to-end: searched an invoice, clicked the real Exchange button, picked a replacement variant from the live dropdown, submitted, and confirmed the real success banner plus the real stock decrement.
- **Two real bugs found+fixed during this item's own live-verification pass, both worth remembering broadly**: (1) A genuinely new, generalizable Prisma gotcha, distinct from the already-documented Git-Bash-mangles-paths one — `npx prisma migrate deploy` run via the PowerShell tool printed "Applying migration ... All migrations have been successfully applied" for this item's own new migration, and `prisma migrate status` afterward agreed the schema was "up to date," yet a direct `node:sqlite` read of the actual dev DB file showed the column never actually existed and the migration was never recorded in `_prisma_migrations` at all — only surfaced because the dev server's own first real query against the new column then failed with a live Prisma schema-mismatch error. Fixed by applying the `ALTER TABLE`/`CREATE INDEX` directly via `node:sqlite` and manually inserting the tracking row, then re-verified `prisma migrate status` agreed and the dev server started clean. **Worth verifying via a direct `node:sqlite` read after ANY future migration in this project, even when the CLI itself reports success** — the CLI's own reported success is not reliable proof by itself. (2) A real E2E test-authoring bug: `page.getByRole('button', { name: 'Search' })` resolved to TWO elements on the Returns screen — the app's own global header search (Ctrl+K) and the screen's own Search button, both accessible-named "Search" — fixed by pressing Enter in the invoice-number input instead of clicking the ambiguous button, sidestepping the collision entirely rather than trying to scope the locator further.
- **One unrelated pre-existing bug found+fixed while getting a clean `tsc --noEmit -p tsconfig.web.json`, not a regression from this item's own work**: `CustomDocumentsScreen.tsx` (General's item 2, committed in `ee3bb4a`) passed an invalid `danger` prop to `ConfirmDialog` — the component's real prop is `confirmVariant?: 'danger' | 'primary'` (already defaulting to `'danger'`), so `danger` was simply typo'd and never existed on the props interface at all. Fixed by removing the redundant prop (the default already produced the intended styling).

Clothing's remaining 1 item (margin by brand/vendor report) remains fully untouched — it can reuse the pre-existing `Product.defaultSupplierId` as "vendor/brand," no new field needed, closing the vertical out entirely once done.

### 45. Clothing's item 5 (Margin by Brand/Vendor Report) — its 5th and FINAL signature item, closing the vertical's entire 5-item set

Grounding confirmed no new capture needed at all: `Product.defaultSupplierId` (already used for purchasing) IS "brand/vendor" per the audit's own field note — no dedicated brand field exists in this schema, and none is needed. Structurally reused General's own `generateCategoryMixReport()` grouping shape verbatim (same "single-period revenue breakdown by X, uncaptured rows left out entirely rather than bucketed as Unassigned" pattern), keyed by supplier instead of category, with COGS added via `getProductCostsBatch()` — the same valuation-method-aware cost resolver `generateProfitAndLossReport()` already uses, deliberately NOT the stale hand-edited `Product.costPrice` field directly.

New `generateVendorMarginReport()`: revenue signed correctly at the DB level (same convention every other report in this file already relies on), COGS sign-corrected for RETURN invoices (quantity always stored positive, so a return's cost must drop too — same reasoning `generateProfitAndLossReport`'s own COGS computation already documents), margin = revenue − COGS, sorted by margin descending. A vendor whose margin comes out negative is reported honestly as a loss (not floored at zero or hidden) — genuinely useful signal a plain revenue-only view would miss entirely. Renderer: new `VendorMarginView` follows the RmaAgingView/VendorRecoveryLedgerView bar-chart template (not Category Mix's own pie — margin can go negative, which a pie slice can't honestly represent), coloring loss-making vendors in the danger color rather than letting them blend in with a merely-thin-margin one. New AI intent `clothing.vendorMargin`, deliberately "vendor/brand/supplier + margin"-scoped so it doesn't collide with the pre-existing `fastSlowMoverMatrix` intent's own bare "margin" wording.

- Schema: none needed — `Product.defaultSupplierId` already existed.
- Unit tests: 12 new (`report.service.test.ts` — 8: groups revenue/COGS/margin by vendor via the real `getProductCostsBatch()`, RETURN sign correction on COGS but not on the already-signed `lineTotal`, sorts by margin descending, reports a negative margin honestly for a loss-making vendor, honest empty result with no suppliers, `marginPercent` 0 when revenue is 0, excludes items with no vendor/supplier assigned, only counts ACTIVE invoices in range; `ai-vertical-templates.service.test.ts` — 3: registration, reports the leading vendor by margin this month, honest empty-state headline; `ai-query.service.test.ts` — 1: fast-path routing). Full suite **3075/3075**, both tsconfigs clean.
- i18n: 5 new keys (`defs.vendorMargin.{label,description}`, `empty.vendorMargin`, `summary.{totalMargin,marginByVendor}` — every other string this item needed, col.supplier/col.margin/col.marginPercent/col.revenue/summary.cogs/summary.totalRevenue/summary.vendorCount, already existed and was reused as-is), all 13 languages, key parity re-verified (**4227/4227 leaf keys**, up from 4222).
- Manual: brand-new `## Margin by Brand/Vendor Report` heading added to Clothing's chapter (Clothing's third new Manual heading this phase), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/16-clothing-variants.js` extended a fifth time, 78/78, two consecutive clean runs): assigned a real vendor to the suite's own product, made a fresh deterministic 3-unit sale, confirmed via the live API the report includes that vendor with internally-consistent revenue/COGS/margin math (`revenue − cogs === margin`, `marginPercent` correctly derived), and confirmed the real Reports UI tile renders with the real vendor name on screen.
- **A real, pre-existing, generalizable cost-basis gotcha surfaced (and worked around, not "fixed") during this item's own live-verification pass, worth remembering broadly**: a product created with zero opening quantity deliberately keeps `Inventory.averageCost` at `0` forever until a real GRN/purchase establishes it (`product.service.ts`'s own documented RULE I007) — this is existing, intentional behavior elsewhere in the codebase, not a bug introduced here, but it meant this item's own E2E test product (created via a plain `products.create()` call with no opening quantity, exactly how most of this suite's own earlier steps already created it) resolved to a real COGS of exactly `0` via `getProductCostsBatch()`, even though the test set a real `costPrice`. The report's math was completely correct; only the test's assumption that costPrice would be used was wrong. Fixed by seeding a real `Inventory.averageCost` directly via `node:sqlite` before running the report — the same "simulate real state via direct DB write" precedent Electronics' own RMA SLA item already established for backdating dates. **Worth remembering for any future E2E test that needs a genuinely non-zero, non-degenerate COGS figure**: a plain `products.create()` call alone is not enough; either pass a real `openingQuantity`, or seed `Inventory.averageCost` directly.
- No new bugs in this item's own code — the Prisma migration workflow needed no schema change at all this time, sidestepping the exact class of risk item 4 hit.

**Clothing's entire 5-item signature-win set is now complete** — the 12th of Phase 67's 23 verticals to reach that, and the 6th of the 17 Product & Retail / Legacy Generic Service verticals (after Restaurant, Retail, Hardware, General, Electronics).

### 46. Footwear's item 1 (Half-size/width matrix) — its 1st of 5 signature items, the first-ever work on this vertical this phase

Grounding first confirmed something important that later checks correctly flagged as a real risk in the roadmap: Footwear was previously rated 4.5/10 in the source audit precisely because it "byte-for-byte" shares Clothing's own module set with zero differentiator of its own — the exact "shipped twice under two names" pattern the codebase had already fixed once for Architect/Civil Engineer, left unfixed here. Fetching the audit artifact directly confirmed Footwear does NOT inherit Clothing's own 5 signature items — it has its own distinct list (half-size/width matrix, brand-wise margin & return-rate report, trial-pair counter workflow, size availability heatmap, seasonal reorder calendar), grounded before any code was written rather than assumed from the shared module set.

New `ProductVariant.width` — free text, same convention as the pre-existing `size`/`color` fields, nullable and simply unused for every business type other than FOOTWEAR (the UI never even renders the field elsewhere). `VariantManagementModal.tsx`'s existing "Generate Size × Colour Matrix" generalized to a real 3rd dimension: a Width column + matrix-generator input, conditionally rendered only when the active business type is FOOTWEAR (checked via `useIndustryStore()`, mirroring how `Product.season`/`gender` are already conditionally shown for Clothing). The matrix-generation algorithm itself was rewritten as a genuine N-dimensional cartesian product (any of size/width/colour can be left blank, collapsing to a placeholder) rather than hand-rolling a 3-way special case — and because width defaults to an always-empty list for every non-Footwear vertical, this degrades to the exact original 2D behavior automatically, with zero behavior change for Clothing.

Width was carried through to every place a variant's identity is already shown, not just Manage Variants: the Billing "Select Variant" picker and cart line, printed price labels (both the on-screen preview and the actual server-side label HTML generator in `billing.handler.ts`), the Returns screen's exchange-variant picker, the Suggested Reorder Split table, and the AI-facing `clothing.sizeCurveReorderSuggestion` intent's own row identity — 9 call sites total, found via a systematic grep sweep rather than touched ad hoc, so no "two same-size-same-colour-different-width pairs look identical" gap was left behind anywhere the feature actually needed it. Deliberately did NOT add a new AI intent for this item — it's a pure data-capture/UI-generation capability (the same category as the pre-existing "Generate Size × Colour Matrix" tool it extends, which itself has never had an AI intent), not a fact answerable by a natural-language question, matching this phase's own established precedent for skipping an intent when there's genuinely nothing to ask.

- Schema: new nullable `ProductVariant.width` column (`ADD COLUMN`, no table rebuild).
- Unit tests: 5 new (`variant.service.test.ts` — width flows correctly through `getSizeCurveReorderSuggestion`'s own row identity distinguishing two same-size-same-colour-different-width variants; `upsertVariants` persists width on create, on update, and correctly clears it to null when omitted; `getVariantSummary` returns the distinct set of widths actually used, empty for a non-Footwear product that never sets one). Full suite **3080/3080**, both tsconfigs clean.
- i18n: none needed — `VariantManagementModal.tsx` is a hardcoded-English screen (matching Clothing item 2's own precedent), so the new Width column/inputs needed no translation.
- Manual: Footwear's own chapter (`business__footwear.md`) previously said, verbatim, "It works identically" to Clothing with zero real content of its own — the first-ever substantive Manual content for this vertical, a new "Half-Size & Width Matrix" section, all 13 languages.
- **Live-verified against the real running app** via a brand-new `tests/e2e/suites/69-footwear-variants.js` (first-ever E2E coverage of this vertical), **30/30, two consecutive clean runs**: generated a real 2×2×1 size×width×colour matrix via the real Manage Variants UI, confirmed two same-size-same-colour-different-width variants persist as genuinely distinct rows; sold a specific width variant via the real Billing "Select Variant" picker (confirmed the picker itself shows width, e.g. "8 / Wide / Black", not just size/colour); confirmed only that exact variant's stock was deducted, the same size in a different width was untouched; ran a real exchange (item 4's own workflow) onto a different width of the same style, confirmed the new invoice line carries the new width; generated a real barcode and print-label preview for a third width/size combination and confirmed the printed label HTML includes it.
- **Two real bugs found+fixed during this item's own live-verification pass**: (1) A genuinely new E2E locator gotcha, worth remembering broadly: `getByPlaceholder()` does a substring match by default, not exact — the matrix generator's own Width input (placeholder `"Regular, Wide"`) was ambiguous against the per-row Width input's placeholder (`"Regular, Wide…"`, present from the modal's own initial blank row before Generate is even clicked), since the former is a substring of the latter. Fixed with `{ exact: true }`. (2) A bug in this item's own new E2E test, not the product: an assertion checking that a same-size-different-width variant's stock was untouched by a sale used the wrong expected constant (mixed up which of two variants started at stock 10 vs. 11) — caught immediately by the test itself failing honestly, not silently passing for the wrong reason.

Footwear's remaining 4 items (brand-wise margin & return-rate report, trial-pair counter workflow, size availability heatmap, seasonal reorder calendar) remain fully untouched.

### 47. Footwear's item 2 (Brand-Wise Margin & Return-Rate Report) — its 2nd of 5 signature items

Grounding confirmed this is deliberately NOT the same item as Clothing's own item 5 (Margin by Brand/Vendor) despite the surface similarity — the audit's own framing is specifically "footwear returns run higher than apparel; track it by brand," a genuinely different question (is this brand's margin being quietly eaten by returns) from a plain margin ranking. Discovering this also surfaced a real pre-existing gap from earlier in this arc: the AI-intent registration for CLOTHING and FOOTWEAR had been sharing one array (`case 'CLOTHING': case 'FOOTWEAR':`), which meant Footwear was incorrectly getting `clothing.vendorMargin`'s own AI intent — split into two separate cases now that the two verticals' item lists have genuinely diverged, keeping the 4 truly-shared variant-tracking intents for both.

New `generateBrandMarginReturnRateReport()` — deliberately a separate function from `generateVendorMarginReport()`, not a `returnRate` field bolted onto it, matching this file's own precedent (Category Sell-Through vs. Season Sell-Through: algorithmically similar reports on different verticals, never merged). Units sold and units returned are tracked SEPARATELY per supplier (not net-summed the way revenue/COGS already are) — a return rate needs the real numerator/denominator, and netting would mask a brand with equally high sales and returns as if nothing were wrong. New `BrandMarginReturnRateView` — the file's first bar+line combo chart on a category (not time-series) X-axis, using the same dual-`yAxisId` ComposedChart mechanism the Distributor scheme-cost-vs-volume chart already established for two differently-scaled series, generalized from line+line to bar+line. New AI intent `footwear.brandMarginReturnRate` — always states the return rate alongside the leading brand's margin, not just "who makes the most."

- Schema: none needed — reuses `Product.defaultSupplierId`.
- Unit tests: 11 new (`report.service.test.ts` — 6: groups revenue/margin/units-sold/units-returned/return-rate by brand with units tracked separately not netted, computes overall return rate across all brands combined, 0% return rate with no units sold yet, sorts by margin descending, honest empty result, excludes unassigned products; `ai-vertical-templates.service.test.ts` — 4: FOOTWEAR does NOT register `clothing.vendorMargin` anymore (a real behavior-correction test, not just a new-feature test), registers `footwear.brandMarginReturnRate` instead, reports the leading brand with its return rate, honest empty-state; `ai-query.service.test.ts` — 1: fast-path routing to the correct per-vertical template despite an overlapping regex, verifying `tryFastPathClassify`'s own registered-template guard actually works, not just assumed). Full suite **3091/3091**, both tsconfigs clean.
- i18n: 6 new keys (`col.{unitsReturned,returnRatePercent}`, `defs.brandMarginReturnRate.{label,description}`, `summary.brandMarginReturnRate`, `empty.brandMarginReturnRate`), all 13 languages, key parity re-verified (**4233/4233 leaf keys**, up from 4227).
- Manual: brand-new `## Brand-Wise Margin & Return-Rate Report` heading added to Footwear's chapter (its 2nd substantive section, after item 1's own first-ever content), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/69-footwear-variants.js` extended a second time, 43/43, two consecutive clean runs): assigned a real vendor, made a real 4-unit sale followed by a real 1-unit partial return for a genuine non-degenerate return rate, confirmed via the live API the report's own math is internally consistent (revenue − cogs = margin, return rate = returned/sold) and the fresh activity is really reflected, confirmed via the real Reports UI the tile renders with the real vendor name on screen. Correctly anticipated (from Clothing item 5's own live-verification finding) that this product would need its `Inventory.averageCost` seeded directly via `node:sqlite` again, per the same RULE I007 gotcha — no new discovery needed this time, applied proactively.
- No new bugs this time — the patterns established across items 4-5 (Clothing) and item 1 (Footwear) held up cleanly on first application.

Footwear's remaining 3 items (trial-pair counter workflow, size availability heatmap, seasonal reorder calendar) remain fully untouched.

### 48. Footwear's item 3 (Trial-pair counter workflow) — its 3rd of 5 signature items

Grounding surfaced a real design question before any code was written: does "track how many pairs get tried on" require an actual stock-reservation/hold mechanism, or is it satisfied by-construction by Sarang's own existing inventory model? Confirmed the latter — `decrementVariantStockTx` has always been the ONLY place stock leaves inventory (see `project_sarang.md`'s own "sale-time only" invariant), so trying on a pair was never a stock movement to begin with, and there is nothing to "restock" afterward. The real gap was analytics, not workflow: a shop had no way to know how many sizes get tried per eventual sale, or what fraction of trial sessions convert at all — so the deliverable narrowed to a lightweight append-only `TrialSession` log (one row per session, recorded once at the point the customer buys or walks away), not a two-phase reservation system. This is a deliberate scope-narrowing decision to match the audit's own one-line "Feature" framing, not a shortcut.

New `TrialSession` model — `productId` (cascades on Product delete), `triedVariantIds` (a JSON array, matching `Invoice.exchangeReturnId`'s own precedent for not normalizing a small internal list into a join table), nullable `purchasedVariantId` (null = walked away without buying), plain unenforced `customerId`/`createdById` columns (same "log, not a relation graph" reasoning as `AuditLog`). New `trial-session.service.ts`: `recordTrialSession()` validates at least 2 tried variants, that any purchased variant was actually one of the tried ones, and that every tried variant genuinely belongs to the product — then `getTrialConversionSummary()` computes total/converted sessions, conversion rate, and average pairs tried (both per-session and per-conversion) from real aggregated data, no vertical-specific report screen needed since this is a single scalar summary, not a drill-down table (matching `retail.loyaltyProgress`'s own precedent for an AI-only intent with no dedicated Reports tile).

UI landed inside Billing's existing "Select Variant" picker rather than a new screen — a Footwear-only **Track Trial (multiple sizes tried on)** toggle switches the same tile grid from "tap to sell" into "tap to mark as tried," followed by either **Purchased: [size]** (records the session AND adds that exact variant to the cart via the same `addToCartDirect()` every other picker path already uses, not a parallel one-off) or **No purchase — record trial only**. New AI intent `footwear.trialConversionRate`, deliberately reading ALL-TIME data rather than `thisMonthRange()` like its sibling reports — a conversion rate needs a meaningful sample size, and trial sessions are a comparatively low-volume, cashier-opt-in log next to a full month of ordinary invoices.

- Schema: new `TrialSession` table (migration `20260825110000_phase67_footwear_trial_session`), applied and verified persisted via direct `node:sqlite` read — the recurring "Prisma migrate silently writes to a phantom `prisma/.dev-data/sarang.db`" gotcha (hit twice earlier this arc) was root-caused and fixed this session: always override with an absolute `DATABASE_URL` before any `prisma migrate` command. No incident this time.
- IPC: `trialSession:record` (permission `billing.createInvoice`, matching every other cashier-facing billing action) and `trialSession:conversionSummary` (permission `billing.view`), wired through channels/handler/validation/preload the standard 4-layer way.
- Unit tests: 13 new (`trial-session.service.test.ts` — 9: rejects fewer than 2 tried variants, rejects a purchased variant not in the tried list, rejects an unknown product, rejects a tried variant not belonging to the product, records with/without a purchase, deduplicates repeated ids, all-zero summary with no sessions, correct conversion-rate/average-pairs math; `ai-vertical-templates.service.test.ts` — 3: registers the new intent for FOOTWEAR, reports the conversion rate and averages, honest empty-state; `ai-query.service.test.ts` — 1: fast-path routing). Full suite **3104/3104**, both tsconfigs clean.
- i18n: none needed — the Select Variant picker modal is a hardcoded-English screen (same precedent as `VariantManagementModal.tsx` and Clothing item 2), so the new trial-mode toggle/buttons needed no translation.
- Manual: new `## Trial-Pair Counter` section added to Footwear's chapter (its 3rd substantive section), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/69-footwear-variants.js` extended a third time, **60/60, two consecutive clean runs**): a real UI-driven trial session — opened the real Billing picker, toggled Track Trial, tapped two real variant tiles to mark them tried, clicked the real "Purchased: 9 / Wide / Black" action, confirmed the purchased pair landed in the cart via the normal path and the invoice submitted cleanly; confirmed the `TrialSession` row persisted with the correct tried-list and purchased-variant id; a second no-purchase session recorded via the API layer; confirmed the conversion summary and the AI Assistant's own live answer (asked "What is our trial conversion rate?" through the real `ai:query` IPC channel) both reflect the real recorded sessions with internally consistent math.
- **Two real bugs found+fixed during this item's own live-verification pass**: (1) `ai_assistant` is off by default for every business type (opt-in via Settings) — the first AI-intent live-verification run for this item failed with an empty response until the test seeded `IndustryTemplateSetting.enabledModules` for FOOTWEAR directly, the same gotcha `uat-70-templates.js` had already independently discovered and worked around; the suite now enables it up front and restores the exact prior state (or removes the row entirely if none existed) in its own cleanup, since this is real shared dev-DB state, not disposable test data. (2) A genuinely new E2E cleanup gotcha, worth remembering broadly alongside the pre-existing ProductVariant one: because `node:sqlite` enforces foreign keys by default, `cleanupByNamePrefix`'s own `DELETE FROM Product` throws when child `TrialSession` rows still reference it and silently falls back to a soft-delete (`isActive=0`) — the Product row is never actually removed, so `TrialSession`'s own `ON DELETE CASCADE` never fires either, and orphaned session rows were accumulating across every suite run (confirmed via direct DB inspection: totals climbing 2→4→6 across consecutive runs). Fixed with the same manual pre-cleanup pattern the suite's own ProductVariant handling had already established, joining on product name before the soft-deleted Product row's rows go stale; confirmed fixed by two consecutive runs both settling back to the same low, run-scoped total.

Footwear's remaining 2 items (size availability heatmap, seasonal reorder calendar) remain fully untouched.

### 49. Footwear's item 4 (Size Availability Heatmap Report) — its 4th of 5 signature items

Grounding confirmed this is deliberately NOT the same report as the shared `clothing.sizeStyleHeatmap` FOOTWEAR already has access to (its tile is gated `['CLOTHING', 'FOOTWEAR']`) — that one answers "what sold" (a sales-history question over a date range); this one answers "what's out right now" (a live current-state stock question with no date range at all), matching `generateDeadStockClearanceReport`'s own precedent for a current-state report rather than `generateSizeStyleHeatmapReport`'s date-ranged one, despite the near-identical styles×sizes grid shape.

New `generateSizeAvailabilityHeatmapReport()` — queries `ProductVariant` directly (not `InvoiceItem`, since this reads live stock, not sales history), sums stock across colour/width for a given style×size cell (matching the audit's own "is size 9 available in THIS style at all" framing, not broken down further by colour), classifies each cell OUT/LOW/IN against a configurable `lowStockThreshold` (default 3), and — unlike the sales heatmap's highest-volume-first ranking — surfaces the styles with the MOST stockouts first, since there's no "units sold" concept for a live snapshot and gap count is the actionable signal. Reuses `MAX_HEATMAP_STYLES`/`compareSizes` from the sales heatmap as-is, no duplication. New `SizeAvailabilityHeatmapView` mirrors `SizeStyleHeatmapView`'s CSS-grid layout exactly, swapping the sales-intensity colour gradient for a status colour (red/amber/green) plus a legend. New AI intent `footwear.sizeAvailabilityHeatmap`, whose fast-path regex deliberately avoids the word "heatmap" — FOOTWEAR has BOTH this intent and the shared `clothing.sizeStyleHeatmap` registered simultaneously, and `tryFastPathClassify` is strictly first-match-wins by array order, so a colliding pattern would have silently misrouted every "size...heatmap"-shaped question to the wrong (sales, not availability) intent for this vertical specifically.

- Schema: none needed — reuses `ProductVariant.stockQty`/`size`/`width`/`color` as-is.
- IPC: `reports:sizeAvailabilityHeatmap` (permission `reports.inventory`, matching every other inventory report), wired through channels/handler/preload the standard way; renderer tile gated `requiredBusinessType: 'FOOTWEAR'` only (not `['CLOTHING','FOOTWEAR']` like the sales heatmap) since availability is Footwear's own distinct signature item, not a shared one.
- Unit tests: 13 new (`report.service.test.ts` — 9: sums stock across colour/width per cell, classifies OUT/LOW/IN correctly against the threshold, respects a custom threshold, defaults to 3, ranks styles by gap count not stock volume, counts out-of-stock/low-stock cells in the summary, clothing-size-aware sort order, honest empty result, queries only active variants of active products with a size set; `ai-vertical-templates.service.test.ts` — 3: registers the intent for FOOTWEAR, leads with the style with the most gaps, two honest empty-state variants; `ai-query.service.test.ts` — 1: fast-path routing, explicitly confirming it does NOT collide with the earlier `clothing.sizeStyleHeatmap` entry despite both being registered for FOOTWEAR simultaneously). Full suite **3118/3118**, both tsconfigs clean.
- i18n: 12 new keys (`defs.sizeAvailabilityHeatmap.{label,description}`, `summary.{sizeAvailabilityHeatmap,totalStyles,outOfStockCells,lowStockCells,styleWithMostGaps}`, `empty.sizeAvailabilityHeatmap`, `col.status`, `status.{out,low,in}`), all 13 languages via a programmatic anchor-based insertion script (dry-run verified against all 12 non-English locales before writing), key parity re-verified (**4245/4245 leaf keys**, up from 4233).
- Manual: new `## Size Availability Heatmap` heading added to Footwear's chapter (its 4th substantive section), all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/69-footwear-variants.js` extended a 4th time, **69/69, two consecutive clean runs**): seeded deterministic stock levels directly (one size fully out, one summing to a healthy total across two different-width variants) rather than relying on inherited state from earlier steps in the same run, confirmed the report's own math live via the API (stock correctly summed across width, OUT/IN classification correct, gap-count summary correct), confirmed the AI Assistant's own live answer to "Which sizes are out of stock?" through the real `ai:query` channel routes to the new intent and not the pre-existing shared one, and confirmed the real Reports UI tile renders with the product's own name on screen.
- **One real infrastructure bug found+fixed during this item's own live-verification pass, worth remembering broadly**: the dev server (electron-vite) had been running continuously since the start of this Footwear item-3/item-4 work session, and its main/preload watcher did NOT automatically rebuild+relaunch Electron after several rounds of `src/preload/index.ts`/`src/main/ipc/channels.ts` edits made while it kept running — the first live-verification attempt failed with `window.api.reports.sizeAvailabilityHeatmap is not a function`, even though every other already-wired channel (including item 3's own newer `trialSession.*` calls, added earlier in the SAME dev-server session) worked fine, meaning the watcher's staleness was specific to this window, not a universal "it never hot-reloads preload" rule. Fixed by fully killing the dev-server node process and every orphaned `electron.exe` process it had spawned across the session's many test runs, then starting fresh — confirmed fixed by two consecutive clean 69/69 runs after the restart.

Footwear's remaining 1 item (seasonal reorder calendar) remains untouched — the LAST item that closes this vertical entirely (14th of 23).

### 50. Footwear's item 5 (Seasonal Reorder Calendar) — its 5th and FINAL signature item, closing the vertical's entire 5-item set

Grounding via the exact audit wording ("monsoon/formal/sports buying cycles, which generic retail reordering doesn't model") settled a real design question before any code was written: does this need a stock-reservation/hold mechanism, or just a way to know WHEN a known seasonal demand spike is coming? Confirmed the latter, and confirmed a genuinely useful reuse: `Product.season` (added earlier this session for Clothing item 1) turned out to already be editable for FOOTWEAR products too — its field in `ProductFormModal.tsx` is gated on the `variant_tracking` module flag, not `businessType === 'CLOTHING'` specifically, and FOOTWEAR already has that module. Zero UI change needed to let a shop tag a product's season; the only new work was letting the shop define what its OWN seasons actually are and when they matter.

New `SeasonalCycle` model — a shop-defined, RECURRING month/day window (e.g. "Monsoon", "Wedding/Formal", "Sports," or genuinely anything, never a fixed enum, matching `Product.season`'s own free-text precedent) with a configurable lead-time in days. New `seasonal-cycle.service.ts`: full CRUD (soft-delete, matching every other "definition" row in this codebase) plus `getSeasonalReorderCalendar()`, which for each cycle resolves its NEXT occurrence (handling a year-wrapping window like Nov 15 → Jan 15 correctly — the same date-arithmetic a birthday reminder needs), classifies it IN_SEASON / REORDER_NOW / UPCOMING against the lead time, and — the actionable part — matches it against every product whose `season` field equals the cycle's name (case-insensitively, matched in JS since SQLite's Prisma provider has no reliable case-insensitive `equals`), summing stock across variants (or falling back to `Inventory.quantity` for a non-variant-tracked product) to flag which of them are currently low or out of stock. Cycles needing reorder NOW sort first, ahead of in-season and upcoming ones, since that's the actual to-do list.

UI is a new Reports tile whose View component is genuinely different from every other report in this file: it owns real mutations (add/edit/delete a season) via an inline "Manage Seasons" panel, not just a `data` prop read — seeded from the initial fetch, then independently refreshed after every change, the same self-contained pattern `VariantManagementModal` already established for its own CRUD. The calendar itself renders as colour-coded cards (red/green/neutral) each showing days until start, the reorder-by date, and a live count of tagged products currently low or out of stock. New AI intent `footwear.seasonalReorderStatus`, leading with whichever season(s) actually need reordering now — informational (in-season/upcoming) framing only when nothing is urgent.

- Schema: new `SeasonalCycle` table (migration `20260825120000_phase67_footwear_seasonal_cycle`), applied and verified persisted via direct `node:sqlite` read with zero incident — the absolute-`DATABASE_URL` fix has now held clean across three consecutive migrations this session. One real snag: the dev-server Electron app's own live SQLite connection was still holding the file open (WAL lock), and `prisma migrate deploy` failed with "database is locked" on the first attempt — resolved by stopping the dev server first, applying the migration, then restarting it, not by retrying blindly.
- IPC: `seasonalCycle:{list,create,update,delete,calendar}`, wired through channels/handler/validation/preload the standard 4-layer way; `create`/`update`/`delete` gated on `products.update` (a definition-management action), `list`/`calendar` on `products.view`/`reports.inventory` respectively.
- Unit tests: 22 new (`seasonal-cycle.service.test.ts` — 15: CRUD validation and soft-delete, plus 7 date-arithmetic tests using `vi.setSystemTime` to deterministically cover IN_SEASON/REORDER_NOW/UPCOMING classification, a year-wrapping window, next-year rollover once a window has fully passed, case-insensitive product matching, variant-stock summing vs. the `Inventory.quantity` fallback, and REORDER_NOW-first sort ordering; `ai-vertical-templates.service.test.ts` — 3: registration, leading with the urgent cycle(s), honest empty-state; `ai-query.service.test.ts` — 1: fast-path routing, confirmed non-colliding with the pre-existing `clothing.seasonSellThrough` entry despite both containing "season"). Full suite **3138/3138**, both tsconfigs clean.
- i18n: 31 new keys (2 defs, 3 col, 26 under a new `seasonalCalendar` block covering every manager-panel label/action plus the 3 status names), all 13 languages via the same programmatic anchor-based insertion script established for item 4 (dry-run verified against all 12 non-English locales before writing), key parity re-verified (**4276/4276 leaf keys**, up from 4245).
- Manual: new `## Seasonal Reorder Calendar` heading added to Footwear's chapter — its 5th and final substantive section, all 13 languages.
- **Live-verified against the real running app** (`tests/e2e/suites/69-footwear-variants.js` extended a 5th and final time, **83/83, two consecutive clean runs**): a real UI-driven cycle creation through the actual Reports tile → Manage Seasons panel → form → Add, with the window computed relative to the REAL date the suite happens to run on (today's own month/day through 3 days later) so the test is correct on any run date, not just when it happens to execute during an actual monsoon month; a second cycle created via the API with a window 10 days out and a 15-day lead time, deterministically REORDER_NOW regardless of run date; confirmed the product's own season tag and forced-low stock correctly surface as a flagged low-stock entry under the in-season cycle; confirmed REORDER_NOW sorts ahead of IN_SEASON; confirmed the AI Assistant's live answer to "What is our seasonal reorder status?" through the real `ai:query` channel names the urgent cycle.
- No new bugs in the app itself this time — the infra patterns established across items 3-4 (dev-server restart discipline, absolute-`DATABASE_URL` migrations, `.locator(selector, { has })` scoping for a form that isn't the only one of its CSS class on the page) all held up cleanly on first application, aside from the DB-lock-during-migration snag above (an operational sequencing issue, not a code defect).

**Footwear is now CLOSED — 5/5, the 13th of Phase 67's 23 verticals fully complete, and the 7th of the 17 Product & Retail / Legacy Generic Service verticals** (after Restaurant, Retail, Hardware, General, Electronics, Clothing).

### 51. Manufacturing — all 5 signature items built, tested, and live-verified in one combined pass, closing the vertical's entire 5-item set

Manufacturing was rated 7.5/10 in the source audit — already "the deepest module set in the app" (raw materials → BOM → work orders → finished goods, with its own analytics), with two specific named gaps: "No machine/labour downtime log, so BOM cost is materials-only, never true per-unit cost. No QC/rejection-rate tracking." Its own 5 grounded items pair up cleanly: item 1 (downtime capture) feeds item 2 (landed-cost report); item 3 (per-stage rejection tracking) feeds item 4 (rejection-rate trend report); item 5 (bottleneck flag) stands alone. Grounding also found that `WorkOrder` already had a binary `isQcStep`/`qcResult` PASS/FAIL gate from Phase 58 §2 — a real pre-existing mechanism, but a whole-batch binary gate, not a rejection COUNT, so items 3-4's actual gap was quantity-level tracking, not the checkpoint concept itself. Similarly, `ProductionOrder.laborCost`/`overheadCost` already existed from Phase 64 — so item 1's real gap was specifically DOWNTIME visibility, not labor/overhead capture generally, and item 2's report needed to reuse those already-correct persisted numbers rather than reinvent a costing model.

**Item 1 (Feature) — Machine/labour downtime capture.** New `WorkOrderDowntimeEntry` model — one row per downtime incident against a work-order step (a step can have more than one, same itemized-log reasoning `ProductionLaborEntry` already established for labor), free-text reason (a shop's real causes vary too much to enumerate). New `logDowntime()`/`listDowntimeEntries()`/`getDowntimeSummary()` in `work-order.service.ts`. UI: a **+ Downtime** action per step in `ProductionOrdersScreen.tsx` (visible while the order is In Progress), a small inline modal, and a running-total badge shown directly on the step once any exist.

**Item 3 (Feature) — Quality-rejection tracking at each stage.** Two new nullable fields on the EXISTING `WorkOrder` model — `qtyInspected`/`qtyRejected` — captured in the SAME QC pass/fail prompt a checkpoint step already required, not a parallel mechanism. Deliberately independent of the overall `qcResult`: a batch can PASS overall with a handful of units rejected at that one checkpoint, which the old binary flag alone could never express. Server-validated that rejected can never exceed inspected.

**Item 2 (Report) — True Landed Cost per Finished Unit, stacked bar (material + labour + overhead).** New `generateLandedCostPerUnitReport()` — deliberately does NOT recompute material cost from CURRENT `RawMaterial.unitCost` (which drifts over time); instead backs it OUT of the already-persisted, historically-accurate `ProductCostHistory.unitCost` for that order (`totalCost − laborCost − overheadCost`), the same real number that actually set the finished good's own `Inventory.averageCost` at completion time. `laborCost`/`overheadCost` are already real persisted Phase-64 fields, so only material needed deriving. New `LandedCostPerUnitView` — a real `Bar...stackId` stacked chart (mirroring the pre-existing `ApAgingView` pattern for the live UI, and the pre-existing `stackedBar` PDF-export chart type for the export path — neither needed inventing, both already existed in this codebase, just never used for this data before).

**Item 4 (Report) — Rejection Rate Trend, line chart by month + breakdown by stage.** New `generateRejectionRateTrendReport()` — reuses item 3's own `qtyInspected`/`qtyRejected` fields directly, trended by month (day-level would be too sparse for real shop-floor QC volume) and broken down by stage (`taskName`), ranked worst-first. A QC step with neither field set (every pre-item-3 row, and every non-QC step) is correctly excluded, not silently counted as a 0%-rejection stage.

**Item 5 (Feature) — Work-order lead-time bottleneck flag.** Needed NO new capture at all — a stage's own duration is simply the gap between its own `completedAt` and the PREVIOUS step's `completedAt` (or the production order's own `startDate` for step 1), since steps already run in strict `stepNumber` sequence and every completed step already has a real timestamp. New `getWorkOrderBottleneckFlag()` averages this per stage (by `taskName`) across every completed order in scope, and surfaces whichever stage is genuinely eating the most real elapsed time — not a guess. Surfaced proactively as a banner above the order list in `ProductionOrdersScreen.tsx` (no click required to see it), plus on-request via its own AI intent.

All 4 new AI intents (`manufacturing.landedCostPerUnit`, `manufacturing.rejectionRateTrend`, `manufacturing.downtimeSummary`, `manufacturing.bottleneckFlag`) registered alongside the pre-existing `manufacturing.production`, with fast-path regexes confirmed non-colliding with each other and with `manufacturing.production`'s own pre-existing pattern.

- Schema: one migration (`20260825130000_phase67_manufacturing_downtime_and_rejection`) covering both the new `WorkOrderDowntimeEntry` table and the two new `WorkOrder` columns — applied and verified persisted via direct `node:sqlite` read. One real snag, same class as Footwear item 5's: the dev-server Electron app's own live SQLite connection was holding the file open (WAL lock) when the migration was first attempted — resolved by stopping the dev server first, applying the migration, then restarting, not by retrying blindly. Zero silent-phantom-copy incident — the absolute-`DATABASE_URL` fix has now held clean across all 4 migrations applied this session.
- IPC: `workOrders:{logDowntime,listDowntime,downtimeSummary,bottleneckFlag}` and `reports:{landedCostPerUnit,rejectionRateTrend}`, wired through channels/handler/validation/preload the standard 4-layer way; `WorkOrder.qtyInspected`/`qtyRejected` folded into the EXISTING `workOrders:updateStatus` payload rather than a new endpoint, matching how `qcResult` itself is already threaded through that one call.
- Unit tests: 44 new (`work-order.service.test.ts` — 22: 3 QC-quantity validation (incl. rejecting rejected > inspected, and clearing both to null when a QC step completes without them), 11 downtime CRUD/summary, 8 bottleneck-flag date-arithmetic incl. multi-order averaging and COMPLETED-only filtering; `report.service.test.ts` — 11: 5 landed-cost incl. backing material cost out of `ProductCostHistory` not current prices, weighted-averaging across multiple orders, sort order; 6 rejection-rate-trend incl. monthly aggregation, per-stage ranking, correct exclusion of non-QC/uncounted steps; `ai-vertical-templates.service.test.ts` — 8: registration + 2 tests per new intent; `ai-query.service.test.ts` — 4: one fast-path test per new intent, confirmed non-colliding with each other and with the pre-existing `manufacturing.production` entry). Full suite **3179/3179**, both tsconfigs clean.
- i18n: 37 new keys (2 new `defs` entries, 4 `summary`, 7 `col`, 2 `empty`, 1 `section`, plus 13 new keys under `manufacturing.qc`/`manufacturing.downtime`/`manufacturing.bottleneck` — this vertical's own `ProductionOrdersScreen.tsx` is a genuinely i18n'd screen, unlike Footwear's several hardcoded-English ones, so the UI strings needed real translation too, not just the Reports/Manual side), all 13 languages via the same programmatic anchor-based insertion script established for Footwear items 4-5 (dry-run verified against all 12 non-English locales before writing), key parity re-verified (**4307/4307 leaf keys**, up from 4276).
- Manual: extended the existing Work Order Steps paragraph in Manufacturing's chapter (§3) with three new paragraphs covering rejection-quantity capture, downtime logging, and the bottleneck banner, plus a brand-new `## 8. True Landed Cost & Rejection Rate Reports` heading — Manufacturing's chapter previously had 7 sections and a good amount of existing real content (unlike several other verticals that started from a near-empty chapter this phase), all 13 languages.
- **Live-verified against the real running app** via a brand-new `tests/e2e/suites/70-manufacturing-quality-downtime.js` (first-ever E2E coverage of Manufacturing's own work-order-step-level workflows — the pre-existing suite 03 only covered the plain production-order lifecycle), **42/42, two consecutive clean runs**: real UI-driven work-order-step creation (one plain step, one QC checkpoint) through the actual step editor; real downtime logging through the **+ Downtime** modal, confirmed the entry persisted and the running total appeared directly on the step; real QC completion through the existing Pass/Fail prompt now also capturing inspected/rejected quantities, confirmed a batch can PASS overall despite recorded rejects; both new Reports tiles confirmed rendering with real data and internally consistent math (material + labour + overhead == total, per unit); the bottleneck banner confirmed visible on the Production Orders screen; all 4 new AI intents confirmed routing correctly and answering with the real recorded numbers through the actual `ai:query` channel.
- **One real bug found+fixed during this item's own live-verification pass**: a JavaScript scoping bug in the E2E suite's own setup code (not the app) — `mfgTemplateRowBefore` (holding the pre-test `ai_assistant` module state, so it can be restored exactly afterward) was declared with `let` INSIDE the `try` block, invisible to the `finally` block that needed to restore it, crashing the suite's own cleanup after all 42 real assertions had already passed. Fixed by hoisting the declaration above the `try`, matching the pattern Footwear's own suite already used correctly. The crash had left `ai_assistant` enabled for MANUFACTURING and some test rows uncleaned in the shared dev DB — both found and manually restored/cleaned via direct `node:sqlite` before re-running to confirm the fix, rather than left for a future session to discover.

**Manufacturing is now CLOSED — 5/5, the 14th of Phase 67's 23 verticals fully complete, and the 8th of the 17 Product & Retail / Legacy Generic Service verticals** (after Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear).

### 52. Agri Inputs — all 5 signature items built, tested, and live-verified in one combined pass, closing the vertical's entire 5-item set

Grounding via the source audit artifact confirmed Agri Inputs' exact 5-item list: item 1 (feature) crop-season-aligned credit terms, due date tied to harvest not a flat 30/60/90 count; item 2 (report) seasonal credit exposure, a line chart across the crop calendar; item 3 (feature) crop-linked product advisory, which fertiliser/pesticide is recommended for which crop at POS; item 4 (report) farmer-wise purchase & repayment history, a ledger-plus-trend separating loyal from risky customers; item 5 (feature) equipment AMC/service reminders for tractors/sprayers. A pre-existing `Invoice.dueDate` field already carried a comment referencing "Agri Inputs' harvest-tied credit terms" from Phase 58 §2 — confirmed that was only ever a free-typed date, with no structured harvest-definition layer behind it at all, so item 1's real gap was that missing layer, not the field itself.

**Item 1 (Feature) — Crop-season-aligned credit terms.** New `CropSeason` model — a shop-defined, RECURRING month/day harvest occurrence (e.g. "Wheat Harvest" on 15 April, never a fixed "Kharif/Rabi/Zaid" enum, matching `SeasonalCycle`'s own free-text precedent from Footwear item 5). New `crop-season.service.ts` with full CRUD (soft-delete) plus `resolveNextHarvestDate()`, a pure function resolving a recurring month/day to its next real occurrence (this year's if not yet passed, else next year's — the same date-arithmetic class `SeasonalCycle` already needed, simpler here since it's a single target date, not a start/end window). New `Invoice.cropSeasonId` column: when set, `billing.service.ts`'s `createInvoice()` computes the invoice's REAL `dueDate` server-side from the season's own next harvest occurrence, overriding whatever's manually typed in the plain `dueDate` field. UI: a "Link to Crop Season" dropdown inside Billing's existing CREDIT-sale panel, gated to Agri Inputs, with an inline "Manage Seasons" mini-CRUD (mirroring `SeasonalReorderCalendarView`'s own real-mutation pattern) and a live "due date will be set to…" hint once a season is picked.

**Item 3 (Feature) — Crop-linked product advisory.** New `Product.recommendedCrop` free-text field (same convention as `Product.season`/`SeasonalCycle.name` — never a fixed list). New `crop-advisory.service.ts`: `listDistinctCrops()` and `getProductsForCrop()`, matched case-insensitively in JS (same SQLite-provider limitation every other free-text match in this codebase already works around). UI: a new "Browse by Crop" row of chips above Billing's product search, shown once any product is tagged — tapping a crop shows every recommended product with live stock/price, addable straight to the cart. A genuine gap was found and closed alongside this: `recommendedCrop` existed on the Prisma schema but nowhere in `product.validation.ts`/`product.service.ts`/`ProductFormModal.tsx` actually let a shop SET it — added to all three (gated on `businessType === 'AGRI_INPUTS'`, mirroring `season`'s own `variant_tracking`-gated field), closing what would otherwise have been a silently unusable feature.

**Item 2 (Report) — Seasonal Credit Exposure.** New `generateSeasonalCreditExposureReport()` — a live current-state view (no date range) of every outstanding credit invoice with a real `dueDate`, bucketed by calendar month (a fixed 12-point `Jan`–`Dec` axis, not just the months with data) AND separately by linked `CropSeason` name, since a season can span a year boundary and two different seasons can share a due month. New `SeasonalCreditExposureView` — a line chart across the 12-month calendar plus a by-season breakdown table, this codebase's first report to render a fixed-length calendar-axis line chart regardless of data sparsity.

**Item 4 (Report) — Farmer-Wise Purchase & Repayment History.** New `generateFarmerRepaymentReport()` — deliberately a CROSS-farmer comparative ranking, distinct from the pre-existing single-customer `generateCustomerLedgerReport()` drill-down. Aggregates every credit-active customer's total purchased/repaid/outstanding, sorted ASCENDING by repayment rate — riskiest (lowest-repayment) accounts first, the actual actionable list a shop needs before extending more credit next season.

**Item 5 (Feature) — Equipment AMC/service reminders.** New `ProductSerial.nextServiceDueDate`/`lastServicedDate` columns — deliberately on the equipment unit itself, not a `JobCard` (the due date is a property of the physical tractor/sprayer, not of any one service EVENT, mirroring how `warrantyExpiryDate` already lives on `ProductSerial` rather than a ticket), and deliberately NOT reusing `CarJobCard`'s own already-proven `nextServiceDueDate`/`scheduleNextServiceReminder` pattern verbatim, since the generic `JobCard` model has no vehicle-equivalent identity to key off of — the same shape, same WhatsApp-via-`notificationQueue` mechanism, adapted to a serial/equipment unit instead. New `updateSerialServiceInfo()`, `listEquipmentDueForService()`, `scheduleEquipmentServiceReminder()` in `serial.service.ts`. UI: a new Equipment Service Due panel on the pre-existing Agri Dashboard, with overdue/due-soon KPI cards, a per-unit "Remind" WhatsApp action, and an inline "Set a Service Date" mini-form covering every piece of equipment on file (not just ones already flagged).

All 4 new AI intents (`agriInputs.seasonalCreditExposure`, `agriInputs.farmerRepayment`, `agriInputs.cropAdvisory`, `agriInputs.equipmentServiceDue`) registered alongside the pre-existing `inventory.batchExpiry` (item 1 has no AI intent of its own — it's a billing-time computation, not a query). `agriInputs.cropAdvisory` deliberately supports an optional `cropName` param for a future direct drill-down, falling back to a distinct-crop-count summary when none is given — the per-crop product list itself lives in the dedicated Billing UI, not duplicated as a deep AI answer.

- Schema: one migration (`20260825140000_phase67_agri_inputs_crop_season_and_equipment`) covering the new `CropSeason` table, `Invoice.cropSeasonId` + index, `Product.recommendedCrop`, and `ProductSerial.nextServiceDueDate`/`lastServicedDate` — applied and verified persisted via direct `node:sqlite` PRAGMA reads. Same now-standard discipline as every migration since Footwear item 5: dev server stopped first, applied via PowerShell with an explicit absolute `DATABASE_URL`, zero silent-phantom-copy or WAL-lock incident this time.
- IPC: `cropSeason:{list,create,update,delete,resolveDueDate}`, `cropAdvisory:{listCrops,productsForCrop}`, `serials:{updateServiceInfo,dueForService,scheduleServiceReminder}` (extending the pre-existing `serials` namespace), and `reports:{seasonalCreditExposure,farmerRepayment}`, wired through channels/handler/validation/preload the standard 4-layer way.
- Unit tests: 60 new (`crop-season.service.test.ts` — 16: CRUD validation and soft-delete, plus date-arithmetic tests for `resolveNextHarvestDate`'s this-year/next-year rollover and its own due-date resolution wrapper; `crop-advisory.service.test.ts` — 9: distinct-crop dedup/sort, case-insensitive matching, variant-stock summing vs. `Inventory.quantity` fallback, name-sorted results; `serial.service.test.ts` — 13: service-info update incl. explicit-null-clears-vs-omitted-leaves-untouched, due-for-service overdue/due-soon classification, reminder scheduling incl. the no-customer-phone quiet no-op and the reminder-date-already-passed rejection; `report.service.test.ts` — 12: 6 seasonal-credit-exposure incl. month-bucketing, by-season breakdown, peak-month identification; 6 farmer-repayment incl. cross-invoice aggregation, riskiest-first sort, honest empty state; `billing.service.test.ts` — 3: crop-season dueDate override, fallback to the manual date when the linked season doesn't exist, null cropSeasonId when unset; `ai-vertical-templates.service.test.ts` — 8: registration + 2 tests per new intent; `ai-query.service.test.ts` — 4: one fast-path test per new intent, confirmed non-colliding with `footwear.seasonalReorderStatus`/`clothing.sizeStyleHeatmap`'s own broader season/heatmap patterns). Full suite **3239/3239**, both tsconfigs clean.
- i18n: new keys under `reports.defs`/`summary`/`col`/`section`/`empty` for both new reports, all 13 languages via the same programmatic anchor-based insertion script established since Footwear item 4 (dry-run verified against all 12 non-English locales before writing), key parity re-verified (**4324/4324 leaf keys**, up from 4307). The Billing crop-season/crop-advisory UI text and the Agri Dashboard's new Equipment Service Due panel are deliberately hardcoded English, matching the same "vertical-gated section of an otherwise-shared screen stays English" precedent Footwear's own trial-mode toggle already established in this same file.
- Manual: two new headings added to Agri Inputs' chapter — `## Crop-Linked Product Advisory` and `## Seasonal Credit Exposure & Farmer Repayment Reports` — plus extended paragraphs in the pre-existing "Harvest-tied credit terms" and "Combined Dashboard" sections, all 13 languages, section count 10 → 12 verified matching across every locale.
- **Live-verified against the real running app** via a brand-new `tests/e2e/suites/71-agri-inputs-crop-season-equipment.js` (first-ever E2E coverage of this vertical), **51/51, two consecutive clean runs**: a real UI-driven CREDIT sale linked to a crop season through the actual Billing dropdown, confirmed the invoice's real `dueDate` and `cropSeasonId` persisted correctly via the API; a real UI-driven Browse-by-Crop chip click adding a tagged product straight to the cart; both new Reports tiles confirmed rendering with real data; a real UI-driven "Set a Service Date" entry on the Agri Dashboard confirmed flagging the equipment as due-for-service, with the WhatsApp reminder scheduling call confirmed succeeding; all 4 new AI intents confirmed routing correctly and answering with the real recorded numbers through the actual `ai:query` channel.
- **Two real bugs found+fixed during this item's own live-verification pass**: (1) `recommendedCrop` existed on the Prisma schema and was read by `crop-advisory.service.ts`, but nothing in the product validation/service/form layer ever let a shop actually SET it — a silently-unusable feature caught only because the E2E test tried to create a crop-tagged product via the real `products.create` API and the field was simply dropped; fixed by adding it to `product.validation.ts`, `product.service.ts`, and `ProductFormModal.tsx`. (2) The same `LocationStock`/`InventoryMovement` E2E product-cleanup FK gotcha already documented from an earlier session (memory: "E2E Product Cleanup FK Gotcha") recurred here — every product this suite creates has real opening stock, so without pre-clearing those two tables the generic `cleanupByNamePrefix` helper silently soft-deleted instead of hard-deleting, leaking 2 products per run (confirmed via direct `node:sqlite` inspection showing exactly which two tables still referenced the leaked rows); fixed with the same explicit pre-clear pattern, confirmed steady (not growing) `productsHandled` counts across two subsequent clean runs.

**Agri Inputs is now CLOSED — 5/5, the 15th of Phase 67's 23 verticals fully complete, and the 9th of the 17 Product & Retail / Legacy Generic Service verticals** (after Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear, Manufacturing).

### 53. Blood Bank — items 1, 3, 4, 5 built, tested, and live-verified in one combined pass; item 2 already existed, closing the vertical's entire 5-item set

Grounding via the source audit artifact confirmed Blood Bank's exact 5-item list — item 1 (feature) donor cooldown auto-reminder, the standard eligibility rule tracked automatically; item 2 (report) blood-group stock level, a gauge/bar per group instantly showing which groups are critically low; item 3 (feature) camp/drive scheduling with donor-turnout tracking per drive; item 4 (report) donation-to-issue cycle time, a waste-risk indicator; item 5 (feature) emergency fast-match search, group + component + quantity across current stock in one query — and, unusually for this vertical, a genuine pre-existing surprise: this is by far the most-built-out vertical touched so far this phase (rated 7.0/10, "purpose-built donor/donation/stock/issue workflow"), and item 2's own report (`generateBloodStockReport()`, wired into the Reports screen as `bloodStock`) already fully existed from Phase 54, complete with a per-group summary grid on its own dedicated Blood Stock screen. The audit's own "missing features" note was precise: "No donor-eligibility cooldown auto-reminder... no camp/drive scheduling" — confirming items 1 and 3 as the real gaps, not a rebuild of anything already working.

**Item 1 (Feature) — Donor cooldown auto-reminder.** The underlying cooldown math (`computeNextEligibleDate`/`cooldownDaysFor`, varying by component type and donor sex) and a one-at-a-time manual "Send Recall Reminder" WhatsApp action already existed — but nothing ever proactively told a shop WHICH donors had already become eligible again; a staffer had to open each donor's own detail view to check. New `listDonorsDueForRecall()` reuses the exact same cooldown calculation to surface every active, non-deferred donor already past their cooldown in one query. New **Recall Due** filter button on the Donor Registry — turns the registry into a real outreach worklist a staffer can work down, sending the existing per-donor reminder to each one directly from the filtered view, matching the audit's own wow-factor framing verbatim ("turns the donor list into an active outreach tool").

**Item 3 (Feature) — Camp/drive scheduling with donor-turnout tracking.** A genuine "backend exists, UI doesn't" gap: `createDonationCamp()`/`listDonationCamps()` (the latter already including a `_count.donations` turnout figure) and `DonationRecord.campId` had existed since Phase 51, but a grep across the entire renderer found zero UI usage of any of it — no shop could ever actually create or view a camp. New `CampsScreen.tsx` (list/create camps, turnout shown directly on each card) plus a new camp picker added to the pre-existing "Record Donation" form, which previously had no way to link a donation to a camp despite the service layer already accepting `campId`.

**Item 4 (Report) — Donation-to-Issue Cycle Time.** New `generateDonationToIssueCycleTimeReport()` — computes real days-elapsed between `DonationRecord.collectionDate` and `BloodIssueItem.createdAt` for every non-cancelled issued unit, broken down by component type and ranked slowest-first. Deliberately never blends components into one number: the same 10-day average is unremarkable for Plasma (365-day shelf life) but a serious red flag for Platelets (5-day shelf life) — the same "generic threshold is wrong for blood" reasoning the pre-existing Blood Stock report's own per-component expiry windows already established, applied here to cycle time instead of expiry.

**Item 5 (Feature) — Emergency fast-match search.** The pre-existing Issue Units form only offered a flat, unfiltered checkbox list of every unit in stock — no way to ask "give me 2 units of O+ Packed RBC" as one query. New `fastMatchSearch()` reuses the exact same `checkCompatibility()` ABO/Rh matrix `createBloodIssue()` itself already enforces (never a second, divergent compatibility rule), filters by component type, sorts soonest-expiring-first (standard blood-bank FIFO-by-expiry practice), and returns an honest shortfall count when fewer compatible units exist than requested. New **Fast Match** panel inside the Issue Units form — group/component/quantity inputs plus a **Find & Select** button that auto-selects the matched units instantly.

All 3 new AI intents (`bloodBank.donorsDueForRecall`, `bloodBank.campTurnout`, `bloodBank.donationToIssueCycleTime`) registered alongside the pre-existing `bloodBank.stock`. Item 5 deliberately gets no AI intent — it needs a quantity parameter fast-path routing can't reliably extract, and it's a pure UI action tool rather than a reportable question, the same precedent several other pure-action items this phase already established.

- Schema: none needed at all — every item reuses existing `Donor`/`DonationCamp`/`DonationRecord`/`BloodIssueItem` fields and relations exactly as they already were, the first vertical this phase whose entire 5-item set needed zero migration.
- IPC: `bloodBank:listDonorsDueForRecall`, `bloodBank:fastMatchSearch`, and `reports:donationToIssueCycleTime`, wired through channels/handler/validation/preload the standard 4-layer way; `bloodBank:createDonationCamp`/`listDonationCamps` needed no new IPC at all, only a UI to finally call them.
- Unit tests: 26 new (`blood-bank.service.test.ts` — 9: 4 recall-due filtering incl. respecting the shorter platelet cooldown not a flat 90-day rule, 5 fast-match incl. compatibility filtering, component-type filtering, quantity capping with an honest shortfall, and an honest empty-match result; `report.service.test.ts` — 5: cycle-time computation, component ranking, min/max/avg aggregation, cancelled-issue exclusion; `ai-vertical-templates.service.test.ts` — 6: registration + 2 tests per new intent; `ai-query.service.test.ts` — 3: one fast-path test per new intent). Full suite **3265/3265**, both tsconfigs clean (one unrelated pre-existing flaky timeout in `backup.service.test.ts`, confirmed not a regression by re-running it standalone clean).
- i18n: 10 new keys for the new report (2 defs, 3 summary, 4 col, 1 empty), all 13 languages via the same programmatic anchor-based insertion script established since Footwear item 4, key parity re-verified (**4334/4334 leaf keys final**). The Camps screen, the Donor Registry's Recall Due filter, and the Issue form's Fast Match panel are deliberately hardcoded English — Blood Bank's own screens have been a fully hardcoded-English vertical screen set since Phase 51 (unlike the shared Reports screen), so these additions match the existing screens around them rather than introducing a newly-translated island in an otherwise-English UI.
- Manual: one new heading (`## Donation-to-Issue Cycle Time`) plus extended paragraphs in the pre-existing Donor registry, Donations & camps, and Issue sections, all 13 languages, section count 8 → 9 verified matching across every locale.
- **Live-verified against the real running app** via a brand-new `tests/e2e/suites/72-blood-bank-recall-camps-fastmatch.js`, **32/32, two consecutive clean runs** (plus the pre-existing `tests/e2e/suites/33-bloodbank-donations.js` full base-chain suite re-run clean, 17/17, confirming no regression to the donor→donation→screening→issue workflow these new features were layered onto): a donor backdated past cooldown confirmed showing up in the real UI's Recall Due filter; a real UI-driven camp scheduled through the new Camps screen, a donation linked to it through the (also newly UI-driven) camp picker, and the camp's own turnout count confirmed incrementing live; a backdated donation issued and the cycle-time report confirmed showing the correct real elapsed days, both via the API and the real Reports-screen tile; a real UI-driven Fast Match search on the Issue Units form confirmed auto-selecting a compatible unit and reporting a correct fulfilled/shortfall result; all 3 new AI intents confirmed routing correctly through the actual `ai:query` channel.
- **One real bug found+fixed during this item's own live-verification pass, in the E2E suite's own setup code, not the app**: the Donations & Screening form's select-element DOM order is Donor(0) → Blood Group(1) → Component Type(2) → the newly-added Donation Camp(3) — the first test draft assumed the camp picker was the 2nd select (`nth(1)`, actually Blood Group) and timed out trying to select a non-existent camp option in the wrong dropdown; fixed by counting the real DOM order and using `nth(3)`, confirmed by two subsequent clean runs.

**Blood Bank is now CLOSED — 5/5, the 16th of Phase 67's 23 verticals fully complete, and the 10th of the 17 Product & Retail / Legacy Generic Service verticals** (after Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear, Manufacturing, Agri Inputs).

### 54. Rental — items 3 and 4 built, tested, and live-verified; items 1, 2, and 5 already existed, closing the vertical's entire 5-item set

Grounding via the source audit artifact confirmed Rental's exact 5-item list, then a direct read of the current schema and UI (`RentalBooking`/`RentalBookingItem`/`RentalUnit`, `RentalBookingsScreen.tsx`, `RentalUnitsScreen.tsx`) — done BEFORE assuming any item needed building, per this arc's own standing discipline — turned up the biggest pre-existing-surprise ratio of any vertical this phase: item 1 (security-deposit ledger), item 2 (damage-charge workflow with condition-in/condition-out and photo documentation via the pre-existing `DocumentPanel`), and item 5 (service-interval maintenance scheduling) were ALL already fully built, backend and UI both, from Phase 58 §2 — `RentalBooking.securityDepositCollected/securityDepositRefunded/damageChargeAmount/lateFeeAmount`, `RentalBookingItem.conditionOut/conditionIn/damageChargeAmount`, and `RentalUnit.serviceIntervalRentals/serviceIntervalDays/rentalCountSinceService/dueForService` with full UI on both screens. Only items 3 and 4 had real gaps, both reports.

**Item 3 (Report) — Asset Utilization Rate, per individual physical unit.** The pre-existing `generateRentalRevenueReport()` already had its own `utilizationPercent` field, but averaged ACROSS every unit of a product — it can't tell you that one specific car sits idle while its identical sibling is rented constantly, which is the real "which assets actually earn their keep" question the audit's own item wording names. Following this phase's own established precedent of keeping a coarser existing report and a finer new one distinct rather than merging different grains (Blood Bank's per-component cycle time, Manufacturing's per-stage bottleneck), built as a genuinely separate `generateAssetUtilizationReport()` — rented-days versus available-days per individual tracked unit, using the same overlap-days-within-range formula `generateRentalRevenueReport()` already established, sorted ascending by `utilizationPercent` so the worst-earning assets surface first (the actionable list), with an honest `idleUnitCount` in the summary. New `AssetUtilizationView` — a horizontal ranked bar chart (red below 25%) plus a full data table.

**Item 4 (Report) — Overdue Returns aging breakdown.** The list half already existed correctly (`generateRentalStatusReport()`'s own `rows`, one per overdue booking item) — only the "aging bar" half the audit's own item wording specifically named was missing. Extended the existing function with a new `agingBuckets` field (1-3 / 4-7 / 8-14 / 15+ days), counted ONE PER BOOKING not one per item row — matching this phase's own established convention (a booking with 3 overdue items is one real-world follow-up call, not three) — and added a new vertical bar chart to the EXISTING `RentalStatusView`'s own case in the Reports screen, rather than building a duplicate report.

Neither item needed a new AI intent registration array entry beyond `rental.assetUtilization` — `rental.status`/`rental.revenue` already existed and needed no change; item 4's aging bucket is surfaced as part of the existing `rental.status` answer's own report, not a new question.

- Schema: none needed — both items compute purely from existing `RentalBooking`/`RentalBookingItem`/`RentalUnit` fields.
- IPC: `reports:assetUtilization`, wired through channels/handler/validation/preload the standard 4-layer way; `reports:rentalStatus` needed no shape change, only a richer return value.
- Unit tests: 13 new (`report.service.test.ts` — 9: 3 aging-bucket correctness on `generateRentalStatusReport`, 6 on the new `generateAssetUtilizationReport` incl. RETIRED-unit exclusion, an honest empty result, near-100%/0% boundary cases, keeping same-product units distinct rather than averaged, and the worst-first sort; `ai-vertical-templates.service.test.ts` — 3: registration plus 2 behavior tests; `ai-query.service.test.ts` — 1 fast-path routing test). Full suite **3278/3278**, both tsconfigs clean.
- i18n: 8 new keys for the new report (2 defs, 2 summary, 2 col, 1 aging-chart title, 1 empty state), all 13 languages via the same programmatic anchor-based insertion script this arc has used since Footwear item 4, key parity re-verified (**4342/4342 leaf keys** at that point). Live-verification then surfaced a real, genuinely pre-existing bug this new report's own DataTable exposed for the first time: `RentalUnit.status` can be `'RENTED'` (see `prisma/schema.prisma`), and BOTH the new `AssetUtilizationView` and the pre-existing `RentalUnitsScreen.tsx` (unchanged since Phase 58) call `t(\`rental.status.${u.status}\`)` on it — but `rental.status.RENTED` had never been added to any of the 13 locale files, so a rented unit's status badge has shown the raw untranslated key instead of a real word since Phase 58, undetected until this report's own E2E test rendered one. Fixed by adding the key to all 13 locales (**4343/4343 leaf keys final**) — a real, pre-existing, generalizable gap fixed as a side effect of this item's own live-verification, not a regression introduced by it.
- Manual: extended the existing `## Reports` paragraph and added one new paragraph introducing Asset Utilization Rate — no new heading, so section count stayed unchanged (9 → 9) in every one of all 13 locales, verified via the same `grep -c "^## "` parity check this arc always uses.
- **Live-verified against the real running app** via a brand-new `tests/e2e/suites/73-rental-asset-utilization-overdue-aging.js` (the first-ever E2E suite to drive `rental.createBooking`/`checkoutBooking` end to end — no prior suite had exercised Rental Booking creation at all), **30/30, two consecutive clean runs**: two real units of one product, one booked and checked out, the other left untouched — confirmed via the real Reports-screen tile that the busy unit ranks after the idle one (worst-first sort holds) and `idleUnitCount` reflects the idle unit; a second booking backdated 6 days past its return date, checked out and never returned — confirmed via the real UI that the pre-existing Rental Status tile now also renders the new Overdue Aging chart and correctly buckets the booking into "4-7 days"; the new `rental.assetUtilization` AI intent confirmed routing correctly through the real `ai:query` channel.
- **One real bug found+fixed during this item's own live-verification pass, in the app itself, not the test** — the `rental.status.RENTED` i18n gap described above.

**Rental is now CLOSED — 5/5 (2 items built this phase, 3 already complete from Phase 58), the 17th of Phase 67's 23 verticals fully complete, and the 11th of the 17 Product & Retail / Legacy Generic Service verticals** (after Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear, Manufacturing, Agri Inputs, Blood Bank).

### 55. Jewellery — all 5 items built, tested, and live-verified in one combined pass, closing the vertical's entire 5-item set

Grounding via the source audit artifact confirmed Jewellery's exact 5-item list — item 1 (Feature) gold savings (chit) scheme ledger, "the single most-requested feature in Indian jewellery retail"; item 2 (Report) making-charge vs. metal-value margin, stacked bar; item 3 (Feature) hallmarking/HUID compliance register; item 4 (Report) metal rate vs. sales volume, dual-axis line chart; item 5 (Feature) purity-adjusted old-gold exchange analytics, "beyond a basic exchange log." A direct read of the current schema/UI — `Product.metalType/purity/hallmarkNumber`, `MetalRate`, `MetalExchange`, `MetalExchangeScreen.tsx`, `MetalRatesScreen.tsx`, and the pre-existing `generateJewelleryReport()` — confirmed all 5 were genuine gaps: the existing report is one blended stock-valuation number, `hallmarkNumber` was captured on the product but never surfaced as a register, `MetalRate` only ever holds today's rate with no history to trend against, and `MetalExchangeScreen` is exactly the "basic exchange log" the audit's own item 5 wording named.

**Item 1 (Feature) — Gold savings (chit) scheme ledger.** New `GoldSavingsScheme`/`GoldSavingsInstallment` models, deliberately mirroring `MetalExchange`'s own original standalone-record design (not wired atomically into `billing.service.ts`'s invoice creation — a brand-new redemption concept, not the proven metal-exchange path, doesn't need to take on that money-critical-path risk). New `gold-savings.service.ts`: `createGoldSavingsScheme`/`recordInstallment` (increments `totalDeposited`)/`redeemGoldSavingsScheme` (atomic ACTIVE→REDEEMED claim, same TOCTOU-safe shape `checkoutBooking`/`returnBooking` already established, optional bonus amount for "pay 11, get the 12th free" terms)/`linkGoldSavingsSchemeToInvoice`. New `GoldSavingsScreen.tsx` + sidebar/router entries — full create/install/redeem lifecycle via real UI.

**Item 2 (Report) — Making-Charge vs. Metal-Value Margin.** New `generateMakingChargeMarginReport()`, deliberately per-SALE (the audit's own wording) rather than `generateJewelleryReport()`'s existing single blended `totalMakingChargeRevenue` number — splits every jewellery invoice into its metal-value and making-charge components from the already-snapshotted `InvoiceItem.jewelleryNetWeight/jewelleryRatePerGram/jewelleryMakingCharge` fields, rendered as a stacked bar (top 10 invoices) matching the audit's own specified chart form.

**Item 3 (Feature) — Hallmarking/HUID Compliance Register.** New `generateHallmarkComplianceReport()` — every active metal-tagged product, flagging which lack a `hallmarkNumber`, non-compliant items sorted first (the actionable worklist, same convention this phase's other ranked reports use). Deliberately surfaced as a Reports tile rather than a dedicated screen — a Reports tile gets CSV/PDF export for free, genuinely more useful for a real BIS inspection than a bespoke screen would be.

**Item 4 (Report) — Metal Rate vs. Sales Volume.** `MetalRate` itself only ever holds today's current rate (upsert overwrites in place) — new `MetalRateHistory` model, appended once per real rate change in `upsertMetalRate()`, the same "history instead of overwrite" fix `ProductCostHistory` already established for `Product.costPrice`. New `generateMetalRateVsSalesVolumeReport()` auto-selects whichever metalType+purity combination sold the most weight in the requested range (falling back to whichever has the most rate-history entries if there were no sales at all) — deliberately no manual picker, since the generic Reports screen has no mechanism for an extra selector beyond a date range, and a single chart mixing multiple metals (gold and silver rates differ 50-100x) would be unreadable anyway. Rendered with the same dual-yAxisId `RCLineChart` mechanism the Distributor scheme-cost-vs-volume report already established.

**Item 5 (Feature) — Purity-Adjusted Exchange Analytics.** New `purityToFineness()` helper parsing both karat notation ("22K" → 22/24) and per-mille fineness notation ("999" → 0.999) into a 0-1 fraction, normalizing every exchange to its pure-metal-equivalent weight — a 24g piece at 22K is 22g of pure gold — so exchanges of different purities can be compared fairly rather than summed as incomparable raw grams. New `generatePurityAdjustedExchangeReport()` (by-metal breakdown + monthly trend, an honest `unparsablePurityCount` for any purity string that doesn't parse) plus a small display-only enhancement to the pre-existing `MetalExchangeScreen.tsx` itself, showing each exchange's own pure-equivalent weight inline — the "beyond a basic exchange log" feature half, not just the aggregate report half.

5 new AI intents (`jewellery.goldSavingsSummary/makingChargeMargin/hallmarkCompliance/metalRateVsSalesVolume/purityAdjustedExchange`) alongside the pre-existing `jewellery.stockAndSales`. Real fast-path routing collision found and fixed during this item's own build (not live-verification): `jewellery.stockAndSales`'s own existing pattern `/making[\s-]charge/i` is broad enough to match a specific margin question too — the new `jewellery.makingChargeMargin` patterns were placed BEFORE it in the fast-path array (first-match-wins by order) so a specific margin/split question routes correctly while a bare "making charge" question still falls through to the older summary unchanged.

- Schema: one migration (`20260825150000_...`) — `GoldSavingsScheme`, `GoldSavingsInstallment`, `MetalRateHistory`, applied via `prisma migrate deploy` after `prisma migrate dev`'s own drift-detection wanted a destructive reset (a known false-positive shape this session has hit before on a long-lived dev DB — migrated the safe way instead), verified against the real DB file via `node:sqlite` before proceeding, per this session's own standing Prisma-gotcha discipline.
- IPC: `goldSavings:list/create/recordInstallment/redeem/linkToInvoice` and `reports:makingChargeMargin/hallmarkCompliance/metalRateVsSalesVolume/purityAdjustedExchange`, wired through channels/handler/validation/preload the standard 4-layer way.
- Unit tests: 42 new (`gold-savings.service.test.ts` — 17: full create/install/redeem/link lifecycle incl. the atomic-claim race test; `metal-rate.service.test.ts` — 1: the new history-append behavior; `report.service.test.ts` — 16: 3 making-charge-margin, 3 hallmark-compliance, 3 metal-rate-vs-sales-volume incl. the auto-select-dominant-combination and fallback-to-rate-history-only cases, 4 purity-adjusted-exchange incl. karat and per-mille parsing; `ai-vertical-templates.service.test.ts` — 9: registration + 2 behavior tests per intent for the 4 report-backed intents plus 2 for the scheme-summary intent; `ai-query.service.test.ts` — 2 fast-path routing tests incl. the margin-vs-stockAndSales collision-avoidance test). Full suite **3320/3320**, both tsconfigs clean.
- i18n: 68 new keys (8 defs, 8 summary, 11 col, 1 section, 4 empty, 36 under the `jewellery` namespace incl. a nested `schemeStatus` block), all 13 languages via the same programmatic anchor-based insertion script this arc has used since Footwear item 4 — anchored on stable English JSON key names rather than translated values, so the same script worked unmodified across all 12 non-English locales. Key parity verified (**4411/4411 leaf keys final**).
- Manual: one new heading (`## Gold Savings`) plus extended Old-metal-exchange and Reports sections, all 13 languages, section count 8 → 9 verified matching every locale.
- **Live-verified against the real running app** via a brand-new `tests/e2e/suites/74-jewellery-gold-savings-margin-hallmark-purity.js`, **45/45, two consecutive clean runs**: a real UI-driven gold savings scheme created, an installment recorded, and the scheme redeemed, each state change confirmed via the API between steps; a compliant and a non-compliant product confirmed correctly flagged and ranked in the real Hallmark Compliance tile; a real jewellery sale confirmed splitting correctly into metal value and making charge in the real Making-Charge Margin tile; the Metal Rate vs. Sales Volume report confirmed auto-selecting GOLD/22K (the only combination with both a rate history entry and a sale) and rendering its dual-axis chart without error; a 24g/22K exchange confirmed showing a 22g pure-equivalent weight both in the real Old-Metal Exchange list and the Purity-Adjusted Exchange report; all 5 new AI intents confirmed routing correctly through the real `ai:query` channel; the pre-existing `tests/e2e/suites/10-new-features.js` full base Jewellery flow (product creation, billing, exchange, dashboard widget, existing report) re-run clean, 55/55, confirming no regression.
- **One known, previously-documented gotcha recurred, not a new bug**: the generic `cleanupByNamePrefix` E2E helper doesn't clear `LocationStock`/`InventoryMovement` before deleting a `Product`, so this suite's two real-stock, real-sold products silently fell back to a soft-delete and leaked across the first several runs (see memory: "E2E Product Cleanup FK Gotcha") — fixed with the same explicit pre-clear pattern Agri Inputs' own suite already established, confirmed by two subsequent clean runs with zero leaked rows. One genuinely transient single-run flake (a "New Scheme" button 30s timeout) self-resolved on retry with no code change and no repeat across 3 further runs — treated as environmental, not a product defect, consistent with this session's own standing discipline on isolated non-reproducing failures.

**Jewellery is now CLOSED — 5/5, the 18th of Phase 67's 23 verticals fully complete, and the 12th of the 17 Product & Retail / Legacy Generic Service verticals** (after Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear, Manufacturing, Agri Inputs, Blood Bank, Rental).

### 56. Service — all 5 items built, tested, and live-verified in one combined pass, closing the vertical's entire 5-item set

Service, Consultant, and Repair are this phase's 3-vertical "Legacy Generic Service" cluster, distinct from the 14 named Product & Retail verticals but counted together with them as the 17 Product & Retail / Legacy Generic Service verticals throughout this report — Service is the first of the three to be touched. Grounding via the source audit artifact confirmed Service's exact 5-item list: item 1 (Feature) SLA timer on tickets; item 2 (Report) resolution time by category; item 3 (Feature) recurring service contract / AMC ledger; item 4 (Report) repeat-business rate; item 5 (Feature) quote-to-job conversion tracking. A direct read of the current schema/UI (`ServiceTicket`, `ServiceTicketsScreen.tsx`, the pre-existing `PestServiceContract`/`pest-contract.service.ts`) confirmed all 5 were genuine gaps — `ServiceTicket` had no due-date concept at all, no report had ever touched it, there was no contract entity for this vertical (only the pest-specific one), and nothing linked a `Quotation` to the ticket it became.

**Item 1 (Feature) — Ticket SLA timer.** New `ServiceTicket.slaDueAt`, set once at creation from the ticket's own priority (Urgent 4h / High 24h / Medium 3d / Low 7d) and never re-derived on a later priority change — same "the deadline is a fact about the transition that committed to it, not a live-recomputed target" reasoning `RepairTicket.vendorSlaDueDate` already established. Breach is never persisted, always computed live from `slaDueAt` + status (only true while still OPEN/IN_PROGRESS), mirroring `repair-ticket.service.ts`'s own `isOverdue()` exactly. Surfaced as a red **SLA Breached** badge on both the list and detail view, plus a header count that takes precedence over the pre-existing urgent-ticket count when non-zero.

**Item 2 (Report) — Resolution Time by Category.** New `generateServiceResolutionTimeReport()`, a genuinely new report — no prior report had ever touched `ServiceTicket`. Groups every RESOLVED ticket by category (falling back to "Uncategorized"), average/fastest/slowest hours-to-resolve per category, sorted worst (slowest average) first — the actionable ranked-list convention this phase's other reports already use.

**Item 3 (Feature) — Recurring Service Contract / AMC ledger.** New `ServiceContract` model, deliberately mirroring `PestServiceContract`'s own exact shape (the same period-keyed `lastInvoicedPeriod` atomic-claim billing pattern `retainer.service.ts`/`engagement.service.ts`/`pest-contract.service.ts` all already established) rather than repurposing the unrelated `RecurringProfile` model, which is scoped to auto-generating invoices from a payload snapshot, not a real contract entity with its own scope/frequency/status. New `ServiceContractsScreen.tsx` (inline-`<Card>`-panel pattern, not a modal overlay, matching Jewellery's own `GoldSavingsScreen.tsx` precedent) with full create + **Generate Invoice** lifecycle via real UI.

**Item 4 (Report) — Repeat-Business Rate.** New `generateRepeatBusinessRateReport()` — the retention indicator this generic scaffold never had. Fetches every ticket with a customer across ALL time (not just the requested range) to find each customer's TRUE first-ever ticket date, then buckets tickets within the requested range by month, classifying a customer as "repeat" only if their real first-ever ticket predates that month — not just a second ticket the same week.

**Item 5 (Feature) — Quote-to-Job Conversion Tracking.** A single nullable, unique `ServiceTicket.quotationId` FK (`onDelete: SetNull`) plus a lightweight `getQuoteToJobConversionStats()` — kept intentionally small since the audit tagged this a Feature, not a Report. Surfaced via a **Convert From Quotation** dropdown (populated from ACCEPTED quotations only) in the ticket-creation form, a **Converted From Quotation** field in the detail view, and server-side guards (`TKT-009` not found / `TKT-010` not yet accepted / `TKT-011` already converted) that a quotation can only ever become one ticket.

5 new AI intents (`service.slaBreaches/resolutionTime/contractSummary/repeatBusinessRate/quoteToJobConversion`), registered for the `SERVICE` business type.

- Schema: one migration (`20260826090000_...`) — `ServiceTicket.slaDueAt`/`quotationId` added via a hand-written "RedefineTables" block (SQLite has no direct `ALTER TABLE ADD COLUMN` with a named FK constraint), plus the new `ServiceContract` table — applied via `prisma migrate deploy` after `prisma migrate dev`'s own drift-detection wanted a destructive reset on this long-lived dev DB, the same known false-positive this session has now hit and worked around twice before (Jewellery, and now Service), verified against the real DB file via `node:sqlite` before proceeding.
- IPC: `tickets.create` extended with optional `quotationId`, new `tickets:getConversionStats`, new `serviceContracts:list/get/create/update/generateInvoice`, new `reports:serviceResolutionTime/repeatBusinessRate` — all wired through channels/handler/validation/preload the standard 4-layer way.
- Unit tests: 35 new (`service-contract.service.test.ts` — 9, a new file, full create/update/generate-invoice lifecycle incl. the period-keyed atomic-claim guard; `service-ticket.service.test.ts` — 8, SLA due-date computation per priority, quotation-conversion guards (TKT-009/010/011), and conversion-stats math; `report.service.test.ts` — 8, 4 resolution-time incl. the worst-first sort, 4 repeat-business-rate incl. the true-first-ever-date classification; `ai-vertical-templates.service.test.ts` — 8, registration plus behavior tests for all 5 new intents; `ai-query.service.test.ts` — 2 fast-path routing tests). Full suite **3355/3355**, both tsconfigs clean. Caught and fixed a real test-mock bug along the way (not a product bug): the new `makeCreateMockDb()` helper's mocked `serviceTicket.create` initially omitted `createdAt`/`updatedAt`/`status` defaults Prisma normally applies, which made `toRecord()` throw on an invalid date and get silently swallowed by `createTicket()`'s own try/catch — fixed by adding the defaults to the mock.
- i18n: 46 new keys (`service.*` namespace incl. a nested `frequency`/`contractStatus` block, 2 `reports.defs`, 3 `reports.summary`, 6 `reports.col`, 2 `reports.empty`), all 13 languages via the same programmatic anchor-based insertion script this arc has used since Footwear item 4 — one real anchor-disambiguation bug hit and fixed during the script's own build (not the app): the `reports.empty` anchor's naive "last occurrence of `purityAdjustedExchange`" search landed on the unrelated last-key-in-the-`jewellery`-namespace occurrence instead (both are plain strings), producing a JSON syntax error since that key has no trailing comma — fixed by searching for the FIRST string-valued occurrence instead, since file order is guaranteed. Key parity verified (**4457/4457 leaf keys final**, up from 4411).
- Manual: extended the shared `business__service-consultant-repair.md` chapter's existing "## Service Tickets (Service, Repair)" section with 2 new paragraphs (SLA timer, quote-to-job conversion), plus two brand-new headings ("## Service Contracts (Service)", "## Reports"), all 13 languages, section count 6 → 8 verified matching every locale — including `ta`/`id`, whose own copy of this specific shared chapter has a pre-existing, apparently deliberate pattern of leaving several headings untranslated (English) while sibling headings in the same file are translated; the new content was left in English there too, matching the established local pattern rather than introducing a different kind of patchwork.
- **Live-verified against the real running app** via a brand-new `tests/e2e/suites/75-service-sla-contract-conversion-repeat-business.js`, **43/43, two consecutive clean runs**: an URGENT ticket created via real UI confirmed its `slaDueAt` computed exactly 4 hours out; a direct DB backdate of that same ticket's `slaDueAt` confirmed the real UI flags it **SLA Breached** on both the list and detail view; an ACCEPTED quotation converted to a ticket via the real **Convert From Quotation** picker confirmed the link both via API and the detail view's own **Converted From Quotation** field, with both double-conversion and not-yet-accepted-quotation guard rejections confirmed via API (`TKT-011`/`TKT-010`); a real Service Contract created and invoiced via the real UI confirmed `lastInvoicedPeriod` updates and a same-period double-invoice is rejected (`SCT-005`); both new Reports tiles confirmed rendering real data without error; all 5 new AI intents confirmed routing correctly through the real `ai:query` channel; the pre-existing base-chain `tests/e2e/suites/02-service-business.js` re-run clean (15/15) to confirm no regression.
- **Two real, generalizable E2E gotchas found and fixed during this suite's own build, both in the test's own code, not the app**: (1) DateTime columns in this SQLite DB are stored as epoch-ms `INTEGER`, not ISO text — a direct-DB backdate written via `.toISOString()` leaves a working single-row Prisma read (lenient deserialization) but silently breaks every SQL-level comparison the report/badge logic depends on; this exact class of bug was already documented in suite 15's own `vendorSlaDueDate` backdate, and recurred here before being caught and fixed the same way (`Date.now() - N*ms`, not `.toISOString()`). (2) Re-navigating the harness's `gotoHash()` to the SAME hash the page is already on does not remount the screen component, so a `useEffect(() => { load() }, [])`-style screen keeps serving stale pre-mutation React state after a direct-DB write or an API call made outside its own handlers — fixed by bouncing through a different route first (`#/dashboard` then back) wherever a step needed to observe a mutation the screen itself didn't trigger.

**Service is now CLOSED — 5/5, the 19th of Phase 67's 23 verticals fully complete, and the 13th of the 17 Product & Retail / Legacy Generic Service verticals** (after Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear, Manufacturing, Agri Inputs, Blood Bank, Rental, Jewellery).

### 57. Consultant — all 5 items built, tested, and live-verified in one combined pass, closing the vertical's entire 5-item set

Consultant is the second of this phase's 3-vertical Legacy Generic Service cluster. Grounding via the source audit artifact confirmed Consultant's exact 5-item list: item 1 (Feature) engagement-letter → project auto-conversion; item 2 (Report) utilization rate, "the #1 consulting metric, currently invisible"; item 3 (Feature) retainer burn-down tracker; item 4 (Report) client profitability; item 5 (Feature) proposal win-rate tracking. A direct read of the current schema/module map — crucially, distinguishing the legacy generic `CONSULTANT` business type from the unrelated `INDEPENDENT_CONSULTANT` (part of the separate Professional Services cluster, with its own `ServiceProject`/`RetainerAgreement`/`TimeEntry` infrastructure already built in an earlier phase) — found items 1, 2, 4, and 5 were genuine gaps, but item 3 was the biggest pre-existing-surprise this phase has found yet: `RetainersScreen.tsx` (create/edit lifecycle) and `TimeEntryScreen.tsx` (hour logging), including a real, already-working hours-used/hours-remaining progress bar, were already fully built and tested for `INDEPENDENT_CONSULTANT`/`MARKETING_AGENCY`/`SOFTWARE_AGENCY` — the plain `CONSULTANT` type simply never had the `retainers`/`time_entries` modules turned on.

**Item 1 (Feature) — Engagement-Letter → Project Auto-Conversion.** New nullable unique `Project.quotationId` FK (`onDelete: SetNull`), the exact same shape as Service item 5's `ServiceTicket.quotationId` — a `Quotation` doubles as the "engagement letter" in this vertical's own vocabulary, since no separate document type exists for one. Server-side guards (`PRJ-009` not found / `PRJ-010` not yet accepted / `PRJ-011` already converted) mirror `TKT-009/010/011` exactly. Surfaced via the same **Convert From Quotation** dropdown pattern (reusing Service's own `service.convertFromQuotation`/`notFromQuotation`/`convertedFromQuotation` i18n keys verbatim, since the wording is generic enough for both).

**Item 2 (Report) — Utilization Rate.** New `generateConsultantUtilizationReport()` — genuinely new, no prior report had ever touched `WorkLog`'s own billable/non-billable split. Per staff member, billable vs. non-billable hours in the requested range, sorted ascending by utilization percent so the least-utilized consultant (who most needs more billable work) surfaces first — a grouped (not stacked) horizontal bar chart, matching the audit's own "(bar: billable vs. non-billable hours)" chart-form note.

**Item 3 (Feature) — Retainer Burn-Down Tracker.** Zero new code — `retainers` and `time_entries` added to `CONSULTANT`'s own module list in `industry-template.service.ts`, reusing `RetainersScreen.tsx`/`TimeEntryScreen.tsx`/`retainer.service.ts` verbatim. `TimeEntry.projectId`/`caseId` simply stay null for this vertical (it has neither `ServiceProject` nor `LegalCase`); only `retainerId` is used, which the model already supports independently of either field.

**Item 4 (Report) — Client Profitability.** New `generateClientProfitabilityReport()` — revenue (from each customer's own invoiced Projects) against hours spent (from `WorkLog`) per client, sorted ascending by revenue-per-hour so the least-profitable client — "which clients are actually worth keeping," per the audit's own item wording — surfaces first. Rendered as a dual-yAxisId bar chart (revenue and hours differ in scale by orders of magnitude), matching the audit's own "(bar: revenue vs. hours spent, per client)" chart-form note.

**Item 5 (Feature) — Proposal Win-Rate Tracking.** New `getProposalWinRateStats()`, the same lightweight shape as Service item 5's own `getQuoteToJobConversionStats()` — a "proposal" is a `Quotation` in this codebase's vocabulary; won = ACCEPTED, lost = EXPIRED, everything else pending. Surfaced as a running win-rate stat directly in `ProjectsScreen.tsx`'s own header, next to the existing active-project count — kept deliberately lightweight since the audit tagged this a Feature, not a Report.

5 new AI intents (`consultant.engagementConversion/utilization/retainerBurnDown/clientProfitability/proposalWinRate`), registered for the `CONSULTANT` business type. `consultant.retainerBurnDown` needed no new retainer-service function at all — its own AI-answer case block aggregates `listRetainers({status:'ACTIVE'})` + the pre-existing `getRetainerHoursUsage()` per retainer directly, since a one-off summary aggregation didn't warrant a new persisted-shape function.

- Schema: one migration (`20260826140000_...`) — `Project.quotationId` added via a hand-written RedefineTables block (SQLite has no direct `ALTER TABLE ADD COLUMN` with a named FK constraint), same known `prisma migrate dev` drift-detection false-positive workaround used for Jewellery's and Service's own migrations this session (3rd time now on this dev DB).
- IPC: `projects.create` extended with optional `quotationId`, new `projects:getProposalWinRateStats`, new `reports:consultantUtilization/clientProfitability` — all wired through channels/handler/validation/preload the standard 4-layer way.
- Unit tests: 30 new (`project.service.test.ts` — 9: 5 engagement-conversion guard tests incl. the "no quotation at all, unaffected" case, 2 `getEngagementConversionStats`, 2 `getProposalWinRateStats`; `report.service.test.ts` — 7: 3 utilization incl. the least-utilized-first sort, 4 client-profitability incl. an uninvoiced-but-hours-logged honest-zero case; `ai-vertical-templates.service.test.ts` — 12: registration plus 2-3 behavior tests per intent for all 5; `ai-query.service.test.ts` — 2 fast-path routing tests). Full suite **3385/3385**, both tsconfigs clean.
- i18n: 14 new keys (2 defs, 1 summary, 6 col, 2 empty, 1 `service.proposalWinRate`, plus the 2 defs' own descriptions counted within), all 13 languages via the same programmatic anchor-based insertion script this arc has used since Footwear item 4 — one anchor-robustness fix made along the way (not a translation bug): the `reports.summary`/`reports.col` anchors both reuse the string `"repeatRatePercent"`, and naively matching each occurrence's very next line (as prior scripts did) broke on `hi.json`, whose own sibling-key ordering at that point differs slightly from `en.json`'s — fixed by anchoring on the FIRST vs. SECOND occurrence of the key itself, which holds for `reports.summary`-before-`reports.col` ordering in every locale, rather than a fragile next-key match. Key parity verified (**4471/4471 leaf keys final**, up from 4457).
- Manual: extended the shared `business__service-consultant-repair.md` chapter's existing "## Projects (Service, Consultant)" section with 2 new paragraphs (engagement-letter conversion, proposal win-rate), a brand-new "## Retainers (Consultant)" heading, and extended the existing "## Reports" paragraph from 2 reports to 4, all 13 languages, section count 8 → 9 verified matching every locale — `ta`/`id` again left the newly-added Reports/Retainers content matching their own established partial-English pattern for this specific shared chapter, same as Service's own item.
- **Live-verified against the real running app** via a brand-new `tests/e2e/suites/76-consultant-engagement-utilization-retainer-profitability.js`, **39/39, two consecutive clean runs**: an accepted Quotation converted to a project via the real **Convert From Quotation** picker, confirmed via the detail view's own **Converted From Quotation** field, with double-conversion and not-yet-accepted guard rejections confirmed via API (`PRJ-011`/`PRJ-010`); the Projects screen header confirmed showing a live proposal win-rate percentage; a real Retainer created via the real UI (Hourly Bucket, 20h/month), a real Time Entry logged against it from the Time Tracking screen, and the Retainers list confirmed showing the live "12.0h / 20h" burn-down bar; both new Reports tiles confirmed rendering real data (utilization sorted least-first, profitability keyed to the real invoiced amount); all 5 new AI intents confirmed routing correctly through the real `ai:query` channel.
- **Two real gotchas found and fixed during this suite's own build**: (1) a test-code mistake, not a product bug — the Client Profitability revenue assertion initially expected the raw pre-tax `estimatedAmount` rather than the real GST-inclusive invoice total, fixed to assert a realistic lower bound instead of hardcoding tax math into the test; (2) a recurrence of this project's own known "electron-vite main-process watcher can silently go stale" class of issue (previously hit and documented during Footwear item 4) — a live-edited fast-path regex pattern (adding a third alternative to `consultant.clientProfitability`'s own pattern set, to catch the natural "which client is least profitable" phrasing alongside "least profitable client") was confirmed correct in the source file but never actually reached the running `out/main/index.js` bundle despite the dev server appearing to rebuild cleanly — a full dev-server restart (not just a file edit) was required before the fix actually took effect, confirmed by inspecting the compiled bundle directly rather than trusting the watcher's own success log.

**Consultant is now CLOSED — 5/5, the 20th of Phase 67's 23 verticals fully complete, and the 14th of the 17 Product & Retail / Legacy Generic Service verticals** (after Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear, Manufacturing, Agri Inputs, Blood Bank, Rental, Jewellery, Service).

## Cumulative testing state so far

- Unit suite: **3385/3385** (up from 2614 at Phase 66's close; one unrelated pre-existing flaky timeout in `backup.service.test.ts` has now re-run clean standalone every time it's flaked across this entire session, confirmed not a regression — did not reproduce on this window's own full-suite run; a separate unrelated dev-DB-pollution false-positive in `ai-readonly-db.test.ts` was found and cleaned up during item 4's own verification pass, see §39).
- Both tsconfigs clean throughout.
- 13 + 11 + 4 + 4 + 6 + 8 + 6 + 2 + 19 + 9 + 11 + 4 + 3 + 3 + 10 + 10 + 3 + 18 + 14 + 3 + 6 + 14 + 7 + 8 + 5 + 11 + 9 + 18 + 8 + 0 + 6 + 8 + 0 + 14 + 19 + 12 + 12 + 13 + 16 + 6 + 21 + 13 + 11 + 9 + 13 + 11 + 10 + 12 + 13 + 13 + 22 + 44 + 60 + 26 + 13 new report/AI-intent/service unit tests plus 3 kit-expansion tests (Hardware's items 1+2 needed no new unit tests — pure UI reuse of an already-tested report function; item 3 added 6: 3 purchase-order rounding, 3 report-service carton-breakdown; item 4 added 8: 5 report-service, 3 AI-intent; item 5 needed none — pure UI logic, no new service function; General's item 1 added 14: 10 service-layer, 4 AI-intent; General's item 2 added 19: 10 service-layer, 6 validation-schema, 3 AI-intent; General's item 3 added 12: 9 report-service, 2 AI-intent, 1 fast-path routing; General's item 4 added 12: 8 report-service, 3 AI-intent, 1 fast-path routing; General's item 5 added 13: 9 quotation-service, 3 AI-intent, 1 fast-path routing; Electronics' item 1 added 16: 6 repair-ticket-service, 4 analytics-alert, 5 AI-intent incl. registration, 1 fast-path routing; Electronics' item 2 added 6 report-service — deliberately no new AI-intent, reused item 1's own; Electronics' item 3 added 21: 7 report-service, 10 repair-ticket-service, 3 AI-intent, 1 fast-path routing; Electronics' item 4 added 13: 6 report-service, 3 repair-ticket-service, 3 AI-intent, 1 fast-path routing; Electronics' item 5 added 11: 6 repair-ticket-service, 4 AI-intent incl. registration, 1 fast-path routing; Clothing's item 1 added 9: 5 report-service, 3 AI-intent incl. registration, 1 fast-path routing; Clothing's item 2 added 13: 8 variant-service, 4 AI-intent, 1 fast-path routing; Clothing's item 3 added 11: 8 report-service, 2 AI-intent, 1 fast-path routing; Clothing's item 4 added 10: 6 exchange-service, 3 AI-intent incl. registration, 1 fast-path routing; Clothing's item 5 added 12: 8 report-service, 3 AI-intent incl. registration, 1 fast-path routing; Footwear's item 1 added 5: variant-service width persistence/summary/reorder-suggestion identity — deliberately no new AI-intent, a pure data-capture feature with no natural question to ask; Footwear's item 2 added 11: 6 report-service, 4 AI-intent incl. a behavior-correction test, 1 fast-path routing; Footwear's item 3 added 13: 9 trial-session-service, 3 AI-intent incl. registration, 1 fast-path routing; Footwear's item 4 added 13: 9 report-service, 3 AI-intent, 1 fast-path routing incl. a no-collision-with-the-shared-intent test; Footwear's item 5 added 22: 15 seasonal-cycle-service incl. 7 date-arithmetic tests using fake timers, 3 AI-intent, 1 fast-path routing, closing Footwear's entire 5-item set; Manufacturing's 5 items — built and tested together in one combined pass — added 44: 22 work-order-service (QC-quantity validation, downtime CRUD/summary, bottleneck-flag date-arithmetic), 11 report-service (landed-cost + rejection-rate-trend), 8 AI-intent incl. registration for all 4 new intents, 4 fast-path routing tests, closing Manufacturing's entire 5-item set; Agri Inputs' 5 items — built and tested together in one combined pass — added 60: 16 crop-season-service (CRUD + date-arithmetic), 9 crop-advisory-service, 13 serial-service (service-info update, due-for-service classification, reminder scheduling), 12 report-service (seasonal-credit-exposure + farmer-repayment), 3 billing-service (crop-season dueDate override), 8 AI-intent incl. registration for all 4 new intents, 4 fast-path routing tests, closing Agri Inputs' entire 5-item set; Blood Bank's items 1/3/4/5 — built and tested together in one combined pass, item 2 already existed — added 26: 9 blood-bank-service (recall-due filtering incl. component-specific cooldown, fast-match compatibility/component/quantity-cap/shortfall), 5 report-service (donation-to-issue cycle time), 6 AI-intent incl. registration for all 3 new intents, 3 fast-path routing tests, closing Blood Bank's entire 5-item set; Rental's items 3/4 — items 1/2/5 already existed — added 13: 9 report-service (3 aging-bucket, 6 asset-utilization), 3 AI-intent incl. registration, 1 fast-path routing test, closing Rental's entire 5-item set; Jewellery's 5 items — built and tested together in one combined pass — added 42: 17 gold-savings-service (create/install/redeem/link lifecycle incl. atomic-claim race), 1 metal-rate-service (history-append), 16 report-service (making-charge-margin, hallmark-compliance, metal-rate-vs-sales-volume incl. auto-select/fallback, purity-adjusted-exchange incl. karat/per-mille parsing), 9 AI-intent incl. registration for all 5 new intents, 2 fast-path routing tests incl. a collision-avoidance test, closing Jewellery's entire 5-item set; Service's 5 items — built and tested together in one combined pass — added 35: 9 service-contract-service (create/update/generate-invoice lifecycle incl. the period-keyed atomic-claim guard), 8 service-ticket-service (SLA due-date computation per priority, quotation-conversion guards, conversion-stats math), 8 report-service (4 resolution-time, 4 repeat-business-rate), 8 AI-intent incl. registration for all 5 new intents, 2 fast-path routing tests, closing Service's entire 5-item set; Consultant's 5 items — built and tested together in one combined pass — added 30: 9 project-service (5 engagement-conversion guard tests, 2 conversion-stats, 2 win-rate-stats), 7 report-service (3 utilization, 4 client-profitability), 12 AI-intent incl. registration for all 5 new intents, 2 fast-path routing tests, closing Consultant's entire 5-item set).
- All touched i18n verified at exact key parity after every addition (**4411/4411 leaf keys final** — Footwear's item 1 added none, being a hardcoded-English UI screen, though its Manual chapter was translated separately; Footwear's item 2 added 6 more; Footwear's item 3 added none — the Billing picker's trial-mode toggle is a hardcoded-English addition to an already hardcoded-English screen, though its Manual chapter was translated separately; Footwear's item 4 added 12 more; Footwear's item 5 added 31 more (2 defs, 3 col, 26 under a new seasonalCalendar block); items 18.3/18.5/20.4/20.2/20.3/20.5/21.4/21.5 needed none for the screen itself, being English-only vertical screens; Restaurant's happy-hour item added 7 new keys, its dish-margin item added 12 more, its table-turnover item added 10 more, its waste-variance item added 10 more, Retail's dead-stock item added 10 more, Retail's price-markdown item added ~24 more, Retail's category-sell-through item added 8 more, Retail's loyalty-program item added 29 more, Retail's basket-composition item added 10 more, Hardware's items 1+2 added 2 more, Hardware's item 3 added none — reused an already-existing key, Hardware's item 4 added 13 more, Hardware's item 5 added 1 more, General's item 1 added 18 more, General's item 2 added 47 more, General's item 3 added 6 more, General's item 4 added 6 more, General's item 5 added 4 more, Electronics' item 1 added 1 more (reused an existing translation), Electronics' item 2 added 8 more, Electronics' item 3 added 9 more, Electronics' item 4 added 11 more, Electronics' item 5 added 7 more, Clothing's item 1 added 7 more, Clothing's item 2 added none — hardcoded-English screen, Clothing's item 3 added 8 more, Clothing's item 4 added 20 more (a shared, app-wide screen, not vertical-locked English), Clothing's item 5 added 5 more, and Manufacturing's 5 items together added 37 more (2 defs, 4 summary, 7 col, 2 empty, 1 section, 13 new manufacturing.qc/downtime/bottleneck keys for its own genuinely i18n'd `ProductionOrdersScreen.tsx` — the first vertical this session whose own screen needed real UI-string translation, not just its Reports/Manual side, since it's not one of the several hardcoded-English vertical screens this phase has otherwise dealt with) — nearly everything it needed already existed and was reused as-is, all across all 13 languages; and Agri Inputs' 5 items together added 17 more under `reports.defs`/`summary`/`col`/`section`/`empty` for its two new reports — the Billing crop-season/crop-advisory UI and the Agri Dashboard's Equipment Service Due panel are hardcoded English, matching the vertical-gated-section-of-a-shared-screen precedent, so needed none; and Blood Bank's items 1/3/4/5 together added 10 more under `reports.defs`/`summary`/`col`/`empty` for its one new report — the Camps screen, Recall Due filter, and Fast Match panel are hardcoded English, matching Blood Bank's own screens' existing hardcoded-English convention since Phase 51, so needed none; and Rental's items 3/4 together added 9 more under `reports.defs`/`summary`/`col`/`rental`/`rental.empty` for its one new report plus the aging-chart title, PLUS one more genuinely pre-existing gap — `rental.status.RENTED`, missing since Phase 58 and surfaced only by this item's own new DataTable — fixed across all 13 locales alongside it; and Jewellery's 5 items together added 68 more (8 defs, 8 summary, 11 col, 1 section, 4 empty, 36 under the `jewellery` namespace incl. a nested `schemeStatus` block) for its new Gold Savings screen, 4 new reports, and Old-Metal Exchange enhancement — Jewellery's own screens are genuinely translated (unlike Rental/Blood Bank's hardcoded-English precedent), so every new UI string needed a real translation across all 13 languages, not a reuse; and Service's 5 items together added 46 more (2 defs, 3 summary, 6 col, 2 empty, 33 under the `service` namespace incl. nested `frequency`/`contractStatus` blocks) for its new Service Contracts screen, SLA/quotation-conversion UI on the pre-existing Service Tickets screen, and 2 new reports — Service's own screens are genuinely translated (this vertical runs in the user's normal chosen language, unlike the several hardcoded-English vertical-specific screens elsewhere in this phase), so every new UI string needed a real translation across all 13 languages, not a reuse; and Consultant's 5 items together added 14 more (2 defs, 1 summary, 6 col, 2 empty, 1 `service.proposalWinRate` reused-namespace key) for its 2 new reports and the Projects-screen win-rate stat — item 1 and item 3 needed no new i18n keys at all, reusing Service's own `convertFromQuotation`/`notFromQuotation`/`convertedFromQuotation` keys verbatim and the pre-existing, already-translated Retainers/Time Tracking screens verbatim respectively).

## What is still fully unstarted

- **~1 item**: Repair, the last of the 3-vertical Legacy Generic Service cluster, fully untouched (Distributor/Pharmacy already partially done above; Service and Consultant are now closed). **GP Clinic, Physio Clinic, Diagnostic Lab, Vet Clinic, Specialist Clinic, Dental Clinic, Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear, Manufacturing, Agri Inputs, Blood Bank, Rental, Jewellery, Service, and Consultant are each fully complete** — every one of their own 5 signature items done, the first 20 of Phase 67's 23 verticals to reach that: all 6 clinical verticals in the phase's scope, plus the first fourteen of the 17 Product & Retail / Legacy Generic Service verticals.

## Self-review (Section 1.6 method) — partial, honest snapshot

| Aspect | Score | Evidence |
|---|---|---|
| Logical correctness | 10/10 (for work done) | Combo ingredient-deduction fix live-verified against a direct DB query, not just a green test |
| Spec coverage | **100/115 items — not scored as a phase-level percentage; scoring only applies once the phase is complete** | 20 of 23 verticals fully closed (5/5 each); GP Clinic, Physio Clinic, Diagnostic Lab, Vet Clinic, Specialist Clinic, Dental Clinic, Restaurant, Retail, Hardware, General, Electronics, Clothing, Footwear, Manufacturing, Agri Inputs, Blood Bank, Rental, Jewellery, Service, and Consultant — all 6 clinical verticals in scope now fully closed, plus the first fourteen Product & Retail / Legacy Generic Service verticals |
| Day-to-day critical-feature coverage | 10/10 (for the 4 ranked items fully done) | Each was the #1-#4 ranked item in Section 9.2's own priority ordering, not chosen arbitrarily |
| Testing completeness | 10/10 (for work done) | Every item live-verified against the real running app, not just unit-tested — including several real verification-script mistakes caught and fixed rather than papered over |
| Language completeness | 10/10 (for work done) | 13/13 locales at parity on every batch shipped so far |
| Manual/Tutorial/AI integration completeness | 10/10 (for work done) | New AI intents added for every new report; Restaurant, Specialist Clinic, and Vet Clinic each got a genuinely new Manual section |

A phase-level overall score is deliberately not given here — Phase 67 is not close enough to done for one to be meaningful, and this arc's own standing discipline is to never round up "some real progress" into a false completion signal.

## Current state

Phase 67 is **in progress**. Commits pushed to `origin/main`: `747ab74` (clinical dashboard fix + Pharmacy's 2 reports), `1483864` (Distributor's scheme report + Restaurant's combo fix), `601c706` (GP Clinic's chronic-condition close + Recall Compliance report), and `b2cdcfd` (everything through Hardware's full closure — GP Clinic's last 3 reports, all of Physio Clinic's own report/enrichment work, all of Diagnostic Lab's work, all of Vet Clinic's work, all of Specialist Clinic's items 20.4/20.2/20.3/20.5, all of Dental Clinic's items 21.1/21.2/21.4/21.5, all of Restaurant's items 2/3/4/5, all of Retail's items 1-5, and all of Hardware's items 1-5). **GP Clinic's, Physio Clinic's, Diagnostic Lab's, Vet Clinic's, Specialist Clinic's, Dental Clinic's, Restaurant's, Retail's, and Hardware's entire 5-item signature-win sets are all complete** — the first 9 of Phase 67's 23 verticals to reach that: all 6 clinical verticals in the phase's scope, plus the first three of the 17 Product & Retail / Legacy Generic Service verticals. Since that commit, all 5 of General's items have been built, tested, and live-verified, closing General out entirely — item 1 ("Which template fits you?" wizard, the first-ever work on this vertical), item 2 (Custom document/field builder, reusing Phase 66's `CustomFieldDefinition` mechanism for the "field builder" half via a namespaced entityType key, and building the genuinely new "custom document" half on top of it), item 3 (User-defined category mix report, a single-period revenue+units breakdown by `ProductCategory`, this file's first live Pie chart), item 4 (Combined cash position trend, a GL-based day-by-day cumulative balance for the "Cash & Bank" account, following the same real-ledger-over-synthesized-figures approach the Trial Balance rewrite established, and along the way proactively catching and fixing a real cross-suite JournalEntry cleanup leak before it could distort any future E2E run), and item 5 (Universal quote→order→invoice pipeline, the missing `SalesOrder.quotationId` link between two previously-disconnected conversion paths, which along the way surfaced and fixed a real dead-code UI bug in the Quotations list screen and a genuine Prisma relative-path-resolution gotcha for schema migrations) — **General is now the 10th of Phase 67's 23 verticals to reach a fully-complete 5/5 signature-win set**. Continuing straight on into Electronics (the first fully-untouched vertical per Section 9.1's own ordering), item 1 (RMA SLA tracker), item 2 (RMA aging report), and item 3 (vendor warranty-claim recovery ledger) are also now built, tested, and live-verified — item 1 a due-date on the pre-existing `RepairTicket` workflow surfaced via a badge, a screen-header count, and a new Dashboard alert (catching a real epoch-ms-vs-ISO-string datetime storage bug in the test's own setup along the way), item 2 the full ranked breakdown as its own Reports-screen entry with a bar chart, deliberately reusing item 1's own AI intent rather than duplicating it (catching a real, generalizable E2E tile-click locator bug along the way — `'button, div'` combined locators pick the non-clickable parent wrapper before the actual button), item 3 a claim/recovery/write-off lifecycle extending `RepairTicket` directly (rather than repurposing the unrelated `DebitNote`/Vendor Credit model) with its own new Reports-screen ledger and its own new, genuinely-distinct AI intent — no new bugs surfaced this time, the patterns established across items 1 and 2 held up cleanly on first application. Item 4 (repair turnaround by technician) is also now built, tested, and live-verified — a new single nullable `technicianId` FK to `Employee` on `RepairTicket` (deliberately not `CarJobCard`'s own multi-technician JSON-array pattern, since one device repair is one person's work), a new Reports-screen entry grouping average/fastest/slowest turnaround by technician, and its own fourth, genuinely-distinct AI intent — along the way, live-verification surfaced and cleaned up one unrelated pre-existing dev-DB pollution issue (a stale leftover `Notification` row that had been masking `ai-readonly-db.test.ts`'s own real assertion for an unknown number of prior sessions), not a regression from this item's own work. Item 5 (serial-number service lookup), Electronics' fifth and final item, is also now built, tested, and live-verified — a new `lookupSerialService()` resolving a raw scanned/typed serial number or IMEI to the unit's product info, real purchase history (invoice/customer/price, pulled from the linked `Invoice`), and full repair ticket history in one call, surfaced via a new Service Lookup box on the (shared, i18n'd) Serial Tracking screen deliberately left alongside — not replacing — the pre-existing narrower IMEI-only lookup box, plus its own fifth AI intent reusing the existing generic search-term-extraction mechanism rather than inventing a second one. **Electronics' entire 5-item signature-win set is now complete** — the 11th of Phase 67's 23 verticals to reach that, and the fifth of the 17 Product & Retail / Legacy Generic Service verticals. Continuing straight on into Clothing (the next fully-untouched vertical per Section 9.1's own ordering), item 1 (season/collection sell-through report) is also now built, tested, and live-verified — a new `Product.season` free-text field, a new report reusing Retail's own `generateCategorySellThroughReport()` algorithm verbatim just grouped differently, and this file's first genuine line+bar combo chart, plus its own new AI intent — along the way, live-verification found and fixed the same `'button, div'` E2E locator bug (already documented from Electronics) present in this suite too, proactively, not just in the new step. Item 2 (size-curve reorder suggestion) is also now built, tested, and live-verified — grounding first confirmed a real architectural boundary (`PurchaseOrderItem` has no `variantId` at all, so this is correctly scoped as a read-only suggestion the owner consults before manually placing the real order, not a new variant-level ordering mechanism), then a new `getSizeCurveReorderSuggestion()` weighting a reorder quantity toward whichever sizes/colours actually sold in the last 90 days (largest-remainder rounding so the split always sums exactly), surfaced as a new collapsible section in the existing Manage Variants modal, plus its own new AI intent reusing an existing product-lookup pattern rather than inventing one. Item 3 (size × style heatmap report) is also now built, tested, and live-verified — needed no new capture at all (Product already IS the style, ProductVariant.size already IS the size), resolved size via a real ProductVariant join rather than InvoiceItem's own ambiguous free-text variantInfo snapshot, reused Restaurant's own pre-existing hand-built CSS-grid heatmap pattern (this codebase's only other heatmap, from Table Turnover by Hour) rather than inventing a second one, plus its own new AI intent. **General's full closure, all five of Electronics' items, and Clothing's items 1-3 were committed and pushed to `origin/main` as `ee3bb4a`** (132 files, 9289 insertions) partway through this arc, at the founder's explicit request. Continuing straight on from there, item 4 (size/color exchange workflow) is also now built, tested, and live-verified — a real architectural landmine was found and deliberately avoided during grounding (a single combined "EXCHANGE" invoice document would have silently corrupted 15+ existing revenue/tax/analytics report call sites that derive an item's sign purely from its own invoice's `invoiceType`), so it's built instead as an orchestration of two ALREADY-correct, separately-proven primitives — a real `RETURN` invoice via `createReturn()` wholesale, and a real `RETAIL` sale via `billingService.createInvoice()` (the same cross-service pattern ~15 other services already use) — linked by a new `Invoice.exchangeReturnId` column, with the pre-existing already-returned-quantity guard extended so a `RETURN`/exchange pair can't be double-dipped against by either workflow. Along the way, live-verification surfaced and fixed a genuinely new, generalizable Prisma gotcha worth remembering broadly: `prisma migrate deploy` (run via PowerShell, not the already-known Git-Bash-path-mangling trap) reported this item's own new migration as successfully applied, and `prisma migrate status` agreed afterward, yet a direct `node:sqlite` read of the actual dev DB file showed the column was never created and the migration was never recorded — only surfaced because the dev server's own first real query against the new column then failed live. Fixed by applying the DDL directly via `node:sqlite` and manually inserting the tracking row. Item 5 (margin by brand/vendor report), Clothing's fifth and final item, is also now built, tested, and live-verified — needed no new schema at all (`Product.defaultSupplierId` already IS "vendor/brand"), reused General's own `generateCategoryMixReport()` grouping shape verbatim just keyed by supplier with COGS added via the same `getProductCostsBatch()` resolver `generateProfitAndLossReport()` already uses, reports a negative margin honestly as a loss rather than hiding it, and its own new AI intent. Live-verification surfaced a real, pre-existing, generalizable cost-basis gotcha (not a bug introduced here) — a product created with zero opening quantity deliberately keeps `Inventory.averageCost` at 0 forever per `product.service.ts`'s own documented RULE I007, so this item's own E2E test product resolved to a real COGS of exactly 0 until a real `averageCost` was seeded directly via `node:sqlite`, the same "simulate real state via direct DB write" precedent Electronics' RMA SLA item already established. **Clothing's entire 5-item signature-win set is now complete** — the 12th of Phase 67's 23 verticals to reach that, and the 6th of the 17 Product & Retail / Legacy Generic Service verticals. **Clothing items 4-5 were committed and pushed to `origin/main` as `1fd9c0a`** (55 files, 1843 insertions) at the founder's explicit request ("do both" — commit+push AND continue). Continuing straight on into Footwear (the next fully-untouched vertical per Section 9.1's own ordering), item 1 (half-size/width matrix) is also now built, tested, and live-verified — grounding first confirmed, by fetching the audit artifact directly rather than assuming, that Footwear does NOT inherit Clothing's own item list despite sharing its module set byte-for-byte; it has its own distinct 5 items. A new nullable `ProductVariant.width` column, the existing 2D "Generate Size × Colour Matrix" generalized to a genuine N-dimensional cartesian product (degrading to the exact original behavior for every non-Footwear vertical, since width stays an always-empty list there), and width carried through all 9 places a variant's identity already appears (Billing picker/cart, printed labels, Returns/Exchange, Suggested Reorder Split, and the AI intent's own row identity) — found via a systematic grep sweep, not touched ad hoc. Deliberately no new AI intent (a pure data-capture feature, matching the pre-existing 2D matrix tool's own precedent of never having one). Live-verified via a brand-new `tests/e2e/suites/69-footwear-variants.js` (first-ever E2E coverage of this vertical), 30/30, two consecutive clean runs — along the way finding and fixing a real, generalizable E2E gotcha (`getByPlaceholder()` does substring matching by default, not exact) and a bug in the test's own assertion (not the product). Item 2 (Brand-Wise Margin & Return-Rate Report) is also now built, tested, and live-verified — deliberately a separate function from Clothing's own `generateVendorMarginReport()`, not a field bolted onto it, tracking units sold and units returned SEPARATELY per brand so the return rate reflects the real numerator/denominator rather than a masked net. Along the way, grounding surfaced and fixed a real pre-existing gap: CLOTHING and FOOTWEAR had been sharing one AI-intent registration array since earlier in this arc (before Footwear's own distinct item list was known), meaning Footwear was incorrectly getting Clothing's own `clothing.vendorMargin` intent — split into two correct per-vertical cases now. New `BrandMarginReturnRateView`, this file's first bar+line combo chart on a category axis. **Footwear now has 2 of its own 5 items done** — none of items 1-2's own work is **committed to git yet**, awaiting explicit commit+push instruction. Founder said "keep going until limit hits" — continuing autonomously into Footwear's remaining 3 items per the audit's own ordering. Item 3 (trial-pair counter workflow) is also now built, tested, and live-verified — resolved a real design question first (does this need a stock-reservation mechanism, or does Sarang's own existing "stock leaves only at sale time" model already satisfy it by construction? Confirmed the latter), landing on a lightweight new `TrialSession` append-only log rather than a two-phase reservation system, plus a Footwear-only "Track Trial" toggle inside Billing's existing Select-Variant picker and a new AI intent. Along the way, live-verification found and fixed two real bugs: `ai_assistant` is off by default for every business type (the E2E test now seeds it and restores the exact prior state afterward), and a new `node:sqlite`-foreign-key-enforcement E2E cleanup gotcha where orphaned `TrialSession` rows were silently accumulating across suite runs (fixed with the same manual pre-cleanup pattern already established for `ProductVariant`). Item 4 (Size Availability Heatmap Report) is also now built, tested, and live-verified — a live current-state stock question deliberately kept separate from the pre-existing, sales-based `clothing.sizeStyleHeatmap` FOOTWEAR already shared with Clothing, with its own AI intent whose fast-path regex deliberately avoids the word "heatmap" specifically so it can't collide with that shared intent's own broader pattern. Live-verification surfaced a real dev-server infrastructure bug (the electron-vite watcher had gone stale mid-session after several preload/channel edits, fixed by a full restart) rather than a product bug. Item 5 (Seasonal Reorder Calendar), Footwear's fifth and final item, is also now built, tested, and live-verified — a new `SeasonalCycle` model for shop-defined recurring buying windows (never a fixed "monsoon/formal/sports" enum, matching `Product.season`'s own free-text precedent), correctly resolving a year-wrapping window and classifying each cycle IN_SEASON/REORDER_NOW/UPCOMING against its own lead time, cross-referenced against tagged products' real stock levels. Its Reports-tile view is genuinely different from every other report in this file — it owns real CRUD mutations via an inline "Manage Seasons" panel, not just a read-only `data` prop. **Footwear's entire 5-item signature-win set is now complete** — the 13th of Phase 67's 23 verticals to reach that, and the 7th of the 17 Product & Retail / Legacy Generic Service verticals. **None of Footwear's 5 items are committed to git yet**, awaiting explicit commit+push instruction. Founder said "until limit hits keep building" — continuing autonomously into the next fully-untouched vertical per Section 9.1's own ordering (Manufacturing). Grounding via the audit's own winrow confirmed Manufacturing's 5 items pair up cleanly (downtime capture feeds the landed-cost report; per-stage rejection tracking feeds the rejection-rate-trend report; the bottleneck flag stands alone) and, unusually for this vertical, two of the five gaps were only PARTIAL — `WorkOrder` already had a binary QC pass/fail gate from Phase 58 §2, and `ProductionOrder` already had real persisted `laborCost`/`overheadCost` from Phase 64, so the real work was narrower than the audit's own framing suggested: downtime VISIBILITY specifically (not labor/overhead capture generally), and rejection QUANTITY specifically (not the checkpoint concept itself). All 5 items were built, tested, and live-verified together in one combined pass rather than one at a time — item 1 (machine/labour downtime capture) added a new itemized `WorkOrderDowntimeEntry` log plus a **+ Downtime** action per work-order step; item 3 (per-stage rejection tracking) added two new nullable fields directly onto the EXISTING `WorkOrder` model, captured in the SAME QC prompt a checkpoint already required, independent of the overall pass/fail result; item 2 (True Landed Cost per Finished Unit) deliberately backs material cost OUT of the already-persisted, historically-accurate `ProductCostHistory.unitCost` rather than recomputing it from today's raw-material prices, rendered as this codebase's now-second stacked-bar chart (reusing the pre-existing `ApAgingView` live pattern and the pre-existing `stackedBar` PDF chart type, neither needed inventing); item 4 (Rejection Rate Trend) reuses item 3's own new fields directly, trended monthly and broken down by stage; item 5 (work-order lead-time bottleneck flag) needed NO new capture at all, computed purely from the gap between consecutive `completedAt` timestamps already on every completed work-order step, surfaced proactively as a banner above the order list. Live-verified via a brand-new `tests/e2e/suites/70-manufacturing-quality-downtime.js` (first-ever E2E coverage of Manufacturing's own work-order-step-level workflows), **42/42, two consecutive clean runs**, after finding and fixing one real bug in the suite's own setup code (a `let` declared inside `try` instead of hoisted above it, invisible to the `finally` block that needed to restore `ai_assistant`'s pre-test state — the same class of scoping mistake, caught and fixed the same way, Footwear's own suite had already gotten right). **Manufacturing's entire 5-item signature-win set is now complete** — the 14th of Phase 67's 23 verticals to reach that, and the 8th of the 17 Product & Retail / Legacy Generic Service verticals. **None of Manufacturing's 5 items are committed to git yet**, awaiting explicit commit+push instruction. Founder said "continue" — continuing autonomously into the next fully-untouched vertical per Section 9.1's own ordering (Agri Inputs). All 5 of Agri Inputs' items have since been built, tested, and live-verified — item 1 (crop-season-aligned credit terms, a new `CropSeason` model plus server-side `dueDate` resolution from the season's own next harvest occurrence), item 3 (crop-linked product advisory, a new `Product.recommendedCrop` field and Billing's own Browse-by-Crop chips — along the way catching and closing a real gap where `recommendedCrop` existed on the schema but was nowhere actually settable), item 2 (Seasonal Credit Exposure report, a 12-month fixed-calendar-axis line chart), item 4 (Farmer-Wise Purchase & Repayment History report, a cross-farmer riskiest-first ranking distinct from the existing single-customer ledger), and item 5 (equipment AMC/service reminders, new `ProductSerial.nextServiceDueDate`/`lastServicedDate` fields and a new Equipment Service Due panel on the Agri Dashboard) — see §52 for full detail. **Agri Inputs' entire 5-item signature-win set is now complete** — the 15th of Phase 67's 23 verticals to reach that, and the 9th of the 17 Product & Retail / Legacy Generic Service verticals. **None of Agri Inputs' 5 items are committed to git yet**, awaiting explicit commit+push instruction. Continuing autonomously into the next fully-untouched vertical per Section 9.1's own ordering (Blood Bank). Grounding confirmed items 1 (donor cooldown auto-reminder) and 3 (camp/drive scheduling) as genuine gaps — item 2 (blood-group stock level report) turned out to already exist in full from Phase 54, the first vertical this phase where a named audit item was already completely built before Phase 67 touched it. All 4 real items were built, tested, and live-verified together in one combined pass — item 1 (new `listDonorsDueForRecall()` plus a **Recall Due** filter turning the Donor Registry into a real outreach worklist), item 3 (a brand-new `CampsScreen.tsx` plus a camp picker added to the pre-existing Record Donation form — the backend had existed since Phase 51 with zero renderer usage anywhere), item 4 (new `generateDonationToIssueCycleTimeReport()`, a genuine waste-risk indicator ranked by component type rather than blended into one number), and item 5 (new `fastMatchSearch()` plus a **Fast Match** panel on the Issue Units form, reusing the exact same ABO/Rh compatibility matrix `createBloodIssue()` itself already enforces) — see §53 for full detail. Live-verified via a brand-new `tests/e2e/suites/72-blood-bank-recall-camps-fastmatch.js`, **32/32, two consecutive clean runs**, plus the pre-existing base-chain suite 33 re-run clean (17/17) to confirm no regression, after finding and fixing one real bug in the E2E suite's own setup code (a wrong select-index assumption in the Donations form, not the app). **Blood Bank's entire 5-item signature-win set is now complete** — the 16th of Phase 67's 23 verticals to reach that, and the 10th of the 17 Product & Retail / Legacy Generic Service verticals. **None of Blood Bank's 4 touched items are committed to git yet**, awaiting explicit commit+push instruction. Continuing autonomously into the next fully-untouched vertical per Section 9.1's own ordering (Rental). A direct read of the current schema and UI — done before assuming any of the 5 audit items needed building, per this arc's own standing discipline — found items 1 (security-deposit ledger), 2 (damage-charge workflow), and 5 (service-interval maintenance scheduling) were ALL already fully built from Phase 58 §2, narrowing the real work to items 3 and 4, both reports. Item 3 (Asset Utilization Rate, per individual physical unit) is a genuinely new `generateAssetUtilizationReport()`, deliberately kept separate from the pre-existing per-product `generateRentalRevenueReport()`'s own averaged utilization figure — rented-days versus available-days per unit, worst-earning first. Item 4 (Overdue Returns aging breakdown) extends the pre-existing `generateRentalStatusReport()`'s own list with the missing aging-bucket half the audit's own item wording named, one bucket count per booking not per item row. Live-verified via a brand-new `tests/e2e/suites/73-rental-asset-utilization-overdue-aging.js` — the first-ever E2E suite to drive Rental Booking creation end to end — **30/30, two consecutive clean runs**, after finding and fixing one real bug in the app itself, not the test: `rental.status.RENTED` had never been added to any of the 13 locale files since Phase 58, so a rented unit's status badge had always shown the raw untranslated key instead of a real word, undetected until this item's own new report table rendered one live — fixed across all 13 locales alongside the report's own new keys. **Rental's entire 5-item signature-win set is now complete** — the 17th of Phase 67's 23 verticals to reach that, and the 11th of the 17 Product & Retail / Legacy Generic Service verticals. **None of Rental's 2 touched items are committed to git yet**, awaiting explicit commit+push instruction. Continuing autonomously into the next fully-untouched vertical per Section 9.1's own ordering (Jewellery). Grounding via the source audit artifact confirmed Jewellery's exact 5-item list; all 5 were built, tested, and live-verified together in one combined pass. Item 1 (Gold Savings chit scheme ledger) is a new `GoldSavingsScheme`/`GoldSavingsInstallment` pair deliberately mirroring `MetalExchange`'s own original standalone-record design rather than wiring atomically into `billing.service.ts` — a brand-new redemption concept doesn't need to take on that money-critical-path risk the way the already-proven metal-exchange path later did. Item 2 (Making-Charge vs. Metal-Value Margin) is a genuinely new per-SALE report, deliberately distinct from the pre-existing `generateJewelleryReport()`'s own single blended making-charge number. Item 3 (Hallmarking/HUID Compliance Register) surfaces the already-captured-but-never-surfaced `Product.hallmarkNumber` as a real audit worklist, non-compliant items ranked first. Item 4 (Metal Rate vs. Sales Volume) needed a real prerequisite fix first — `MetalRate` only ever held today's current rate with no history — so a new `MetalRateHistory` model now appends on every real rate change, the same "history instead of overwrite" fix `ProductCostHistory` already established; the report itself auto-selects whichever metal+purity combination actually sold the most in range, since the generic Reports screen has no mechanism for a manual picker beyond a date range. Item 5 (Purity-Adjusted Exchange Analytics) normalizes old-metal exchanges to a pure-metal-equivalent weight via a new karat/per-mille-parsing helper, both as a new aggregate report and as a small inline enhancement to the pre-existing `MetalExchangeScreen.tsx` itself — the "beyond a basic exchange log" feature half the audit's own item wording specifically named. Live-verified via a brand-new `tests/e2e/suites/74-jewellery-gold-savings-margin-hallmark-purity.js`, **45/45, two consecutive clean runs**, plus the pre-existing base-chain suite 10 re-run clean (55/55) to confirm no regression, after finding and fixing one real bug in the E2E suite's own cleanup code — the known "E2E Product Cleanup FK Gotcha" (LocationStock/InventoryMovement must be cleared before a real-stock Product can hard-delete) recurred here too, same fix Agri Inputs' own suite already established. **Jewellery's entire 5-item signature-win set is now complete** — the 18th of Phase 67's 23 verticals to reach that, and the 12th of the 17 Product & Retail / Legacy Generic Service verticals. **None of Jewellery's 5 items are committed to git yet**, awaiting explicit commit+push instruction. Continuing autonomously into the next fully-untouched vertical per Section 9.1's own ordering (Service, the first of this phase's 3-vertical Legacy Generic Service cluster). Grounding via the source audit artifact confirmed Service's exact 5-item list; all 5 were built, tested, and live-verified together in one combined pass. Item 1 (SLA timer) is a new `ServiceTicket.slaDueAt` set once at creation from the ticket's own priority, breach computed live never persisted, mirroring `RepairTicket.vendorSlaDueDate`'s own `isOverdue()` pattern exactly. Item 2 (Resolution Time by Category) is a genuinely new report — no prior report had ever touched `ServiceTicket` at all. Item 3 (recurring Service Contract / AMC ledger) is a new `ServiceContract` model deliberately mirroring `PestServiceContract`'s own proven period-keyed atomic-claim billing pattern rather than repurposing the unrelated `RecurringProfile` model. Item 4 (Repeat-Business Rate) fetches each customer's TRUE first-ever ticket date across all time, not just the report's own requested range, so "repeat" reflects real history rather than a same-week double-ticket. Item 5 (quote-to-job conversion tracking) is a single nullable unique `ServiceTicket.quotationId` FK plus a lightweight stats function, kept deliberately small since the audit tagged it a Feature, not a Report, with server-side guards preventing a quotation from ever converting into more than one ticket. Live-verified via a brand-new `tests/e2e/suites/75-service-sla-contract-conversion-repeat-business.js`, **43/43, two consecutive clean runs**, plus the pre-existing base-chain suite 02 re-run clean (15/15) to confirm no regression, after finding and fixing two real, generalizable E2E gotchas in the test's own code (not the app): the same epoch-ms-vs-ISO-string DateTime backdating trap already documented in suite 15 recurred here, and a new one — re-navigating to the SAME hash the page is already on doesn't remount the screen or refetch its data, so a step observing a mutation made outside that screen's own handlers needs to bounce through a different route first. **Service's entire 5-item signature-win set is now complete** — the 19th of Phase 67's 23 verticals to reach that, and the 13th of the 17 Product & Retail / Legacy Generic Service verticals. **None of Service's 5 items are committed to git yet**, awaiting explicit commit+push instruction. Continuing autonomously into the next fully-untouched vertical per Section 9.1's own ordering (Consultant, the second of this phase's 3-vertical Legacy Generic Service cluster). Grounding via the source audit artifact confirmed Consultant's exact 5-item list; all 5 were built, tested, and live-verified together in one combined pass. Item 1 (engagement-letter → project auto-conversion) is a new nullable unique `Project.quotationId` FK, the same shape as Service item 5's own `ServiceTicket.quotationId`. Item 2 (Utilization Rate) is a genuinely new report — no prior report had ever touched `WorkLog`'s own billable/non-billable split. Item 3 (Retainer Burn-Down Tracker) needed zero new code at all — the biggest pre-existing-surprise this phase has found yet: `RetainersScreen.tsx`/`TimeEntryScreen.tsx`/`retainer.service.ts`, including a real working hours-used/hours-remaining progress bar, were already fully built for `INDEPENDENT_CONSULTANT`/`MARKETING_AGENCY`/`SOFTWARE_AGENCY` — the plain legacy `CONSULTANT` type simply never had the `retainers`/`time_entries` modules turned on, so this item closed with a two-line module-list change reusing that infrastructure verbatim. Item 4 (Client Profitability) is a new report ranking clients ascending by revenue-per-hour, worst-first, rendered as a dual-yAxisId bar chart since revenue and hours differ in scale. Item 5 (proposal win-rate tracking) is a lightweight new stats function surfaced directly in the Projects screen's own header, the same deliberately-small-Feature pattern Service item 5 already established. Live-verified via a brand-new `tests/e2e/suites/76-consultant-engagement-utilization-retainer-profitability.js`, **39/39, two consecutive clean runs**, after finding and fixing two real gotchas: a test-only assertion mistake (asserting a pre-tax amount instead of the real GST-inclusive invoice total), and a genuine recurrence of this project's own known electron-vite main-process-watcher-goes-stale class of issue (previously hit during Footwear item 4) — a live-edited fast-path regex was confirmed correct in source but never reached the running compiled bundle until a full dev-server restart, caught only by inspecting the compiled `out/main/index.js` directly rather than trusting the watcher's own success log. **Consultant's entire 5-item signature-win set is now complete** — the 20th of Phase 67's 23 verticals to reach that, and the 14th of the 17 Product & Retail / Legacy Generic Service verticals. **None of Consultant's 5 items are committed to git yet**, awaiting explicit commit+push instruction. Continuing autonomously into the last fully-untouched vertical per Section 9.1's own ordering (Repair, the final member of this phase's 3-vertical Legacy Generic Service cluster).
