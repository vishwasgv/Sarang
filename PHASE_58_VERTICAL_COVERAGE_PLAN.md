# Phase 58 — Full Vertical Coverage Plan (target: 10/10 on all 43 business types)

Source: the 2026-07-16 fresh audit (8 parallel research passes across every business-type template, cross-checked against real code, not the manuals). Full findings: `tests/e2e/shots/` screenshots aside, the canonical writeup is the published artifact from that audit session. This file is the execution tracker — check items off as they land, with real UAT/unit-test evidence, not just "implemented."

**Working rule for this whole phase, per established project convention**: every item below needs (a) real code, (b) a unit test where the logic is non-trivial, (c) a live UAT pass (dev app, driven, not just typecheck), before being checked off. No item gets checked from claiming completion alone.

---

## 0. Real bugs (fix first — these are defects in shipped code, not gaps)

- [x] **LAN QR codes could encode an unreachable VirtualBox/virtual-adapter IP instead of the real WiFi LAN address** — found from a real user screenshot 2026-07-16. Fixed in `qr-order-server.ts` and `kitchen-display-server.ts`: `getLanIPv4Addresses()` now sorts real network adapters before virtual ones (VirtualBox/VMware/Hyper-V/WSL/Docker/Tailscale/etc. name patterns). Needs: live UAT re-scan confirmation on the reporting user's machine.
- [x] **Returns don't restore per-variant (size/colour) stock** — Fixed 2026-07-16: `variant.service.ts` gained `restoreVariantStockTx` (mirror of the existing `decrementVariantStockTx`); `returns.service.ts` now matches return lines to the exact original `(productId, variantId)` sold (via a `sameLine()` helper that normalizes Prisma's `null` vs. plain-object `undefined` — caught a real bug in my own first pass via the unit suite before it shipped), restores that specific variant's stock, and carries `variantId`/`variantInfo` onto the created return line. `operations.validation.ts`'s `CreateReturnSchema` and `ReturnScreen.tsx` (search, qty-adjust buttons, submit payload, row display) now thread `variantId` end-to-end. Unit: `returns.service.test.ts` +5 tests (16/16 passing). Live UAT: extended `tests/e2e/suites/16-clothing-variants.js` with a real return-of-one-variant flow — 26/26 passing against the live dev app, confirming the specific variant's `stockQty` is restored while the sibling variant is untouched and the aggregate `Inventory.quantity` is correct. (Also fixed a real harness gotcha found along the way: `h.login()` didn't dismiss the first-run "Choose a Backup Folder" prompt, which silently blocked every suite when the dev DB lacked the `backup_prompt_dismissed` Setting row — added `dismissBackupPrompt()` to `harness.js`.)
- [x] **"Projects" report queries the wrong Prisma model** — Fixed 2026-07-16/17: split into two reports. `generateProjectReport()` now genuinely queries the legacy `Project` model (SERVICE/CONSULTANT, gated on `projects` module); new `generateServiceProjectReport()` covers the original `ServiceProject`-backed logic (Independent Consultant/Architect/Civil Engineer/Marketing Agency/Software Agency/Real Estate, gated on the new `serviceProjects` tile → `service_projects` module). Threaded through `channels.ts`, `reports.handler.ts`, `preload/index.ts`, `ReportsScreen.tsx` (interfaces, tile defs, dispatch, CSV export, summary cards, charts, view components), and `en.json`. `ai-vertical-templates.service.ts`'s `service.projects` AI capability repointed at `generateServiceProjectReport` (no behavior change — it was already ServiceProject-shaped; SERVICE/CONSULTANT's own AI gap here is a separate, already-tracked §1 item). Unit: `report.service.test.ts` split into two describe blocks, +3 new tests for the fixed legacy-model report (112/112 passing in that file). Live UAT: ad-hoc script drove the real dev app — switched to SERVICE via the real Settings UI, created a `Project`, confirmed the "Project Report" tile now shows real (previously always-empty) data with correct amount/client; switched to ARCHITECT, created a `ServiceProject`, confirmed the tile (previously never shown at all for this vertical) is now visible and shows correct contract-value data — 22/22 passing. (Found and fixed 2 real bugs in the UAT script itself along the way, not the product: raw-IPC business-type switches leave the renderer's industry store stale — documented harness Gotcha 4, fixed by using `h.switchBusinessType()`; and a `toISOString()`-based date-range computation crossing a local-midnight/UTC boundary — same latent pattern also present in the *product's* own `today()`/`monthStart()` report-screen defaults, flagged as a separate narrow pre-existing issue, not in scope for this fix.) Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1162/1162.

---

## 1. Cross-cutting systemic fixes (each one closes a gap on multiple verticals at once — do these before per-vertical work)

- [x] **Extend document attachment (`DocumentEntityType`) to the entities missing it** — Done 2026-07-17: added `LEGAL_CASE`, `COMPLIANCE_TASK`, `ENGAGEMENT`, `ROC_FILING`, `BOARD_MEETING`, `VISIT_NOTE`, `TREATMENT_PLAN`, `LAB_TEST_ORDER` to the entity-type union (`document.service.ts`, `document.validation.ts`, `DocumentPanel.tsx`) and mounted `<DocumentPanel compact />` in each screen's real edit/detail modal: `LegalCasesScreen.tsx` (case detail card), `ComplianceScreen.tsx` (Update Status modal), `EngagementsScreen.tsx` (edit form, existing engagements only), `ROCFilingsScreen.tsx` (filing edit form + board resolution modal), `VisitNoteScreen.tsx` (new Documents section), `DentalPatientScreen.tsx` (Treatment Plan modal — used `TreatmentPlan` not `ToothRecord` as the attach point: a scanned X-ray/consent form naturally belongs to a whole plan, not one tooth number; `ToothRecord`-level attachment deliberately skipped as excess granularity), `LabOrdersScreen.tsx` (order detail modal). No unit test needed (pure enum extension + UI wiring onto an already-proven generic component, no new branching logic — same class of change as the original `DRAWING_REVISION`/`SITE_VISIT` precedent). Live UAT: ad-hoc script drove all 6 affected business types (Lawyer/CA Firm/Company Secretary/GP/Dental/Diagnostic Lab) via the real UI's business-type switch, created a real record of each type, attached+listed-back+deleted a file via the same IPC path the real "Attach file" button uses (native OS file dialogs can't be Playwright-driven — same technique as the existing `DRAWING_REVISION` UAT in `10-new-features.js`) — 40/40 passing after a stale-dev-server restart (electron-vite's watcher didn't pick up the validation-schema change automatically; documented gotcha, not a product bug). Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1162/1162.
- [x] **Wire the two confirmed dead-backend features into UI** — Done 2026-07-17: `MembershipsScreen.tsx` gained an "Expiring Soon" tab (configurable 7/14/30/60-day window, calls the previously-dead `getExpiringMemberships()`) and a per-row "Attendance history" button opening a modal (calls the previously-dead `getMembershipAttendance()`). `PropertiesScreen.tsx` gained a photo gallery in each property's expanded row — reuses the existing generic `dialog:openFile` picker + `file://<path>` preview pattern already proven on `ProductFormModal.tsx`'s product-image field (no new IPC needed, `property:update` already accepted a `photos` array), plus thumbnails in the print listing sheet. No unit test needed (thin UI wiring onto already-correct, already-tested backend functions — no new business logic). Live UAT: ad-hoc script drove both real screens — created a membership expiring within 30 days, confirmed it appears on the real "Expiring Soon" tab; checked in a member, confirmed the real attendance-history modal shows the check-in; attached a real photo file to a property via the same IPC path the real UI's picker uses, confirmed the real Properties screen renders an `<img>` with the correct `file://` src, and that the real "Remove" button correctly clears it — 21/21 passing. Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1162/1162.
- [x] **Add dedicated Reports-screen entries** — Done 2026-07-17: 10 new report tiles, one per data model (`generateCarJobCardReport`, `generateTailoringOrderReport`, `generatePestContractReport`, `generateRealEstatePipelineReport`, `generateRetainerReport`, `generateShootBookingReport`, `generateEventBookingReport`, `generatePlacementReport`, `generateDrawingRegisterReport`, `generateSiteVisitLogReport` — all in `report.service.ts`), each wired end-to-end (IPC handler + channel + preload + `ReportsScreen.tsx` interfaces/tile/dispatch/CSV-export/summary-cards/chart/view-component) exactly like every existing report. Retainer report covers all three of Independent Consultant/Marketing Agency/Software Agency (they share the `retainers` module, same pattern as `appointmentUtilisation` covering multiple appointment-based verticals). Notable modeling calls: Car Service technician productivity fans out the JSON `technicianIds` array per job (a job with 2 techs counts once per tech); Pest Control's "revenue by pest type" attributes a job sheet's full amount to *every* pest type listed on its parent contract (job sheets don't carry pest type directly, no existing convention splits it); Retainer "collected this period" is a proxy via `lastInvoicedPeriod` matching the report's target month, since no `RetainerInvoice` FK exists to check real payment. Unit: 13 new tests in `report.service.test.ts` covering exactly this non-trivial logic (technician fan-out, unparsable-JSON fallback, expiry-window boundary, pest-type multi-attribution, REGISTERED-only brokerage, period-matching, null-amount handling) — 124/124 passing in that file. Live UAT: ad-hoc script drove all 10 target business types via the real UI, created one real record per vertical, confirmed both the API-level report and the real Reports-screen tile (previously nonexistent for all 10) render the real data — 61/61 passing. Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1174/1174.
- [x] **Give the legacy Service/Consultant/Repair family an invoicing bridge** — Done 2026-07-17: added `invoiceId String?` to the `Project`/`ServiceTicket`/`JobCard` Prisma models (none had one — real schema gap, required a migration: `20260716193131_legacy_service_invoicing_bridge`; hit and resolved a real Prisma-migration-engine quirk along the way, see note below) plus `generateProjectInvoice()`/`generateTicketInvoice()`/`generateJobCardInvoice()` matching the pattern already proven on CarJobCard/Placement/etc. (lookup-or-create a generic SAC-coded service product, single-line invoice via `billingService.createInvoice`). Notable: `ServiceTicket` has no stored monetary field at all (nor does its `WorkLog` — hours only, no rate), so `generateTicketInvoice` takes an explicit caller-supplied amount rather than deriving one; `JobCard` bills `actualCost` once recorded, falling back to `estimatedCost` before that. Wired end-to-end (IPC handlers/channels/preload, `invoiceId` added to each service's `toRecord()`) with a real "Generate Invoice" button in `ProjectsScreen.tsx`/`JobCardsScreen.tsx`'s detail modals and an amount-entry-plus-button in `ServiceTicketsScreen.tsx`'s. Unit: 3 new test files (`project.service.test.ts`, `job-card.service.test.ts`, `service-ticket.service.test.ts`, 21 tests total) covering the real guard logic (no customer, already invoiced, zero amount, actualCost-vs-estimatedCost fallback, product reuse-vs-create) — caught and fixed a real TS inference bug in my own code along the way (an unannotated `return result` forwarding call site made the failure-branch return type balloon into an undiscriminated union; fixed by reconstructing `{ success: false, error }` explicitly instead of forwarding the whole object). Live UAT: ad-hoc script drove all three real screens end-to-end (SERVICE vertical's Projects + Tickets, REPAIR vertical's Job Cards) — created real records, generated real invoices via the actual UI buttons, confirmed a real `Invoice` row exists with the correct GST-inclusive total, confirmed duplicate-generation is correctly rejected — 24/24 passing. Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1190/1190.
  - **Migration-engine quirk hit and resolved**: `prisma migrate dev` failed twice on this schema change — first because it bundled in unrelated pre-existing schema drift (6 SQLite indexes with `sqlite_autoindex_*` names instead of Prisma's expected explicit names — a known, already-flagged, cosmetic-only drift dating back to before this session, confirmed via `prisma migrate diff` to be the *only* remaining drift and unrelated to this change), then a second time with a spurious "duplicate column" error despite the column provably not existing yet (confirmed via direct `PRAGMA table_info` before *and* after the failed attempt). Resolved by applying the 3 `ALTER TABLE ADD COLUMN` statements directly via raw SQL against the dev DB, then `prisma migrate resolve --applied` to reconcile Prisma's migration ledger with reality, then `prisma generate`. Verified clean afterward via `prisma migrate status` (up to date) and `prisma migrate diff` (zero drift attributable to this change — the same 6 pre-existing cosmetic index-naming mismatches remain, unrelated and out of scope).
- [x] **Give Service/Consultant/Repair appointment scheduling** — Done 2026-07-17: spread `SERVICE_BASE_MODULES` (`appointments`/`service_catalog`/`provider_schedule`/`notification_queue`) onto `SERVICE`/`CONSULTANT`/`REPAIR` in `TEMPLATE_DEFAULTS`, matching every newer service vertical. Pure config change onto already-proven generic infrastructure (same Appointments system every clinic/salon/studio vertical already shares) — no new code, no unit test needed. Live UAT: real UI drove all three verticals — confirmed the Appointments nav item is now visible (previously absent), created a real appointment via the module-gated IPC channel, confirmed the real Appointments screen renders it without crashing — 16/16 passing across all three business types.
- [x] **Enable `time_entries` (hourly billing) for Software Agency** — Done 2026-07-17: added to `SOFTWARE_AGENCY`'s `TEMPLATE_DEFAULTS`, and added a "Billable Time Tracking" toggle to `BusinessFeaturesSection` in Settings so any business type can self-enable it. Added `TimeEntry.retainerId` FK (schema migration — same raw-SQL-apply-plus-`migrate resolve --applied` workaround as bug #4's migration; this one specifically needed a full SQLite table-redefine to get a real FK CONSTRAINT clause, not just a bare `ADD COLUMN`, since SQLite can't add an FK constraint via plain `ALTER TABLE` — ADD COLUMN alone would have left the relation present in Prisma's schema but silently unenforced at the DB level) and `RetainerAgreement.getRetainerHoursUsage()` — deliberately a derived sum over `TimeEntry.hours` for the target period, not a mutable running counter on the retainer itself (avoids the "did the monthly-reset job run" class of bug, consistent with how this codebase's other usage/expiry reports already work). Wired into `TimeEntryScreen.tsx` (retainer picker when logging hours) and `RetainersScreen.tsx` (a Hours-used progress bar per `HOURLY_BUCKET` retainer, computed on-demand). Unit: 4 new tests in `retainer.service.test.ts` (period-boundary correctness — including a real local-vs-UTC date bug caught in my own test the same way as the earlier reports date-boundary bug — floor-at-zero, null-hours-bucket case, default-to-current-month). Live UAT: real UI — confirmed Time Tracking nav is on by default for Software Agency and absent for a non-default vertical (Blood Bank) until self-enabled via the real Settings toggle; logged 4 real hours against a real HOURLY_BUCKET retainer through the real Log Hours form, confirmed the Retainers screen's real progress bar correctly showed "4.0h / 10h" — 17/17 passing. (Caught and reverted one real side effect from the UAT script itself: it left `time_entries` toggled on for Blood Bank in the dev DB after the toggle test — fixed by hand before moving on, not a product bug.) Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1195/1195.

**§1 is now fully complete (all 6 items).**
- [x] **Narrow latent date-boundary bug in ReportsScreen.tsx's default range** — Fixed 2026-07-18: `today()`/`monthStart()` now build their date strings from the `Date` object's own local `getFullYear()/getMonth()/getDate()` via a new `localDateString()` helper, instead of `.toISOString().slice(...)` (UTC-based, which silently returned "yesterday" as the default `dateTo` in a timezone ahead of UTC within the first ~5.5 hours after local midnight — exactly the window this was found in live). Caught and confirmed as a REAL live bug, not theoretical: while live-UAT-ing the new Pharmacy prescription report (see below) at 00:38 local time (IST, UTC+5:30, UTC still on the previous calendar day), the report's default view showed 0 sales despite a real sale existing from moments earlier — same session, same day. Fixing this one shared helper immediately fixed the Pharmacy report's live UAT with zero other changes needed, confirming the report backend itself was already correct.

---

## 2. Per-vertical work

### Hospitality
- **Restaurant** (8→10) — split into sub-items 2026-07-17 for accurate tracking (research found the actual Table→KOT→Invoice flow is Invoice-first/KOT-attached-after with no staff table-picker UI at all, not the assumed table-first flow — split-bill and reservations are each their own real subsystem, not incremental additions):
  - [x] Tip / service-charge line on invoices (no ad-hoc-line precedent anywhere in the app — plan is a lookup-or-create generic non-taxed "Service Charge" `Product`, same `findOrCreateServiceProduct` pattern as time-entry.service.ts, not a schema change to `Invoice`/`InvoiceItem`) — `billingService.getOrCreateTipProduct()`, "+ Add Tip / Service Charge" button + modal on BillingScreen; unit tests (create + reuse) + live UAT confirmed a real invoice with both a ₹50 tip line and a product line, 2026-07-17
  - [x] Waiter/staff assignment per table (`RestaurantTable.waiterId` → `Employee`, mirroring `TailoringOrder.assignedToId`/`PestServiceContract.assignedToId`) — `restaurantService.assignWaiter()` + waiter `<select>` on each table card in RestaurantTablesScreen; unit tests (assign + clear-to-null) + live UAT confirmed assignment and clearing both persist, 2026-07-17
  - [x] Per-item "86 today" toggle independent of deactivating the product (`Product` has no time-dimension field at all today — needs a new field) — `Product.unavailableUntil` self-expiring field (RedefineTables migration), `productService.setProductAvailability()`, 86 toggle button on ProductsScreen, blocks cart-add on BillingScreen, excluded from QR customer menu via `listMenuProducts()` filter; unit tests + live UAT confirmed toggle, cart-block, DB state, and restore, 2026-07-17
  - [ ] Table-merge for large parties — DEFERRED alongside split-bill below, same root cause (no real table→bill linkage in the manual billing flow to merge onto)
  - [ ] **Real split-bill** (divide one table's KOT items across N separate checks) — DEFERRED, needs its own design pass: `KOT.invoiceId` is a strict 1:1 `@unique` FK today, so this is a real schema/flow redesign (splitting `InvoiceItem` rows across multiple new invoices with a claim-based guard, mirroring `generateTimeEntryInvoice`'s atomic-claim pattern), not a quick add
  - [ ] **Reservations with date/time/party-size/phone** — DEFERRED, needs its own design pass: today "RESERVED" is a bare status string on `RestaurantTable` with zero persisted metadata (no time, no party size, no customer) — a real reservation feature is a new `Reservation` model + screen, not an extension of the existing flag
- **Hotel/Lodge** (7→10) — ALL 6 DONE 2026-07-17 (real code + 18 new unit tests + 24/24 live UAT via the real app):
  - [x] Seasonal/date-range rate calendar — new `HotelRateCalendar` model (blank roomType = blanket tier, specific roomType overrides), `resolveNightlyRate`/`resolveCalendarRoomChargeTotal` consulted per-night (not once per booking) so a stay spanning a season boundary prices correctly; new "Manage Seasonal Rates" modal on Rooms screen. Day-of-week granularity explicitly dropped from scope (date-range only) — noted as a judgment call given 6 items in this vertical alone.
  - [x] Group/multi-room bookings with one combined invoice — no new "group booking" entity; N bookings already share one `Customer`, new `generateGroupHotelInvoice` atomically claims all N booking ids (all-or-nothing, rolls back cleanly on any partial failure) and makes one `billingService.createInvoice` call; multi-select checkboxes + "Generate Combined Bill" button on the Bookings screen (only for checked-out, uninvoiced, same-customer rows).
  - [x] OTA/channel tagging — `HotelBooking.channel` free-text-with-comment field (same convention as `Lead.source`), Booking Source selector in New Booking, channel column + badge in the bookings table/detail.
  - [x] Returning-guest lookup — reused the existing `HotelBooking.customerId → Customer` FK (already present, just unused) + existing `searchCustomers` phone search via `CustomerPicker`; new `getCustomerStayHistory` shows "N previous stays" inline in New Booking when a returning customer is picked.
  - [x] Real housekeeping checklist/assignment — new `HotelHousekeepingTask` model (mirrors `ProjectTask`'s shape), auto-queued on every checkout, assignable to an `Employee`, new dedicated Housekeeping screen; completing a room's last open task auto-flips it from CLEANING back to AVAILABLE, replacing the previously-unconditional manual status dropdown with a real "housekeeping actually finished" signal.
  - [x] Day-use/half-day rate — `HotelBooking.bookingType` (OVERNIGHT/DAY_USE) + `HotelRoom.dayUseRate` (falls back to half of baseRate); a DAY_USE booking is forced server-side to a full checkIn+1-day hold (deliberately not time-of-day-aware, avoiding a zero-width-interval double-booking bug in the existing conflict check) — documented as a conservative scope decision, same as a real front desk blocking the whole day for housekeeping turnover.
- **Rental** (7→10) — ALL 4 DONE 2026-07-17 (real code + 13 new unit tests + 23/23 live UAT via the real app; full 13-locale i18n added since Rental is languageLock:'multi', unlike Restaurant/Hotel):
  - [x] Maintenance/servicing schedule tied to rental count or elapsed time, blocking re-rental until cleared — new `RentalUnit.serviceIntervalRentals`/`serviceIntervalDays`/`rentalCountSinceService`/`lastServicedAt` fields; `returnBooking()` routes a due unit to MAINTENANCE instead of AVAILABLE (blocking re-rental via the existing `status==='AVAILABLE'` availability filter, zero changes needed there); new `markUnitServiced()` + "Mark Serviced" action + "Due for Service" badge on Rental Units screen.
  - [x] Photo attachment on condition-out/condition-in — reused the existing generic `Document`/`entityType`+`entityId` attachment system (added `'RENTAL_BOOKING_ITEM'` as a new entity type, keyed to `RentalBookingItem.id` for correct per-unit attribution on a multi-unit booking) via the already-built `DocumentPanel` component — no new attachment infrastructure; `checkoutBooking()` also gained a per-item `conditionOut` capture (previously only `conditionIn` at return was ever written, despite the schema field existing on both sides).
  - [x] Recurring/standing rental support — new `RentalBooking.recurrenceIntervalDays`/`parentBookingId`/`nextCycleGenerated` fields; a staff-triggered "Create Next Cycle" action (atomic claim-flag, same pattern as `ServiceRetainerAgreement`'s `lastInvoicedPeriod`) rather than a background scheduler, since no cron/timer infra for business logic exists anywhere in this codebase — deliberate consistency choice, not a shortcut.
  - [x] Itemized damage charge per unit on multi-unit bookings — `RentalBookingItem.damageChargeAmount` (booking-level aggregate now derived as the sum when any item has one set, falls back to the legacy whole-booking field for BULK items/old bookings); `generateRentalInvoice` itemizes one damage line per damaged unit instead of one aggregate line; `NewBookingModal` rebuilt for real multi-item booking creation (previously silently capped at 1 item despite the schema/service already supporting N).

### Retail & Product
- **Retail / General** (6→10) — ALL 3 DONE 2026-07-17 (real code + 13 new unit tests + 16/16 live UAT via the real app; full 13-locale i18n, since Billing is a shared screen used by every vertical including languageLock:'multi' ones — also fixed a pre-existing hardcoded-English gap this session's own earlier Restaurant tip-line/86-today work left in this same shared file):
  - [x] Hold/park-sale — new lightweight `HeldSale` model (single JSON-blob cart snapshot, deliberately NOT built on `Quotation`: reusing it would burn a real customer-facing QT-xxxxx sequence number per hold, clutter the Quotations screen/reports, and can't round-trip variant/serial/batch/jewellery cart detail or currency-amount per-line discounts) — "Hold Sale"/"Resume Sale" buttons + a resume-list modal on BillingScreen; resuming a held sale deletes its row (consumed, not re-listed).
  - [x] MRP field distinct from selling price — new nullable `Product.mrp` field, added to the Add/Edit Product form and shown as a strikethrough next to the selling price in the Billing search dropdown when set and higher than sellingPrice.
  - [x] Fast favorites/frequently-sold grid — new `billingService.getFrequentlySoldProducts()` (ranked by units sold across non-`RETURN` invoices, returns full `Product` records so a tile tap calls the existing `addToCart()` directly) rendered as a tappable tile grid above the product search box.
- **Hardware** (7→10) — ALL 3 DONE 2026-07-17 (real code + 8 new unit tests + 15/15 live UAT via the real app; full 13-locale i18n; also fixed a pre-existing hardcoded-English gap in BillingScreen's own area-pricing calculator, found while building the Quotations equivalent):
  - [x] Carton/box-to-loose-piece unit conversion — new opt-in `pack_billing` module flag (same cross-cutting, off-by-default convention as `loose_billing`) + `Product.sellByPack`/`packUnit`/`unitsPerPack`; `Inventory.quantity` never changes meaning (always the base/piece unit) — this is a stock-RECEIVING convenience only: Stock Adjustment gained a "packs received" entry mode that computes the equivalent absolute quantity, not a new inventory concept requiring changes to any existing stock-quantity read site (billing checks, low-stock alerts, valuation).
  - [x] Damage/breakage write-off as its own adjustment reason — `adjustStock` gained an optional `reasonCategory`; a DAMAGE-categorized decrease now records `movementType: 'DAMAGE'` instead of the generic `'ADJUSTMENT'` bucket (never for an increase — you can't damage stock into existence); Stock Adjustment UI gained a Reason Category dropdown; Inventory Movements screen and the AI query `inventory.biggestStockAdjustment` both updated to recognize the new type (the latter explicitly includes DAMAGE so it doesn't silently under-report).
  - [x] Area-pricing calculator in Quotations — the exact same L×W-sets-quantity calculator BillingScreen already had, rebuilt in `QuotationFormScreen.tsx` gated by the same `area_pricing` module flag (module flag gates the UI, not product type — `AREA_BASED` `productType` is an unrelated, decoupled enum value).
- [x] **Distributor** (5→10) — ALL 3 DONE 2026-07-18 (real code + 3 new test files/~35 new unit tests + 36/36 live UAT via the real app; full 13-locale i18n for the new Stops UI + Field Orders + Customer Pricing screens, since Distributor is languageLock:'multi'):
  - [x] Customer-class/negotiated price lists — new `Customer.customerClass` (free text) + `CustomerClassPrice` model (unique per productId+customerClass); new `product.service.ts` `resolveCustomerPrice(productId, customerId)` helper, resolved fresh server-side (never client-trusted) everywhere a price needs to reflect the customer's negotiated rate — wired into `BulkOrderScreen.tsx` (add-to-cart + re-price-on-customer-change) and the field-order accept flow below; new `CustomerPricingScreen.tsx` for CRUD; `customerClass` field added to `CustomerFormModal.tsx` (English-only, matching that file's own pre-existing zero-i18n convention — a systemic gap predating this session, not retrofitted). Deliberately NOT retrofitted into BillingScreen/QuotationFormScreen (generic shared cart-first screens used by all 43 verticals) in this pass — BulkOrderScreen is Distributor's own customer-first primary order screen, the natural integration point.
  - [x] Route/beat planning (multi-stop shipments) — new `ShipmentStop` model (additive beat points on top of the shipment's own existing primary `destinationAddress`, which stays the de-facto first stop for every pre-existing single-destination shipment) + `ShipmentItem.stopId` (nullable, optional per-line stop attribution); `logistics-shipment.service.ts` gained `addShipmentStop`/`updateShipmentStopStatus`/`deleteShipmentStop` — deliberately never touches `Vehicle.status`/inventory itself (a vehicle stays "in transit" for the whole run, matching `updateShipmentStatus`'s existing behavior); new Stops section in `ShipmentsScreen.tsx`'s edit modal (only reachable once a real shipment exists, since a stop needs a real `shipmentId`).
  - [x] Field-rep order capture — structural mirror of Phase 47's QR table ordering: new `FieldOrderRequest`/`FieldOrderRequestItem` models (mirroring `TableOrderRequest`/`TableOrderRequestItem`'s shape exactly), new `field-order.service.ts` (`createFieldOrderRequest`/`listFieldOrderRequests`/`acceptFieldOrderRequest`/`rejectFieldOrderRequest`) capturing only `productId`+`quantity` from the rep (never a price), re-resolving price fresh via `resolveCustomerPrice` at accept time, and calling the same `billingService.createInvoice` every other flow uses — the one credit-limit check happens exactly once, inside that call, never duplicated. New `field-order-server.ts` LAN HTTP server (port 8422, structurally cloned from `kitchen-display-server.ts`: per-LAN-IPv4+loopback binding, rate-limited, Origin-checked, per-install random token) + static `resources/field-order/index.html` capture page; new `FieldOrdersScreen.tsx` office-side accept/reject inbox + LAN link/QR display; new `field_order_capture` `TemplateModule` flag, shipped as a DISTRIBUTOR **default** (not opt-in, unlike `pack_billing`/`loose_billing`) since it's the core ask for this vertical; new `distributor.manageFieldOrders` permission (Manager/Cashier, mirroring `restaurant.manageOrderRequests`'s trust level).
  - Live UAT proved, against the real running app end-to-end: a negotiated WHOLESALER price (₹75) correctly overrides list price (₹100) in both the BulkOrderScreen cart and the real created invoice; a customer with no class price on file still bills at list price; two shipment stops persist with correct sequence numbers and independent status (one DELIVERED, one still PENDING); a field order submitted over the real LAN server (loopback, real token) appears in the office inbox and — critically — bills at the *current* negotiated price at accept time (not whatever the rep's app estimated), while a second line with no class price on file correctly falls back to list price on the same invoice; reject flow closes a request with no invoice created; wrong-token and cross-origin LAN requests are both correctly rejected (403). Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1292/1292.
- [x] **Clothing / Footwear** (5→10) — ALL 3 DONE 2026-07-18 (returns/variant-stock bug already fixed in §0 bug #2; the other 2 items done this pass with real code + 7 new unit tests + 18/18 live UAT via the real app):
  - [x] Returns/variant-stock bug — already fixed, see §0 bug #2 above (not redone here).
  - [x] Bulk/matrix variant generation — new "Generate Size × Colour Matrix" section in `VariantManagementModal.tsx`: two comma-separated chip-style inputs (Sizes, Colours) compute the cartesian product client-side and append new rows (skipping any (size,color) pair already present, capped at 300 combos to avoid a pathological paste), composing with manually-added rows rather than replacing them — no backend change needed, `variant.service.ts`'s existing `upsertVariants` already accepted an array.
  - [x] Variant-aware barcode label printing — real gap confirmed: `ProductVariant.barcode`/`sku` already existed in the schema but were manual-only free-text fields with no generator, and the entire label-print pipeline (`PrintLabelsScreen.tsx` → `PrintLabelsSchema` → `buildLabelHtml` in `billing.handler.ts`) only ever touched `Product`, meaning a variant-tracked item could only ever print its ambiguous parent-product barcode. Fixed: new `ProductVariant.barcodeSource` field (migration) + generalized `barcode.service.ts`'s `maxPlainItemCode`/`generateUniquePlainBarcode` to share ONE counter/collision space across `Product` AND `ProductVariant` (a real latent cross-table collision risk that would otherwise have been introduced — two independently-unique-constrained columns in different tables, nothing previously stopped the same 13-digit code being assigned to an unrelated product and a variant) + new `generateVariantBarcode`/`bulkGenerateMissingVariantBarcodes`; `PrintLabelsSchema.items[].variantId` (optional) + `buildLabelHtml` branches to resolve a variant line's own barcode/price (`product.sellingPrice + variant.additionalPrice`) and a `"ProductName (Size / Colour)"` label — deliberately does NOT track a variant-level reprint-price-staleness warning (no `ProductVariant.lastLabelPrintedAt/Price` field, out of scope for this pass, `Product`'s own reprint-warning is untouched). `PrintLabelsScreen.tsx` gained a variant picker (shown automatically when an added product has variants, with a "print the whole product's own barcode instead" escape hatch and an inline "generate missing barcodes for these variants" bulk action); `VariantManagementModal.tsx` gained a per-row barcode-generate icon button (and a Barcode input column that plainly didn't exist in the UI before, despite the schema already supporting it).
  - Live UAT proved, against the real running app: a real "S, M, L" × "Black, White" matrix generated exactly 6 correct variant rows via the real modal, saved and persisted with the right size/colour combinations; generating one variant's barcode via the real per-row button persisted a valid EAN-13 (`20`-prefix) code immediately (before the redundant Save click, proving generation itself is real, not deferred); adding that variant-tracked product to Print Labels opened the real variant picker instead of a plain product line; the "generate missing barcodes" bulk action filled in the remaining 5 variants' barcodes, all 6 confirmed distinct; a real `print:previewLabels` IPC call scoped to one specific variant succeeded and returned real HTML; and — the critical cross-table collision proof — generating a barcode for the PARENT PRODUCT itself afterward produced a 7th code that was still distinct from all 6 variant codes, confirming the shared counter/collision space works as designed. Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1299/1299.

### Specialized Product
- [x] **Pharmacy** (5→10) — ALL 3 DONE 2026-07-18 (real code + 16 new unit tests + 25/25 live UAT via the real app; also found+fixed a real pre-existing date-boundary bug along the way, see §1 above):
  - [x] Schedule H/H1 prescription-drug sale record-keeping — new `Product.isPrescriptionRequired` flag (checkbox in `ProductFormModal.tsx`, English-only — that file has zero pre-existing i18n, matching its own convention); `InvoiceItem` gained `prescriptionPatientName`/`prescriptionDoctorName`/`prescriptionDate` snapshot fields (same "capture at sale time, discarded otherwise" pattern the jewellery per-line fields already established); `billingService.createInvoice` server-side REQUIRES both names present for a flagged product (`RX-001`, never trusts the UI alone) — `BillingScreen.tsx` gained a prescription-capture modal (real i18n, `billing.rx.*`, all 13 locales — this file already has full i18n so new text had to match); new `generatePrescriptionDrugSalesReport` register/report (Phase 58 §1's report pattern, gated by a new `requiredBusinessType` field on `ReportDef` since no module flag fits — `isPrescriptionRequired` is a plain Product field, not a `TemplateModule`).
  - [x] Drug-license number on the business profile — new `BusinessProfile.drugLicenseNumber`, shown only for PHARMACY in Settings → Business Profile (same conditional-field pattern as `clinicSpecialty` for SPECIALIST_CLINIC).
  - [x] Auto-reorder triggered from expiry/low-stock alerts — deliberately built GENERIC (not Pharmacy-specific), since `Inventory.reorderLevel`/`reorderQuantity` already existed for every vertical but were badge-only with zero automation behind them: new `Product.defaultSupplierId` (+ Default Supplier picker in `ProductFormModal.tsx`, next to Reorder Level/Quantity) and `purchaseOrderService.generateReorderDraftPOs()` — manually triggered (a "Generate Reorder POs" button on the Inventory screen's low-stock alert bar), drafts one DRAFT-status PO per supplier grouping every due product that has a default supplier set, skips a product already on an open (DRAFT/APPROVED) PO so repeated runs never duplicate orders, and skips (with a reported count) any low-stock product that has no default supplier configured — never guesses.
  - Live UAT proved, against the real running app end-to-end: the drug license number persisted via the real Settings form; a real prescription-required product created via the real Add-Product form correctly showed the checkbox and Default Supplier picker; adding it to a real cart opened the real prescription-capture modal, blocked "Add to Cart" until both names were filled, and the completed sale's `InvoiceItem` correctly snapshotted both names; a raw IPC call bypassing the UI entirely was still correctly rejected server-side with `RX-001`; the new Prescription Drug Sales Register report tile (visible only for PHARMACY) showed the real sale with correct patient/doctor/amount; adjusting the same product's stock below its reorder level and clicking "Generate Reorder POs" created a real DRAFT PO for the correct supplier at the configured reorder quantity, and running it again correctly skipped the same product (no duplicate PO) since it was already on an open PO.
  - **Real test-script bug found and fixed while building the UAT** (not a product bug): the harness's `cleanupByNamePrefix` soft-deactivates (`isActive=0`) rather than hard-deletes a `Product` row whenever an FK from a child table (here, a `PurchaseOrderItem` from the reorder-PO test) still blocks the delete — if that child-row cleanup runs AFTER `cleanupByNamePrefix` instead of before, the product silently survives as a stale *inactive* row with the exact same test-run name, which a later run's own `WHERE productName = ?` lookup can then pick up instead of the fresh one it just created (no unique-name constraint on Product), cascading into confusing unrelated failures (e.g. "product is archived and cannot be sold") several steps later. Fix: order the `finally` block's own child-row deletes (PurchaseOrderItem/PurchaseOrder/InvoiceItem/Invoice/InventoryMovement) strictly BEFORE calling `cleanupByNamePrefix`, and scope any such lookup query to `isActive = 1 ORDER BY createdAt DESC LIMIT 1` as defense in depth. Also worth remembering for future UAT scripts in this repo: **Playwright toast-detection selectors must target this app's REAL toast DOM shape** (`div.fixed.bottom-4.right-4 p` — no `role="alert"`/`class*="toast"` anywhere in `Toast.tsx`) — a wrong selector silently returns an empty array indistinguishable from "no toast fired," which cost real debugging time chasing a phantom checkout bug that was actually a mundane "product has 0 opening stock" `INV-002` rejection the whole time.
- [x] **Electronics** (6→10) — DONE 2026-07-19 (real code + 16 new unit tests + 22/22 live UAT via the real app):
  - [x] Real repair/RMA workflow linked to a specific serial/IMEI — new `RepairTicket` model (claim number via the shared `generateSequenceNumber` — `RMA-00001` etc; `RECEIVED → DIAGNOSED → SENT_TO_VENDOR → AWAITING_PARTS → REPAIRED/REPLACED → RETURNED_TO_CUSTOMER`, `CANCELLED` only reachable before a real side effect (REPLACED) could have happened; vendor RMA tracking — `vendorId`/`vendorRmaNumber`/`sentToVendorDate`/`vendorResponseDate` stamped automatically on the relevant transitions; replacement-unit linkage — `replacementSerialId` is `@unique` so one physical replacement can never be double-assigned, requires the SAME product and an `AVAILABLE` serial, and on REPLACED reuses `ProductSerial.status` rather than inventing a parallel enum: original serial → `DEFECTIVE` (never resold), replacement → `SOLD` inheriting the original sale's `invoiceId`, `Inventory.quantity` decremented exactly like any other sale; turnaround time derived at read time (days from intake to hand-back, or days-open for an in-progress ticket) — never a stored/stale column. New `repair_rma` `TemplateModule` flag, ELECTRONICS default. New `repairTickets.view`/`.create`/`.manage` permissions (Cashier gets view+create — bounded intake, same trust level as `billing.createInvoice`; `.manage` — advancing status, especially REPLACED, which is effectively a free replacement sale — is Manager+ only).
  - [x] Service-history view per serial — `getSerialServiceHistory(serialId)` returns every ticket ever opened against that physical unit plus `replacedOnTicket` (lineage: a replacement unit that itself later needed repair shows which original ticket it replaced) — surfaced via a new "Repair" button on `SerialTrackingScreen.tsx` (shown only for `SOLD` serials, gated on the `repair_rma` module) that navigates to a new `RepairTicketsScreen.tsx` pre-filtered to that unit, with a one-click "New Ticket for This Unit" that prefills the sold-unit picker (no re-search needed).
  - Live UAT proved, against the real running app end-to-end: switched to Electronics, created a real sold serial, confirmed the server-side rule that a repair ticket cannot be opened against a never-sold (`AVAILABLE`) serial (`RPR-002`, bypassing the UI via a raw IPC call); the real "Repair" button appeared only for the sold serial on `SerialTrackingScreen.tsx` and navigated correctly; the service-history panel correctly showed "no tickets yet" and its "New Ticket for This Unit" button correctly pre-filled the serial (no manual re-search); creating the ticket via the real form generated a real `RMA-00001` claim visible in the list with `RECEIVED` status; advancing DIAGNOSED → REPLACED via the real status-transition UI (picking the in-stock replacement serial from a real dropdown) correctly flipped the original serial to `DEFECTIVE`, the replacement to `SOLD` with the original's `invoiceId` inherited, decremented `Inventory.quantity`, and the replacement serial's own service-history call correctly showed `replacedOnTicket` lineage back to the RMA ticket; confirmed Cashier's real seeded permissions include `repairTickets.view`/`.create` but not `.manage`.
  - **Real gotcha found and fixed while running this UAT** (infra, not this feature specifically — but hit for the first time here): `prisma migrate diff --from-schema-datasource` resolves a relative `DATABASE_URL` against `prisma/`'s own directory, not the project root — silently diffing against a stale, tiny `prisma/.dev-data/sarang.db` (3MB) instead of the real 74MB dev DB at the project root, which made the diff falsely show dozens of already-applied tables/indexes as "missing." Fixed by using `--from-url` with an explicit absolute path to the real dev DB instead of `--from-schema-datasource` whenever the schema's `DATABASE_URL` is a relative path — worth remembering for every future phase's migration step in this repo.
  - **Real test-script bugs found and fixed while building the UAT** (not product bugs): (1) two Playwright strict-mode ambiguities — "New Repair Ticket" matches both the modal's own `<h2>` and the always-visible header button, and "Diagnosed" matches both a status-transition button inside the detail modal and one of the page's own status-filter pills; fixed by scoping to the modal's own tag/container (`h.topModal(page)` / `h2:has-text(...)`) instead of a bare page-wide text locator.
- [x] **Jewellery** (7→10) — DONE 2026-07-19 (real code + 7 new unit tests + 16/16 live UAT via the real app):
  - [x] Hallmark/HUID number on the printed invoice — new `InvoiceItem.jewelleryHallmarkNumber`, snapshotted from `Product.hallmarkNumber` at sale time (same "snapshot at sale, discarded otherwise" pattern as the other jewellery fields), captured in `BillingScreen.tsx`'s `jewelleryDetail` when the line is priced, sent through `createInvoice`, and printed via `print.service.ts`'s `jewelleryDetailLine()` (`HUID: <number>` appended after making charge).
  - [x] Per-transaction making-charge override — a new editable "Making charge" input directly on the jewellery cart line in `BillingScreen.tsx` (`updateMakingChargeOverride`), recomputing `unitPrice = metalValue + override` without touching the product's own `makingChargeType`/`makingChargeValue` config; visually flagged (brand-colored border) when overridden so staff can see at a glance a line isn't using the standard charge.
  - [x] Atomic old-metal-exchange-to-invoice linking — `createInvoice` gained an optional `metalExchangeId`: fetches and validates the exchange (exists, still unlinked, same customer if both are set) *before* the totals are computed, folds its `valueGiven` into the discount, then re-claims it with a conditional `updateMany` *inside* the same transaction as the invoice itself — so a concurrent second invoice trying to claim the same exchange loses the race and rolls back cleanly (`INVOC-013`), instead of the old "type the same number into the discount box, then remember to separately call linkToInvoice" two-step manual process (still available for edge cases, e.g. correcting an older invoice). New "Apply Old-Metal Exchange" picker in `BillingScreen.tsx` (jewellery_pricing-gated) searches unlinked exchanges and shows the credit that will be applied on checkout; on-screen totals/split-payment validation correctly fold in the exchange credit via a new `effectiveGlobalDiscount` derived value, so what the cashier sees pre-submit matches what the server actually charges.
  - Live UAT proved, against the real running app end-to-end: switching to Jewellery, setting today's GOLD 22K rate, and creating a real hallmark-bearing product; adding it to a real cart correctly showed both the hallmark and the computed price (net weight × rate + making charge); overriding the making charge via the real inline input correctly recomputed the line price; creating a real old-metal exchange for the same customer and applying it via the real picker showed the correct credit amount and correctly folded it into the invoice's `discountAmount` (30,000) on the completed sale; confirmed in the DB that the hallmark and the overridden making charge were both correctly persisted on the `InvoiceItem`, and that the exchange was atomically linked to the exact invoice created; confirmed server-side that attempting to reuse the now-already-linked exchange on a second invoice is correctly rejected with `INVOC-013`.
  - **Real infra gotcha found and fixed while writing the UAT script** (not a product bug, but wasted real debugging time): the correct hash route for the actual "new sale" screen is `#/billing/new`, not `#/billing` (which is the Invoice List screen — same URL prefix, completely different page, so every selector silently failed against the wrong screen's DOM). Confirmed via `router.tsx`. Worth remembering for any future billing-flow UAT script in this repo.
- [x] **Agri Inputs** (5→10) — DONE 2026-07-19 (real code + 6 new unit tests + 24/24 live UAT via the real app):
  - [x] Seasonal/harvest-tied credit terms — `credit_limit_enforcement`/`outstanding_analytics` added to AGRI_INPUTS's default module set (same trust level Distributor already had). Deferred-until-harvest due dates: `createInvoice` gained an optional `dueDate` — `Invoice.dueDate` already existed in the schema and `report.service.ts`'s aging already read it (falling back to `invoiceDate`) — the ONLY missing piece was ever writing it, which nothing did. Now captured via a real date picker shown for CREDIT sales in `BillingScreen.tsx` (generic across every vertical that sells on credit, not agri-specific), and surfaced on `InvoiceDetailScreen.tsx` as a "Due <date>" or red "Overdue since <date>" badge once the balance is still outstanding.
  - [x] Category-specific expiry curves — new `Product.expiryAlertLeadDays` (nullable; null = the generic 30-day default), settable per-product in `ProductFormModal.tsx` (gated on `expiry_tracking`, not agri-specific) with copy explicitly calling out seeds/fertilizer needing a longer heads-up window than a pharmacy's medicine cutoff. `batch.service.ts`'s `getExpiryAlerts` now resolves "expiring soon" PER BATCH against its own product's lead time (Prisma can't compare a per-row value in one WHERE, so candidates are queried once and bucketed in memory — same pattern `generateReorderDraftPOs` already established for its own per-row comparison); `BatchManagementScreen.tsx`'s per-row `ExpiryBadge` now colors using that same per-product threshold instead of a hardcoded 30. Deliberately scoped OUT and documented in-code: the "Expiring" TAB's own explicit 30-day filter stays a flat manual query (a different, literal "what's due in the next N days" use case) — the per-row badge coloring is what actually reflects the category-specific curve.
  - [x] Combined consumables+equipment dashboard — new `AgriInputsDashboardScreen.tsx` (`#/agri/dashboard`, new `agri_dashboard` module flag, AGRI_INPUTS default) showing KPIs and top-N lists for both product families in one screen: low-stock consumables, expiring/expired batches (reusing `getExpiryAlerts`), and equipment count + warranty-expiring-soon (reusing `serials.list`) — closing the "two unrelated screens, no unified view" gap for the one vertical that has both `batch_tracking` and `serial_tracking` on by default.
  - Live UAT proved, against the real running app end-to-end: switching to Agri Inputs showed `credit_limit_enforcement`/`outstanding_analytics`/`agri_dashboard` all correctly enabled by default; a real seed product created via the real Add-Product form with a 90-day expiry lead time correctly showed the new field; a batch expiring in 60 days correctly rendered a WARNING badge (60 ≤ 90) while an identical 60-day batch on a default-lead-time product correctly rendered SUCCESS (60 > 30), and the automatic alerts endpoint correctly included one and excluded the other; the Agri Dashboard correctly showed the flagged consumable and a real equipment item with a warranty expiring in 20 days; a real CREDIT sale with a due date set 10 days in the past (real UI, real farmer customer) correctly showed an "Overdue since" badge on the invoice, persisted `dueDate` to the DB, and the Outstanding Analytics aging report correctly bucketed it by the due date (1-30 days overdue) rather than the invoice date (created today) — proving the "harvest-tied" aging actually works end-to-end, not just that the field exists.
  - **Real test-script bugs found and fixed while building the UAT** (not product bugs): (1) module-enablement state lives in `IndustryTemplateSetting.enabledModules`, not a `Setting` row — wrong table guessed first; (2) `ExpiryBadge` always renders `"{days}d"` text regardless of urgency, so a text-presence check can't distinguish warning from success — must check the Badge's own color class (`.text-warning` vs `.text-success`); (3) this schema's SQLite `DateTime` columns store epoch milliseconds, not a date string — a raw `UPDATE ... SET warrantyExpiryDate = 'YYYY-MM-DD'` silently produces an unparseable value; (4) same-hash navigation (`#/agri/dashboard` → `#/agri/dashboard`) does not remount a React Router screen, so a second visit without bouncing through a different route first shows stale first-load data, not fresh data.
- [x] **Manufacturing** (6→10) — DONE 2026-07-19 (real code + 27 new unit tests + 19/19 live UAT via the real app):
  - [x] Multi-level BOM — `BillOfMaterialItem`/`ProductionMaterialUsage` gained an optional `componentProductId` alongside the now-optional `rawMaterialId` (exactly one of the two set, enforced in `bom.validation.ts`'s `.refine` and again server-side in `bom.service.ts`); a real cycle guard (`wouldCreateCycle`) walks every candidate component's own BOM chain breadth-first before allowing a save, catching both a direct self-reference and a multi-hop cycle (A→B→C→A); `production-order.service.ts`'s `startProductionOrder`/`cancelProductionOrder` branch per usage row — a component-product line deducts/restores that PRODUCT's own `Inventory.quantity` (via a new `PRODUCTION_OUT`/`PRODUCTION_RETURN` movement type, not `SALE`) while a raw-material line still deducts `RawMaterial.currentStock` as before. `BillOfMaterialsScreen.tsx` gained a per-row Material/Sub-assembly type toggle with its own search-as-you-type picker for each.
  - [x] Labor costing folded into produced-unit cost — `ProductionOrder.laborCost`, captured on the real "Mark Complete" form, added to material cost before dividing by `producedQty` to get the finished good's `averageCost` — the exact same weighted-average inventory-costing formula this codebase already uses everywhere else, just fed a cost basis that finally includes labor.
  - [x] Scrap/reject quantity distinct from a low yield number — `ProductionOrder.scrapQty`, captured alongside labor cost on completion; total cost is deliberately spread over `producedQty` ALONE (not `producedQty + scrapQty`) since a scrapped unit still consumed real material/labor but adds zero inventory value — its cost is absorbed into the good units' unit cost, same "real cost lands on what actually shipped" reasoning this codebase already applies to weighted-average costing.
  - [x] QC/inspection gate — `WorkOrder` gained `isQcStep`/`qcResult`/`qcNotes`; `work-order.service.ts`'s `updateWorkOrderStatus` server-side REJECTS (`WO-007`) marking a QC-flagged step DONE without a `qcResult`, never trusting the UI alone; `ProductionOrdersScreen.tsx`'s step toggle routes a QC step to a real pass/fail prompt instead of a plain checkbox-toggle.
  - [x] Raw-material lot/batch traceability — new `RawMaterialBatch` model (mirrors `ProductBatch`'s shape, an ADDITIONAL ledger alongside the existing `currentStock` aggregate, never replacing it) plus a `ProductionMaterialBatchConsumption` join table recording exactly which lot(s) a production order actually drew on; `startProductionOrder` FIFO-consumes oldest-lot-first (same pattern `deductBatchStockFIFO` already established for finished-goods batches), a no-op for any material with no lot records at all; `cancelProductionOrder` reverses the exact consumption rows. New "Receive Lot" UI + lot list on `RawMaterialsScreen.tsx`; the production order detail view shows which lot(s) covered each material line.
  - Live UAT proved, against the real running app end-to-end: received two real raw-material lots (LOT-A 5kg, LOT-B 20kg) via the real UI; built a real single-level BOM (Engine ← Steel) and a real multi-level BOM (Car ← Engine as a sub-assembly) via the real BOM editor's type toggle; started a real Engine production order needing 24kg of steel and confirmed — via the DB, not just the UI — that it correctly FIFO-drew all 5kg from the older lot first and the remaining 19kg from the newer one, leaving exactly 1kg remaining in LOT-B; added a real QC checkpoint step, confirmed clicking it opened a real pass/fail prompt (not a plain toggle) and that PASS was actually persisted server-side; completed the Engine order with a real ₹60 labor cost entry and confirmed the resulting `averageCost` was exactly 60 (= (120 material + 60 labor) / 3 produced units); started a real Car production order and confirmed — via the DB — that it deducted 2 units from the ENGINE product's own inventory (not any raw material), correctly linked via `componentProductId`.
  - **Real test-script bugs found and fixed while building the UAT** (not product bugs): (1) a bare `div:has-text(...)` selector for a work-step row resolved to the innermost text-containing div, which has no button descendant at all (the real toggle button is a SIBLING of the text, not inside the same div) — fixed by matching the row's own specific class combination; (2) the order-detail modal does not auto-close after "Mark Complete" (by design — staff may want to keep reviewing it), which combined with the already-documented "same-hash navigation doesn't remount a screen" gotcha meant a second `#/manufacturing/production` visit right after completing an order silently reused the stale, still-open modal instead of a fresh page — fixed by explicitly closing the detail modal before navigating onward.

### Medical & Clinical
- [x] **Vet Clinic** (4→10) — DONE 2026-07-19 (real code + 3 new/updated unit test files + 22/22 live UAT via the real app):
  - [x] Real consultation/visit-note screen reachable for vets — `industry-template.service.ts`'s `VET_CLINIC` template was simply missing `visit_notes` from its module list (the actual `VisitNoteScreen.tsx` already existed and worked for GP/Specialist/Dental/Physio), so the whole feature was invisible for this vertical despite being fully built. One-line fix surfaces the Sidebar's "Clinical Notes" entry and the Appointments screen's "Start consultation note" button with zero other wiring, confirming the module-gate architecture is genuinely generic.
  - [x] `NormalRangeReference` gained a `species` column (`@default("ALL")`, part of a new `@@unique([testName, gender, species])`) with cascading fallback species → gender → fully-generic ALL/ALL in `findNormalRange`/`evaluateAgainstNormalRange`/`computeVitalsFlags`; seeded real Dog/Cat reference ranges for Body Temperature and Pulse Rate. `visit-note.service.ts` looks up the linked pet's species SERVER-SIDE (`appointment.petId → pet.species`), never trusting a client-sent value. `NormalRangesScreen.tsx` gained a Species picker + column.
  - [x] **Newly-discovered blocking gap, fixed as part of this item**: `Appointment.petId` had existed in the schema since Phase 23 and was already read by `getAppointment`, but was never SETTABLE anywhere — `createAppointment`/`updateAppointment` never accepted it and the booking form had no pet picker at all, meaning no appointment could ever actually be linked to a pet and the whole consultation-note feature was practically unreachable even after the module fix above. Added `petId` end-to-end: validation schemas, both service functions, IPC channel payload types, and a real "Patient (Pet)" dropdown in `AppointmentsScreen.tsx`'s booking modal (populated from `api.pets.list({ customerId })`, shown only when Vet Clinic's `vet_patients` module is enabled).
  - [x] `VisitNoteScreen.tsx` prefills `patientName`/`patientAge` from the linked pet (not the owner) and shows a new species/breed/sex/owner context card.
  - **Real product bug found and fixed while building the UAT** (not a test-script bug): `seedDefaultNormalRanges()` bailed out entirely if the `NormalRangeReference` table had ANY existing row — so on this dev DB (already seeded long ago with only the original human-only ranges), the new Dog/Cat rows added in this same phase would have silently NEVER reached it, despite the function's own comment claiming it "runs on every launch (self-healing) so already-installed databases... get it." The same would happen to any real installed copy of the app. Rewrote it to `upsert` per-row (keyed on the same `testName+gender+species` uniqueness the table enforces, `update: {}` so it never overwrites a value an owner may have since edited) — confirmed live: a Dog's 101.5°F reading incorrectly flagged HIGH before the fix (evaluated against the generic 97.0-99.0 human range) and correctly flagged NORMAL after a dev-server restart re-ran the healed seed function (evaluated against the newly-backfilled 101.0-102.5 Dog range).
  - Live UAT proved, against the real running app end-to-end: switched to Veterinary Clinic via the real Settings UI, confirmed `visit_notes` and `vet_patients` both enabled; created a real customer + a real pet (Rex, Dog, Labrador) via the API; booked a real appointment via the real UI using the NEW pet picker and confirmed — via the DB, not just the UI — `Appointment.petId` was persisted correctly; opened the consultation note and confirmed the pet context card correctly showed Species: Dog / Labrador / owner name, and that `patientName` was prefilled to "Rex" (not the owner's name); entered a 101.5°F temperature reading, saved, and confirmed via the DB that `vitalsFlags.temperatureF` was `NORMAL` (proving species-aware evaluation actually ran, not a silent fallback to the human range which would have flagged HIGH); confirmed `NormalRangesScreen.tsx`'s new Species field renders, accepts a selection, and the newly-seeded Dog Body Temperature range is visible in the table.
  - **Real test-script bugs found and fixed while building the UAT** (not product bugs): (1) the Species `<Select>` on `NormalRangesScreen.tsx` is inside a collapsed "Add Range" form that must be opened first — the script's first pass checked for the field before clicking the toggle button.
- [x] **GP Clinic / Specialist Clinic** (6→10) — DONE 2026-07-20 (real code + 1 new unit test file section (22 tests) + 20/20 live UAT via the real app):
  - [x] Structured prescription — new `PrescriptionItem` model (drugName/dosage/frequency/duration/instructions, one row per drug), distinct from the pre-existing free-text `VisitNote.plan` field which is left untouched for general treatment-plan notes. Edited as a whole list in a new "Prescription" section on `VisitNoteScreen.tsx` (add/edit/remove rows), saved via `savePrescriptionItems()` which replaces the full set atomically inside a transaction (same "delete-then-recreate" pattern this session has used for other multi-row child collections) and is server-blocked (`VN-010F`) from editing once the note is finalized, same lock every other field on the note already respects.
  - [x] Real printable prescription — new `PrescriptionPrint` component, structurally cloned from the existing `VisitSummaryPrint` (same letterhead/watermark/signature-block treatment, same `window.print()` CSS-media technique) but a genuinely separate document: a real itemized ℞ table instead of a SOAP paragraph, and — deliberately, since this one IS meant to serve as an actual prescription — it does NOT carry the existing summary's "NOT a validated medical record, prescription, or clinical report" disclaimer; instead it notes the practitioner's signature/stamp is required for validity.
  - [x] Vitals trending chart — new `getVitalsTrend()` walks back through the owning `Appointment` to find every OTHER visit for the same patient (same linked `Pet` for a vet visit, same `Customer` otherwise — `VisitNote` has no dedicated Patient entity of its own) and returns each visit's vitals across time; `VitalsTrendSection`/`TrendLineChart` on `VisitNoteScreen.tsx` reuse the exact same single-series SVG line-chart construction as `PetProfileScreen.tsx`'s pre-existing `WeightChart` (generalized to any numeric field), with a metric-selector chip row (BP Systolic/Diastolic, Pulse, Temperature, Weight) since a visit note has several plottable vitals where a pet only has one (weight). Only renders once a metric actually has ≥2 real data points, same guard as the pet-weight chart's own "add at least 2 entries" message.
  - [x] Referral-letter print — new `ReferralLetterPrint` component, distinct from both the full SOAP `VisitSummaryPrint` and the new `PrescriptionPrint`: a real formal letter ("To, Dr. X ... I am referring the above-named patient ... Re: [reason]") addressed to the specific referred-to provider, generated from the EXISTING "Refer to Another Provider" real in-app referral action (a new "Print Referral Letter" button appears per referral row).
  - Live UAT proved, against the real running app end-to-end: switched to Specialist Clinic via the real Settings UI; created a real patient with an older visit (98.4°F) and a current visit (100.2°F saved via the real Vitals form) — the real Vitals Trend section rendered with a live 2-point SVG chart selectable by metric; added a real Amoxicillin/500mg/1-0-1/5 days/After food row via the real Prescription table, saved it, and confirmed — via the DB, not just the UI — the row persisted correctly linked to the visit note; opened the real Print Prescription preview and confirmed it showed the drug and patient name; used the existing real "Refer to Another Provider" flow to book a referral to a second provider with a real reason, then opened the NEW "Print Referral Letter" button and confirmed the letter correctly showed the referred-to doctor's name and the referral reason, and confirmed it is a genuinely distinct document from the full SOAP note (no "Subjective (History)" section present).
  - **Real test-script bugs found and fixed while building the UAT** (not product bugs): (1) both new print components render TWO `div.fixed.inset-0` nodes each (the visible on-screen preview at `z-50`, plus a `print:block`-only node hidden on screen at `z-[9999]`) — the shared harness's generic `h.topModal()`/`h.closeTopModal()` helpers (`.last()` on that bare selector) aren't safe to reuse here since they can resolve to the hidden print-only node instead of the visible preview, and — more importantly — `closeTopModal()`'s "click the modal's first button" convention would click the header's "Print" button (triggering a real `window.print()` OS dialog that can hang the renderer) instead of the intended "✕" close button; fixed by scoping reads to the `.z-50` class explicitly and closing via the Escape key (both components already had a keydown handler for it) instead of a button click. (2) Two more section titles ("Vitals Trend", "Referral Letter") render through the same pre-existing Tailwind `uppercase` CSS class this session has hit before (Retail's "Frequently Sold") — Playwright's `innerText()` reflects the CSS-transformed text, not the literal JSX string, so both checks needed a case-insensitive match.
- [x] **Dental Clinic** (7→10) — DONE 2026-07-20 (real code + 5 new unit tests + 11/11 live UAT via the real app):
  - [x] Per-tooth chronological history — `ToothRecord` itself is UNCHANGED (still one row per `(patientId, toothNumber)`, still the fast "current state" the chart reads) but a new `ToothRecordHistory` model is now an ADDITIONAL append-only ledger alongside it, same "ledger alongside the aggregate" pattern as `RawMaterialBatch`/`ProductBatch` this session used repeatedly: `upsertTooth()` now writes both the ToothRecord upsert AND a new history row inside the SAME transaction, every single save — so a repeat save of the same tooth (CARIES → later FILLED) leaves BOTH entries queryable via a new `getToothHistory()`, instead of the second save silently erasing all trace of the first. New "History" button on the tooth editor panel (`DentalPatientScreen.tsx`) shows the real chronological list (condition/surfaces/notes/date/recordedBy), most-recent-first.
  - [x] X-ray/clinical image attachment — verified (not rebuilt) as already fully covered by this phase's earlier §0 cross-cutting document-attachment fix, which deliberately mounted `DocumentPanel` on the Treatment Plan EDIT modal (not per-tooth — a scanned X-ray naturally belongs to a whole plan, not one tooth number).
  - Live UAT proved, against the real running app end-to-end: created a real patient, saved tooth #11 as CARIES with a note, then saved the SAME tooth again as FILLED with a different note — confirmed via the DB that `ToothRecord`'s current-state row correctly shows only the latest (FILLED), while `ToothRecordHistory` correctly has BOTH entries in chronological order; opened the real "History" panel and confirmed both conditions and both notes render on screen; created a real Treatment Plan, reopened it in edit mode, and confirmed the Document panel is genuinely reachable there (re-verifying the pre-existing §0 fix still works, not a new build).
  - No real product bugs found this vertical — the one test-script hiccup (checking for the Document panel on the "New Plan" CREATE modal, where it's deliberately absent by design — only the EDIT modal shows it, since a plan needs to exist first) was caught and fixed by reading the component's own `{plan && <DocumentPanel .../>}` guard before re-testing, not a defect.
- [x] **Physio Clinic** (7→10) — DONE 2026-07-20 (real code + 5 new unit tests + 13/13 live UAT via the real app):
  - [x] Outcome-measure trending — new `VisitNote.functionalScore` (0-100, higher = better function) alongside the existing `painScore` (0-10), captured on the same Physio-only "Pain & Treatment" form section, clamped server-side the same way `painScore` already was. Both measures now flow through the exact same `getVitalsTrend()`/`VitalsTrendSection` mechanism built for GP Clinic's vitals trend — no new trending infrastructure needed, just 2 new entries in the `TREND_METRICS` config and 2 new fields in the trend query's select/filter/map. Deliberately NOT gated by an `isPhysio` check anywhere in the trend code: since these fields only ever get a value from the Physio-only form section, they naturally never appear as an "available" metric chip for any other vertical's visits — same "generic mechanism, naturally scoped by data" pattern as the rest of this trend feature.
  - Live UAT proved, against the real running app end-to-end: created a real patient with an older session (Pain 8/10, Functional 35/100) and a current session; entered an improved Pain Score (4/10, via the real button grid) and Functional Score (70/100, via the real new field) on the current session and saved — confirmed via the DB both persisted correctly; the real Vitals Trend section showed BOTH new metric chips ("Pain Score", "Functional Score"), and switching to the Functional Score chip rendered a real 2-point SVG chart.
  - No real product bugs found this vertical — a genuinely small, clean addition (1 additive column, 2 new lines in an existing trend-metrics config) since GP Clinic's earlier trend-chart work already built the entire generic mechanism this reuses.
- [x] **Diagnostic Lab** (6→10) — DONE 2026-07-20 (real code + 2 new/updated unit test files (46 tests) + 12/12 live UAT via the real app):
  - [x] Critical/panic-value tier — new `RangeFlag = 'LOW' | 'NORMAL' | 'HIGH' | 'CRITICAL'`, backed by new `NormalRangeReference.criticalLow`/`criticalHigh` thresholds (distinct from `minValue`/`maxValue` — a real panic value sits OUTSIDE the normal range, not at its edge), checked FIRST in `evaluateAgainstNormalRange` before the ordinary LOW/HIGH bounds. Seeded real clinical panic values (FBS <40 or >400 mg/dL, BP Systolic <70 or >180, Pulse <40 or >150, Body Temperature <95°F or >104°F) — deliberately NOT added to every test (HbA1c/Total Cholesterol have no recognized acute panic value). `LabTestOrderItem.resultParameters`' `flag` union extended to include `CRITICAL`, auto-suggested by the EXISTING `normalRange.evaluate` result-entry pipeline (no new UI mechanism — CRITICAL is just a new possible answer from a lookup that already ran). `NormalRangesScreen.tsx` gained Critical Low/High inputs + a table column.
  - [x] Escalation workflow — new `LabTestOrderItem.hasCriticalResult` (derived/cached from `resultParameters` on every result save, so a pending-escalation view never parses JSON), `criticalNotifiedAt`/`criticalNotifiedById`/`criticalNotifiedNotes` recording that the referring doctor was ACTUALLY called (not just that the result was flagged). New `acknowledgeCriticalResult()`/`listPendingCriticalEscalations()`. `LabOrdersScreen.tsx` gained a red "Critical" badge (order-list level AND per-item) and a real "Record Doctor Notified" action that captures free-text notes — deliberately does NOT wire a `notifiedById` from the logged-in session, since `criticalNotifiedById` is FK'd to Employee, not User (the exact same trap the Phase 25 `ToothRecord.recordedById` fix already documents elsewhere in this codebase).
  - [x] Image/scan attachment — verified (not rebuilt) as already fully covered by this phase's earlier §0 cross-cutting document-attachment fix, which explicitly lists Diagnostic Lab (`LabOrdersScreen.tsx`'s order detail modal) among the 6 affected business types.
  - **Real product bug found and fixed twice over while building this** (not test-script bugs): (1) `VisitNoteScreen.tsx`'s `FLAG_VARIANT` badge-color mapping had no `CRITICAL` entry, so extending the shared `RangeFlag` type would have silently fallen through to the `?? 'success'` default — a life-threatening vitals reading rendered in a GREEN badge. Caught and fixed before it could ship, by inspection while wiring the new flag through the shared vitals-evaluation function GP Clinic's visit notes also use. (2) A second, subtler version of the earlier Vet Clinic self-healing-seed bug: `seedDefaultNormalRanges`'s per-row `upsert` with `update: {}` correctly fixed missing ROWS but does nothing for a NEW FIELD added onto an ALREADY-EXISTING row — the real dev DB's pre-existing FBS row (created before this phase) kept `criticalLow`/`criticalHigh` permanently null even after the "self-healing" seed function ran, caught live when the UAT's own DB-level assertion failed. Fixed by rewriting the seed to `findUnique` first and explicitly backfill ONLY the still-null critical-threshold fields, never touching `minValue`/`maxValue`/`notes` (which really might be a genuine owner edit) — confirmed fixed by restarting the dev server and re-querying the real DB before re-running the UAT.
  - Live UAT proved, against the real running app end-to-end: switched to Diagnostic & Pathology Lab; confirmed the seeded FBS panic thresholds (40/400) present in the DB; created a real order via the real "New Order" form; collected the sample and entered a real FBS value of 25 mg/dL in the result editor — the real auto-suggest correctly returned `CRITICAL` (not just LOW) and the saved item persisted `hasCriticalResult: true` in the DB; the real red "Critical" badge appeared on the item; used the real "Record Doctor Notified" action with a real note, confirmed via the DB that `criticalNotifiedAt`/`criticalNotifiedNotes` persisted correctly and the UI badge flipped to the notified state; confirmed the Document panel is genuinely reachable on the order detail view.
  - **Real test-script bug found and fixed while building the UAT** (not a product bug): the script initially guessed the route as `#/lab-orders`; the real route (confirmed via `router.tsx`) is `#/lab/orders` — same class of route-guessing miss as earlier verticals this session (`#/billing` vs `#/billing/new`).
- [x] **Blood Bank** (8→10) — DONE 2026-07-20 (real code + 15 new unit tests + 19/19 live UAT via the real app):
  - [x] Compatibility check now BLOCKS issuance by default — `createBloodIssue` computed a `compatibilityNote` before this but never actually stopped an incompatible unit from going out (purely advisory). Now rejects (`BB-023`) unless an explicit `overrideIncompatibility` flag PLUS a non-empty documented `overrideReason` (`BB-024` if missing) are provided — the rare legitimate emergency-release case, never a silent bypass. New `BloodIssueItem.overrideReason` persists the documented justification; a real `BLOOD_ISSUE_INCOMPATIBLE_OVERRIDE` audit-log entry is written whenever an override is actually used. `BloodIssueScreen.tsx`'s "Issue Units" button is now genuinely disabled for an incompatible selection until the cashier checks "Override — emergency release" AND types a reason — the pre-existing warning panel used to say "this is an advisory check only" right next to a button that was never actually gated by it.
  - [x] Cooldown now varies by component type and donor sex — new `Donor.lastDonationComponentType` (denormalized alongside the existing `lastDonationDate`, same write site in `updateScreeningStatus`'s PASSED branch) feeds a new `cooldownDaysFor()`: 90 days for whole-blood/RBC (male), 120 days for whole-blood/RBC (female — commonly-cited iron-loss guidance, documented as a simplification not a medical claim), 14 days for platelet apheresis, 28 days for plasma apheresis — replacing the previous flat 90-day interval for every donation type. **Newly-discovered gap, fixed as part of this same item**: `Donor.gender` has existed in the schema (and backend validation/IPC) since Phase 51, but was never collected anywhere in the UI — the sex-aware half of this feature would have been practically unreachable without a real Gender field on the donor registration form, so one was added to `DonorsScreen.tsx`.
  - Live UAT proved, against the real running app end-to-end: registered a real female donor via the real form (confirming the new Gender field persists); recorded a real whole-blood donation for her and a real platelet donation for a male donor, both passed screening via the real workflow — confirmed via the DB and the real Donors screen that the female whole-blood donor's next-eligible-date is exactly 120 days out while the male platelet donor's is exactly 14 days out; seeded a real B+ PACKED_RBC unit and opened the real Issue Units form against an A+ recipient — confirmed the submit button is genuinely disabled, stays disabled after checking the override box with no reason typed, and only enables once a real reason is entered; submitted the override and confirmed via the DB that `overrideReason` and the override-annotated `compatibilityNote` both persisted correctly.
  - **Real test-script bugs found and fixed while building the UAT** (not product bugs): (1) called `window.api.bloodBank.getDonor(id)` with a raw string instead of `{ id }` — the handler destructures `{ id }` from the payload, so a bare string silently produced `undefined` and a `NaN` day-count, not an error; (2) the stock-unit-selection checkbox and the new override checkbox both match a generic `input[type="checkbox"]` locator, and the dev DB already had other B+ units in stock from earlier seed data — a `.first()`-based row match risked selecting the wrong (possibly platelet, hence always-"compatible") unit; fixed by matching the row via its own real `donationNumber` instead of just its blood group text.

### Personal Services
- [x] **Beauty Salon** (6→10) — DONE 2026-07-21 (real code + 2 new/updated unit test files (16 tests) + 19/19 live UAT via the real app):
  - [x] Before/after photo attachment — reused the existing generic `Document`/`DocumentPanel` attachment system wholesale (added `'APPOINTMENT'` as a new entity type, keyed to `Appointment.id`) — a new "Photos" camera-icon button per appointment row, gated on `multi_service_booking` (Beauty Salon's own distinguishing module, not hardcoded to the business type).
  - [x] Stylist skill-matching — new `ServiceProviderSkill` join table (employeeId × serviceCatalogId), deliberately OPT-IN: a service with zero configured rows means "any provider can perform it" (the pre-existing behavior, unchanged) — a business that never configures skills sees no behavior change at all. A new "Qualified Services" checklist on the Employee edit form (`EmployeesScreen.tsx`, gated on the generic `service_catalog` module every service vertical already has) lets an owner mark which services a stylist can perform; the booking form's Provider picker (`AppointmentsScreen.tsx`) now narrows to only qualified providers once a service with configured skills is selected, falling back to every provider otherwise. New i18n keys translated across all 13 locales (this is a cross-vertical HR screen, not a Beauty-Salon-only one).
  - [x] Unified retail upsell into the SAME appointment checkout — `generateAppointmentInvoice` gained optional `retailItems`/`paymentMethod` parameters; retail line price/tax are resolved fresh from the real `Product` record server-side (never trusted from the client, same discipline this codebase applies everywhere pricing matters). The old single-click "Generate Invoice" action was replaced with a real `CheckoutModal` — shows the service total, lets staff search and add a real retail product with quantity, pick an actual payment method (previously hardcoded to `CREDIT` with no override), and submits everything as ONE invoice.
  - Live UAT proved, against the real running app end-to-end: checked a real "Haircut" skill on Stylist A only (not Stylist B) via the real Employee edit form, confirmed it persisted; opened the real booking form, selected that service, and confirmed the Provider dropdown correctly included Stylist A and excluded Stylist B; booked and completed a real appointment; opened the new Photos button and confirmed a real DocumentPanel is reachable; opened the real Checkout modal, added a real retail product, chose CASH, and confirmed — via the DB, not just the UI — the resulting invoice had BOTH the service line (₹500) and the retail line (₹350) and used the real chosen CASH payment method (not the old hardcoded CREDIT).
  - **Real test-script bugs found and fixed while building the UAT** (not product bugs): (1) the salon multi-service `<select>`'s option label includes a price suffix ("Haircut — ₹500"), so matching by exact label text failed — fixed by selecting by option value (the real serviceId) instead; (2) a legitimate "Insufficient stock" rejection surfaced because the test's own seeded retail product had no opening stock — confirmed this as CORRECT billing behavior (retail products go through the same stock-check as any other sale), not a bug, fixed by seeding `openingQuantity` on the test product; (3) `paymentMethod` lives on `Payment`, not `Invoice` (an invoice can have split/multiple payments) — a query against the wrong table silently returned nothing; (4) a leftover `InventoryMovement` row from an earlier interrupted debugging run FK-blocked test product deletion during cleanup — same known harness gap ("InventoryMovement rows aren't traced by cleanupByNamePrefix's own joins") documented earlier this session, applied here explicitly.
- [x] **Gym/Studio** (5→10) — DONE 2026-07-18 (real code + 2 updated unit test files (11 new tests) + 11/11 live UAT via the real app):
  - [x] Verified (not rebuilt) the two "dead" membership features: an earlier §0 cross-cutting fix had already wired `getExpiringMemberships` into the Expiring Soon tab and `getMembershipAttendance` into an Attendance History modal — both confirmed live against real data.
  - [x] Real freeze/resume date math — `Membership.freezeHistory` (JSON array of `{frozenOn, resumedOn, reason}`) was written but never actually used: freezing just flipped `status` via the generic `updateMembership`, with `endDate` never adjusted, so a member frozen for 3 weeks silently lost that time. New dedicated `freezeMembership`/`resumeMembership` functions: freeze appends an open history entry; resume finds the most recent open entry, computes `frozenDays = Math.ceil(elapsedMs / 86400000)`, closes the entry, and pushes `endDate` forward by exactly that many days (with a defensive fallback if the history was ever hand-edited into an inconsistent state). Replaced the generic status-dropdown "Freeze"/"Unfreeze" options in `MembershipsScreen.tsx` with real Freeze/Resume buttons — Freeze opens a proper in-app reason modal (see bug note below), Resume is a single click.
  - [x] Trainer-client persistent PT assignment — new nullable `ClientSessionPack.assignedTrainerId` FK (`onDelete: SetNull`), deliberately opt-in and non-enforcing: it only ever PRE-FILLS the booking form's provider picker on customer pick (`AppointmentsScreen.tsx`), never forces it, so a substitute trainer for one session is always still possible. New `assignPackTrainer` service function + trainer picker/reassign `<select>` on `PhysioPatientScreen.tsx`'s Session Packs tab (the shared screen every `session_packs` vertical already routes through).
  - Live UAT proved, against the real running app end-to-end: the Expiring Soon tab and Attendance History modal both render real data for a real membership; froze a real membership via the actual Freeze button + reason modal, confirmed `status=FROZEN` and a new open history entry in the DB; backdated that entry ~10 days and clicked Resume, confirmed via direct DB query that `endDate` moved forward by exactly 10 days and the history entry closed; created a real session pack with an assigned trainer, confirmed the name renders on `PhysioPatientScreen.tsx`; opened a real New Appointment form for that same customer and confirmed the Provider field auto-filled with the assigned trainer.
  - **Real product bug found and fixed before UAT could even run**: `handleFreeze` originally used `window.prompt()` for the freeze reason — the only use of a blocking native browser dialog anywhere in this renderer (verified via a full-codebase grep) — and it hung the real Electron app indefinitely on click (confirmed live, not a test artifact: Electron's renderer doesn't reliably support `window.prompt()`, unlike `alert`/`confirm`). Replaced with a proper in-app modal (reason textarea + Freeze/Cancel buttons), consistent with every other input flow in this codebase.
  - **Real bug found and fixed in this session's own earlier work**: the `freezeMembership`/`resumeMembership` error codes as first written (`M27-005` through `M27-009`) collided with pre-existing codes already used by `checkInMember`, `getMembershipAttendance`, `getExpiringMemberships`, and — most importantly — `generateMembershipInvoice`'s "not found"/"already invoiced" codes. Renumbered to unused `M27-012` through `M27-016`; same collision existed in `session-pack.service.ts`'s first-written `assignPackTrainer` codes (`SP-007`/`SP-008` collided with `deductSession`'s catch and `generateSessionPackInvoice`'s "not found"), renumbered to `SP-012`/`SP-013`.
  - **Test-script-only bugs found and fixed while building the UAT** (not product bugs): wrong route guessed (`#/memberships` vs the real `#/gym/memberships`); the Attendance modal has no Escape-to-close handler (only its ✕ button does) — an earlier `page.keyboard.press('Escape')` left its backdrop covering the page and silently hung a later button click; the Memberships list defaults to filtering `status=ACTIVE`, so a freshly-frozen row drops out of view until the filter is switched to see the Resume button; `CustomerPicker`'s search input placeholder contains "client" not "customer", and its results render as `<button>` rows, not `<li>`/`[role=option]`; the freeze/resume day-math test needed a small time buffer when backdating, since `resumeMembership`'s `Math.ceil` computes against its own fresh timestamp at call time, a few hundred ms after the backdate write.
- [x] **Driving School** (6→10) — DONE 2026-07-18 (real code + 1 updated unit test file (16 new tests, 36 total) + 9/9 live UAT via the real app):
  - [x] Vehicle maintenance scheduling tied to real usage — new `DrivingVehicle` fields (`odometerKm`, `serviceIntervalKm`, `serviceIntervalSessions`, `lastServiceOdometerKm`, `lastServiceDate`, `sessionsSinceService`) plus a new `DrivingVehicleMaintenanceLog` ledger (same "ledger alongside the aggregate" pattern used elsewhere this phase). `sessionsSinceService` auto-increments exactly once per `DrivingSession` transition into `COMPLETED` (not on every edit, not twice on a resave); a derived `dueForService` flag fires once either the session count OR the km-since-service crosses its configured interval. New `logVehicleMaintenance`/`listVehicleMaintenanceLogs` service functions; a real "Maintenance" modal on `DrivingSchoolScreen.tsx`'s Vehicles tab shows the due badge, logs a service (with odometer never allowed to move backwards), and lists the full service history.
  - [x] `DrivingTest.instructorId` — nullable FK to Employee, so which instructor actually taught a learner is a real recorded fact. New `getInstructorPassRates()` aggregates real pass/fail counts per instructor; a "Pass Rate by Instructor" summary card + an Instructor column now render on the Tests tab, and the "Schedule Test" form has a real instructor picker.
  - Live UAT proved, against the real running app end-to-end: created a vehicle with a 2-session service interval, completed 2 real sessions and confirmed via DB query the vehicle flipped to `dueForService=true`; confirmed a re-save of an already-COMPLETED session does NOT double-count; logged a real service via the actual Maintenance modal (odometer + service type + cost), confirmed the counter reset to 0 and the new entry appeared in the visible Service History; created a real DrivingTest with an instructor via the API, marked it PASSED, and confirmed the real Tests tab renders both the instructor name and a 100% pass-rate summary card.
  - No real product bugs found this vertical — clean build, all 9 UAT checks passed on the first live run.
- [x] **Coaching Institute** (8→10) — DONE 2026-07-18 (real code + 3 new/updated unit test files (34 tests) + 9/9 live UAT via the real app):
  - [x] Batch waitlist — a batch at capacity used to hard-reject any new enrollment outright. Enrolling into a full batch now creates a real `WAITLISTED` enrollment instead (excluded from `generateMonthlyFees`, which only ever queries `status: 'ACTIVE'` — never billed until promoted). New `promoteFromWaitlist` re-checks capacity at promote time (not just trusting the UI's stale view) before flipping a waitlisted enrollment to Active. `BatchesScreen.tsx`'s Enroll button switches to "Join Waitlist" once full, and a promote icon appears next to each waitlisted student the moment a seat is actually free.
  - [x] Syllabus/topic-coverage tracking — new `SyllabusTopic` model (batchId, topicName, sequenceOrder, plannedDate, status, completedDate). Completing a topic stamps `completedDate` automatically; reverting it back to PENDING clears that stamp rather than leaving a stale date on a topic no longer marked done. A collapsible Syllabus panel on each batch card in `BatchesScreen.tsx` shows a real progress bar (X/Y topics, %).
  - [x] Parent-facing printable progress report — new `getStudentProgressReport`, assembling the SAME underlying data already tracked elsewhere (per-batch attendance % from `CoachingBatchAttendance`'s present/absent JSON arrays, `StudentTestScore` rows, `CoachingFeeRecord` rows) into one printable page instead of three separate screens a parent never sees. A new printer icon on each row in `StudentsScreen.tsx` opens it.
  - [x] Documented the existing Performances/recitals feature in the manual (it was real and working, just undocumented) — added a dedicated section distinguishing it from Test Scores (event/recital roster vs. academic marks).
  - Live UAT proved, against the real running app end-to-end: created a batch with capacity 1, enrolled one student (went ACTIVE) then a second (went WAITLISTED, confirmed via the real Batches screen's "Batch Full"/WAITLISTED badges); a bare `promoteFromWaitlist` call correctly rejected while still full; dropped the active student via the real app, then clicked the real Promote button and confirmed via the DB the waitlisted student flipped to ACTIVE; added two real syllabus topics via the actual Syllabus panel, checked one off, and confirmed the progress bar showed exactly "1/2 topics covered — 50%"; recorded real attendance and generated a real fee record, then confirmed `getStudentProgressReport` correctly aggregated a 100% attendance rate and the fee record for that student's batch.
  - No real product bugs found this vertical — clean build. **Real test-script bugs found and fixed while building the UAT** (not product bugs): `gotoHash()` to a hash the page is already on doesn't fire a `hashchange`, so a batch card's expand/collapse React state (and its cached enrollment list) survives across steps — clicking the chevron unconditionally could collapse an already-open row, and even a "check if already open, skip if so" guard left STALE enrollment data on screen after a raw IPC call changed the DB underneath it; fixed by always forcing a real collapse-then-reopen cycle to guarantee a fresh `loadEnrollments()` call. Also: the outer `AppLayout` wrapper also carries an `overflow-hidden` class, so a loose `div.overflow-hidden` locator matched the whole app shell instead of the batch card — fixed by scoping to the card's more specific `div.rounded-xl.overflow-hidden`. And: the Syllabus toggle button's rendered text has a leading space from its icon's flex gap, so an anchored `/^Syllabus$/` regex failed to match while a plain substring match succeeded.

### Trade Services
- [x] **Service / Consultant / Repair** (3-4→10) — DONE 2026-07-19 (real code + 1 updated unit test file (19 tests) + 10/10 live UAT via the real app):
  - [x] Invoicing bridge + appointments — already done cross-cuttingly in §1 (2026-07-17); verified still working, not re-tested here.
  - [x] Repair: parts-used tracking with REAL inventory deduction — new `JobCardPart` model (jobCardId, productId, quantity, unitPrice snapshotted at time of use), backed by `inventoryService.reduceStockTx` (the exact same helper billing/logistics/quotation already use to decrement real stock — not a free-text JSON blob like `CarJobCard.partsItems`, which never touches `Inventory` at all). Removing a part restores the exact quantity via a `REPAIR_RETURN` movement, deliberately never touching `averageCost` (a correction, not a purchase — same convention `returns.service.ts`'s `RETURN_IN` uses). New "Parts Used" section on `JobCardsScreen.tsx`'s detail modal: product search, running Parts Total, per-line remove.
  - [x] Repair: warranty-on-repair — new `JobCard.warrantyDays`/`warrantyExpiryDate` (expiry only ever computed once there's a real `deliveredDate` to count from) plus `warrantyClaimAgainstId` (self-relation, informational only — a shop may still accept an out-of-warranty comeback as a fresh paid job, so this never blocks creating a new job card). New Warranty section on the detail modal (editable days pre-delivery, a real Under-Warranty/Expired badge post-delivery); the "New Job Card" form gained a warranty-claim-against picker that surfaces the original job's live warranty status.
  - Cross-vertical i18n: `JobCardsScreen.tsx` also serves AGRI_INPUTS (a PRODUCT-category, `languageLock: 'multi'` vertical reusing the same generic job-card system) even though REPAIR itself is English-locked — 24 new `service.*`/`common.inStock` keys translated across all 13 locales, verified for key parity programmatically.
  - Live UAT proved, against the real running app end-to-end: added a real part via the actual UI, confirmed via direct DB query that `Inventory.quantity` dropped by exactly the used amount and a real `InventoryMovement` row was written; confirmed adding more than available stock is correctly rejected; removed the part via the real UI and confirmed the quantity was fully restored with a `REPAIR_RETURN` movement, and the `JobCardPart` row was actually deleted; delivered a job with `warrantyDays` pre-set via the real "Save" button and confirmed the Under-Warranty badge appeared **immediately, with no manual refresh** (the actual regression this session's own bug fix, below, targets); created a second job card as a warranty claim against the first and confirmed the real "New Job Card" form surfaced the original's live warranty status via its dropdown.
  - **Real product bug found and fixed while building the UAT**: `handleAdvanceStatus` in `JobCardsScreen.tsx` only merged a hand-picked `{status, actualCost}` patch into local React state after a status-transition API call, discarding the rest of the server's response — meaning a delivery transition's server-computed `warrantyExpiryDate` (from a `warrantyDays` value set earlier) silently vanished from the UI until a manual page refresh, even though the DB had the correct value all along. Fixed by merging the full server-returned `JobCardRecord` instead of a partial patch — confirmed fixed via a dedicated live-UI regression check (deliver a job with warranty pre-set, assert the badge appears with zero refresh).
  - **Test-script-only bugs found and fixed while building the UAT** (not product bugs): the detail modal has no Escape-to-close handler (only its own × button does) — a still-open modal's backdrop blocks pointer events to anything underneath, so a later click on an unrelated page-level button (e.g. the header Refresh button) timed out waiting for actionability instead of failing with a clear error; fixed with a shared `closeAnyOpenModal()` helper used before every subsequent navigation step. Also: the finally-block's own cleanup ran `cleanupByNamePrefix` BEFORE deleting the test's `InventoryMovement` rows, so the Product's hard-delete failed its own FK check and silently fell back to soft-deactivate, leaving 5 accumulated hidden-but-present rows in the dev DB across reruns — cleaned up by hand.
- [x] **Car Service Center** (6→10) — DONE 2026-07-19 (real code + 1 updated unit test file (20 tests) + 8/8 live UAT via the real app):
  - [x] Dedicated report — already done cross-cuttingly in §1 (2026-07-17, `generateCarJobCardReport`); verified the tile still works, not rebuilt.
  - [x] Next-service-due tracking by date or odometer, with a real reminder — new `CarJobCard.nextServiceDueDate`/`nextServiceDueKm` (both independently nullable — a shop may recommend either or both). There's no separate Vehicle master table (`vehicleNumber` is a free-standing field on each job card, same as everything else here), so "the current state of a vehicle" is always derived from its MOST RECENT job card, never a stored counter. New `listVehiclesDueForService()` groups by vehicle and flags Due-Soon/Overdue; new `scheduleNextServiceReminder()` reuses the exact same `notificationQueue`/`buildWhatsAppLink` mechanism appointment reminders already use (not a new mechanism) — schedules a real WhatsApp reminder N days ahead of the due date, addressed to the job's real client.
  - [x] A grouped vehicle service-history view by registration number — new `getVehicleServiceHistory()` returns every job card ever created for one exact registration number, newest first, distinct from the flat job-card list's free-text search box. New "Vehicles" tab on `CarJobCardsScreen.tsx` (one row per distinct vehicle, Due-Soon/Overdue badges, a Remind button, a History button opening the full grouped history in a modal) plus a History shortcut on every row of the existing flat list.
  - Live UAT proved, against the real running app end-to-end: created two real job cards for the same registration number, confirmed `getVehicleServiceHistory` groups both newest-first; set a real next-service-due date+km on the latest job card, confirmed `listVehiclesDueForService` correctly flags it Due Soon (not yet Overdue) and always references the LATEST job card even with an older one present; scheduled a real reminder and confirmed an actual `NotificationQueue` row was written with the correct type/status; drove the real "Vehicles" tab and confirmed the Due Soon badge and both grouped job numbers render in the real History modal; clicked the real "Remind" button and confirmed a second reminder scheduled successfully; confirmed the pre-existing "Job Card Report" tile on the real Reports screen still renders correctly (verification, not a rebuild).
  - No real product bugs found this vertical — clean build. **Test-script-only bugs found and fixed while building the UAT** (not product bugs): an anchored button-text regex (`/^Vehicles$/`) failed against the tab button's rendered text because an icon+text flex layout adds a leading whitespace-like gap even with no literal space character in the JSX — same class of gotcha hit earlier this session on `JobCardsScreen.tsx`'s Syllabus toggle, fixed the same way (plain substring match instead of an anchored regex). Also: the finally-block's own cleanup deleted the test customer via `cleanupByNamePrefix` before a separate manual `NotificationQueue` cleanup step that filtered by that same now-already-deleted customer's name, so its own subquery matched nothing and left 3 accumulated rows in the dev DB — same class of ordering bug as the Repair vertical's leftover-Product issue, fixed by deleting the notification rows by their own distinctive `notificationType` directly instead of via a customer-name join.
- [x] **Tailor Boutique** (6→10) — DONE 2026-07-19 (real code + 1 updated unit test file (+14 new tests, 21 total in file) + 33/33 live UAT via the real app):
  - [x] Dedicated report — already done cross-cuttingly in §1 (`generateTailoringOrderReport`); verified via the real IPC call it still generates correctly after this vertical's schema changes, not rebuilt.
  - [x] Trial/fitting date is now a REAL `Appointment`, not a mutable date field — new `TailoringOrder.trialAppointmentId` (unique, FK to `Appointment`) set by new `scheduleTrialAppointment()`, which calls the EXISTING `createAppointment()` and then the EXISTING `createAppointmentReminder()` — riding the SAME 24h/2h WhatsApp reminder pipeline every other appointment type already uses (deliberately NOT a bespoke one-shot reminder like Car Service Center's `scheduleNextServiceReminder`: a trial fitting genuinely IS a calendar appointment with a specific time slot, whereas Car Service's "next service due" is a date-only recommendation with no slot — a real domain distinction, not inconsistency). Scheduling a second trial for the same order is blocked server-side (`TO-006`) regardless of UI state. Row buttons on `TailoringScreen.tsx` show "Schedule Trial" until set, then a "Trial" badge.
  - [x] Fabric-stock deduction when the shop supplies material — new `fabricProductId`/`fabricQuantity` fields (alongside the pre-existing free-text `fabricDescription`, not replacing it), backed by `inventoryService.reduceStockTx` (the SAME helper billing/logistics/quotation/Repair's parts-tracking already use — real `Inventory.quantity` deduction, not a JSON blob). Set-once-then-clear-to-change pattern (mirrors Repair's `JobCardPart` add/remove pair): `setOrderFabric()` rejects if fabric is already linked (`TOF-003`, must clear first) and translates insufficient-stock into a friendly `TOF-005`; `clearOrderFabric()` restores the exact quantity via a `TAILORING_RETURN` movement, deliberately never touching `averageCost` (a correction, not a purchase). New "Set Fabric"/"Fabric ✓" (clear-on-click) button on `TailoringScreen.tsx`, gated on `fabricSupplied === 'SHOP'`.
  - Cross-vertical i18n: TAILOR_BOUTIQUE is itself the sole named `LANGUAGE_LOCK_EXCEPTIONS` entry (confirmed via the pre-existing 13-locale manual files for this vertical), so all new UI text (`tailoring.trial.*`, `tailoring.fabric.*`, plus new `tailoring.actions.*`/`tailoring.errors.*` keys) was hand-translated across all 13 locales, key-parity verified programmatically.
  - Live UAT proved, against the real running app end-to-end: created a real order with shop-supplied fabric via the actual UI; scheduled a real trial appointment via the actual "Schedule Trial" button and confirmed via direct API/DB checks that a real `Appointment` row exists (correct customer, correct service title referencing the order number), that BOTH the 24h and 2h `NotificationQueue` reminder rows were actually created (scheduled 5 days out so neither fire-time had already passed), and that the order's status flipped to `TRIAL_SCHEDULED`; confirmed a second scheduling attempt is rejected server-side with `TO-006` even after the UI button disappears; set fabric via the actual "Set Fabric" modal (product search + quantity) and confirmed real `Inventory.quantity` dropped by exactly 6.5 with a real `InventoryMovement` row (`referenceType: 'TAILORING_ORDER'`, `referenceId` = the order number); cleared fabric via the real "Fabric ✓" button and confirmed stock was restored to exactly the original quantity via a `TAILORING_RETURN` movement with `averageCost` untouched; confirmed the pre-existing Tailoring report tile still generates correctly.
  - No real product bugs found this vertical — clean build. **Test-script-only bugs found and fixed while building the UAT** (not product bugs): `api.appointments.get()` expects a `{id}` object payload, not a bare string — passing a bare string silently returned an empty result instead of erroring; and `inventoryService.reduceStockTx` hardcodes `movementType: 'SALE'` for every caller (the `referenceType`/`referenceId` pair is what actually distinguishes a fabric deduction from a sale or any other reduction, not `movementType`) — an assumed-but-wrong query filtering on `movementType = 'TAILORING_ORDER'` found nothing until corrected to filter on `referenceType` instead.
- [x] **Pest Control** (7→10) — DONE 2026-07-19 (real code + 1 updated unit test file (+20 new tests, 20 total in file — file was newly extended, pre-existing 0) + 23/23 live UAT via the real app):
  - [x] Dedicated report — already done cross-cuttingly in §1 (`generatePestContractReport`); verified via the real IPC call it still generates correctly, not rebuilt.
  - [x] Structured pesticide dosage/quantity — new `PestJobSheetPesticide` model, an add/remove ledger per visit (mirrors Repair's `JobCardPart`; a visit commonly uses more than one chemical across different areas, so this is NOT a set-once field like Tailor's fabric). Replaces relying on the single free-text `pesticideUsed` field (kept as-is for a quick summary/search string) for anything needing a real quantity — safety/compliance record-keeping and, when the chemical IS tracked as inventory, real stock deduction. `productId` is deliberately OPTIONAL (unlike `JobCardPart`'s mandatory link) — not every pest control shop tracks its chemical stock as an Inventory Product; when a line IS linked, `addJobSheetPesticide()` deducts real stock via the SAME `inventoryService.reduceStockTx` every other consumption path uses, and `removeJobSheetPesticide()` restores it via a `PEST_RETURN` movement. New "Pesticides Used" section on `PestControlScreen.tsx`'s job sheet edit modal (name/quantity/unit/target-pest/dosage-note fields, plus an optional inventory-product search-and-link).
  - No i18n needed — `PestControlScreen.tsx` is a plain hardcoded-English file (0 `t()` calls, confirmed via grep), consistent with PEST_CONTROL's `service` category placement.
  - Live UAT proved, against the real running app end-to-end: created a real job sheet via the actual UI; added a pesticide line with NO inventory link via the real form and confirmed via the DB it persisted correctly with all fields (name/quantity/target pest/dosage note) and touched no `Inventory` row; added a second line WITH a real inventory-linked product and confirmed exact `Inventory.quantity` deduction (1000→920) plus a real `InventoryMovement` row (`referenceType: 'PEST_JOB_SHEET'`); removed that linked line via the real UI and confirmed exact stock restoration (920→1000) via a `PEST_RETURN` movement, while the unlinked line remained untouched; confirmed the pre-existing Pest Contract report tile still generates.
  - **No real product bugs found this vertical — clean build.** No test-script bugs either — a genuinely clean build end to end.

### Regulated Professional
- [x] **Lawyer** (5→10) — DONE 2026-07-19 (real code + 1 updated unit test file (+14 new tests, 18 total in file) + 18/18 live UAT via the real app):
  - [x] Document attachment on case files — already done cross-cuttingly in an earlier §0/§1 fix (`DocumentPanel entityType="LEGAL_CASE"` already wired into `LegalCasesScreen.tsx`); verified genuinely reachable on a real case detail panel, not rebuilt.
  - [x] Basic conflict-of-interest check — new structured `LegalCase.opposingPartyName` field (distinct from the free-text `caseTitle`, which previously had no queryable opposing-party data at all). New `checkConflictOfInterest()` checks BOTH directions: (1) the proposed opposing party already matches an existing client elsewhere, and (2) the proposed client was previously recorded as an opposing party elsewhere. Deliberately ADVISORY, not blocking — a real conflict call requires professional judgment the software shouldn't force (unlike Blood Bank's compatibility check, an objective safety fact). Live-checked (debounced) in the New Case form as `clientId`/`opposingPartyName` are filled, surfaced as a non-blocking warning banner.
  - [x] Statute-of-limitations/deadline field with a reminder — new `LegalCase.limitationDate`, reusing the EXACT SAME `notificationQueue`/`buildWhatsAppLink` mechanism `hearing.service.ts`'s `scheduleHearingReminder` already established (confirmed hearing reminders already existed in this vertical — 2d/7d before a hearing — before building this). Uses longer 30d/7d lead tiers instead of hearing's 2d/7d, since a limitation deadline typically needs time to gather documents/instructions rather than just showing up in court. Settable at case creation AND updatable later from a small inline editor on the case detail panel (no full case-edit modal exists in this screen for anything else either, so this stays a targeted addition, not scope creep) — changing the date cancels the old pending reminder rows and schedules fresh ones, mirroring `hearing.service.ts`'s `rescheduleHearingReminder` reschedule-on-change discipline exactly.
  - No i18n needed — `LegalCasesScreen.tsx` is a plain hardcoded-English file (0 `t()` calls), consistent with LAWYER's `service` category placement.
  - Live UAT proved, against the real running app end-to-end: created a real case (Client B, opposing party = Client A's name) via the actual UI; confirmed via the API both directions of the conflict check correctly flag it; created a SECOND real case (Client A, opposing party = Client B's name) that walks into both traps at once — confirmed the live advisory banner in the real New Case form shows both conflict reasons, and that submission is NOT blocked; confirmed a real limitation date set at creation produced BOTH `LIMITATION_DUE_30D` and `LIMITATION_DUE_7D` real `NotificationQueue` rows; updated the deadline via the real inline editor on the case detail panel and confirmed the old reminder rows were cancelled and exactly 2 fresh ones scheduled for the new date, with the new date correctly persisted.
  - **No real product bugs found this vertical — clean build.** **Test-script bugs found and fixed while building the UAT** (not product bugs): a `getByPlaceholder` substring match collided between the Case Title field's placeholder ("...State of Maharashtra") and the new Opposing Party field's placeholder ("State of Maharashtra") — fixed with `{ exact: true }`; the case-detail's inline limitation-date editor block needed CSS-class scoping (a bare `text=` locator only matches the innermost `<p>`, no input/button descendants — same recurring gotcha class hit repeatedly this phase); `res.data.limitationDate` survives Electron's IPC structured clone as a real `Date` instance, not a string — `.slice()` on it directly throws (fixed by wrapping in `new Date(...)` first, the same Decimal/Date-serialization gotcha class this codebase's test suites document repeatedly).
- [x] **CA Firm** (6→10) — DONE 2026-07-20 (real code + 2 updated/new unit test files (+21 new tests, 24 total across both) + 18/18 live UAT via the real app, including an actual app close+relaunch to prove the startup evaluator picks up new data):
  - [x] Document attachment on compliance tasks/engagements — already done cross-cuttingly in an earlier §0/§1 fix (`DocumentPanel entityType="COMPLIANCE_TASK"`/`"ENGAGEMENT"` already wired into `ComplianceScreen.tsx`/`EngagementsScreen.tsx`); verified genuinely reachable, not rebuilt.
  - [x] 3 of the 4 AGM-relative ROC events made generate-able — new `Customer.lastAgmDate` (a client fact, not an event fact) + new `ComplianceEvent.agmOffsetDays` (set only for MGT-7:60/AOC-4:30/ADT-1:15 — precise, non-negotiable statutory offsets from a company's AGM date). `generateComplianceTasksForAllClients()` gained a second, independently-gated loop that computes `dueDate = client.lastAgmDate + agmOffsetDays` for clients who have captured one — reuses the exact same idempotent (event, client, dueDate) dedup as the pre-existing fixed-date loop, and runs from the SAME hourly/startup evaluator in `main/index.ts`, not a new mechanism. The 4th event (AGM itself) deliberately stays un-auto-generated — its real due date depends on FY-end + board scheduling, not a fixed offset from the PREVIOUS AGM, so guessing it would risk asserting a wrong statutory deadline (this session's recurring "never guess a derived fact" discipline). New "Clients & Checklists" modal on `ComplianceScreen.tsx` — pick a client, set their AGM date via a small dedicated `setClientAgmDate()` mutation (not routed through the generic `updateCustomer`, which needs the full record).
  - [x] Client-wise document checklist — new standalone `ClientDocumentChecklistItem` model (PENDING/COLLECTED status + a real `collectedDate` timestamp), deliberately NOT linked to the generic Document/DocumentPanel file-attachment system (this is a collection-STATUS tracker, not a file store — a CA can separately attach the actual scanned file via the DocumentPanel already on compliance tasks). New `seedStandardChecklist()` one-click-seeds the 4 most common types (PAN/Aadhaar/Bank Statement/GST Certificate) for a client, idempotently skipping any already present. Same "Clients & Checklists" modal — add/mark-collected/mark-pending/remove per item.
  - No i18n needed — `ComplianceScreen.tsx` only reuses pre-existing shared `common.error`/`compliance.disclaimer*` keys (already translated everywhere); all new UI text is plain hardcoded English, consistent with the rest of the screen's own body text and CA_FIRM's `service` category placement.
  - **Real bug found and fixed BEFORE it ever shipped** (caught by a genuinely useful unit test, not live UAT): my first draft of `generateComplianceTasksForAllClients()` placed the new AGM-relative loop AFTER an early `return` that fires whenever there are zero ACTIVE fixed-date events — meaning if a firm's admin ever deactivated all fixed-date compliance events while AGM-relative ones stayed active, AGM generation would have silently stopped too, for a reason completely unrelated to AGM data. Restructured so both loops run independently (each gated only on its OWN query being non-empty), verified by rewriting the shared test-mock helper to be `where`-clause-aware (matching real Prisma filtering) so the two branches could be tested in true isolation.
  - Live UAT proved, against the real running app end-to-end: set a real AGM date (2026-08-15) on a real client via the actual "Clients & Checklists" modal; added a real PAN checklist item, then used "Add Standard Checklist" and confirmed it correctly skipped the already-present PAN and added exactly the other 3; marked PAN Collected via the real toggle and confirmed a real `collectedDate` stamp; removed a item and confirmed the DB count dropped by exactly one. **Then closed the real Electron app and relaunched it** (not a test-only hook) — confirmed via the DB that the real startup evaluator generated all 3 AGM-relative tasks with EXACTLY correct offset due dates (MGT-7 = AGM+60d, confirmed to the exact millisecond), and confirmed the generated MGT-7 task is visible in the real task list UI with its Document panel reachable.
  - **Test-script-only bug found and fixed while building the UAT** (not a product bug): `getByRole('button', {name:'Add'})` collided with the neighboring "Add Standard Checklist" button (Playwright's default name-match is substring, not exact) — fixed with `{exact:true}`.
- [x] **Company Secretary** (6→10) — DONE 2026-07-20 (real code + 2 updated unit test files (+19 new tests, 19 total across both) + 14/14 live UAT via the real app):
  - [x] Document attachment on ROC filings/board meetings — already done cross-cuttingly in an earlier §0/§1 fix (`DocumentPanel entityType="ROC_FILING"`/`"BOARD_MEETING"` already wired into `ROCFilingsScreen.tsx`); verified genuinely reachable, not rebuilt.
  - [x] Per-company annual-compliance rollup — new `getComplianceRollup(financialYear)` (`roc-filing.service.ts`) aggregates real `ROCFiling` (MGT-7/AOC-4/ADT-1 status) and `BoardMeeting` (AGM held in the FY window) per client, for clients identified as "companies" by having EVER had a ROCFiling or BoardMeeting record (Customer has no dedicated entity-type flag, so this is the closest real signal without adding a new schema field just for this one view). New "Compliance Rollup" tab on `ROCFilingsScreen.tsx` — one row per company, AGM held/date + 3 status badges (defaulting to a real "Not Started" state, not a silent blank, when no filing row exists yet for that form type this FY).
  - [x] Auto-sequenced resolution numbering — `createBoardResolution()`'s `resolutionNumber` is now OPTIONAL; when omitted, it's auto-assigned as `(count of this CLIENT's existing resolutions) + 1` — scoped PER CLIENT/COMPANY (not a global sequence), since a CS files these against separate companies and ABC Ltd's resolutions shouldn't share a running number with XYZ Pvt Ltd's. Count-based rather than parsing the previous number, since the pre-existing free-text field may already hold non-integer formats (e.g. "2024-03") from before auto-sequencing existed. An explicit number is still honored if the CS types one (kept for edge cases/corrections). The "Resolution No." field in the real Add Resolution form now reads "Auto-assigned if left blank" and is no longer required to submit.
  - No i18n needed — `ROCFilingsScreen.tsx` is a plain hardcoded-English file (a grep for `t(` was a false positive, matching inside identifiers like `setResText(`), consistent with COMPANY_SECRETARY's `service` category placement.
  - Live UAT proved, against the real running app end-to-end: created a real MGT-7 filing and a real AGM board meeting for a real client via the actual forms; confirmed the real "Compliance Rollup" tab correctly showed AGM Held (with the real date) and MGT-7 "Pending" / AOC-4 "Not Started" for FY2025-26 (both via the on-screen row and a direct API check); added two resolutions via the real "Add Resolution" form WITHOUT typing a number and confirmed via the DB they were auto-assigned "1" then "2" in order; confirmed the Document panel is reachable on a real ROC filing's edit form.
  - **No real product bugs found this vertical.** **Test-script-only bug found and fixed while building the UAT** (not a product bug): the first UAT draft created the AGM board meeting with a date falling in a DIFFERENT financial year than the one being queried in the rollup check (FY boundaries are 1 April – 31 March, not calendar year) — the rollup correctly reported `agmHeld:false`, which was the right answer to a wrongly-dated test input; fixed by moving the meeting date inside the queried FY window.
- [x] **Architect** (6→10) — DONE 2026-07-20 (real code + 1 new unit test file (14 tests) + 19/19 live UAT via the real app):
  - [x] Dedicated report — already done cross-cuttingly in §1 (`generateServiceProjectReport`); verified via the real IPC call it still generates correctly, not rebuilt.
  - [x] Real drawing revision history — new `DrawingRevision.supersedesId` self-relation (1:1, `@unique`) links successive revisions of the same drawing number into a real chain instead of independent rows a mutable `revisionNumber` field could silently overwrite. New `issueNewRevision()` is the ONLY path that advances a drawing: creates a genuinely NEW row pointing back at its predecessor via `supersedesId`, and flips the predecessor's status to `SUPERSEDED` inside one transaction — rejects issuing against an already-superseded revision (`DR-010`, prevents branching). `DrawingRegisterScreen.tsx` now groups the flat row list by `drawingNumber` client-side (no extra round-trip — all rows already loaded), showing the current revision as the primary row with a "History (N)" toggle revealing every past revision (status, title, issued date, approver) for real Rev A vs Rev B comparison.
  - [x] Client approval/sign-off trail — new `DrawingRevision.approvedByName`/`approvedDate` fields. `updateDrawingRevision()` now REJECTS (`DR-007`) moving status to `APPROVED` unless a real signer name is provided in the same call or already on record — a status flip alone is no longer enough. New approval-prompt modal on `DrawingRegisterScreen.tsx` intercepts the status dropdown's direct-to-APPROVED transition (server-side guard enforced regardless of UI path too).
  - No i18n needed — `DrawingRegisterScreen.tsx` is a plain hardcoded-English file (0 `t()` calls), consistent with ARCHITECT's `service` category placement.
  - Live UAT proved, against the real running app end-to-end: created a real Rev A via the actual form; issued a real Rev B via the actual "New Revision" button and confirmed via the DB that TWO genuinely separate rows exist (Rev A flipped to `SUPERSEDED`, Rev B correctly `supersedesId`-linked back to Rev A and still `DRAFT`); confirmed the real "History (1)" toggle reveals the superseded Rev A; confirmed a second supersede attempt against the now-superseded Rev A is rejected server-side (`DR-010`); tried to approve via the real status dropdown and confirmed the approval-prompt modal intercepted it (Approve button correctly disabled with no name typed), then approved with a real signer name and confirmed via the DB both `approvedByName` and a real `approvedDate` persisted; confirmed the server-side guard ALSO independently rejects an approval attempt with no name (not just the UI prompt); confirmed the pre-existing Service Project report still generates.
  - **No real product bugs found this vertical — clean build.** **Test-script-only bug found and fixed while building the UAT** (not a product bug): `selectOption({ label: new RegExp(...) })` — Playwright's `selectOption` requires a plain string for `label`, not a RegExp; fixed by constructing the exact option-label string instead (recurrence of the same mistake class hit once already this session on the Lawyer vertical, worth remembering going forward).
- [x] **Civil Engineer** (6→10) — DONE 2026-07-20 (real code + 1 new unit test file (17 tests) + 13/13 live UAT via the real app):
  - [x] Dedicated report — already done cross-cuttingly in §1 (`generateServiceProjectReport`); verified via the real IPC call it still generates correctly, not rebuilt.
  - [x] GPS tagging on site visits — new `SiteVisit.latitude`/`longitude`/`locationAccuracy` fields. Captured from the real device Geolocation API (`navigator.geolocation.getCurrentPosition`) via a "Use Current Location" button on `SiteVisitsScreen.tsx`, with the lat/lng inputs always left manually editable as a fallback/correction — a desktop without real GPS hardware falls back to coarse IP-based geolocation, self-documented by a real `locationAccuracy` (meters) reading shown to the user rather than hidden. Deliberately did NOT add a "View on Map" external link — this app's `setWindowOpenHandler` only allows `aszurex.com`/`github.com` domains, so a Google Maps link would have silently failed to open in the packaged app; coordinates are shown as plain text instead, avoiding shipping a broken button.
  - [x] Structured material-test-result fields — new `MaterialTestResult` model (testType/materialDescription/testValue/unit/requiredMinValue/result/testedDate/notes), linked to a SiteVisit, replacing free-text `findings` for anything needing a real measured value. `result` (PENDING/PASS/FAIL) is auto-computed server-side ONLY when both a real measured value AND a real pass threshold are supplied (a well-defined `>=` comparison) — stays PENDING otherwise rather than guessing (e.g. a test with only a value and no threshold, or a test whose pass criteria isn't a simple minimum). An explicit result is still honored if manually set (e.g. correcting for a retest). New "Tests" panel per site-visit row on `SiteVisitsScreen.tsx` — add/list/remove, with a real PASS/FAIL/PENDING badge.
  - No i18n needed — `SiteVisitsScreen.tsx` is a plain hardcoded-English file (0 `t()` calls), consistent with CIVIL_ENGINEER's `service` category placement.
  - Live UAT proved, against the real running app end-to-end: logged a real site visit via the actual form with manually-entered GPS coordinates and confirmed via the DB they persisted exactly, and that they're genuinely visible on the real visit row; added a real material test (30 MPa vs a 25 MPa minimum) via the actual "Tests" panel and confirmed it auto-computed to a real `PASS`; added a second test (18 vs 25) and confirmed real `FAIL`; added a third test with a value but no threshold and confirmed it correctly stayed `PENDING` rather than being guessed; confirmed all three real badges (PASS/FAIL/PENDING) render correctly on screen; confirmed the pre-existing Service Project report still generates.
  - **No real product bugs found this vertical — clean build.** **Test-script-only bug found and fixed while building the UAT** (not a product bug): a raw SQL join query selected an unqualified `id` column from two joined tables that both have one (`SiteVisit`/`ServiceProject`), causing "ambiguous column name: id" — fixed by qualifying `sv.id AS id`.

### Agency & Project-Based
- [x] **Real Estate** (5→10) — DONE 2026-07-20 (real code + 2 updated/new unit test files (+18 new tests, 18 total) + 17/17 live UAT via the real app):
  - [x] Property photo gallery — already done cross-cuttingly in §0 (2026-07-17); verified the real `property.list` API still works, not rebuilt.
  - [x] Structured site-visit scheduling with feedback capture — new `PropertySiteVisit` model (linked to `PropertyInquiry`), replacing the bare `SITE_VISIT_SCHEDULED` status label with a real scheduled date/time/outcome record. `schedulePropertySiteVisit()` creates the visit AND flips the inquiry's own status to `SITE_VISIT_SCHEDULED` (a real side effect, not a manual dropdown pick), and schedules a real 1-day-before WhatsApp reminder to the buyer — reusing the exact same `notificationQueue`/`buildWhatsAppLink` mechanism every other reminder in this codebase already uses. Rescheduling (changing `scheduledDate`) cancels the stale reminder and schedules a fresh one, mirroring `hearing.service.ts`'s reschedule-on-change discipline. Completing a visit captures real `feedback` text + a structured `interestLevel` (HIGH/MEDIUM/LOW/NONE), not just a status flip. New "Visits" panel per inquiry row on `PropertiesScreen.tsx` — schedule/complete/cancel, plus a feedback-capture modal.
  - [x] Co-broker commission-split tracking — new `PropertyDeal.coBrokerName`/`coBrokerSharePercent`/`coBrokerShareAmount` fields. `coBrokerShareAmount` is ALWAYS computed server-side from `brokerageAmount * coBrokerSharePercent / 100` — never accepted directly from the caller, and correctly RECOMPUTED whenever any of `dealValue`/`brokeragePercent`/`coBrokerSharePercent` changes (not just when the co-broker percent itself is touched), preventing a stale split from being left behind after an unrelated edit. New co-broker fields on the real "Create Deal" form, with the split (own share vs. co-broker share) shown on the real deal card.
  - No i18n needed — `PropertiesScreen.tsx` is a plain hardcoded-English file (a grep for `t(` was a false positive, matching inside identifiers like `set('...`), consistent with REAL_ESTATE's `service` category placement.
  - Live UAT proved, against the real running app end-to-end: added a real inquiry via the actual form; scheduled a real site visit via the actual "Visits" panel and confirmed via the DB the visit persisted, the inquiry's status genuinely flipped to `SITE_VISIT_SCHEDULED`, and a real `PROPERTY_SITE_VISIT_REMINDER` `NotificationQueue` row was created; completed the visit via the real feedback modal (interest = HIGH, real feedback text) and confirmed both persisted along with a real `completedDate`; created a real deal with a co-broker split (2% brokerage, 40% co-broker share) via the actual form and confirmed via the DB the exact computed amounts (₹1,00,000 brokerage → ₹40,000 co-broker share), visible on the real deal card.
  - **No real product bugs found this vertical — clean build, first UAT run passed 17/17 with zero fixes needed.**
- [x] **Independent Consultant** (7→10) — DONE 2026-07-20 (100% pre-existing cross-cutting coverage, zero new code — verified via 10/10 live UAT):
  - [x] Projects-report fix — `generateServiceProjectReport()` (§0/§1) explicitly covers Independent Consultant; verified via the real IPC call.
  - [x] MRR/recurring-revenue report — discovered `generateRetainerReport()` ALREADY EXISTS in `report.service.ts`, complete with `summary.totalMRR`/`activeRetainers`/`billedThisPeriod` tracking, and is ALREADY fully wired through `reports.handler.ts` → `channels.ts` → `preload/index.ts` → `ReportsScreen.tsx` (`RetainerReportView`), gated on `requiredModule: 'retainers'` — which is exactly Independent Consultant's own module (`INDEPENDENT_CONSULTANT: [...SERVICE_BASE_MODULES, 'leads', 'service_projects', 'retainers', 'time_entries']`). Nothing to build; this plan item was already fully satisfied by earlier work.
  - No unit tests added (no new code — `generateRetainerReport` already has its own pre-existing test coverage in `report.service.test.ts`).
  - Live UAT proved, against the real running app end-to-end (10/10): created a real ₹25,000/month `RetainerAgreement` via the actual "New Retainer" form; confirmed via the DB it persisted ACTIVE with the correct amount; confirmed the real MRR report correctly includes this retainer with the exact monthly amount and rolls it into `summary.totalMRR`; confirmed the pre-existing Service Project report still generates.
  - **No real product bugs found — genuinely nothing left to build for this vertical.** **Test-script bugs found and fixed while building the verification UAT** (not product bugs): `h.switchBusinessType()`'s substring-based tile matching couldn't disambiguate "Consultant" (INDEPENDENT_CONSULTANT) from the pre-existing "Consultant / Freelancer" (legacy CONSULTANT) tile, since the former is literally a substring of neither but BOTH tiles contain "Consultant" — fixed with a raw IPC business-type switch + page reload instead; and the retainer form has TWO `input[type="date"]` fields (Start Date, End Date) — an unscoped locator hit Playwright's strict-mode violation, fixed by targeting `.first()`.
- [x] **Marketing Agency** (4→10): Done 2026-07-19. Added `CampaignPerformanceEntry` (period start/end, impressions, clicks, conversions, actual spend) and `ContentCalendarItem` (date, type, title, platform, status PLANNED/IN_PROGRESS/PUBLISHED/CANCELLED) as new models on `ServiceProject`, gated on the existing `marketing_campaigns` module. New service `marketing-campaign.service.ts` (CPE-001..008/CCI-001..007 error codes) including `getCampaignPerformanceSummary()`, which sums entries and computes CTR%/conversion-rate%/cost-per-conversion — each only computed when its real denominator is present and non-zero, `null` otherwise (never a misleading 0%/Infinity, same discipline as Real Estate's co-broker split). Wired end-to-end (validation/IPC/channels/preload) and into the shared `ProjectsScreen.tsx` (Consultant/Architect/Civil Engineer/Marketing Agency/Software Agency/Real Estate's common screen) with two new gated tabs (Performance, Content Calendar) per project row, plus a "Client Summary" modal with a client-side "Print for Client" HTML generator (same pattern as `printBoardMeetingMinutes`/`printPropertySheet`). Unit: new `marketing-campaign.service.test.ts`, 17 tests covering validation errors, CRUD for both entity types, and the summary aggregation's zero-denominator-stays-null cases explicitly — 1631/1631 passing full suite. Live UAT: ad-hoc script drove the real dev app — switched to Marketing Agency, created a real client + `ServiceProject`, added a real performance entry via the actual Performance tab UI, confirmed persistence via API, added a second entry via API for deterministic summary math, confirmed the real summary aggregation (30,000 impressions / 1,500 clicks / 75 conversions / ₹15,000 spend → 5% CTR / 5% conversion rate / ₹200 cost-per-conversion) both via API and rendered correctly in the real "Client Summary" modal, added a real content calendar item via the UI and confirmed a real status transition (PLANNED→PUBLISHED) persisted — 23/23 passing. (Found and fixed 2 real bugs in the UAT script itself, not the product: wrong route hash — `#/service/projects` is a *different*, legacy `Project`-model screen at `@modules/service/ui/ProjectsScreen`, while the `ServiceProject`-backed screen with these new tabs is mounted at `#/service/service-projects`; and `window.api.customers` has no `delete`, only `archive(id)` — a soft-delete, matching the app's real "no hard customer delete" convention. Also cleaned up 2 stray active test-customer records left by an earlier crashed debug run before rerunning; confirmed zero project/campaign/content-row residue via direct DB query afterward.) Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1631/1631.
- [x] **Software Agency** (4→10): Done 2026-07-19. Hourly billing default — verified (not rebuilt), already closed by §1's 2026-07-17 fix (`time_entries` in `SOFTWARE_AGENCY`'s `TEMPLATE_DEFAULTS`). Story points — new `Issue.storyPoints` (nullable Int). Comments — new `IssueComment` model, attributed to the real logged-in `User` via `getCurrentSession()?.userId` passed handler→service (never Employee — this codebase has a documented "User-id-into-Employee-FK" bug class from an earlier phase, so this deliberately links to `User`, the actual session identity). Subtasks — new `IssueSubtask` model, a simple per-issue checklist (title + isDone), not nested child Issues (the plan's "subtasks" reads as lightweight checklist items, not full sub-tickets with their own status/priority/assignee). Kanban board — new List/Board view toggle on `IssuesScreen.tsx`; Board view reuses `LeadsScreen.tsx`'s exact drag-and-drop pattern (draggable cards, `onDragOver`/`onDrop` per status column), dropping a card calls the same `issue.update({status})` the edit form already uses — no parallel status-mutation path. New Issue Detail modal (reachable from both List and Board views) hosts the comments thread + subtask checklist. Sprint burndown/velocity — new `getSprintBurndown()`/`getProjectVelocity()` in `sprint.service.ts`, derived ENTIRELY from real `Issue.resolvedDate`/`storyPoints`/`status` (no new daily-snapshot mechanism needed, since `resolvedDate` is already reliably stamped by `updateIssue()` on every status transition); weight is real `storyPoints` when ANY issue in the sprint has one set, else falls back to counting issues (1 each) — an unestimated issue in a points-tracking sprint contributes 0, never a guessed value. Velocity averages `completedPoints` (RESOLVED/CLOSED only) across the project's last N COMPLETED sprints. Both charts render via `recharts` (already a dependency) — a `LineChart` (remaining vs. an ideal straight-line reference) on a new Burndown modal reachable from each sprint row, and a `BarChart` (Total vs. Completed per sprint) inline in the existing Sprints tab of the shared `ProjectsScreen.tsx`, gated on the `issues` module (`isSoftwareAgency`) alongside the pre-existing ungated Sprints tab.

  21 new unit tests: 5 in `issue-comment.service.test.ts`, 6 in `issue-subtask.service.test.ts`, 10 in a new `sprint.service.test.ts` covering the burndown/velocity math specifically — points-weighted remaining-by-day correctness (a resolved issue's weight drops only on/after its real `resolvedDate`, never before), the ideal line reaching exactly 0 on the sprint's last day, the issue-count fallback when nobody sets story points, an unestimated issue counting as 0 (not 1) once ANY issue in that same sprint has real points set, a same-day sprint not producing a phantom extra day or dividing by zero, and velocity's COMPLETED-only filter + RESOLVED/CLOSED-only `completedPoints` + correct averaging. `npm run typecheck` 0 errors, `npx vitest run` 1652/1652.

  Live-UAT'd 24/24 via a real driven Electron app: created a real issue with 5 story points via the actual New Issue form and confirmed persistence; added a second issue via API for deterministic burndown/velocity math; resolved the first issue and confirmed the real burndown API returns correct totals (8 points) and a correctly-shaped 5-day array; opened the real Issue Detail modal, added a real subtask via the actual UI and toggled it done, added a real comment and confirmed it was attributed to the real logged-in user (not a caller-supplied name) — both confirmed via direct DB query; switched to the real Kanban Board view and confirmed both the RESOLVED and OPEN columns render the correct real issues; marked the sprint COMPLETED and confirmed the real velocity API (5 completed / 8 total points); confirmed the real Velocity bar chart section renders in the Sprints tab of the actual Projects screen with the correct real numbers.

  **No real product bugs — clean build.** **Test-script bugs found+fixed** (3, all traced to one root cause plus one unrelated): a stricter Playwright `.check()` actionability wait timed out on the subtask checkbox because its `onChange` handler awaits an IPC round-trip before flipping the controlled-input's `checked` state (the click and the DB write both succeeded regardless — confirmed via a direct DB query); that same thrown exception aborted the step BEFORE the detail modal's close button ever ran, leaving its backdrop open and blocking two later clicks (Board view toggle, Sprints tab button) — fixing the checkbox check (switched `.check()` to a plain `.click()`) fixed both downstream failures for free. Unrelated: `bodyText.includes('Velocity')` never matched because the section header has a CSS `uppercase text-transform` class — Playwright's `innerText()` returns the CSS-rendered text ("VELOCITY"), not the literal DOM string ("Velocity") — fixed with a case-insensitive check.

  Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1652/1652.
- [x] **Photo Studio** (5→10): Done 2026-07-19. Delivered photo count — new `DeliveryTracker.deliveredPhotosCount`, shown alongside the pre-existing `ShootBooking.expectedPhotosCount` in the expanded booking panel. Shoot-day equipment/crew checklist — new `ShootChecklistItem` (label + EQUIPMENT/CREW category + isDone), a simple add/toggle/delete list per booking. Itemized add-ons — new `ShootAddOnItem` (description/quantity/unitPrice); `generateShootInvoice()` now builds the invoice from the base package fee (`finalAmount`, unchanged) PLUS one real invoice line per add-on, each looked up/created by its OWN description text as a generic reusable Product (same `findOrCreate` discipline as the existing base "Photography & Videography Services" line) — never folded into a single manually-typed total. Add-on mutation is blocked once a booking is invoiced (`SAO-005`/`SAO-006`), matching the existing `finalAmount` edit-lock. New expanded-panel UI in `ShootsScreen.tsx` shows a running "Final Amount + Add-ons = Grand Total" breakdown before invoicing.

  25 new unit tests: 6 in `shoot-checklist.service.test.ts`, 10 in `shoot-addon.service.test.ts`, 2 appended to `shoot-booking.service.test.ts` specifically covering the invoice line-item integration (each add-on becomes its own line via its own looked-up/created Product; a no-add-ons booking still generates a clean single-line invoice, unaffected). `npm run typecheck` 0 errors, `npx vitest run` 1671/1671.

  Live-UAT'd 24/24 via a real driven Electron app: created a real client + booking via the actual New Booking form; added a real equipment checklist item via the UI and a crew item via API, toggled one done via the real checkbox, confirmed both persisted correctly; set a real delivered-photo-count (480 of 500 expected) via the actual input and confirmed persistence; added two real add-on items (extra prints + album copy) via the UI/API, confirmed the real running total (₹3,300); set a final amount and generated a real invoice via the actual button, confirmed via the real `billing.getInvoice` API that the invoice has exactly 3 lines (base package + both add-ons) summing to the correct ₹53,300 grand total; confirmed a further add-on-add attempt is correctly rejected (`SAO-005`) once invoiced.

  **No real product bugs — clean build.** **Test-script bugs found+fixed** (recurring gotcha classes, not new ones): the booking row needed the exact `div.flex.items-center.gap-4.px-5.py-4.cursor-pointer` class combo (a bare `div`+hasText locator matched an unrelated ancestor and silently failed every subsequent lookup); the checklist and add-on forms both render a bare Plus-icon button, so an unscoped page-wide button locator risked hitting the header's "New Booking" button instead — fixed by scoping to each form's own `div.flex.gap-1\.5` row; a CSS sibling combinator (`label + input`) must be written as ONE compound selector string, not chained via `.locator('+ input')`, which doesn't apply sibling combinators the way a single CSS string does. Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1671/1671.
- [x] **Event Management** (6→10): Done 2026-07-20. Guest-count-to-costing — new `EventVendorBooking.pricingType` (FLAT|PER_HEAD) + `perHeadRate`; when PER_HEAD, `quotedAmount` is ALWAYS server-computed from `perHeadRate × EventBooking.expectedGuestCount` (never accepted from the caller), and `updateEventBooking()` now recomputes every PER_HEAD line for an event whenever its guest count changes (`recomputePerHeadVendorBookings()`) — same "recompute on any relevant change, never trust the derived number directly" discipline as Real Estate's co-broker split. Rejects (not silently zeros) when the event has no real guest count yet. Run-of-show timeline — new `EventRunOfShowItem` (scheduledTime/activity/responsibleParty/isDone), deliberately a separate model from the pre-existing `EventVendorBooking` procurement checklist since they track different things (what time things happen on the day, vs. which vendors are booked). Calendar view — new List/Calendar toggle on `EventsScreen.tsx`; a from-scratch month grid (no new dependency) showing every event whose `[eventDate, eventEndDate]` span touches each day, so concurrent/overlapping bookings are visible at a glance; clicking a day's event chip switches to List view with that event expanded.

  **Real bug caught by live UAT, not a unit test this time**: `EventVendorBooking.perHeadRate` is a Prisma Decimal field exactly like the pre-existing `quotedAmount`/`advancePaid`, but the shared `serializeVendorBooking()` helper (reused by `event-booking.service.ts` for its nested `vendorBookings[]`) was never updated to convert it — every PER_HEAD vendor create/list crashed the real running app over IPC with "An object could not be cloned" the instant a real per-head line was added through the actual UI. Fixed by extending `serializeVendorBooking()`; caught immediately by the live UAT run (the exact class of Decimal-serialization bug this session has hit and fixed repeatedly on nearly every new Decimal column added this phase — worth remembering as a standing checklist item: any new Decimal field needs its serializer updated, not just its validation schema).

  33 new/modified unit tests: 8 appended to `event-vendor-booking.service.test.ts` covering per-head computation (server-computed regardless of a caller-sent flat amount, rejects when guest count is unset or zero, recomputes on rate change, clears `perHeadRate` when switched back to FLAT) plus 2 specifically regression-testing the Decimal-serialization fix; 2 appended to `event-booking.service.test.ts` confirming `updateEventBooking` triggers the PER_HEAD recompute only when `expectedGuestCount` is actually part of the update; a new `event-run-of-show.service.test.ts` (7 tests). `npm run typecheck` 0 errors, `npx vitest run` 1690/1690.

  Live-UAT'd 20/20 via a real driven Electron app (after the Decimal fix — the pre-fix run correctly caught and diagnosed the bug above via a real IPC crash, not a false negative): added a real PER_HEAD vendor line via the actual UI (₹800/guest × 200 guests), confirmed the live preview and the persisted `quotedAmount` (₹1,60,000) both correct; changed the event's guest count to 250 via the API and confirmed the vendor line's `quotedAmount` auto-recomputed to ₹2,00,000 with zero further action; added a real run-of-show item via the actual timeline form, confirmed persistence and a real checkbox toggle; switched to the real Calendar view and confirmed the event's chip renders on its correct real day.

  Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1690/1690.
- [x] **Placement Agency** (5→10): Done 2026-07-20. Resume/CV attachment — extended the existing generic `DocumentEntityType`/`DocumentPanel` system with a new `CANDIDATE` entity type (same pattern as the 9 other types added across §0/§1), mounted `<DocumentPanel entityType="CANDIDATE" compact />` in the candidate edit form (existing-candidate only, matching `EngagementsScreen.tsx`'s precedent). Interview-round tracking — new `InterviewRound` model, a real sub-record of a candidate+job-order PAIRING (not just the candidate, and not the same thing as `Placement` which only exists once an offer/join actually happens): round number (auto-sequenced per pairing, same discipline as Company Secretary's board-resolution auto-numbering), type, scheduled date, status, interviewer, and a genuine per-round client-feedback text field. Fee-agreement/replacement-guarantee terms — new `JobOrder.feeAgreementTerms` (free text) + `replacementGuaranteeDays` (plain Int) — deliberately NOT Decimal fields, sidestepping the Decimal-serializer bug class outright (see below). Placements list now shows a computed "Replacement guarantee until" date (joiningDate + the job order's guarantee-day count) wherever set.

  **This closes out §2 entirely — all 43 business-type verticals are now at 10/10.**

  17 new unit tests in a new `interview-round.service.test.ts` (auto-sequencing per candidate/job-order pairing, explicit-override honoring, missing-candidate/missing-job-order guards, status/feedback updates) + 3 appended to `job-order.service.test.ts` for the new plain fields. `npm run typecheck` 0 errors, `npx vitest run` 1701/1701.

  Live-UAT'd 18/18 via a real driven Electron app: created a real candidate via the actual form; added a real interview round via the Interview Rounds panel and confirmed the real round-1 auto-sequencing; updated status to PASSED and typed real client feedback via the actual UI, confirmed both persisted; added a second round via API and confirmed it auto-sequenced to round 2 for the SAME pairing; opened the real Job Order edit form and confirmed the fee-agreement terms and 90-day replacement guarantee both pre-populated correctly from the persisted record.

  **No real product bugs — clean build.** **Test-script bugs found+fixed** (all recurring gotcha classes from earlier this session, applied here rather than newly discovered): `input[0]` on the Candidates tab is the page-level search box, not the form's own Full Name field — an unscoped `.locator('input').first()` silently filled the wrong element and the create call never fired; the same CSS-sibling-combinator mistake as Photo Studio/Event Management (`label:has-text() + input` must be one compound selector string, not chained via `.locator('+ input')`); the same bare-`div`-with-hasText row-matching mistake as every other vertical this phase, fixed by scoping to the row's own `div.flex.items-start.justify-between.gap-3` class combo. **One genuine test-script cleanup gap** (not a product bug, but worth noting): `h.cleanupByNamePrefix()` only ever operates on Customer/Product rows — `Candidate` has no FK relationship to `Customer` at all, so a leftover test candidate silently survived the standard cleanup call; found via the post-UAT residue check and deleted by hand. `InterviewRound` rows correctly cascade-deleted via `JobOrder`→`Customer`, so only the orphaned `Candidate` needed manual cleanup.

  Full regression: `npm run typecheck` 0 errors, `npx vitest run` 1701/1701.

---

## 3. Manual updates

- [x] Update every manual page touched above to reflect the new capability — DONE 2026-07-20, English pass. Scoped to "every vertical touched anywhere in Phase 58" (§0/§1/§2, not just the last dozen), per explicit confirmation. Updated 26 of the 39 business-type manual pages plus the universal `billing.md` chapter (Hold Sale, Frequently Sold grid, MRP — genuinely cross-vertical Billing-screen features, homed there instead of duplicated per-vertical): Restaurant, Hotel/Lodge, Rental, Hardware, Distributor, Clothing (Footwear inherits via its own pointer to the Clothing chapter), Pharmacy, Electronics, Jewellery, Agri Inputs, Manufacturing, Vet Clinic, GP Clinic, Specialist Clinic, Dental Clinic, Physio Clinic, Diagnostic Lab, Blood Bank, Beauty Salon, Gym/Studio, Driving School, Service/Consultant/Repair, Car Service Center, Tailor Boutique, Pest Control, Lawyer, CA Firm, Company Secretary, Architect, Civil Engineer, Real Estate, Marketing Agency, Software Agency, Photo Studio, Event Management, Placement Agency. Retail/General verified as correctly pointing to the universal Billing chapter rather than duplicating; Independent Consultant and Coaching Institute verified as already fully documenting their (zero new / already-closed) capabilities, no changes needed. Live-verified: a 27-page sample of the updated chapters all render in the real running app with zero crashes and the new content genuinely present (not just saved to disk unverified).
  - **Translation to the other 12 locales is a separate, not-yet-started sub-task** — per established project discipline, this needs explicit confirmation before starting given its scale (39 chapters × 12 languages).
- [x] Document the Coaching Institute Performances/recitals feature (already real, currently undocumented) — DONE 2026-07-18, folded into the Coaching Institute §2 item above.

---

## Progress log

- **2026-07-16**: Plan created from the fresh 43-vertical audit. QR/LAN virtual-adapter bug fixed in both LAN servers (needs live re-scan confirmation). Starting execution on §0 remaining items (returns/variant bug, Projects report bug) next.
