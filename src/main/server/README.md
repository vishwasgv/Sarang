# LAN mini-servers

Small HTTP servers started from the app for phones/tablets on the same Wi-Fi: QR ordering, Doctor Pad, Owner View, field orders, kitchen display, token queue. Each is opt-in, token-protected and serves only its own screen. Not a general multi-user server (planned as step U2).
